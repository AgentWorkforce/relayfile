import { workflow } from '@relayflows/core';
import * as rickyWorkflowFs from 'node:fs';
import * as rickyWorkflowPath from 'node:path';

// IMPLEMENTATION_WORKFLOW_CONTRACT: implementation specs must produce source changes, tests, non-empty diff evidence, and PR/result reporting.
// RICKY_WORKFLOW_ENV_LOADER: load repo-local env files before spawning workflow agents.

function loadRickyWorkflowEnv(cwd = process.cwd()) {
  for (const file of ['.env.local', '.env']) {
    const path = rickyWorkflowPath.join(cwd, file);
    if (!rickyWorkflowFs.existsSync(path)) continue;
    const body = rickyWorkflowFs.readFileSync(path, 'utf8');
    for (const rawLine of body.split(/\r?\n/)) {
      const line = rawLine.trim();
      if (!line || line.startsWith('#')) continue;
      const match = /^(?:export\s+)?([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.*)$/.exec(line);
      if (!match) continue;
      const [, key, rawValue] = match;
      if (!key || rawValue === undefined || process.env[key] !== undefined) continue;
      process.env[key] = unquoteRickyWorkflowEnvValue(rawValue);
    }
  }
}

function unquoteRickyWorkflowEnvValue(value: string): string {
  const trimmed = value.trim();
  if ((trimmed.startsWith('"') && trimmed.endsWith('"')) || (trimmed.startsWith("'") && trimmed.endsWith("'"))) {
    return trimmed.slice(1, -1);
  }
  return trimmed;
}

function assertRickyWorkflowEnv(names: string[]): void {
  const missing = names.filter((name) => !process.env[name]);
  if (missing.length > 0) {
    throw new Error(`MISSING_ENV_VAR: ${missing.join(', ')}. Add missing values to .env.local or export them before rerunning.`);
  }
}

interface RickyGeneratedContextFile {
  path: string;
  content: string;
}

interface RickyGeneratedTargetContext {
  value: string;
  outputPath: string;
}

function writeRickyGeneratedContextFiles(files: RickyGeneratedContextFile[], targetContext?: RickyGeneratedTargetContext): void {
  for (const file of files) {
    rickyWorkflowFs.mkdirSync(rickyWorkflowPath.dirname(file.path), { recursive: true });
    rickyWorkflowFs.writeFileSync(file.path, ensureTrailingNewline(file.content));
  }

  if (!targetContext) return;

  rickyWorkflowFs.mkdirSync(rickyWorkflowPath.dirname(targetContext.outputPath), { recursive: true });
  const targetContextSourcePath = resolveRickyGeneratedTargetContextPath(targetContext.value);
  if (targetContextSourcePath) {
    rickyWorkflowFs.copyFileSync(targetContextSourcePath, targetContext.outputPath);
    return;
  }

  rickyWorkflowFs.writeFileSync(targetContext.outputPath, ensureTrailingNewline(targetContext.value));
}

function resolveRickyGeneratedTargetContextPath(value: string): string | null {
  if (rickyWorkflowPath.isAbsolute(value)) return null;

  const workspaceRoot = rickyWorkflowFs.realpathSync(process.cwd());
  const candidatePath = rickyWorkflowPath.resolve(workspaceRoot, value);

  try {
    if (!rickyWorkflowFs.existsSync(candidatePath) || !rickyWorkflowFs.statSync(candidatePath).isFile()) {
      return null;
    }

    const realCandidatePath = rickyWorkflowFs.realpathSync(candidatePath);
    if (realCandidatePath === workspaceRoot) return null;
    if (!realCandidatePath.startsWith(`${workspaceRoot}${rickyWorkflowPath.sep}`)) return null;
    return realCandidatePath;
  } catch {
    return null;
  }
}

function ensureTrailingNewline(value: string): string {
  return value.endsWith('\n') ? value : `${value}\n`;
}

async function main() {
  loadRickyWorkflowEnv();
  writeRickyGeneratedContextFiles([
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.txt", content: "# VFS-Watch Dispatcher\n\nStatus: ready for implementation\nOwner: proactive-runtime\nTracks: agent-gateway deploy (#827) · integration dispatcher (#855 follow-up)\n\n## Why this spec exists\n\nThe proactive runtime currently has **two conceptual dispatch paths**, and only one of them is shipping:\n\n1. **Synchronous inline dispatch** (live, since #855). Every provider's `handle<Provider>Forward` in `packages/web/lib/integrations/nango-webhook-router.ts` calls `dispatchIntegrationWatchEventBestEffort(...)` immediately after triggering the Nango sync. The dispatcher resolves matching agents by `watch_globs ∩ paths` and invokes `deliverDeploymentTrigger` to spawn a Daytona sandbox.\n2. **VFS-watch dispatch** (designed, not deployed). `cloud/services/agent-gateway/src/watch-subscriber.ts` (`WorkspaceWatchSubscriber` + `matchesAnyGlob`) was built to subscribe to Relayfile filesystem watch events, match each write against `agents.watch_globs`, and dispatch via the Durable Object gateway. The worker has no SST infra file and isn't deployed in the production CF account.\n\nPath 1 is the minimum that gets event-driven personas firing today. Path 2 is the durable, general-purpose substrate the runtime was originally specced against. This document defines what shipping path 2 looks like, why it matters beyond what path 1 already covers, and how the two coexist during the migration.\n\n## What path 1 doesn't cover\n\nThe inline dispatch only fires for events that pass through `nango-webhook-router.ts`. Concretely, that excludes:\n\n- **Non-webhook VFS writes.** Bulk sync results, replay/backfill jobs, `npx workforce admin` operations, and any future direct-write surface (e.g. Relayfile SDK write from a sibling worker) never invoke the inline handler. Agents watching `/<provider>/...` paths are not invoked.\n- **Cross-provider watch globs.** An agent declaring `watch_globs: [\"/**/issues/opened/**\"]` (provider-agnostic; meant to watch issues from any wired integration) gets dispatched only by the specific provider handler that knows about issues. Adding a new provider means touching every cross-cutting persona's wiring assumption.\n- **Failure isolation.** If `handleGitHubForward` throws after the Nango sync but before `dispatchIntegrationWatchEventBestEffort` (or the dispatcher itself throws), the agent never fires *and* there is no durable record of \"event happened, dispatch owed.\" VFS-watch consumes from a stream that survives handler crashes.\n- **Replay.** Operators cannot replay a window of historical events (post-incident, after a persona is added that should have caught past events, etc.). VFS rewind + re-emit is the natural way to do it.\n- **Multi-consumer fan-out.** Today, only the inline dispatcher consumes the event. Future consumers (metrics, audit, secondary projections, sage) each need their own hook in `handleForward`. A VFS-watch stream lets any number of subscribers attach without modifying the producer.\n\nPath 1 is \"good enough\" for the demo loop. Path 2 is the substrate the runtime architecture documents (`docs/proactive-runtime/spec.md`, `docs/proactive-runtime/event-shape.md`) assume.\n\n## Goals\n\n- **G1**: A persona with `watch_globs: [\"/github/issues/opened/**\"]` fires when *any* writer (Nango sync, admin SDK, replay tool, manual write) creates a matching file in Relayfile VFS — not only when a webhook flows through the provider's `handleForward`.\n- **G2**: Inline dispatch (path 1) does not regress during the migration; existing personas keep firing through the same `deliverDeploymentTrigger` they use today.\n- **G3**: Exactly-once delivery for the agent perspective. If both inline and VFS-watch dispatch resolve the same `(workspace, agentId, eventId)`, only one Daytona spawn happens.\n- **G4**: Operator-visible. A given write should be traceable: did it fan out? to which agents? did each delivery succeed? was it deduped? Surfaced in CF Observability and Workers Logs, not buried in a Durable Object.\n\n## Non-goals\n\n- Replacing Relayfile VFS itself. The dispatcher consumes the existing watch stream; the spec assumes Relayfile is already the source of truth.\n- Replacing `deliverDeploymentTrigger`. That function stays the single agent invocation primitive — the dispatcher only changes *who calls it and on what trigger*.\n- Solving the agent-gateway `WorkspaceGatewayDO` cron-tick path. That hop is unblocked by the daytona-auth fix; this spec is scoped to the VFS-watch path.\n\n## Design\n\n### Components\n\n```\n┌──────────────────┐\n│ Relayfile (VFS)  │   --- write event ---->  ┌─────────────────────────┐\n│                  │                          │ agent-gateway worker    │\n└──────────────────┘                          │  (Cloudflare Worker)    │\n       ▲                                      │                         │\n       │ writes from any source:              │  WorkspaceWatchSubscriber\n       │   - nango syncs                      │    - dedupe (workspace, │\n       │   - admin SDK                        │      path, version)     │\n       │   - replay tool                      │    - matchesAnyGlob     │\n       │   - sage / sibling workers           │    - fan out to agents  │\n       │                                      │                         │\n       │                                      │  ↓ for each matched     │\n       │                                      │                         │\n       │                                      │  deliverDeploymentTrigger\n       │                                      │    (shared with inline +│\n       │                                      │     cron path)          │\n       │                                      │       ↓                 │\n       │                                      │  Daytona sandbox runs   │\n       │                                      │  persona.onEvent        │\n       └──────────────────────────────────────┴─────────────────────────┘\n```\n\n### Trigger surface\n\nThe agent-gateway worker exposes a single durable-object-backed entrypoint that consumes Relayfile watch events via **push delivery**: Relayfile POSTs `{ workspace, path, writeId, occurredAt, provider?, eventType?, payload? }` to an authenticated endpoint on agent-gateway whenever a write completes. The endpoint is HMAC-authenticated using `RELAYFILE_INTERNAL_HMAC_SECRET` (the same secret pattern used by other internal worker-to-worker calls).\n\nPush was chosen over pull/WebSocket for three reasons: (1) lowest latency — no per-workspace cursor round-trip; (2) operability — every event is a discrete HTTP invocation visible in CF Observability + Workers Logs without DO introspection; (3) Relayfile already has outbound webhook capability, so this reuses an existing path instead of standing up a new stream protocol. The cost is that Relayfile owns retry on push failure — which it already does for its other outbound webhooks.\n\n### Dispatcher contract\n\n```ts\ntype VfsWatchEvent = {\n  workspaceId: string;\n  path: string;             // e.g. \"/github/issues/opened/123-foo.json\"\n  writeId: string;          // monotonic, per-workspace, dedupe key\n  provider?: string;        // hint when the writer knows it (Nango sync sets this)\n  eventType?: string;       // hint when the writer knows it (\"issues.opened\")\n  payload?: unknown;        // optional, for writers that already have it in hand\n  occurredAt: string;\n};\n\nasync function onVfsWatchEvent(event: VfsWatchEvent): Promise<VfsDispatchResult> {\n  // 1. Dedupe by (workspaceId, writeId). DO storage keys: `dedupe:${ws}:${writeId}`.\n  //    TTL = 24h.\n  if (await isAlreadyDelivered(event)) return { skipped: \"dedupe\" };\n\n  // 2. Match candidate agents in workspaceId where status='active'\n  //    AND array_length(watch_globs,1) > 0\n  //    AND any glob intersects event.path (uses cloud `pathCouldIntersect`).\n  const candidates = await readCandidateAgents(event.workspaceId);\n  const matched = candidates.filter(row => agentMatchesEvent(row, event));\n\n  if (matched.length === 0) return { matched: 0, delivered: 0, failed: 0 };\n\n  // 3. Build payload using the same envelope shape inline dispatch builds.\n  const payload = buildPayload(event);\n\n  // 4. Deliver. Same primitive cron + inline use today. Parallelize across\n  //    matched agents (same semantics as the existing inline dispatcher's\n  //    Promise.allSettled fan-out); per-agent serialization is not enforced\n  //    at the dispatcher layer — Daytona's own sandbox-per-deployment\n  //    isolation is the right place for any single-agent ordering.\n  const outcomes = await Promise.allSettled(\n    matched.map(row => deliverDeploymentTrigger({ workspaceId, agentId: row.id, payload }))\n  );\n\n  // 5. Record dedupe key only on success outcomes; transient failures retry on next event.\n  await recordDedupe(event);\n\n  return { matched: matched.length, ... };\n}\n```\n\nThe matching logic must reuse the **exact** module that inline dispatch uses (`packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent`) — no parallel reimplementation. The agent-gateway worker imports it directly, or it is moved to `packages/core/proactive-runtime/match.ts` so both workers depend on the same library.\n\n### Idempotency / dedupe\n\nTwo writers can produce the same effective event:\n\n- A Nango forward arrives at `cloud-web-worker`, `handleGitHubForward` triggers a sync and calls `dispatchIntegrationWatchEventBestEffort` inline. The sync also writes to Relayfile VFS, which fires a watch event consumed by agent-gateway.\n- An admin replay re-writes a VFS file. No inline dispatch fires (no webhook), only the VFS-watch dispatch.\n\nTo deliver an agent exactly once, both dispatchers share a **dedupe key**:\n\n```\ndedupe key = sha256(workspaceId : agentId : writeId | deliveryId)\n```\n\nEither dispatcher records the key before invoking `deliverDeploymentTrigger`. A second dispatch that finds the key present logs `skipped=\"dedupe\"` and short-circuits.\n\nThe store is the agent-gateway's DO (strong consistency per workspace). `cloud-web-worker` calls into the DO via service binding named **`AGENT_GATEWAY_DEDUPE_BROKER`** to record/check.\n\n**Dedupe TTL: 24h.** Records expire from the DO 24 hours after the originating write. The window is set to:\n\n- comfortably cover any same-day replay (operator replays a window post-incident within hours, not days);\n- bound the DO storage cost (no unbounded growth as workspaces accumulate writes);\n- be long enough that a same-event redelivery (Nango retry storms, gateway restart, etc.) within a working day is caught.\n\n### Coexistence with inline dispatch\n\nThe rollout is two phases over one week:\n\n**Phase A — dual-dispatch.** Inline (`dispatchIntegrationWatchEventBestEffort` in `nango-webhook-router.ts`) and VFS-watch both fire; the dedupe key collapses double-delivery. Lowest risk to existing event flow; cleanest rollback (revert agent-gateway deploy, inline keeps working).\n\n**Phase B — VFS-watch only.** After ~one week of healthy dual-dispatch (verified by `vfs-watch-dispatch` logs showing dedupe coverage matches inline-dispatch coverage 1:1 and `failed=0`), downgrade `dispatchIntegrationWatchEventBestEffort` to a metrics-only no-op. Single dispatcher, one place to reason about ordering, lower duplicate-work cost.\n\nPhase A is the default the implementation PR ships. Phase B is a follow-up PR that flips a single function in `nango-webhook-router.ts`.\n\n### Failure modes & observability\n\nEvery code path emits a single line keyed `area: \"vfs-watch-dispatch\"`, with at minimum:\n\n- `workspaceId`, `path`, `writeId`\n- `matched` count\n- `dedupe` outcome (`first` / `skipped`)\n- `delivered` / `failed` counts\n- `error` chain (cause-walked, matches the persona-bundle-deploy pattern at `route.ts:560`)\n\nWorkers Logs persistent (from #863) is required — these go straight into the same dashboard query operators already use for inline dispatch.\n\nFailure cases the spec is opinionated about:\n\n- **Daytona spawn fails**: log `failed`, do *not* record dedupe key, allow next event to retry. (Identical to current cron tick behaviour now that #886 is fixed.)\n- **DB read fails**: return error, do not record dedupe; watch-stream consumer treats as `retry`.\n- **Watch-stream cursor lag**: the consumer surfaces a `vfs-watch-lag-seconds` metric; alert at >60s sustained.\n\n### Boundary cases\n\n- **Persona deployed mid-write**. A persona registered at `t1` with watch globs that intersect a write at `t0 < t1` does **not** fire (no backfill on registration). Backfill semantics are out of scope; the replay tool (step 7 of the migration plan) covers operator-opt-in backfill over an explicit window.\n- **Persona destroyed mid-write**. The agent row's `status` is checked in the candidate query; destroyed agents are filtered. Already correct via the existing `agentMatchesEvent`.\n- **Watch globs include cross-provider patterns** (`/**/issues/opened/**`). `pathCouldIntersect` already handles glob prefix intersection across multiple providers — covered by the existing helper, no change required.\n- **No agents match the event**. The dispatcher still runs the candidate query (`matched=0`) and logs the outcome. The query is a single indexed read against `agents.workspace_id`; the cost is negligible at any anticipated scale and the resulting log line is load-bearing for the \"did anyone watch this?\" diagnostic. No early-out optimization.\n\n## Migration plan\n\n1. **#827 follow-up: deploy agent-gateway via SST.** Currently `cloud/services/agent-gateway/wrangler.toml` exists but no `infra/agent-gateway.ts` declares `new sst.cloudflare.Worker(...)`. Add it, with the DO bindings + service bindings to `cloud-web-worker` (for the dedupe broker), and the `RELAYFILE_*` envs needed by `WorkspaceWatchSubscriber`.\n2. **Move matching helpers to a shared package.** Either:\n   - Export `agentMatchesEvent` from `packages/core/proactive-runtime/match.ts` and import from both `cloud-web-worker` (inline dispatch) and `agent-gateway` (vfs-watch).\n   - Or service-bind `cloud-web-worker` from `agent-gateway` and call the existing module across the boundary. The first option is simpler.\n3. **Wire dedupe service binding.** `cloud-web-worker` already imports the gateway URL; add a service binding so `dispatchIntegrationWatchEventBestEffort` can check/record dedupe before calling `deliverDeploymentTrigger`.\n4. **Implement `onVfsWatchEvent`.** Push endpoint on the gateway DO (HMAC-authed by `RELAYFILE_INTERNAL_HMAC_SECRET`), as defined in §Trigger surface. Relayfile owns retry on push failure.\n5. **Roll out dual-dispatch (Phase A above).** Observe `vfs-watch-dispatch` logs, confirm dedupe coverage matches inline-dispatch coverage 1:1.\n6. **Downgrade inline dispatch (Phase B).** After ~one week of dual-dispatch with healthy dedupe, change `dispatchIntegrationWatchEventBestEffort` to emit a metrics-only record (no `deliverDeploymentTrigger`). Both surfaces still log; only VFS-watch invokes agents.\n7. **Document replay tool**. Spec it separately, but the dispatcher's design (dedupe on `writeId`, agent-side delivery is idempotent because `deliverDeploymentTrigger` uses the deployment row state) enables an operator command of the form `relayfile admin replay --workspace ... --since ... --globs ...` that re-emits a window of writes back through the VFS-watch stream.\n\n## Test plan\n\n- [ ] Unit: `onVfsWatchEvent` dedupe behaviour across `(first | second | third)` deliveries with the same `writeId`\n- [ ] Unit: matched / unmatched candidate filtering (mirror the existing `integration-watch-dispatcher.test.ts`)\n- [ ] Integration: deploy a persona watching `/github/issues/opened/**`, write a file directly to the workspace VFS via the admin SDK (no webhook involved). Expect `vfs-watch-dispatch` to dispatch and the persona to fire.\n- [ ] Integration: same persona, this time produce the event via a real GitHub webhook. Expect exactly one `deliverDeploymentTrigger` call (dedupe collapses inline + VFS-watch).\n- [ ] Observability: CF logs query for `area: \"vfs-watch-dispatch\"` shows `matched`/`delivered`/`failed`/`dedupe` fields and `workspaceId` for each event.\n- [ ] Failure: stub Daytona to throw; confirm the dispatcher records the failure but does *not* mark the writeId as delivered, so the next VFS event with the same id retries." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md", content: "# Normalized Spec\n\n# VFS-Watch Dispatcher\n\nStatus: ready for implementation\nOwner: proactive-runtime\nTracks: agent-gateway deploy (#827) · integration dispatcher (#855 follow-up)\n\n## Why this spec exists\n\nThe proactive runtime currently has **two conceptual dispatch paths**, and only one of them is shipping:\n\n1. **Synchronous inline dispatch** (live, since #855). Every provider's `handle<Provider>Forward` in `packages/web/lib/integrations/nango-webhook-router.ts` calls `dispatchIntegrationWatchEventBestEffort(...)` immediately after triggering the Nango sync. The dispatcher resolves matching agents by `watch_globs ∩ paths` and invokes `deliverDeploymentTrigger` to spawn a Daytona sandbox.\n2. **VFS-watch dispatch** (designed, not deployed). `cloud/services/agent-gateway/src/watch-subscriber.ts` (`WorkspaceWatchSubscriber` + `matchesAnyGlob`) was built to subscribe to Relayfile filesystem watch events, match each write against `agents.watch_globs`, and dispatch via the Durable Object gateway. The worker has no SST infra file and isn't deployed in the production CF account.\n\nPath 1 is the minimum that gets event-driven personas firing today. Path 2 is the durable, general-purpose substrate the runtime was originally specced against. This document defines what shipping path 2 looks like, why it matters beyond what path 1 already covers, and how the two coexist during the migration.\n\n## What path 1 doesn't cover\n\nThe inline dispatch only fires for events that pass through `nango-webhook-router.ts`. Concretely, that excludes:\n\n- **Non-webhook VFS writes.** Bulk sync results, replay/backfill jobs, `npx workforce admin` operations, and any future direct-write surface (e.g. Relayfile SDK write from a sibling worker) never invoke the inline handler. Agents watching `/<provider>/...` paths are not invoked.\n- **Cross-provider watch globs.** An agent declaring `watch_globs: [\"/**/issues/opened/**\"]` (provider-agnostic; meant to watch issues from any wired integration) gets dispatched only by the specific provider handler that knows about issues. Adding a new provider means touching every cross-cutting persona's wiring assumption.\n- **Failure isolation.** If `handleGitHubForward` throws after the Nango sync but before `dispatchIntegrationWatchEventBestEffort` (or the dispatcher itself throws), the agent never fires *and* there is no durable record of \"event happened, dispatch owed.\" VFS-watch consumes from a stream that survives handler crashes.\n- **Replay.** Operators cannot replay a window of historical events (post-incident, after a persona is added that should have caught past events, etc.). VFS rewind + re-emit is the natural way to do it.\n- **Multi-consumer fan-out.** Today, only the inline dispatcher consumes the event. Future consumers (metrics, audit, secondary projections, sage) each need their own hook in `handleForward`. A VFS-watch stream lets any number of subscribers attach without modifying the producer.\n\nPath 1 is \"good enough\" for the demo loop. Path 2 is the substrate the runtime architecture documents (`docs/proactive-runtime/spec.md`, `docs/proactive-runtime/event-shape.md`) assume.\n\n## Goals\n\n- **G1**: A persona with `watch_globs: [\"/github/issues/opened/**\"]` fires when *any* writer (Nango sync, admin SDK, replay tool, manual write) creates a matching file in Relayfile VFS — not only when a webhook flows through the provider's `handleForward`.\n- **G2**: Inline dispatch (path 1) does not regress during the migration; existing personas keep firing through the same `deliverDeploymentTrigger` they use today.\n- **G3**: Exactly-once delivery for the agent perspective. If both inline and VFS-watch dispatch resolve the same `(workspace, agentId, eventId)`, only one Daytona spawn happens.\n- **G4**: Operator-visible. A given write should be traceable: did it fan out? to which agents? did each delivery succeed? was it deduped? Surfaced in CF Observability and Workers Logs, not buried in a Durable Object.\n\n## Non-goals\n\n- Replacing Relayfile VFS itself. The dispatcher consumes the existing watch stream; the spec assumes Relayfile is already the source of truth.\n- Replacing `deliverDeploymentTrigger`. That function stays the single agent invocation primitive — the dispatcher only changes *who calls it and on what trigger*.\n- Solving the agent-gateway `WorkspaceGatewayDO` cron-tick path. That hop is unblocked by the daytona-auth fix; this spec is scoped to the VFS-watch path.\n\n## Design\n\n### Components\n\n```\n┌──────────────────┐\n│ Relayfile (VFS)  │   --- write event ---->  ┌─────────────────────────┐\n│                  │                          │ agent-gateway worker    │\n└──────────────────┘                          │  (Cloudflare Worker)    │\n       ▲                                      │                         │\n       │ writes from any source:              │  WorkspaceWatchSubscriber\n       │   - nango syncs                      │    - dedupe (workspace, │\n       │   - admin SDK                        │      path, version)     │\n       │   - replay tool                      │    - matchesAnyGlob     │\n       │   - sage / sibling workers           │    - fan out to agents  │\n       │                                      │                         │\n       │                                      │  ↓ for each matched     │\n       │                                      │                         │\n       │                                      │  deliverDeploymentTrigger\n       │                                      │    (shared with inline +│\n       │                                      │     cron path)          │\n       │                                      │       ↓                 │\n       │                                      │  Daytona sandbox runs   │\n       │                                      │  persona.onEvent        │\n       └──────────────────────────────────────┴─────────────────────────┘\n```\n\n### Trigger surface\n\nThe agent-gateway worker exposes a single durable-object-backed entrypoint that consumes Relayfile watch events via **push delivery**: Relayfile POSTs `{ workspace, path, writeId, occurredAt, provider?, eventType?, payload? }` to an authenticated endpoint on agent-gateway whenever a write completes. The endpoint is HMAC-authenticated using `RELAYFILE_INTERNAL_HMAC_SECRET` (the same secret pattern used by other internal worker-to-worker calls).\n\nPush was chosen over pull/WebSocket for three reasons: (1) lowest latency — no per-workspace cursor round-trip; (2) operability — every event is a discrete HTTP invocation visible in CF Observability + Workers Logs without DO introspection; (3) Relayfile already has outbound webhook capability, so this reuses an existing path instead of standing up a new stream protocol. The cost is that Relayfile owns retry on push failure — which it already does for its other outbound webhooks.\n\n### Dispatcher contract\n\n```ts\ntype VfsWatchEvent = {\n  workspaceId: string;\n  path: string;             // e.g. \"/github/issues/opened/123-foo.json\"\n  writeId: string;          // monotonic, per-workspace, dedupe key\n  provider?: string;        // hint when the writer knows it (Nango sync sets this)\n  eventType?: string;       // hint when the writer knows it (\"issues.opened\")\n  payload?: unknown;        // optional, for writers that already have it in hand\n  occurredAt: string;\n};\n\nasync function onVfsWatchEvent(event: VfsWatchEvent): Promise<VfsDispatchResult> {\n  // 1. Dedupe by (workspaceId, writeId). DO storage keys: `dedupe:${ws}:${writeId}`.\n  //    TTL = 24h.\n  if (await isAlreadyDelivered(event)) return { skipped: \"dedupe\" };\n\n  // 2. Match candidate agents in workspaceId where status='active'\n  //    AND array_length(watch_globs,1) > 0\n  //    AND any glob intersects event.path (uses cloud `pathCouldIntersect`).\n  const candidates = await readCandidateAgents(event.workspaceId);\n  const matched = candidates.filter(row => agentMatchesEvent(row, event));\n\n  if (matched.length === 0) return { matched: 0, delivered: 0, failed: 0 };\n\n  // 3. Build payload using the same envelope shape inline dispatch builds.\n  const payload = buildPayload(event);\n\n  // 4. Deliver. Same primitive cron + inline use today. Parallelize across\n  //    matched agents (same semantics as the existing inline dispatcher's\n  //    Promise.allSettled fan-out); per-agent serialization is not enforced\n  //    at the dispatcher layer — Daytona's own sandbox-per-deployment\n  //    isolation is the right place for any single-agent ordering.\n  const outcomes = await Promise.allSettled(\n    matched.map(row => deliverDeploymentTrigger({ workspaceId, agentId: row.id, payload }))\n  );\n\n  // 5. Record dedupe key only on success outcomes; transient failures retry on next event.\n  await recordDedupe(event);\n\n  return { matched: matched.length, ... };\n}\n```\n\nThe matching logic must reuse the **exact** module that inline dispatch uses (`packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent`) — no parallel reimplementation. The agent-gateway worker imports it directly, or it is moved to `packages/core/proactive-runtime/match.ts` so both workers depend on the same library.\n\n### Idempotency / dedupe\n\nTwo writers can produce the same effective event:\n\n- A Nango forward arrives at `cloud-web-worker`, `handleGitHubForward` triggers a sync and calls `dispatchIntegrationWatchEventBestEffort` inline. The sync also writes to Relayfile VFS, which fires a watch event consumed by agent-gateway.\n- An admin replay re-writes a VFS file. No inline dispatch fires (no webhook), only the VFS-watch dispatch.\n\nTo deliver an agent exactly once, both dispatchers share a **dedupe key**:\n\n```\ndedupe key = sha256(workspaceId : agentId : writeId | deliveryId)\n```\n\nEither dispatcher records the key before invoking `deliverDeploymentTrigger`. A second dispatch that finds the key present logs `skipped=\"dedupe\"` and short-circuits.\n\nThe store is the agent-gateway's DO (strong consistency per workspace). `cloud-web-worker` calls into the DO via service binding named **`AGENT_GATEWAY_DEDUPE_BROKER`** to record/check.\n\n**Dedupe TTL: 24h.** Records expire from the DO 24 hours after the originating write. The window is set to:\n\n- comfortably cover any same-day replay (operator replays a window post-incident within hours, not days);\n- bound the DO storage cost (no unbounded growth as workspaces accumulate writes);\n- be long enough that a same-event redelivery (Nango retry storms, gateway restart, etc.) within a working day is caught.\n\n### Coexistence with inline dispatch\n\nThe rollout is two phases over one week:\n\n**Phase A — dual-dispatch.** Inline (`dispatchIntegrationWatchEventBestEffort` in `nango-webhook-router.ts`) and VFS-watch both fire; the dedupe key collapses double-delivery. Lowest risk to existing event flow; cleanest rollback (revert agent-gateway deploy, inline keeps working).\n\n**Phase B — VFS-watch only.** After ~one week of healthy dual-dispatch (verified by `vfs-watch-dispatch` logs showing dedupe coverage matches inline-dispatch coverage 1:1 and `failed=0`), downgrade `dispatchIntegrationWatchEventBestEffort` to a metrics-only no-op. Single dispatcher, one place to reason about ordering, lower duplicate-work cost.\n\nPhase A is the default the implementation PR ships. Phase B is a follow-up PR that flips a single function in `nango-webhook-router.ts`.\n\n### Failure modes & observability\n\nEvery code path emits a single line keyed `area: \"vfs-watch-dispatch\"`, with at minimum:\n\n- `workspaceId`, `path`, `writeId`\n- `matched` count\n- `dedupe` outcome (`first` / `skipped`)\n- `delivered` / `failed` counts\n- `error` chain (cause-walked, matches the persona-bundle-deploy pattern at `route.ts:560`)\n\nWorkers Logs persistent (from #863) is required — these go straight into the same dashboard query operators already use for inline dispatch.\n\nFailure cases the spec is opinionated about:\n\n- **Daytona spawn fails**: log `failed`, do *not* record dedupe key, allow next event to retry. (Identical to current cron tick behaviour now that #886 is fixed.)\n- **DB read fails**: return error, do not record dedupe; watch-stream consumer treats as `retry`.\n- **Watch-stream cursor lag**: the consumer surfaces a `vfs-watch-lag-seconds` metric; alert at >60s sustained.\n\n### Boundary cases\n\n- **Persona deployed mid-write**. A persona registered at `t1` with watch globs that intersect a write at `t0 < t1` does **not** fire (no backfill on registration). Backfill semantics are out of scope; the replay tool (step 7 of the migration plan) covers operator-opt-in backfill over an explicit window.\n- **Persona destroyed mid-write**. The agent row's `status` is checked in the candidate query; destroyed agents are filtered. Already correct via the existing `agentMatchesEvent`.\n- **Watch globs include cross-provider patterns** (`/**/issues/opened/**`). `pathCouldIntersect` already handles glob prefix intersection across multiple providers — covered by the existing helper, no change required.\n- **No agents match the event**. The dispatcher still runs the candidate query (`matched=0`) and logs the outcome. The query is a single indexed read against `agents.workspace_id`; the cost is negligible at any anticipated scale and the resulting log line is load-bearing for the \"did anyone watch this?\" diagnostic. No early-out optimization.\n\n## Migration plan\n\n1. **#827 follow-up: deploy agent-gateway via SST.** Currently `cloud/services/agent-gateway/wrangler.toml` exists but no `infra/agent-gateway.ts` declares `new sst.cloudflare.Worker(...)`. Add it, with the DO bindings + service bindings to `cloud-web-worker` (for the dedupe broker), and the `RELAYFILE_*` envs needed by `WorkspaceWatchSubscriber`.\n2. **Move matching helpers to a shared package.** Either:\n   - Export `agentMatchesEvent` from `packages/core/proactive-runtime/match.ts` and import from both `cloud-web-worker` (inline dispatch) and `agent-gateway` (vfs-watch).\n   - Or service-bind `cloud-web-worker` from `agent-gateway` and call the existing module across the boundary. The first option is simpler.\n3. **Wire dedupe service binding.** `cloud-web-worker` already imports the gateway URL; add a service binding so `dispatchIntegrationWatchEventBestEffort` can check/record dedupe before calling `deliverDeploymentTrigger`.\n4. **Implement `onVfsWatchEvent`.** Push endpoint on the gateway DO (HMAC-authed by `RELAYFILE_INTERNAL_HMAC_SECRET`), as defined in §Trigger surface. Relayfile owns retry on push failure.\n5. **Roll out dual-dispatch (Phase A above).** Observe `vfs-watch-dispatch` logs, confirm dedupe coverage matches inline-dispatch coverage 1:1.\n6. **Downgrade inline dispatch (Phase B).** After ~one week of dual-dispatch with healthy dedupe, change `dispatchIntegrationWatchEventBestEffort` to emit a metrics-only record (no `deliverDeploymentTrigger`). Both surfaces still log; only VFS-watch invokes agents.\n7. **Document replay tool**. Spec it separately, but the dispatcher's design (dedupe on `writeId`, agent-side delivery is idempotent because `deliverDeploymentTrigger` uses the deployment row state) enables an operator command of the form `relayfile admin replay --workspace ... --since ... --globs ...` that re-emits a window of writes back through the VFS-watch stream.\n\n## Test plan\n\n- [ ] Unit: `onVfsWatchEvent` dedupe behaviour across `(first | second | third)` deliveries with the same `writeId`\n- [ ] Unit: matched / unmatched candidate filtering (mirror the existing `integration-watch-dispatcher.test.ts`)\n- [ ] Integration: deploy a persona watching `/github/issues/opened/**`, write a file directly to the workspace VFS via the admin SDK (no webhook involved). Expect `vfs-watch-dispatch` to dispatch and the persona to fire.\n- [ ] Integration: same persona, this time produce the event via a real GitHub webhook. Expect exactly one `deliverDeploymentTrigger` call (dedupe collapses inline + VFS-watch).\n- [ ] Observability: CF logs query for `area: \"vfs-watch-dispatch\"` shows `matched`/`delivered`/`failed`/`dedupe` fields and `workspaceId` for each event.\n- [ ] Failure: stub Daytona to throw; confirm the dispatcher records the failure but does *not* mark the writeId as delivered, so the next VFS event with the same id retries.\n\n## Target Context\n\nSee .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json", content: "{\n  \"description\": \"# VFS-Watch Dispatcher\\n\\nStatus: ready for implementation\\nOwner: proactive-runtime\\nTracks: agent-gateway deploy (#827) · integration dispatcher (#855 follow-up)\\n\\n## Why this spec exists\\n\\nThe proactive runtime currently has **two conceptual dispatch paths**, and only one of them is shipping:\\n\\n1. **Synchronous inline dispatch** (live, since #855). Every provider's `handle<Provider>Forward` in `packages/web/lib/integrations/nango-webhook-router.ts` calls `dispatchIntegrationWatchEventBestEffort(...)` immediately after triggering the Nango sync. The dispatcher resolves matching agents by `watch_globs ∩ paths` and invokes `deliverDeploymentTrigger` to spawn a Daytona sandbox.\\n2. **VFS-watch dispatch** (designed, not deployed). `cloud/services/agent-gateway/src/watch-subscriber.ts` (`WorkspaceWatchSubscriber` + `matchesAnyGlob`) was built to subscribe to Relayfile filesystem watch events, match each write against `agents.watch_globs`, and dispatch via the Durable Object gateway. The worker has no SST infra file and isn't deployed in the production CF account.\\n\\nPath 1 is the minimum that gets event-driven personas firing today. Path 2 is the durable, general-purpose substrate the runtime was originally specced against. This document defines what shipping path 2 looks like, why it matters beyond what path 1 already covers, and how the two coexist during the migration.\\n\\n## What path 1 doesn't cover\\n\\nThe inline dispatch only fires for events that pass through `nango-webhook-router.ts`. Concretely, that excludes:\\n\\n- **Non-webhook VFS writes.** Bulk sync results, replay/backfill jobs, `npx workforce admin` operations, and any future direct-write surface (e.g. Relayfile SDK write from a sibling worker) never invoke the inline handler. Agents watching `/<provider>/...` paths are not invoked.\\n- **Cross-provider watch globs.** An agent declaring `watch_globs: [\\\"/**/issues/opened/**\\\"]` (provider-agnostic; meant to watch issues from any wired integration) gets dispatched only by the specific provider handler that knows about issues. Adding a new provider means touching every cross-cutting persona's wiring assumption.\\n- **Failure isolation.** If `handleGitHubForward` throws after the Nango sync but before `dispatchIntegrationWatchEventBestEffort` (or the dispatcher itself throws), the agent never fires *and* there is no durable record of \\\"event happened, dispatch owed.\\\" VFS-watch consumes from a stream that survives handler crashes.\\n- **Replay.** Operators cannot replay a window of historical events (post-incident, after a persona is added that should have caught past events, etc.). VFS rewind + re-emit is the natural way to do it.\\n- **Multi-consumer fan-out.** Today, only the inline dispatcher consumes the event. Future consumers (metrics, audit, secondary projections, sage) each need their own hook in `handleForward`. A VFS-watch stream lets any number of subscribers attach without modifying the producer.\\n\\nPath 1 is \\\"good enough\\\" for the demo loop. Path 2 is the substrate the runtime architecture documents (`docs/proactive-runtime/spec.md`, `docs/proactive-runtime/event-shape.md`) assume.\\n\\n## Goals\\n\\n- **G1**: A persona with `watch_globs: [\\\"/github/issues/opened/**\\\"]` fires when *any* writer (Nango sync, admin SDK, replay tool, manual write) creates a matching file in Relayfile VFS — not only when a webhook flows through the provider's `handleForward`.\\n- **G2**: Inline dispatch (path 1) does not regress during the migration; existing personas keep firing through the same `deliverDeploymentTrigger` they use today.\\n- **G3**: Exactly-once delivery for the agent perspective. If both inline and VFS-watch dispatch resolve the same `(workspace, agentId, eventId)`, only one Daytona spawn happens.\\n- **G4**: Operator-visible. A given write should be traceable: did it fan out? to which agents? did each delivery succeed? was it deduped? Surfaced in CF Observability and Workers Logs, not buried in a Durable Object.\\n\\n## Non-goals\\n\\n- Replacing Relayfile VFS itself. The dispatcher consumes the existing watch stream; the spec assumes Relayfile is already the source of truth.\\n- Replacing `deliverDeploymentTrigger`. That function stays the single agent invocation primitive — the dispatcher only changes *who calls it and on what trigger*.\\n- Solving the agent-gateway `WorkspaceGatewayDO` cron-tick path. That hop is unblocked by the daytona-auth fix; this spec is scoped to the VFS-watch path.\\n\\n## Design\\n\\n### Components\\n\\n```\\n┌──────────────────┐\\n│ Relayfile (VFS)  │   --- write event ---->  ┌─────────────────────────┐\\n│                  │                          │ agent-gateway worker    │\\n└──────────────────┘                          │  (Cloudflare Worker)    │\\n       ▲                                      │                         │\\n       │ writes from any source:              │  WorkspaceWatchSubscriber\\n       │   - nango syncs                      │    - dedupe (workspace, │\\n       │   - admin SDK                        │      path, version)     │\\n       │   - replay tool                      │    - matchesAnyGlob     │\\n       │   - sage / sibling workers           │    - fan out to agents  │\\n       │                                      │                         │\\n       │                                      │  ↓ for each matched     │\\n       │                                      │                         │\\n       │                                      │  deliverDeploymentTrigger\\n       │                                      │    (shared with inline +│\\n       │                                      │     cron path)          │\\n       │                                      │       ↓                 │\\n       │                                      │  Daytona sandbox runs   │\\n       │                                      │  persona.onEvent        │\\n       └──────────────────────────────────────┴─────────────────────────┘\\n```\\n\\n### Trigger surface\\n\\nThe agent-gateway worker exposes a single durable-object-backed entrypoint that consumes Relayfile watch events via **push delivery**: Relayfile POSTs `{ workspace, path, writeId, occurredAt, provider?, eventType?, payload? }` to an authenticated endpoint on agent-gateway whenever a write completes. The endpoint is HMAC-authenticated using `RELAYFILE_INTERNAL_HMAC_SECRET` (the same secret pattern used by other internal worker-to-worker calls).\\n\\nPush was chosen over pull/WebSocket for three reasons: (1) lowest latency — no per-workspace cursor round-trip; (2) operability — every event is a discrete HTTP invocation visible in CF Observability + Workers Logs without DO introspection; (3) Relayfile already has outbound webhook capability, so this reuses an existing path instead of standing up a new stream protocol. The cost is that Relayfile owns retry on push failure — which it already does for its other outbound webhooks.\\n\\n### Dispatcher contract\\n\\n```ts\\ntype VfsWatchEvent = {\\n  workspaceId: string;\\n  path: string;             // e.g. \\\"/github/issues/opened/123-foo.json\\\"\\n  writeId: string;          // monotonic, per-workspace, dedupe key\\n  provider?: string;        // hint when the writer knows it (Nango sync sets this)\\n  eventType?: string;       // hint when the writer knows it (\\\"issues.opened\\\")\\n  payload?: unknown;        // optional, for writers that already have it in hand\\n  occurredAt: string;\\n};\\n\\nasync function onVfsWatchEvent(event: VfsWatchEvent): Promise<VfsDispatchResult> {\\n  // 1. Dedupe by (workspaceId, writeId). DO storage keys: `dedupe:${ws}:${writeId}`.\\n  //    TTL = 24h.\\n  if (await isAlreadyDelivered(event)) return { skipped: \\\"dedupe\\\" };\\n\\n  // 2. Match candidate agents in workspaceId where status='active'\\n  //    AND array_length(watch_globs,1) > 0\\n  //    AND any glob intersects event.path (uses cloud `pathCouldIntersect`).\\n  const candidates = await readCandidateAgents(event.workspaceId);\\n  const matched = candidates.filter(row => agentMatchesEvent(row, event));\\n\\n  if (matched.length === 0) return { matched: 0, delivered: 0, failed: 0 };\\n\\n  // 3. Build payload using the same envelope shape inline dispatch builds.\\n  const payload = buildPayload(event);\\n\\n  // 4. Deliver. Same primitive cron + inline use today. Parallelize across\\n  //    matched agents (same semantics as the existing inline dispatcher's\\n  //    Promise.allSettled fan-out); per-agent serialization is not enforced\\n  //    at the dispatcher layer — Daytona's own sandbox-per-deployment\\n  //    isolation is the right place for any single-agent ordering.\\n  const outcomes = await Promise.allSettled(\\n    matched.map(row => deliverDeploymentTrigger({ workspaceId, agentId: row.id, payload }))\\n  );\\n\\n  // 5. Record dedupe key only on success outcomes; transient failures retry on next event.\\n  await recordDedupe(event);\\n\\n  return { matched: matched.length, ... };\\n}\\n```\\n\\nThe matching logic must reuse the **exact** module that inline dispatch uses (`packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent`) — no parallel reimplementation. The agent-gateway worker imports it directly, or it is moved to `packages/core/proactive-runtime/match.ts` so both workers depend on the same library.\\n\\n### Idempotency / dedupe\\n\\nTwo writers can produce the same effective event:\\n\\n- A Nango forward arrives at `cloud-web-worker`, `handleGitHubForward` triggers a sync and calls `dispatchIntegrationWatchEventBestEffort` inline. The sync also writes to Relayfile VFS, which fires a watch event consumed by agent-gateway.\\n- An admin replay re-writes a VFS file. No inline dispatch fires (no webhook), only the VFS-watch dispatch.\\n\\nTo deliver an agent exactly once, both dispatchers share a **dedupe key**:\\n\\n```\\ndedupe key = sha256(workspaceId : agentId : writeId | deliveryId)\\n```\\n\\nEither dispatcher records the key before invoking `deliverDeploymentTrigger`. A second dispatch that finds the key present logs `skipped=\\\"dedupe\\\"` and short-circuits.\\n\\nThe store is the agent-gateway's DO (strong consistency per workspace). `cloud-web-worker` calls into the DO via service binding named **`AGENT_GATEWAY_DEDUPE_BROKER`** to record/check.\\n\\n**Dedupe TTL: 24h.** Records expire from the DO 24 hours after the originating write. The window is set to:\\n\\n- comfortably cover any same-day replay (operator replays a window post-incident within hours, not days);\\n- bound the DO storage cost (no unbounded growth as workspaces accumulate writes);\\n- be long enough that a same-event redelivery (Nango retry storms, gateway restart, etc.) within a working day is caught.\\n\\n### Coexistence with inline dispatch\\n\\nThe rollout is two phases over one week:\\n\\n**Phase A — dual-dispatch.** Inline (`dispatchIntegrationWatchEventBestEffort` in `nango-webhook-router.ts`) and VFS-watch both fire; the dedupe key collapses double-delivery. Lowest risk to existing event flow; cleanest rollback (revert agent-gateway deploy, inline keeps working).\\n\\n**Phase B — VFS-watch only.** After ~one week of healthy dual-dispatch (verified by `vfs-watch-dispatch` logs showing dedupe coverage matches inline-dispatch coverage 1:1 and `failed=0`), downgrade `dispatchIntegrationWatchEventBestEffort` to a metrics-only no-op. Single dispatcher, one place to reason about ordering, lower duplicate-work cost.\\n\\nPhase A is the default the implementation PR ships. Phase B is a follow-up PR that flips a single function in `nango-webhook-router.ts`.\\n\\n### Failure modes & observability\\n\\nEvery code path emits a single line keyed `area: \\\"vfs-watch-dispatch\\\"`, with at minimum:\\n\\n- `workspaceId`, `path`, `writeId`\\n- `matched` count\\n- `dedupe` outcome (`first` / `skipped`)\\n- `delivered` / `failed` counts\\n- `error` chain (cause-walked, matches the persona-bundle-deploy pattern at `route.ts:560`)\\n\\nWorkers Logs persistent (from #863) is required — these go straight into the same dashboard query operators already use for inline dispatch.\\n\\nFailure cases the spec is opinionated about:\\n\\n- **Daytona spawn fails**: log `failed`, do *not* record dedupe key, allow next event to retry. (Identical to current cron tick behaviour now that #886 is fixed.)\\n- **DB read fails**: return error, do not record dedupe; watch-stream consumer treats as `retry`.\\n- **Watch-stream cursor lag**: the consumer surfaces a `vfs-watch-lag-seconds` metric; alert at >60s sustained.\\n\\n### Boundary cases\\n\\n- **Persona deployed mid-write**. A persona registered at `t1` with watch globs that intersect a write at `t0 < t1` does **not** fire (no backfill on registration). Backfill semantics are out of scope; the replay tool (step 7 of the migration plan) covers operator-opt-in backfill over an explicit window.\\n- **Persona destroyed mid-write**. The agent row's `status` is checked in the candidate query; destroyed agents are filtered. Already correct via the existing `agentMatchesEvent`.\\n- **Watch globs include cross-provider patterns** (`/**/issues/opened/**`). `pathCouldIntersect` already handles glob prefix intersection across multiple providers — covered by the existing helper, no change required.\\n- **No agents match the event**. The dispatcher still runs the candidate query (`matched=0`) and logs the outcome. The query is a single indexed read against `agents.workspace_id`; the cost is negligible at any anticipated scale and the resulting log line is load-bearing for the \\\"did anyone watch this?\\\" diagnostic. No early-out optimization.\\n\\n## Migration plan\\n\\n1. **#827 follow-up: deploy agent-gateway via SST.** Currently `cloud/services/agent-gateway/wrangler.toml` exists but no `infra/agent-gateway.ts` declares `new sst.cloudflare.Worker(...)`. Add it, with the DO bindings + service bindings to `cloud-web-worker` (for the dedupe broker), and the `RELAYFILE_*` envs needed by `WorkspaceWatchSubscriber`.\\n2. **Move matching helpers to a shared package.** Either:\\n   - Export `agentMatchesEvent` from `packages/core/proactive-runtime/match.ts` and import from both `cloud-web-worker` (inline dispatch) and `agent-gateway` (vfs-watch).\\n   - Or service-bind `cloud-web-worker` from `agent-gateway` and call the existing module across the boundary. The first option is simpler.\\n3. **Wire dedupe service binding.** `cloud-web-worker` already imports the gateway URL; add a service binding so `dispatchIntegrationWatchEventBestEffort` can check/record dedupe before calling `deliverDeploymentTrigger`.\\n4. **Implement `onVfsWatchEvent`.** Push endpoint on the gateway DO (HMAC-authed by `RELAYFILE_INTERNAL_HMAC_SECRET`), as defined in §Trigger surface. Relayfile owns retry on push failure.\\n5. **Roll out dual-dispatch (Phase A above).** Observe `vfs-watch-dispatch` logs, confirm dedupe coverage matches inline-dispatch coverage 1:1.\\n6. **Downgrade inline dispatch (Phase B).** After ~one week of dual-dispatch with healthy dedupe, change `dispatchIntegrationWatchEventBestEffort` to emit a metrics-only record (no `deliverDeploymentTrigger`). Both surfaces still log; only VFS-watch invokes agents.\\n7. **Document replay tool**. Spec it separately, but the dispatcher's design (dedupe on `writeId`, agent-side delivery is idempotent because `deliverDeploymentTrigger` uses the deployment row state) enables an operator command of the form `relayfile admin replay --workspace ... --since ... --globs ...` that re-emits a window of writes back through the VFS-watch stream.\\n\\n## Test plan\\n\\n- [ ] Unit: `onVfsWatchEvent` dedupe behaviour across `(first | second | third)` deliveries with the same `writeId`\\n- [ ] Unit: matched / unmatched candidate filtering (mirror the existing `integration-watch-dispatcher.test.ts`)\\n- [ ] Integration: deploy a persona watching `/github/issues/opened/**`, write a file directly to the workspace VFS via the admin SDK (no webhook involved). Expect `vfs-watch-dispatch` to dispatch and the persona to fire.\\n- [ ] Integration: same persona, this time produce the event via a real GitHub webhook. Expect exactly one `deliverDeploymentTrigger` call (dedupe collapses inline + VFS-watch).\\n- [ ] Observability: CF logs query for `area: \\\"vfs-watch-dispatch\\\"` shows `matched`/`delivered`/`failed`/`dedupe` fields and `workspaceId` for each event.\\n- [ ] Failure: stub Daytona to throw; confirm the dispatcher records the failure but does *not* mark the writeId as delivered, so the next VFS event with the same id retries.\",\n  \"desiredAction\": {\n    \"kind\": \"generate\",\n    \"summary\": \"# VFS-Watch Dispatcher Status: ready for implementation Owner: proactive-runtime Tracks: agent-gateway deploy (#827) · integration dispatcher (#855 follow-up) ## Why this spec e...\",\n    \"specText\": \"# VFS-Watch Dispatcher\\n\\nStatus: ready for implementation\\nOwner: proactive-runtime\\nTracks: agent-gateway deploy (#827) · integration dispatcher (#855 follow-up)\\n\\n## Why this spec exists\\n\\nThe proactive runtime currently has **two conceptual dispatch paths**, and only one of them is shipping:\\n\\n1. **Synchronous inline dispatch** (live, since #855). Every provider's `handle<Provider>Forward` in `packages/web/lib/integrations/nango-webhook-router.ts` calls `dispatchIntegrationWatchEventBestEffort(...)` immediately after triggering the Nango sync. The dispatcher resolves matching agents by `watch_globs ∩ paths` and invokes `deliverDeploymentTrigger` to spawn a Daytona sandbox.\\n2. **VFS-watch dispatch** (designed, not deployed). `cloud/services/agent-gateway/src/watch-subscriber.ts` (`WorkspaceWatchSubscriber` + `matchesAnyGlob`) was built to subscribe to Relayfile filesystem watch events, match each write against `agents.watch_globs`, and dispatch via the Durable Object gateway. The worker has no SST infra file and isn't deployed in the production CF account.\\n\\nPath 1 is the minimum that gets event-driven personas firing today. Path 2 is the durable, general-purpose substrate the runtime was originally specced against. This document defines what shipping path 2 looks like, why it matters beyond what path 1 already covers, and how the two coexist during the migration.\\n\\n## What path 1 doesn't cover\\n\\nThe inline dispatch only fires for events that pass through `nango-webhook-router.ts`. Concretely, that excludes:\\n\\n- **Non-webhook VFS writes.** Bulk sync results, replay/backfill jobs, `npx workforce admin` operations, and any future direct-write surface (e.g. Relayfile SDK write from a sibling worker) never invoke the inline handler. Agents watching `/<provider>/...` paths are not invoked.\\n- **Cross-provider watch globs.** An agent declaring `watch_globs: [\\\"/**/issues/opened/**\\\"]` (provider-agnostic; meant to watch issues from any wired integration) gets dispatched only by the specific provider handler that knows about issues. Adding a new provider means touching every cross-cutting persona's wiring assumption.\\n- **Failure isolation.** If `handleGitHubForward` throws after the Nango sync but before `dispatchIntegrationWatchEventBestEffort` (or the dispatcher itself throws), the agent never fires *and* there is no durable record of \\\"event happened, dispatch owed.\\\" VFS-watch consumes from a stream that survives handler crashes.\\n- **Replay.** Operators cannot replay a window of historical events (post-incident, after a persona is added that should have caught past events, etc.). VFS rewind + re-emit is the natural way to do it.\\n- **Multi-consumer fan-out.** Today, only the inline dispatcher consumes the event. Future consumers (metrics, audit, secondary projections, sage) each need their own hook in `handleForward`. A VFS-watch stream lets any number of subscribers attach without modifying the producer.\\n\\nPath 1 is \\\"good enough\\\" for the demo loop. Path 2 is the substrate the runtime architecture documents (`docs/proactive-runtime/spec.md`, `docs/proactive-runtime/event-shape.md`) assume.\\n\\n## Goals\\n\\n- **G1**: A persona with `watch_globs: [\\\"/github/issues/opened/**\\\"]` fires when *any* writer (Nango sync, admin SDK, replay tool, manual write) creates a matching file in Relayfile VFS — not only when a webhook flows through the provider's `handleForward`.\\n- **G2**: Inline dispatch (path 1) does not regress during the migration; existing personas keep firing through the same `deliverDeploymentTrigger` they use today.\\n- **G3**: Exactly-once delivery for the agent perspective. If both inline and VFS-watch dispatch resolve the same `(workspace, agentId, eventId)`, only one Daytona spawn happens.\\n- **G4**: Operator-visible. A given write should be traceable: did it fan out? to which agents? did each delivery succeed? was it deduped? Surfaced in CF Observability and Workers Logs, not buried in a Durable Object.\\n\\n## Non-goals\\n\\n- Replacing Relayfile VFS itself. The dispatcher consumes the existing watch stream; the spec assumes Relayfile is already the source of truth.\\n- Replacing `deliverDeploymentTrigger`. That function stays the single agent invocation primitive — the dispatcher only changes *who calls it and on what trigger*.\\n- Solving the agent-gateway `WorkspaceGatewayDO` cron-tick path. That hop is unblocked by the daytona-auth fix; this spec is scoped to the VFS-watch path.\\n\\n## Design\\n\\n### Components\\n\\n```\\n┌──────────────────┐\\n│ Relayfile (VFS)  │   --- write event ---->  ┌─────────────────────────┐\\n│                  │                          │ agent-gateway worker    │\\n└──────────────────┘                          │  (Cloudflare Worker)    │\\n       ▲                                      │                         │\\n       │ writes from any source:              │  WorkspaceWatchSubscriber\\n       │   - nango syncs                      │    - dedupe (workspace, │\\n       │   - admin SDK                        │      path, version)     │\\n       │   - replay tool                      │    - matchesAnyGlob     │\\n       │   - sage / sibling workers           │    - fan out to agents  │\\n       │                                      │                         │\\n       │                                      │  ↓ for each matched     │\\n       │                                      │                         │\\n       │                                      │  deliverDeploymentTrigger\\n       │                                      │    (shared with inline +│\\n       │                                      │     cron path)          │\\n       │                                      │       ↓                 │\\n       │                                      │  Daytona sandbox runs   │\\n       │                                      │  persona.onEvent        │\\n       └──────────────────────────────────────┴─────────────────────────┘\\n```\\n\\n### Trigger surface\\n\\nThe agent-gateway worker exposes a single durable-object-backed entrypoint that consumes Relayfile watch events via **push delivery**: Relayfile POSTs `{ workspace, path, writeId, occurredAt, provider?, eventType?, payload? }` to an authenticated endpoint on agent-gateway whenever a write completes. The endpoint is HMAC-authenticated using `RELAYFILE_INTERNAL_HMAC_SECRET` (the same secret pattern used by other internal worker-to-worker calls).\\n\\nPush was chosen over pull/WebSocket for three reasons: (1) lowest latency — no per-workspace cursor round-trip; (2) operability — every event is a discrete HTTP invocation visible in CF Observability + Workers Logs without DO introspection; (3) Relayfile already has outbound webhook capability, so this reuses an existing path instead of standing up a new stream protocol. The cost is that Relayfile owns retry on push failure — which it already does for its other outbound webhooks.\\n\\n### Dispatcher contract\\n\\n```ts\\ntype VfsWatchEvent = {\\n  workspaceId: string;\\n  path: string;             // e.g. \\\"/github/issues/opened/123-foo.json\\\"\\n  writeId: string;          // monotonic, per-workspace, dedupe key\\n  provider?: string;        // hint when the writer knows it (Nango sync sets this)\\n  eventType?: string;       // hint when the writer knows it (\\\"issues.opened\\\")\\n  payload?: unknown;        // optional, for writers that already have it in hand\\n  occurredAt: string;\\n};\\n\\nasync function onVfsWatchEvent(event: VfsWatchEvent): Promise<VfsDispatchResult> {\\n  // 1. Dedupe by (workspaceId, writeId). DO storage keys: `dedupe:${ws}:${writeId}`.\\n  //    TTL = 24h.\\n  if (await isAlreadyDelivered(event)) return { skipped: \\\"dedupe\\\" };\\n\\n  // 2. Match candidate agents in workspaceId where status='active'\\n  //    AND array_length(watch_globs,1) > 0\\n  //    AND any glob intersects event.path (uses cloud `pathCouldIntersect`).\\n  const candidates = await readCandidateAgents(event.workspaceId);\\n  const matched = candidates.filter(row => agentMatchesEvent(row, event));\\n\\n  if (matched.length === 0) return { matched: 0, delivered: 0, failed: 0 };\\n\\n  // 3. Build payload using the same envelope shape inline dispatch builds.\\n  const payload = buildPayload(event);\\n\\n  // 4. Deliver. Same primitive cron + inline use today. Parallelize across\\n  //    matched agents (same semantics as the existing inline dispatcher's\\n  //    Promise.allSettled fan-out); per-agent serialization is not enforced\\n  //    at the dispatcher layer — Daytona's own sandbox-per-deployment\\n  //    isolation is the right place for any single-agent ordering.\\n  const outcomes = await Promise.allSettled(\\n    matched.map(row => deliverDeploymentTrigger({ workspaceId, agentId: row.id, payload }))\\n  );\\n\\n  // 5. Record dedupe key only on success outcomes; transient failures retry on next event.\\n  await recordDedupe(event);\\n\\n  return { matched: matched.length, ... };\\n}\\n```\\n\\nThe matching logic must reuse the **exact** module that inline dispatch uses (`packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent`) — no parallel reimplementation. The agent-gateway worker imports it directly, or it is moved to `packages/core/proactive-runtime/match.ts` so both workers depend on the same library.\\n\\n### Idempotency / dedupe\\n\\nTwo writers can produce the same effective event:\\n\\n- A Nango forward arrives at `cloud-web-worker`, `handleGitHubForward` triggers a sync and calls `dispatchIntegrationWatchEventBestEffort` inline. The sync also writes to Relayfile VFS, which fires a watch event consumed by agent-gateway.\\n- An admin replay re-writes a VFS file. No inline dispatch fires (no webhook), only the VFS-watch dispatch.\\n\\nTo deliver an agent exactly once, both dispatchers share a **dedupe key**:\\n\\n```\\ndedupe key = sha256(workspaceId : agentId : writeId | deliveryId)\\n```\\n\\nEither dispatcher records the key before invoking `deliverDeploymentTrigger`. A second dispatch that finds the key present logs `skipped=\\\"dedupe\\\"` and short-circuits.\\n\\nThe store is the agent-gateway's DO (strong consistency per workspace). `cloud-web-worker` calls into the DO via service binding named **`AGENT_GATEWAY_DEDUPE_BROKER`** to record/check.\\n\\n**Dedupe TTL: 24h.** Records expire from the DO 24 hours after the originating write. The window is set to:\\n\\n- comfortably cover any same-day replay (operator replays a window post-incident within hours, not days);\\n- bound the DO storage cost (no unbounded growth as workspaces accumulate writes);\\n- be long enough that a same-event redelivery (Nango retry storms, gateway restart, etc.) within a working day is caught.\\n\\n### Coexistence with inline dispatch\\n\\nThe rollout is two phases over one week:\\n\\n**Phase A — dual-dispatch.** Inline (`dispatchIntegrationWatchEventBestEffort` in `nango-webhook-router.ts`) and VFS-watch both fire; the dedupe key collapses double-delivery. Lowest risk to existing event flow; cleanest rollback (revert agent-gateway deploy, inline keeps working).\\n\\n**Phase B — VFS-watch only.** After ~one week of healthy dual-dispatch (verified by `vfs-watch-dispatch` logs showing dedupe coverage matches inline-dispatch coverage 1:1 and `failed=0`), downgrade `dispatchIntegrationWatchEventBestEffort` to a metrics-only no-op. Single dispatcher, one place to reason about ordering, lower duplicate-work cost.\\n\\nPhase A is the default the implementation PR ships. Phase B is a follow-up PR that flips a single function in `nango-webhook-router.ts`.\\n\\n### Failure modes & observability\\n\\nEvery code path emits a single line keyed `area: \\\"vfs-watch-dispatch\\\"`, with at minimum:\\n\\n- `workspaceId`, `path`, `writeId`\\n- `matched` count\\n- `dedupe` outcome (`first` / `skipped`)\\n- `delivered` / `failed` counts\\n- `error` chain (cause-walked, matches the persona-bundle-deploy pattern at `route.ts:560`)\\n\\nWorkers Logs persistent (from #863) is required — these go straight into the same dashboard query operators already use for inline dispatch.\\n\\nFailure cases the spec is opinionated about:\\n\\n- **Daytona spawn fails**: log `failed`, do *not* record dedupe key, allow next event to retry. (Identical to current cron tick behaviour now that #886 is fixed.)\\n- **DB read fails**: return error, do not record dedupe; watch-stream consumer treats as `retry`.\\n- **Watch-stream cursor lag**: the consumer surfaces a `vfs-watch-lag-seconds` metric; alert at >60s sustained.\\n\\n### Boundary cases\\n\\n- **Persona deployed mid-write**. A persona registered at `t1` with watch globs that intersect a write at `t0 < t1` does **not** fire (no backfill on registration). Backfill semantics are out of scope; the replay tool (step 7 of the migration plan) covers operator-opt-in backfill over an explicit window.\\n- **Persona destroyed mid-write**. The agent row's `status` is checked in the candidate query; destroyed agents are filtered. Already correct via the existing `agentMatchesEvent`.\\n- **Watch globs include cross-provider patterns** (`/**/issues/opened/**`). `pathCouldIntersect` already handles glob prefix intersection across multiple providers — covered by the existing helper, no change required.\\n- **No agents match the event**. The dispatcher still runs the candidate query (`matched=0`) and logs the outcome. The query is a single indexed read against `agents.workspace_id`; the cost is negligible at any anticipated scale and the resulting log line is load-bearing for the \\\"did anyone watch this?\\\" diagnostic. No early-out optimization.\\n\\n## Migration plan\\n\\n1. **#827 follow-up: deploy agent-gateway via SST.** Currently `cloud/services/agent-gateway/wrangler.toml` exists but no `infra/agent-gateway.ts` declares `new sst.cloudflare.Worker(...)`. Add it, with the DO bindings + service bindings to `cloud-web-worker` (for the dedupe broker), and the `RELAYFILE_*` envs needed by `WorkspaceWatchSubscriber`.\\n2. **Move matching helpers to a shared package.** Either:\\n   - Export `agentMatchesEvent` from `packages/core/proactive-runtime/match.ts` and import from both `cloud-web-worker` (inline dispatch) and `agent-gateway` (vfs-watch).\\n   - Or service-bind `cloud-web-worker` from `agent-gateway` and call the existing module across the boundary. The first option is simpler.\\n3. **Wire dedupe service binding.** `cloud-web-worker` already imports the gateway URL; add a service binding so `dispatchIntegrationWatchEventBestEffort` can check/record dedupe before calling `deliverDeploymentTrigger`.\\n4. **Implement `onVfsWatchEvent`.** Push endpoint on the gateway DO (HMAC-authed by `RELAYFILE_INTERNAL_HMAC_SECRET`), as defined in §Trigger surface. Relayfile owns retry on push failure.\\n5. **Roll out dual-dispatch (Phase A above).** Observe `vfs-watch-dispatch` logs, confirm dedupe coverage matches inline-dispatch coverage 1:1.\\n6. **Downgrade inline dispatch (Phase B).** After ~one week of dual-dispatch with healthy dedupe, change `dispatchIntegrationWatchEventBestEffort` to emit a metrics-only record (no `deliverDeploymentTrigger`). Both surfaces still log; only VFS-watch invokes agents.\\n7. **Document replay tool**. Spec it separately, but the dispatcher's design (dedupe on `writeId`, agent-side delivery is idempotent because `deliverDeploymentTrigger` uses the deployment row state) enables an operator command of the form `relayfile admin replay --workspace ... --since ... --globs ...` that re-emits a window of writes back through the VFS-watch stream.\\n\\n## Test plan\\n\\n- [ ] Unit: `onVfsWatchEvent` dedupe behaviour across `(first | second | third)` deliveries with the same `writeId`\\n- [ ] Unit: matched / unmatched candidate filtering (mirror the existing `integration-watch-dispatcher.test.ts`)\\n- [ ] Integration: deploy a persona watching `/github/issues/opened/**`, write a file directly to the workspace VFS via the admin SDK (no webhook involved). Expect `vfs-watch-dispatch` to dispatch and the persona to fire.\\n- [ ] Integration: same persona, this time produce the event via a real GitHub webhook. Expect exactly one `deliverDeploymentTrigger` call (dedupe collapses inline + VFS-watch).\\n- [ ] Observability: CF logs query for `area: \\\"vfs-watch-dispatch\\\"` shows `matched`/`delivered`/`failed`/`dedupe` fields and `workspaceId` for each event.\\n- [ ] Failure: stub Daytona to throw; confirm the dispatcher records the failure but does *not* mark the writeId as delivered, so the next VFS event with the same id retries.\",\n    \"targetFiles\": [\n      \"packages/web/lib/integrations/nango-webhook-router.ts\",\n      \"cloud/services/agent-gateway/src/watch-subscriber.ts\",\n      \"/<provider>/...\",\n      \"docs/proactive-runtime/spec.md\",\n      \"docs/proactive-runtime/event-shape.md\",\n      \"packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent\",\n      \"packages/core/proactive-runtime/match.ts\",\n      \"/**/issues/opened/**\",\n      \"cloud/services/agent-gateway/wrangler.toml\",\n      \"infra/agent-gateway.ts\",\n      \"/github/issues/opened/**\"\n    ]\n  },\n  \"targetFiles\": [\n    \"packages/web/lib/integrations/nango-webhook-router.ts\",\n    \"cloud/services/agent-gateway/src/watch-subscriber.ts\",\n    \"/<provider>/...\",\n    \"docs/proactive-runtime/spec.md\",\n    \"docs/proactive-runtime/event-shape.md\",\n    \"packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent\",\n    \"packages/core/proactive-runtime/match.ts\",\n    \"/**/issues/opened/**\",\n    \"cloud/services/agent-gateway/wrangler.toml\",\n    \"infra/agent-gateway.ts\",\n    \"/github/issues/opened/**\"\n  ],\n  \"constraints\": [\n    {\n      \"constraint\": \"Non-goal: Replacing Relayfile VFS itself. The dispatcher consumes the existing watch stream; the spec assumes Relayfile is already the source of truth.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: Replacing `deliverDeploymentTrigger`. That function stays the single agent invocation primitive — the dispatcher only changes *who calls it and on what trigger*.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: Solving the agent-gateway `WorkspaceGatewayDO` cron-tick path. That hop is unblocked by the daytona-auth fix; this spec is scoped to the VFS-watch path.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"must reuse the **exact** module that inline dispatch uses (`packages/web/lib/proactive-runtime/integration-watch-dispatcher.\",\n      \"category\": \"other\"\n    },\n    {\n      \"constraint\": \"do not record dedupe; watch-stream consumer treats as `retry`.\",\n      \"category\": \"other\"\n    }\n  ],\n  \"evidenceRequirements\": [],\n  \"acceptanceGates\": [],\n  \"executionPreference\": \"local\",\n  \"pattern\": {\n    \"selected\": \"dag\",\n    \"reason\": \"Selected dag using choosing-swarm-patterns because the request is high risk and benefits from parallel implementation, review, and validation gates.\",\n    \"riskLevel\": \"high\",\n    \"specSignals\": [\n      \"many target files\",\n      \"proof or deterministic evidence requested\",\n      \"critical or production constraint\",\n      \"parallel work suggested\",\n      \"choosing-swarm-patterns skill loaded\"\n    ]\n  },\n  \"generatedArtifactsDir\": \".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa\",\n  \"requiredLeadPlanHeadings\": [\n    \"Non-goals\",\n    \"Routing contract\",\n    \"Implementation contract\"\n  ],\n  \"requiredLeadPlanSentinel\": \"GENERATION_LEAD_PLAN_READY\",\n  \"implementationContract\": {\n    \"sourceChangesRequired\": true,\n    \"requireNonEmptyDiffEvidence\": true,\n    \"requireResultOrPrReporting\": true\n  },\n  \"routingContract\": {\n    \"local\": \"Run through Agent Relay using the generated workflow artifact.\",\n    \"cloud\": \"Cloud callers receive the same generated artifact contract unless the normalized spec explicitly requests a separate cloud path.\",\n    \"mcp\": \"Generated runtime agents must not use Relaycast management or messaging tools.\"\n  }\n}" },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/non-goals.md", content: "# Non-goals\n\n- Non-goal: Replacing Relayfile VFS itself. The dispatcher consumes the existing watch stream; the spec assumes Relayfile is already the source of truth.\n- Non-goal: Replacing `deliverDeploymentTrigger`. That function stays the single agent invocation primitive — the dispatcher only changes *who calls it and on what trigger*.\n- Non-goal: Solving the agent-gateway `WorkspaceGatewayDO` cron-tick path. That hop is unblocked by the daytona-auth fix; this spec is scoped to the VFS-watch path." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/deliverables.md", content: "# Deliverables\n\n## Declared File Targets\n\n- packages/web/lib/integrations/nango-webhook-router.ts\n- cloud/services/agent-gateway/src/watch-subscriber.ts\n- /<provider>/...\n- docs/proactive-runtime/spec.md\n- docs/proactive-runtime/event-shape.md\n- packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent\n- packages/core/proactive-runtime/match.ts\n- /**/issues/opened/**\n- cloud/services/agent-gateway/wrangler.toml\n- infra/agent-gateway.ts\n- /github/issues/opened/**\n\n## Output Manifest\n\nDeclared target files define the expected source-change boundary." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md", content: "# Verification Plan\n\nRun or satisfy these verification requirements before signoff:\n\n- file_exists gate for declared targets\n- deterministic sanity gate using POSIX grep, git grep, or an equivalent assertion\n- active-reference gate for deleted manifest paths\n- npx tsc --noEmit\n- npm test --workspace='packages/core' && npm test --workspace='packages/web'\n- git diff gate comparing git diff --name-status against the declared change inventory and requiring a non-empty diff\n- PR URL or explicit result summary\n\nGenerated workflow quality:\n\n- Include a real deterministic sanity gate over produced files, not just prose saying one exists.\n- Prefer POSIX grep, git grep, or a small inline assertion command that exits non-zero when expected content/state is missing.\n- If using rg, guard it with command -v rg and provide a grep or git grep fallback.\n- For cleanup or deletion work, persist a changed-files inventory with statuses, active-reference evidence for deleted paths, and command summaries for final signoff.\n- For cleanup or deletion work, start from .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/cleanup-candidate-prescan.txt and cite that exact path in .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/cleanup-report.md so the evidence trail names its prescan input.\n- Keep each agent step bounded to one coherent slice. Split broad implementation or test-writing work into sequential/fan-out steps with deterministic gates between them instead of relying on a single long agent timeout." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan-instructions.md", content: "# Lead Plan Instructions\n\nPlan the workflow execution from the packaged context files, not from the short task prompt.\n\nRequired sections:\n\n- Non-goals\n- Routing contract\n- Implementation contract\n- Deliverables\n- Verification gates\n\nUse this exact section heading in the lead plan. Do not rename \"Non-goals\" to \"Out of scope\" or another synonym.\n\nWrite .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md and end it with GENERATION_LEAD_PLAN_READY.\n\nGeneration-time skill boundary:\n\n- Read .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json and treat it as generator metadata only.\n- Skills are applied by Ricky during selection, loading, and template rendering.\n- Do not claim generated agents load, retain, or embody skill files at runtime unless a future runtime test proves that path.\n\nLoaded skills summary:\n\nchoosing-swarm-patterns confidence=1 reason=Spec text mentions \"multiple\". Spec text mentions \"agents\". Spec text mentions \"agent\". Spec text mentions \"relay\". Spec text mentions \"right\". Spec text mentions \"pattern\". Spec text mentions \"covers\". Spec text mentions \"core\". Spec text mentions \"patterns\". Spec text mentions \"fan-out\". evidence=keyword:multiple, keyword:agents, keyword:agent, keyword:relay, keyword:right, keyword:pattern, keyword:covers, keyword:core, keyword:patterns, keyword:fan-out\ncomposio-cli confidence=1 reason=Spec text mentions \"help\". Spec text mentions \"find\". Spec text mentions \"right\". Spec text mentions \"tool\". Spec text mentions \"subscribe\". Spec text mentions \"trigger\". Spec text mentions \"events\". Spec text mentions \"call\". Spec text mentions \"authenticated\". Spec text mentions \"known\". Spec text mentions \"flows\". evidence=keyword:help, keyword:find, keyword:right, keyword:tool, keyword:subscribe, keyword:trigger, keyword:events, keyword:call, keyword:authenticated, keyword:known, keyword:flows\nrelay-80-100-workflow confidence=1 reason=Spec text mentions \"must\". Spec text mentions \"before\". Spec text mentions \"covers\". Spec text mentions \"pattern\". Spec text mentions \"beyond\". Spec text mentions \"code\". Spec text mentions \"works\". Spec text mentions \"test\". Spec text mentions \"sandbox\". Spec text mentions \"patterns\". Spec text mentions \"after\". Spec text mentions \"every\". Spec text mentions \"implementation\". Spec text mentions \"through\". evidence=keyword:must, keyword:before, keyword:covers, keyword:pattern, keyword:beyond, keyword:code, keyword:works, keyword:test, keyword:sandbox, keyword:patterns, keyword:after, keyword:every, keyword:implementation, keyword:through\nreview-fix-signoff-loop confidence=1 reason=Spec text mentions \"agent\". Spec text mentions \"relay\". Spec text mentions \"must\". Spec text mentions \"loop\". Spec text mentions \"agents\". Spec text mentions \"both\". Spec text mentions \"work\". Spec text mentions \"covers\". Spec text mentions \"dual\". evidence=keyword:agent, keyword:relay, keyword:must, keyword:loop, keyword:agents, keyword:both, keyword:work, keyword:covers, keyword:dual\nwriting-agent-relay-workflows confidence=1 reason=Spec text mentions \"relay\". Spec text mentions \"covers\". Spec text mentions \"agents\". Spec text mentions \"test\". Spec text mentions \"error\". Spec text mentions \"event\". Spec text mentions \"step\". Spec text mentions \"workers\". Spec text mentions \"parallel\". evidence=keyword:relay, keyword:covers, keyword:agents, keyword:test, keyword:error, keyword:event, keyword:step, keyword:workers, keyword:parallel" },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/implementation-instructions.md", content: "# Implementation Instructions\n\nIMPLEMENTATION_WORKFLOW_CONTRACT:\n\n- For implementation specs, edit source files and produce code changes, not just plan.md, mapping.json, or analysis artifacts.\n- Keep a non-empty implementation diff outside transient artifact directories.\n- Add or update tests that prove the changed behavior.\n- Keep execution routing explicit for local, cloud, and MCP callers.\n- Materialize outputs to disk, then stop for deterministic gates." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-checklist.md", content: "# Review Checklist\n\nAssess:\n\n- Declared file targets and non-goals.\n- Deterministic gates and evidence quality.\n- Review/fix/final-review 80-to-100 loop shape.\n- Local/cloud/MCP routing clarity.\n- Whether source changes, tests, non-empty diff evidence, and PR/result reporting satisfy the implementation contract." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/pattern-decision.txt", content: "pattern=dag; reason=Selected dag using choosing-swarm-patterns because the request is high risk and benefits from parallel implementation, review, and validation gates." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/loaded-skills.txt", content: "choosing-swarm-patterns confidence=1 reason=Spec text mentions \"multiple\". Spec text mentions \"agents\". Spec text mentions \"agent\". Spec text mentions \"relay\". Spec text mentions \"right\". Spec text mentions \"pattern\". Spec text mentions \"covers\". Spec text mentions \"core\". Spec text mentions \"patterns\". Spec text mentions \"fan-out\". evidence=keyword:multiple, keyword:agents, keyword:agent, keyword:relay, keyword:right, keyword:pattern, keyword:covers, keyword:core, keyword:patterns, keyword:fan-out\ncomposio-cli confidence=1 reason=Spec text mentions \"help\". Spec text mentions \"find\". Spec text mentions \"right\". Spec text mentions \"tool\". Spec text mentions \"subscribe\". Spec text mentions \"trigger\". Spec text mentions \"events\". Spec text mentions \"call\". Spec text mentions \"authenticated\". Spec text mentions \"known\". Spec text mentions \"flows\". evidence=keyword:help, keyword:find, keyword:right, keyword:tool, keyword:subscribe, keyword:trigger, keyword:events, keyword:call, keyword:authenticated, keyword:known, keyword:flows\nrelay-80-100-workflow confidence=1 reason=Spec text mentions \"must\". Spec text mentions \"before\". Spec text mentions \"covers\". Spec text mentions \"pattern\". Spec text mentions \"beyond\". Spec text mentions \"code\". Spec text mentions \"works\". Spec text mentions \"test\". Spec text mentions \"sandbox\". Spec text mentions \"patterns\". Spec text mentions \"after\". Spec text mentions \"every\". Spec text mentions \"implementation\". Spec text mentions \"through\". evidence=keyword:must, keyword:before, keyword:covers, keyword:pattern, keyword:beyond, keyword:code, keyword:works, keyword:test, keyword:sandbox, keyword:patterns, keyword:after, keyword:every, keyword:implementation, keyword:through\nreview-fix-signoff-loop confidence=1 reason=Spec text mentions \"agent\". Spec text mentions \"relay\". Spec text mentions \"must\". Spec text mentions \"loop\". Spec text mentions \"agents\". Spec text mentions \"both\". Spec text mentions \"work\". Spec text mentions \"covers\". Spec text mentions \"dual\". evidence=keyword:agent, keyword:relay, keyword:must, keyword:loop, keyword:agents, keyword:both, keyword:work, keyword:covers, keyword:dual\nwriting-agent-relay-workflows confidence=1 reason=Spec text mentions \"relay\". Spec text mentions \"covers\". Spec text mentions \"agents\". Spec text mentions \"test\". Spec text mentions \"error\". Spec text mentions \"event\". Spec text mentions \"step\". Spec text mentions \"workers\". Spec text mentions \"parallel\". evidence=keyword:relay, keyword:covers, keyword:agents, keyword:test, keyword:error, keyword:event, keyword:step, keyword:workers, keyword:parallel" },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-matches.json", content: "[{\"id\":\"choosing-swarm-patterns\",\"name\":\"choosing-swarm-patterns\",\"confidence\":1,\"reason\":\"Spec text mentions \\\"multiple\\\". Spec text mentions \\\"agents\\\". Spec text mentions \\\"agent\\\". Spec text mentions \\\"relay\\\". Spec text mentions \\\"right\\\". Spec text mentions \\\"pattern\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"core\\\". Spec text mentions \\\"patterns\\\". Spec text mentions \\\"fan-out\\\".\",\"evidence\":[{\"trigger\":\"multiple\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"multiple\\\".\"},{\"trigger\":\"agents\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"agents\\\".\"},{\"trigger\":\"agent\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"agent\\\".\"},{\"trigger\":\"relay\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"relay\\\".\"},{\"trigger\":\"right\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"right\\\".\"},{\"trigger\":\"pattern\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"pattern\\\".\"},{\"trigger\":\"covers\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"covers\\\".\"},{\"trigger\":\"core\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"core\\\".\"},{\"trigger\":\"patterns\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"patterns\\\".\"},{\"trigger\":\"fan-out\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"fan-out\\\".\"}]},{\"id\":\"composio-cli\",\"name\":\"composio-cli\",\"confidence\":1,\"reason\":\"Spec text mentions \\\"help\\\". Spec text mentions \\\"find\\\". Spec text mentions \\\"right\\\". Spec text mentions \\\"tool\\\". Spec text mentions \\\"subscribe\\\". Spec text mentions \\\"trigger\\\". Spec text mentions \\\"events\\\". Spec text mentions \\\"call\\\". Spec text mentions \\\"authenticated\\\". Spec text mentions \\\"known\\\". Spec text mentions \\\"flows\\\".\",\"evidence\":[{\"trigger\":\"help\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"help\\\".\"},{\"trigger\":\"find\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"find\\\".\"},{\"trigger\":\"right\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"right\\\".\"},{\"trigger\":\"tool\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"tool\\\".\"},{\"trigger\":\"subscribe\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"subscribe\\\".\"},{\"trigger\":\"trigger\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"trigger\\\".\"},{\"trigger\":\"events\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"events\\\".\"},{\"trigger\":\"call\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"call\\\".\"},{\"trigger\":\"authenticated\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"authenticated\\\".\"},{\"trigger\":\"known\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"known\\\".\"},{\"trigger\":\"flows\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"flows\\\".\"}]},{\"id\":\"relay-80-100-workflow\",\"name\":\"relay-80-100-workflow\",\"confidence\":1,\"reason\":\"Spec text mentions \\\"must\\\". Spec text mentions \\\"before\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"pattern\\\". Spec text mentions \\\"beyond\\\". Spec text mentions \\\"code\\\". Spec text mentions \\\"works\\\". Spec text mentions \\\"test\\\". Spec text mentions \\\"sandbox\\\". Spec text mentions \\\"patterns\\\". Spec text mentions \\\"after\\\". Spec text mentions \\\"every\\\". Spec text mentions \\\"implementation\\\". Spec text mentions \\\"through\\\".\",\"evidence\":[{\"trigger\":\"must\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"must\\\".\"},{\"trigger\":\"before\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"before\\\".\"},{\"trigger\":\"covers\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"covers\\\".\"},{\"trigger\":\"pattern\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"pattern\\\".\"},{\"trigger\":\"beyond\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"beyond\\\".\"},{\"trigger\":\"code\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"code\\\".\"},{\"trigger\":\"works\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"works\\\".\"},{\"trigger\":\"test\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"test\\\".\"},{\"trigger\":\"sandbox\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"sandbox\\\".\"},{\"trigger\":\"patterns\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"patterns\\\".\"},{\"trigger\":\"after\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"after\\\".\"},{\"trigger\":\"every\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"every\\\".\"},{\"trigger\":\"implementation\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"implementation\\\".\"},{\"trigger\":\"through\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"through\\\".\"}]},{\"id\":\"review-fix-signoff-loop\",\"name\":\"review-fix-signoff-loop\",\"confidence\":1,\"reason\":\"Spec text mentions \\\"agent\\\". Spec text mentions \\\"relay\\\". Spec text mentions \\\"must\\\". Spec text mentions \\\"loop\\\". Spec text mentions \\\"agents\\\". Spec text mentions \\\"both\\\". Spec text mentions \\\"work\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"dual\\\".\",\"evidence\":[{\"trigger\":\"agent\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"agent\\\".\"},{\"trigger\":\"relay\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"relay\\\".\"},{\"trigger\":\"must\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"must\\\".\"},{\"trigger\":\"loop\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"loop\\\".\"},{\"trigger\":\"agents\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"agents\\\".\"},{\"trigger\":\"both\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"both\\\".\"},{\"trigger\":\"work\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"work\\\".\"},{\"trigger\":\"covers\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"covers\\\".\"},{\"trigger\":\"dual\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"dual\\\".\"}]},{\"id\":\"writing-agent-relay-workflows\",\"name\":\"writing-agent-relay-workflows\",\"confidence\":1,\"reason\":\"Spec text mentions \\\"relay\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"agents\\\". Spec text mentions \\\"test\\\". Spec text mentions \\\"error\\\". Spec text mentions \\\"event\\\". Spec text mentions \\\"step\\\". Spec text mentions \\\"workers\\\". Spec text mentions \\\"parallel\\\".\",\"evidence\":[{\"trigger\":\"relay\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"relay\\\".\"},{\"trigger\":\"covers\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"covers\\\".\"},{\"trigger\":\"agents\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"agents\\\".\"},{\"trigger\":\"test\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"test\\\".\"},{\"trigger\":\"error\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"error\\\".\"},{\"trigger\":\"event\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"event\\\".\"},{\"trigger\":\"step\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"step\\\".\"},{\"trigger\":\"workers\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"workers\\\".\"},{\"trigger\":\"parallel\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"parallel\\\".\"}]}]" },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/tool-selection.json", content: "[{\"stepId\":\"lead-plan\",\"agent\":\"lead-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"implement-artifact\",\"agent\":\"impl-primary-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"review-claude\",\"agent\":\"reviewer-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"fix-loop\",\"agent\":\"validator-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-review-claude\",\"agent\":\"reviewer-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-fix-claude\",\"agent\":\"validator-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"review-codex\",\"agent\":\"reviewer-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"fix-loop-codex\",\"agent\":\"validator-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-review-codex\",\"agent\":\"reviewer-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-fix-codex\",\"agent\":\"validator-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-signoff\",\"agent\":\"validator-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"}]" },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json", content: "{\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"boundary\":\"Skills influence Ricky generator selection, loading, template rendering, workflow contract, validation gates, and metadata. Generated runtime agents receive only the rendered workflow instructions; they do not load or embody skill files at runtime.\",\"loadedSkills\":[\"choosing-swarm-patterns\",\"composio-cli\",\"relay-80-100-workflow\",\"review-fix-signoff-loop\",\"writing-agent-relay-workflows\"],\"applicationEvidence\":[{\"skillName\":\"choosing-swarm-patterns\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected choosing-swarm-patterns during workflow generation. Spec text mentions \\\"multiple\\\". Spec text mentions \\\"agents\\\". Spec text mentions \\\"agent\\\". Spec text mentions \\\"relay\\\". Spec text mentions \\\"right\\\". Spec text mentions \\\"pattern\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"core\\\". Spec text mentions \\\"patterns\\\". Spec text mentions \\\"fan-out\\\".\"},{\"skillName\":\"choosing-swarm-patterns\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded choosing-swarm-patterns descriptor before template rendering.\"},{\"skillName\":\"composio-cli\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected composio-cli during workflow generation. Spec text mentions \\\"help\\\". Spec text mentions \\\"find\\\". Spec text mentions \\\"right\\\". Spec text mentions \\\"tool\\\". Spec text mentions \\\"subscribe\\\". Spec text mentions \\\"trigger\\\". Spec text mentions \\\"events\\\". Spec text mentions \\\"call\\\". Spec text mentions \\\"authenticated\\\". Spec text mentions \\\"known\\\". Spec text mentions \\\"flows\\\".\"},{\"skillName\":\"composio-cli\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded composio-cli descriptor before template rendering.\"},{\"skillName\":\"relay-80-100-workflow\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected relay-80-100-workflow during workflow generation. Spec text mentions \\\"must\\\". Spec text mentions \\\"before\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"pattern\\\". Spec text mentions \\\"beyond\\\". Spec text mentions \\\"code\\\". Spec text mentions \\\"works\\\". Spec text mentions \\\"test\\\". Spec text mentions \\\"sandbox\\\". Spec text mentions \\\"patterns\\\". Spec text mentions \\\"after\\\". Spec text mentions \\\"every\\\". Spec text mentions \\\"implementation\\\". Spec text mentions \\\"through\\\".\"},{\"skillName\":\"relay-80-100-workflow\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded relay-80-100-workflow descriptor before template rendering.\"},{\"skillName\":\"review-fix-signoff-loop\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected review-fix-signoff-loop during workflow generation. Spec text mentions \\\"agent\\\". Spec text mentions \\\"relay\\\". Spec text mentions \\\"must\\\". Spec text mentions \\\"loop\\\". Spec text mentions \\\"agents\\\". Spec text mentions \\\"both\\\". Spec text mentions \\\"work\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"dual\\\".\"},{\"skillName\":\"review-fix-signoff-loop\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded review-fix-signoff-loop descriptor before template rendering.\"},{\"skillName\":\"writing-agent-relay-workflows\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected writing-agent-relay-workflows during workflow generation. Spec text mentions \\\"relay\\\". Spec text mentions \\\"covers\\\". Spec text mentions \\\"agents\\\". Spec text mentions \\\"test\\\". Spec text mentions \\\"error\\\". Spec text mentions \\\"event\\\". Spec text mentions \\\"step\\\". Spec text mentions \\\"workers\\\". Spec text mentions \\\"parallel\\\".\"},{\"skillName\":\"writing-agent-relay-workflows\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded writing-agent-relay-workflows descriptor before template rendering.\"},{\"skillName\":\"choosing-swarm-patterns\",\"stage\":\"generation_rendering\",\"effect\":\"pattern_selection\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Rendered the selected swarm pattern into the workflow builder so Ricky chooses the coordination shape before authoring tasks.\"},{\"skillName\":\"writing-agent-relay-workflows\",\"stage\":\"generation_rendering\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Rendered 12 workflow tasks with dedicated channel setup, explicit agents, step dependencies, review stages, and final signoff.\"},{\"skillName\":\"relay-80-100-workflow\",\"stage\":\"generation_rendering\",\"effect\":\"validation_gates\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Rendered 14 deterministic gates including initial soft validation, fix-loop checks, final hard validation, git diff, and regression gates.\"},{\"skillName\":\"review-fix-signoff-loop\",\"stage\":\"generation_rendering\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Rendered dual-reviewer review-fix-signoff loop with 6 reviewer/fix tasks, repairable post-fix re-review, and final signoff so the workflow exits only on independent Claude and Codex agreement.\"}]}" },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-runtime-boundary.txt", content: "Skills influence Ricky generator selection, loading, template rendering, workflow contract, validation gates, and metadata. Generated runtime agents receive only the rendered workflow instructions; they do not load or embody skill files at runtime." },
    { path: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/matched-skills.md", content: "\n# choosing-swarm-patterns\nreason=Spec text mentions \"multiple\". Spec text mentions \"agents\". Spec text mentions \"agent\". Spec text mentions \"relay\". Spec text mentions \"right\". Spec text mentions \"pattern\". Spec text mentions \"covers\". Spec text mentions \"core\". Spec text mentions \"patterns\". Spec text mentions \"fan-out\".\n---\nname: choosing-swarm-patterns\ndescription: Use when coordinating multiple AI agents with Agent Relay's workflow engine and need to pick the right orchestration pattern - covers the 10 core patterns (fan-out, pipeline, hub-spoke, consensus, mesh, handoff, cascade, dag, debate, hierarchical) plus 14 specialized ones, with decision framework and accurate SDK/YAML examples.\n---\n\n### Overview\n\nThe Agent Relay SDK (`@agent-relay/sdk`) supports 24 swarm patterns via a single `swarm.pattern` field. Patterns are configured declaratively in YAML or programmatically via the `workflow()` fluent builder — there are no standalone `fanOut(...)` / `hubAndSpoke(...)` helpers. Pick the simplest pattern that solves the problem; add complexity only when the system proves it's insufficient.\n\n### Two ways to run a pattern\n\n#### **1. YAML (portable):**\n\n```ts\nimport { runWorkflow } from \"@relayflows/core\";\n\nconst run = await runWorkflow(\"workflows/feature-dev.yaml\", {\n  vars: { task: \"Add OAuth login\" },\n});\n```\n\n\n### Quick Decision Framework\n\n#### ```\n\n```\nIs the task independent per agent?\n  YES → fan-out (parallel workers, hub collects)\n\nDoes each step need the previous step's output?\n  YES → Is it strictly linear?\n    YES → pipeline\n    NO  → dag (parallel where possible, `dependsOn` edges)\n\nDoes a coordinator need to stay alive and adapt?\n  YES → hub-spoke (single-level hub + workers)\n        hierarchical (structurally identical in current impl; use for naming/intent)\n\nIs the task about making a decision?\n  YES → Do agents need to argue opposing sides?\n    YES → debate (adversarial, full mesh)\n    NO  → consensus (cooperative, full mesh + coordination.consensusStrategy)\n\nDoes the right specialist emerge during processing?\n  YES → handoff (sequential chain, one active at a time)\n\nDo all agents need to freely collaborate?\n  YES → mesh (full peer-to-peer edges)\n\nIs cost the primary concern?\n  YES → cascade (chain of increasingly capable agents; each step's prompt\n        decides whether to pass through or redo the prior output)\n```\n\n\n### Pattern Reference (Core 10)\n\n| # | Pattern | Topology (actual edges) | Best For |\n|---|---------|------------------------|----------|\n| 1 | **fan-out** | Hub broadcasts to N workers; workers reply to hub only | Independent subtasks (reviews, research, tests) |\n| 2 | **pipeline** | Linear chain (agent_i → agent_{i+1}) | Ordered stages (design → implement → test) |\n| 3 | **hub-spoke** | Hub ↔ spokes (bidirectional); no spoke-to-spoke | Dynamic coordination, lead reviews/adjusts |\n| 4 | **consensus** | Full mesh; decision via `coordination.consensusStrategy` | Architecture decisions, approval gates |\n| 5 | **mesh** | Full mesh (every agent ↔ every other) | Brainstorming, collaborative debugging |\n| 6 | **handoff** | Chain; passes control forward | Triage, specialist routing |\n| 7 | **cascade** | Chain of `dependsOn` steps; all run on success, downstream skipped on upstream failure (no built-in \"fall through\") | Cost optimization: cheap first, each step's prompt passes through or redoes |\n| 8 | **dag** | Edges from step `dependsOn` | Mixed dependencies, parallel where possible |\n| 9 | **debate** | Full mesh (same topology as mesh; roles drive behavior) | Rigorous adversarial examination |\n| 10 | **hierarchical** | Hub + subordinates (single-level in current impl) | Large teams; semantic distinction from hub-spoke |\n\n> **Heads up:** `hierarchical` resolves to the same edge structure as `hub-spoke` in `coordinator.ts:313-319`. Multi-level tree topology is not currently implemented — use pattern name for intent, but expect the same runtime graph.\n\n### Additional Patterns (role-driven)\n\nThese 14 additional patterns exist in `SwarmPattern` (types.ts:114-139). The coordinator has role-based auto-selection heuristics (`coordinator.ts:51-165`), but they only fire when `swarm.pattern` is **omitted** — YAML validation requires it (`runner.ts:2105-2117`), so auto-selection is effectively a programmatic-API feature. In YAML, set `swarm.pattern` explicitly.\n\nTopology is still resolved per-pattern once selected; the \"Triggering roles\" column reflects what the coordinator looks for to shape edges (per `coordinator.ts:250-450`):\n\n| Pattern | Roles the topology keys off | Topology |\n|---------|-----------------------------|----------|\n| `map-reduce` | `mapper` + `reducer` | coordinator → mappers → reducers → coordinator |\n| `scatter-gather` | — | hub → workers → hub |\n| `supervisor` | `supervisor` | supervisor ↔ workers |\n| `reflection` | `critic` or `reviewer` (auto-select uses `critic` only) | producers → critic → producers (loop) |\n| `red-team` | `attacker`/`red-team` + `defender`/`blue-team` | adversarial mesh with optional judges |\n| `verifier` | `verifier` | producers → verifiers → back to producers |\n| `auction` | `auctioneer` | auctioneer → bidders → auctioneer |\n| `escalation` | `tier-*` | tiered chain, escalate up / report down |\n| `saga` | `saga-orchestrator`, `compensate-handler` | orchestrator ↔ participants |\n| `circuit-breaker` | `primary` + `fallback`/`backup` | try primary, fallback on failure |\n| `blackboard` | `blackboard` / `shared-workspace` | shared state hub |\n| `swarm` | `hive-mind` / `swarm-agent` | stigmergy-style |\n| `competitive` | — (declared explicitly) | independent parallel implementations + judge |\n| `review-loop` | `implement*` + 2+ `reviewer*` | implementer ↔ reviewers |\n\n### Structured Squad Review Loop\n\n- Split the work into bounded implementation squads. Each squad owns a non-overlapping file or subsystem scope.\n- Give each squad an implementer plus a shadow/review partner. The shadow follows the implementer in real time, checks alignment with the spec, and posts concise feedback before the work drifts.\n- Require the implementer to self-reflect before external review: compare the final diff against the spec, AGENTS.md / CLAUDE.md, recent local conventions, tests, and declared non-goals.\n- Run an independent self-review/fresh-eyes agent that reads the actual files and recent repo context, not just the chat transcript.\n- Send that review back to the implementer for one repair round.\n- After squads converge, run a final two-agent review team, usually one Claude reviewer and one Codex reviewer, independently. They compare notes, merge findings, and produce one final verdict.\n- Spawn fresh fix agents for final-review findings. Those fix agents self-reflect, then the final reviewers re-check the post-fix state until the spec is fully satisfied or a blocker is documented.\n- Use `supervisor` or `hub-spoke` when a lead needs to coordinate live squads.\n- Use `review-loop` when the main risk is code quality and feedback iteration.\n- Use `reflection` when critic feedback should loop directly back to producers.\n- Use `verifier` when completion evidence matters more than design debate.\n- Use `competitive` only when independent alternative implementations are useful; otherwise split by ownership scope.\n\n### Pattern Details\n\n#### 1. fan-out — Parallel Workers\n\n```ts\nawait workflow(\"review\")\n  .pattern(\"fan-out\")\n  .agent(\"lead\",     { cli: \"claude\", role: \"lead\" })\n  .agent(\"auth-rev\", { cli: \"claude\", role: \"worker\", interactive: false })\n  .agent(\"db-rev\",   { cli: \"claude\", role: \"worker\", interactive: false })\n  .step(\"review-auth\", { agent: \"auth-rev\", task: \"Review auth.ts\" })\n  .step(\"review-db\",   { agent: \"db-rev\",   task: \"Review db.ts\" })\n  .run();\n```\n\n#### 2. pipeline — Sequential Stages\n\n```yaml\nswarm: { pattern: pipeline }\nagents:\n  - { name: designer,    cli: claude }\n  - { name: implementer, cli: codex, interactive: false }\n  - { name: tester,      cli: codex, interactive: false }\nworkflows:\n  - name: build\n    steps:\n      - { name: design,    agent: designer,    task: \"Design the API schema\",\n          verification: { type: output_contains, value: DONE } }\n      - { name: implement, agent: implementer, dependsOn: [design],\n          task: \"Implement: {{steps.verify.output}}\" }\n      - { name: test,      agent: tester,      dependsOn: [implement],\n          task: \"Write integration tests\" }\n```\n\n#### 3. hub-spoke — Persistent Coordinator\n\n```ts\nawait workflow(\"api-build\")\n  .pattern(\"hub-spoke\")\n  .channel(\"swarm-api\")\n  .agent(\"lead\",       { cli: \"claude\", role: \"lead\" })\n  .agent(\"db-worker\",  { cli: \"claude\", role: \"worker\" })   // interactive by default — hub DMs it\n  .agent(\"api-worker\", { cli: \"claude\", role: \"worker\" })   // interactive by default — hub DMs it\n  .step(\"models\",  { agent: \"db-worker\",  task: \"Build database models\" })\n  .step(\"routes\",  { agent: \"api-worker\", task: \"Build route handlers\", dependsOn: [\"models\"] })\n  .step(\"review\",  { agent: \"lead\",       task: \"Review everything\",    dependsOn: [\"routes\"] })\n  .run();\n```\n\n#### 4. consensus — Cooperative Voting\n\n```yaml\nswarm: { pattern: consensus }\nagents:\n  - { name: perf,  cli: claude, role: reviewer }\n  - { name: dx,    cli: claude, role: reviewer }\n  - { name: sec,   cli: claude, role: reviewer }\ncoordination:\n  consensusStrategy: majority   # declarative marker: majority | unanimous | quorum\n  votingThreshold: 0.66\nworkflows:\n  - name: decide\n    steps:\n      - { name: evaluate-perf, agent: perf, task: \"Evaluate perf of Fastify migration\" }\n      - { name: evaluate-dx,   agent: dx,   task: \"Evaluate DX of Fastify migration\" }\n      - { name: evaluate-sec,  agent: sec,  task: \"Evaluate security of Fastify migration\" }\n```\n\n#### 5. mesh — Peer Collaboration\n\n```ts\nawait workflow(\"debug-auth\")\n  .pattern(\"mesh\")\n  .channel(\"swarm-debug\")\n  .agent(\"logs\",     { cli: \"claude\" })\n  .agent(\"code\",     { cli: \"claude\" })\n  .agent(\"repro\",    { cli: \"claude\" })\n  .step(\"logs\",  { agent: \"logs\",  task: \"Check server logs\" })\n  .step(\"code\",  { agent: \"code\",  task: \"Review auth code\" })\n  .step(\"repro\", { agent: \"repro\", task: \"Write repro test\" })\n  .run();\n```\n\n#### 6. handoff — Dynamic Routing\n\n```yaml\nswarm: { pattern: handoff }\nagents:\n  - { name: triage,   cli: claude }\n  - { name: billing,  cli: claude }\n  - { name: tech,     cli: claude }\nworkflows:\n  - name: support\n    steps:\n      - { name: triage,  agent: triage,  task: \"Triage: {{request}}\" }\n      - { name: billing, agent: billing, dependsOn: [triage], task: \"Handle billing\" }\n      - { name: tech,    agent: tech,    dependsOn: [triage], task: \"Handle tech issues\" }\n```\n\n#### 7. cascade — Cost-Aware Fallthrough\n\n```ts\nawait workflow(\"answer\")\n  .pattern(\"cascade\")\n  .agent(\"haiku\",  { cli: \"claude\", model: \"claude-haiku-4-5-20251001\" })\n  .agent(\"sonnet\", { cli: \"claude\", model: \"claude-sonnet-4-6\" })\n  .agent(\"opus\",   { cli: \"claude\", model: \"claude-opus-4-7\" })\n  .step(\"try-haiku\",  { agent: \"haiku\",  task: \"{{question}}\" })\n  .step(\"try-sonnet\", { agent: \"sonnet\",\n                        task: \"If this is a complete answer, echo it verbatim. Otherwise answer anew:\\n{{steps.try-haiku.output}}\",\n                        dependsOn: [\"try-haiku\"] })\n  .step(\"try-opus\",   { agent: \"opus\",\n                        task: \"Final-tier answer, using prior attempts for context:\\n{{steps.try-sonnet.output}}\",\n                        dependsOn: [\"try-sonnet\"] })\n  .run();\n```\n\n#### 8. dag — Directed Acyclic Graph\n\n```ts\nawait workflow(\"fullstack\")\n  .pattern(\"dag\")\n  .maxConcurrency(3)\n  .agent(\"dev\", { cli: \"codex\", role: \"worker\" })\n  .step(\"scaffold\",  { agent: \"dev\", task: \"Create project scaffold\" })\n  .step(\"frontend\",  { agent: \"dev\", task: \"Build React UI\", dependsOn: [\"scaffold\"] })\n  .step(\"backend\",   { agent: \"dev\", task: \"Build API\",       dependsOn: [\"scaffold\"] })\n  .step(\"integrate\", { agent: \"dev\", task: \"Wire together\",   dependsOn: [\"frontend\", \"backend\"] })\n  .run();\n```\n\n#### 9. debate — Adversarial Refinement\n\n```yaml\nswarm: { pattern: debate }\nagents:\n  - { name: pro,   cli: claude, role: debater, task: \"Argue FOR monorepo\" }\n  - { name: con,   cli: claude, role: debater, task: \"Argue FOR polyrepo\" }\n  - { name: judge, cli: claude, role: judge,   task: \"Decide after 3 rounds\" }\ncoordination:\n  barriers:\n    - { name: debate-done, waitFor: [pro-round-3, con-round-3] }\n```\n\n#### 10. hierarchical — Multi-Level (structurally hub-spoke today)\n\n```ts\nawait workflow(\"large-team\")\n  .pattern(\"hierarchical\")\n  .agent(\"lead\",     { cli: \"claude\", role: \"lead\" })\n  .agent(\"fe-coord\", { cli: \"claude\", role: \"coordinator\" })\n  .agent(\"be-coord\", { cli: \"claude\", role: \"coordinator\" })\n  .agent(\"fe-dev\",   { cli: \"codex\",  role: \"worker\", interactive: false })\n  .agent(\"be-dev\",   { cli: \"codex\",  role: \"worker\", interactive: false })\n  .step(\"plan\",    { agent: \"lead\",     task: \"Coordinate full-stack app\" })\n  .step(\"fe-plan\", { agent: \"fe-coord\", task: \"Manage frontend\",  dependsOn: [\"plan\"] })\n  .step(\"be-plan\", { agent: \"be-coord\", task: \"Manage backend\",   dependsOn: [\"plan\"] })\n  .step(\"fe-impl\", { agent: \"fe-dev\",   task: \"Build components\", dependsOn: [\"fe-plan\"] })\n  .step(\"be-impl\", { agent: \"be-dev\",   task: \"Build API\",        dependsOn: [\"be-plan\"] })\n  .run();\n```\n\n\n### Verification & Completion Signals\n\n#### An agent step can complete in several ways (`runner.ts:5353-5395`, `runner.ts:4527-4538`):\n\n```yaml\nverification:\n  type: output_contains   # or: exit_code | file_exists | custom\n  value: DONE             # or: PLAN_COMPLETE, IMPLEMENTATION_COMPLETE, REVIEW_COMPLETE\n```\n\n\n### Relaycast MCP — Correct Tool Names\n\nThe skill previously referenced `mcp__relaycast__send` / `mcp__relaycast__dm` — those names are wrong. The real tools (the first three are cited in the workflow convention-injection at `relay-adapter.ts:31-35`; the rest are exposed by the live `relaycast` MCP server):\n\n| Purpose | Tool | Source |\n|---------|------|--------|\n| Send DM to another agent | `mcp__relaycast__message_dm_send` | `relay-adapter.ts:31` |\n| Check inbox | `mcp__relaycast__message_inbox_check` | `relay-adapter.ts:35` |\n| List agents | `mcp__relaycast__agent_list` | `relay-adapter.ts:35` |\n| Post to a channel | `mcp__relaycast__message_post` | relaycast MCP server |\n| Reply in a thread | `mcp__relaycast__message_reply` | relaycast MCP server |\n| Spawn sub-agent | `mcp__relaycast__agent_add` | relaycast MCP server |\n| Remove sub-agent | `mcp__relaycast__agent_remove` | relaycast MCP server |\n\n> `interactive: false` agents run as non-interactive subprocesses with no relay connection — they must NOT call any `mcp__relaycast__*` tool (validator warns on this at `validator.ts:138-150`, check `NONINTERACTIVE_RELAY`).\n\n### Reflection (Trajectories)\n\n#### Reflection is **not** a `reflectionThreshold` callback. It's configured via the `trajectories:` block:\n\n```yaml\ntrajectories:\n  enabled: true\n  reflectOnBarriers: true   # config flag exists but runner does NOT currently invoke this path\n  reflectOnConverge: true   # fires at parallel convergence points (runner.ts:2762-2779)\n  autoDecisions: true       # record retry/skip/fail decisions\n```\n\n\n### Common Mistakes\n\n| Mistake | Why It Fails | Fix |\n|---------|-------------|-----|\n| Using mesh/debate for everything | Full-mesh blows up message volume past ~5 agents | Use hub-spoke or dag for most tasks |\n| Pipeline for independent work | Sequential bottleneck | Use fan-out or dag |\n| Hub-spoke for 2 agents | Hub is unnecessary overhead | Use pipeline or fan-out |\n| Expecting `consensusStrategy` to tally votes | Runner has no vote-tally logic; field only affects coordinator auto-selection | Aggregate votes in a judge/lead step that reads `{{steps.plan.output}}` |\n| Handoff with \"routing = skip other branches\" | Skipping only fires on upstream **failure**, not routing decisions | Emit a routing token in triage output; downstream prompts self-no-op if token doesn't match |\n| Cascade expecting skip-on-success | Runner has no cascade skip logic; failed upstream skips downstream | Chain downstream prompts to pass-through or redo based on `{{steps.preflight.output}}` |\n| Relying on `reflectOnBarriers` | Config flag exists but runner never calls it | Use `reflectOnConverge` for convergence reflection; use `reflection` pattern for critic loops |\n| `interactive: false` agent calling MCP | Non-interactive subprocess has no relay | Use `interactive: true` (default) or emit output on stdout |\n| Relying on multi-level `hierarchical` | Topology is single-level hub in current impl | Use pattern for naming; model levels via `dependsOn` graph |\n| Writing `mcp__relaycast__send(...)` | Wrong tool name | Use `mcp__relaycast__message_post` or `message_dm_send` |\n\n### Resume & Re-run\n\n#### ```ts\n\n```ts\n// Resume a failed run:\nawait runWorkflow(\"feature-dev.yaml\", { resume: \"<runId>\" });\n\n// Skip ahead, re-using cached outputs from an earlier run:\nawait runWorkflow(\"feature-dev.yaml\", {\n  startFrom: \"review\",\n  previousRunId: \"<runId>\",\n});\n```\n\n\n### Complete YAML Example\n\n#### ```yaml\n\n```yaml\nversion: \"1.0\"\nname: feature-dev\ndescription: \"Blueprint-style feature development with quality gates.\"\nswarm:\n  pattern: hub-spoke\n  maxConcurrency: 2\n  timeoutMs: 3600000\n  channel: swarm-feature-dev\n  idleNudge: { nudgeAfterMs: 120000, escalateAfterMs: 120000, maxNudges: 1 }\nagents:\n  - { name: lead,      cli: claude, role: lead,     permissions: { access: full } }\n  - { name: planner,   cli: codex,  role: planner,  interactive: false, permissions: { access: readonly } }\n  - { name: developer, cli: codex,  role: worker,   interactive: false, permissions: { access: readwrite } }\n  - { name: reviewer,  cli: claude, role: reviewer, permissions: { access: readonly } }\nworkflows:\n  - name: feature-delivery\n    onError: retry\n    preflight:\n      - { command: \"git status --porcelain\", failIf: non-empty, description: \"Clean worktree\" }\n    steps:\n      - name: plan\n        agent: planner\n        task: \"Plan: {{task}}\"\n        verification: { type: output_contains, value: PLAN_COMPLETE }\n      - name: implement\n        agent: developer\n        dependsOn: [plan]\n        task: \"Implement: {{steps.plan.output}}\"\n        verification: { type: output_contains, value: IMPLEMENTATION_COMPLETE }\n      - name: test\n        type: deterministic\n        dependsOn: [implement]\n        command: npm test\n      - name: review\n        agent: reviewer\n        dependsOn: [test]\n        task: \"Review implementation\"\n        verification: { type: output_contains, value: REVIEW_COMPLETE }\ncoordination:\n  barriers:\n    - { name: delivery-ready, waitFor: [plan, implement, review], timeoutMs: 900000 }\ntrajectories:\n  enabled: true\n  reflectOnBarriers: true\n  reflectOnConverge: true\nerrorHandling:\n  strategy: retry\n  maxRetries: 2\n  retryDelayMs: 5000\n```\n\n\n### Source of Truth\n\n| Claim | File |\n|-------|------|\n| Pattern enum (24 patterns) | `packages/sdk/src/workflows/types.ts:114-139` |\n| Topology resolution per pattern | `packages/sdk/src/workflows/coordinator.ts:240-450` |\n| Interactive-only topology edges | `packages/sdk/src/workflows/coordinator.ts:218-237` |\n| Pattern auto-selection heuristics (programmatic API only) | `packages/sdk/src/workflows/coordinator.ts:51-165` |\n| `WorkflowBuilder` fluent API | `packages/sdk/src/workflows/builder.ts` |\n| `runWorkflow(yamlPath, options)` | `packages/sdk/src/workflows/run.ts` |\n| YAML validation requires `version` + `name` + `swarm.pattern` | `packages/sdk/src/workflows/runner.ts:2105-2117` |\n| MCP tool names cited in convention-injection | `packages/sdk/src/relay-adapter.ts:29-36` |\n| Completion modes (verification / evidence / owner / process-exit) | `packages/sdk/src/workflows/runner.ts:5353-5395`, `4527-4538` |\n| Completion via PTY + summary fallback | `packages/sdk/src/workflows/runner.ts:6600-6615` |\n| Downstream skip on upstream failure (not success) | `packages/sdk/src/workflows/runner.ts:7057-7088`, `step-executor.ts:329-334` |\n| Trajectory reflection (only `reflectOnConverge` wired) | `packages/sdk/src/workflows/runner.ts:2762-2779`, `trajectory.ts:173-190` |\n\n\n# composio-cli\nreason=Spec text mentions \"help\". Spec text mentions \"find\". Spec text mentions \"right\". Spec text mentions \"tool\". Spec text mentions \"subscribe\". Spec text mentions \"trigger\". Spec text mentions \"events\". Spec text mentions \"call\". Spec text mentions \"authenticated\". Spec text mentions \"known\". Spec text mentions \"flows\".\n---\nname: composio-cli\ndescription: Help users operate the published Composio CLI to find the right tool, connect accounts, inspect schemas, execute tools, subscribe to trigger events with `composio listen`, script workflows with `composio run`, and call authenticated app APIs with `composio proxy`. Use when the user asks how to do something with `composio`, wants to run a known tool slug, needs to discover a slug with `composio search`, fix a missing connection with `composio link`, inspect tool inputs with `--get-schema` or `--dry-run`, troubleshoot top-level CLI flows, or explicitly needs `composio dev` guidance.\n---\n\n<!-- AUTO-GENERATED: edit skills-src/composio-cli/index.ts and rebuild -->\n<!-- release-channel: stable -->\n\n# Composio CLI\n\n## Default Workflow\n\n1. Start with `composio execute <slug>` whenever the slug is known.\n2. If several independent tool calls must happen at once, use `composio execute -p/--parallel` with repeated `<slug> -d <json>` groups.\n3. If `execute` says the toolkit is not connected, run `composio link <toolkit>` and retry.\n4. If the arguments are unclear, run `composio execute <slug> --get-schema` or `--dry-run` before guessing.\n5. Reach for `composio search \"<task>\"` only when the slug is unknown. `search` accepts one or more queries, so batch related discovery work into a single command when useful.\n\n## `execute` - Run A Tool\n\nUse `execute` when the tool slug is already known.\n\n```bash\ncomposio execute GITHUB_GET_THE_AUTHENTICATED_USER -d '{}'\n```\n\nInspect required inputs without executing:\n```bash\ncomposio execute GITHUB_CREATE_AN_ISSUE --get-schema\n```\n\nPreview safely:\n```bash\ncomposio execute GITHUB_CREATE_AN_ISSUE --skip-connection-check --dry-run -d '{ owner: \"acme\", repo: \"app\", title: \"Bug report\", body: \"Steps to reproduce...\" }'\n```\n\nPass data from a file or stdin:\n```bash\ncomposio execute GITHUB_CREATE_AN_ISSUE -d @issue.json\ncat issue.json | composio execute GITHUB_CREATE_AN_ISSUE -d -\n```\n\nUpload a local file:\n```bash\ncomposio execute SLACK_UPLOAD_OR_CREATE_A_FILE_IN_SLACK \\\n  --file ./image.png \\\n  -d '{ channels: \"C123\" }'\n```\n\nRun independent tool calls in parallel:\n```bash\ncomposio execute --parallel \\\n  GMAIL_SEND_EMAIL -d '{ recipient_email: \"a@b.com\", subject: \"Hi\" }' \\\n  GITHUB_CREATE_AN_ISSUE -d '{ owner: \"acme\", repo: \"app\", title: \"Bug\" }'\n```\n\nKey flags:\n- `--get-schema`: Inspect required arguments without executing the tool.\n- `--dry-run`: Preview the request shape without performing the action.\n- `--file`: Inject a local file path into a tool that exposes exactly one uploadable file argument.\n- `--parallel`: Execute multiple independent tool calls in the same invocation.\n- `--account`: Select which connected account to use by alias, word_id, or account id when multiple accounts exist for the same toolkit.\n\n- `--file` only works when the tool exposes a single uploadable file input. Otherwise use explicit `-d` JSON.\n\n## `search` - Find The Slug\n\nUse `search` only when the tool slug is not already known.\n\n```bash\ncomposio search \"create a github issue\"\ncomposio search \"send an email\" --toolkits gmail\ncomposio search \"send an email\" \"create a github issue\"\ncomposio search \"my emails\" \"my github issues\" --toolkits gmail,github\n```\n\n- Batch related discovery work into one `search` invocation, then move back to `execute` once the correct slugs are known.\n\n## `link` - Connect An Account\n\nUse `link` when `execute` reports that a toolkit is not connected, or when the user explicitly wants to authorize an account.\n\n```bash\ncomposio link gmail\ncomposio link googlecalendar --no-browser\n```\n\nKey flags:\n- `--alias`: Assign an alias to the connected account. Required when creating an additional account for the same toolkit.\n\n- Retry the original `execute` command after linking succeeds.\n\n## `proxy` - Raw API Access\n\nUse `proxy` when a toolkit supports a raw API operation that is easier than finding a dedicated tool slug.\n\n```bash\ncomposio proxy https://api.github.com/user --toolkit github --method GET </dev/null\n```\n\n## `run` - Scripting, LLMs, and Programmatic Workflows\n\nFor programmatic calls, loops, output plumbing, or anything beyond a single tool call, prefer `composio run`.\n\n`composio run` executes an inline ESM JavaScript/TypeScript snippet with authenticated `execute()`, `search()`, `proxy()`, and the experimental `experimental_subAgent()` helper pre-injected. No SDK setup required.\n\nChain multiple tools:\n```bash\ncomposio run '\n  const me = await execute(\"GITHUB_GET_THE_AUTHENTICATED_USER\");\n  const emails = await execute(\"GMAIL_FETCH_EMAILS\", { max_results: 1 });\n  console.log({ login: me.data.login, fetchedEmails: !!emails.data });\n'\n```\n\nFan out with Promise.all:\n```bash\ncomposio run '\n  const [me, emails] = await Promise.all([\n    execute(\"GITHUB_GET_THE_AUTHENTICATED_USER\"),\n    execute(\"GMAIL_FETCH_EMAILS\", { max_results: 5 }),\n  ]);\n  console.log({ login: me.data.login, emailCount: emails.data.messages?.length });\n'\n```\n\nFeed tool output into an LLM and get structured JSON back:\n```bash\ncomposio run --logs-off '\n  const emails = await execute(\"GMAIL_FETCH_EMAILS\", { max_results: 5 });\n  const brief = await experimental_subAgent(\n    `Summarize these emails and count them.\\n\\n${emails.prompt()}`,\n    { schema: z.object({ summary: z.string(), count: z.number() }) }\n  );\n  console.log(brief.structuredOutput);\n'\n```\n\n- Use top-level `execute --parallel` instead when the user only needs a few independent tool calls and does not need script logic.\n\n## Auth\n\n```bash\ncomposio whoami   # check current session\ncomposio login    # authenticate if whoami fails\n```\n\n## Escalate Only When Needed\n\nIf the user is stuck on top-level commands or needs fallback inspection commands, load [references/troubleshooting.md](references/troubleshooting.md).\n\nIf the user explicitly asks about developer projects, auth configs, connected accounts, triggers, logs, orgs, or projects, load [references/composio-dev.md](references/composio-dev.md). `composio dev` is not the default end-user path.\n\n\n# relay-80-100-workflow\nreason=Spec text mentions \"must\". Spec text mentions \"before\". Spec text mentions \"covers\". Spec text mentions \"pattern\". Spec text mentions \"beyond\". Spec text mentions \"code\". Spec text mentions \"works\". Spec text mentions \"test\". Spec text mentions \"sandbox\". Spec text mentions \"patterns\". Spec text mentions \"after\". Spec text mentions \"every\". Spec text mentions \"implementation\". Spec text mentions \"through\".\n---\nname: relay-80-100-workflow\ndescription: Use when writing agent-relay workflows that must fully validate features end-to-end before merging. Covers the 80-to-100 pattern - going beyond \"code compiles\" to \"feature works, tested E2E locally.\" Includes repair-before-failure validation gates, mandatory sequential Claude-then-Codex fresh-eyes review/fix loops with test hardening, PGlite for in-memory Postgres testing, mock sandbox patterns, test-fix-rerun loops, verify gates after every edit, and the full lifecycle from implementation through passing tests to commit.\n---\n\n### Overview\n\nMost agent workflows get features to ~80%: code written, types check, maybe a build passes. This skill covers the **80-to-100 gap** — making workflows that fully validate features end-to-end before committing. The goal: every feature merged via these workflows is **tested, verified, and known-working**, not just \"it compiles.\"\n\n### When to Use\n\n- Writing workflows where the deliverable must be **production-ready**, not just code-complete\n- Features that touch databases, APIs, or infrastructure that can be tested locally\n- Any workflow where \"it compiles\" is not sufficient proof of correctness\n- When you want confidence that the commit actually works before deploying\n\n### Core Principle: Test In The Workflow\n\n#### The key insight: **run tests as deterministic steps inside the workflow itself**. Don't just write test files — execute them, verify they pass, fix failures, and re-run. The workflow doesn't commit until tests are green.\n\n```\nimplement → write tests → run tests → fix failures → re-run → build check → regression check → commit\n```\n\n\n### Repair Before Failure\n\nAn 80-to-100 workflow should not stop merely because a test, typecheck, lint, schema, or E2E gate turns red. That red output is work for the agent team. Capture it, hand it to a repair owner, fix it, and rerun. Workflow-owned validation gates should never terminate the run with `FAILED`. If the team exhausts its repair budget or hits an external blocker such as missing credentials, wrong repository, or unsafe dirty worktree, write a `BLOCKED_NO_COMMIT` artifact and end without committing or opening a PR instead of crashing the workflow.\n\nUse this shape for every meaningful gate:\n\n1. `run-*`: deterministic command with `captureOutput: true` and `failOnError: false`.\n2. `fix-*`: agent step that reads `{{steps.run-e2e.output}}`, fixes source/tests/config, and reruns the command locally until green.\n3. `verify-*`: deterministic rerun, usually still `failOnError: false`, followed by a final repair step if red.\n4. `commit-if-green`: deterministic step that reruns the full acceptance command and commits only when every exit code is zero. If anything is still red, it writes `BLOCKED_NO_COMMIT` with the failing evidence and exits successfully so the workflow reports a handled blocked state, not a runtime failure.\n\nAgentWorkforce/relay#827 added repair-aware reliability to the SDK (`.reliable()` / `.repairable()` and repair-aware retry-mode workflows). Prefer those presets when available, but still model explicit repair owners when gate output needs domain-specific fixing.\n\n### Keep Repairable Gates On The Critical Path\n\nRepair-before-failure only works after the workflow reaches a deterministic gate. If a long-running interactive agent step is a hard dependency for the first gate, then a dropped PTY, agent spawn error, or transport failure can stop the workflow before the repair loop ever sees evidence.\n\nFor large rollouts, treat implementation agents as advisory producers and put a deterministic reconciliation step on the critical path:\n\n1. Start implementation/review agents in parallel if useful, but require them to write durable artifacts such as `.workflow-artifacts/<task>/runtime.md`, self-review notes, changed-file lists, and command evidence.\n2. Add `implementation-reconcile`: a deterministic step that inspects `git status --short -- <paths>`, required files, artifact files, and diff stats. It should use `captureOutput: true` and `failOnError: false`.\n3. Add `repair-implementation-reconcile`: a focused repair owner that reads the reconcile output and finishes missing artifacts or code before validation gates run.\n4. Make discovery, typecheck, E2E, and final acceptance depend on the reconcile/repair path, not directly on every long-lived implementation agent.\n5. Keep the final commit deterministic and green-only; red final evidence becomes a repair/blocking artifact, not a failed workflow.\n\nThis shape prevents \"agent transport failed\" from masquerading as \"the product failed.\" The product still has to pass the same gates; the difference is that the workflow can reach the gates and repair them.\n\n### Squad Review Before Final Acceptance\n\nFor high-stakes implementation workflows, validation should include human-like review structure, not only command gates. Use small implementation squads and make review state durable:\n\n1. Split independent scopes into 2-3 agent squads. Each squad has an implementer, a shadow reviewer, and optionally a validation/test owner.\n2. The shadow reviewer follows the implementer while work is happening and flags spec drift early.\n3. Before external review, the implementer writes a self-reflection artifact under `.workflow-artifacts/<task>/` covering spec coverage, changed files, tests/proofs, repo-rule alignment, and known risks.\n4. A fresh self-review agent reads the actual files, AGENTS.md / CLAUDE.md, recent related work, and local conventions. It writes findings to disk.\n5. The implementer repairs valid findings, then deterministic gates rerun from captured output.\n6. After all squads converge, run the mandatory sequential fresh-eyes review/fix loops: Claude reviews the final diff and artifacts, a fixer repairs valid findings and adds or updates appropriate tests/proofs, Claude reviews the post-fix state again, then Codex repeats the same cycle from scratch over the post-Claude-fix state.\n7. If either final review still finds issues, run another explicit fix pass or write `BLOCKED_NO_COMMIT` with exact evidence.\n8. Commit or PR creation is allowed only after final deterministic acceptance and post-Codex-fix review are green. Otherwise write a `BLOCKED_NO_COMMIT` artifact with exact evidence.\n\nThis keeps \"100%\" tied to both executable evidence and independent review over the final state.\n\n### The Test-Fix-Rerun Pattern\n\n#### Every testable feature in a workflow should follow this four-step pattern:\n\n```typescript\n// Step 1: Run tests (allow failure — we expect issues on first run)\n.step('run-tests', {\n  type: 'deterministic',\n  dependsOn: ['create-tests'],\n  command: 'npx tsx --test tests/my-feature.test.ts 2>&1 | tail -60',\n  captureOutput: true,\n  failOnError: false,  // <-- Don't fail the workflow, let the agent fix it\n})\n\n// Step 2: Agent reads output, fixes issues, re-runs until green\n.step('fix-tests', {\n  agent: 'tester',\n  dependsOn: ['run-tests'],\n  task: `Check the test output and fix any failures.\n\nTest output:\n{{steps.run-tests.output}}\n\nIf all tests passed, do nothing.\nIf there are failures:\n1. Read the failing test file and source files\n2. Fix the issues (could be in test or source)\n3. Re-run: npx tsx --test tests/my-feature.test.ts\n4. Keep fixing until ALL tests pass.`,\n  verification: { type: 'exit_code' },\n})\n\n// Step 3: Deterministic rerun — capture result for a final repair pass\n.step('run-tests-final', {\n  type: 'deterministic',\n  dependsOn: ['fix-tests'],\n  command: 'npx tsx --test tests/my-feature.test.ts 2>&1',\n  captureOutput: true,\n  failOnError: false,\n})\n\n// Step 4: Repair again if the rerun is still red\n.step('fix-tests-final', {\n  agent: 'tester',\n  dependsOn: ['run-tests-final'],\n  task: `If the final test rerun passed, record the green evidence.\nIf it failed, fix the remaining issue and rerun until green:\n{{steps.run-tests-final.output}}`,\n  verification: { type: 'exit_code' },\n})\n```\n\n\n### PGlite: In-Memory Postgres for Database Testing\n\n#### Setup\n\n```typescript\n.step('install-pglite', {\n  type: 'deterministic',\n  command: 'npm install --save-dev @electric-sql/pglite 2>&1 | tail -5',\n  captureOutput: true,\n})\n```\n\n#### Test Helper Pattern\n\n```typescript\n// tests/helpers/pglite-db.ts\nimport { PGlite } from '@electric-sql/pglite';\nimport { drizzle } from 'drizzle-orm/pglite';\nimport * as schema from '../../packages/web/lib/db/schema.js';\n\n// Raw DDL matching your Drizzle schema — PGlite doesn't run Drizzle migrations\nconst MY_TABLE_DDL = `\nCREATE TABLE IF NOT EXISTS my_table (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  name TEXT NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n`;\n\nexport async function createTestDb() {\n  const pg = new PGlite();\n  await pg.exec(MY_TABLE_DDL);\n  const db = drizzle(pg, { schema });\n  return { db, pg, schema, cleanup: () => pg.close() };\n}\n```\n\n#### Test Structure\n\n```typescript\n// tests/my-feature.test.ts\nimport { describe, it } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { randomUUID } from 'node:crypto';\nimport { createTestDb } from './helpers/pglite-db.js';\n\ndescribe('my feature', () => {\n  it('does the thing correctly', async () => {\n    const { db, schema, cleanup } = await createTestDb();\n    try {\n      // Arrange\n      const testId = randomUUID();\n      // Act — use your module against the real (in-memory) Postgres\n      // Assert\n      assert.equal(result.name, 'expected');\n    } finally {\n      await cleanup();\n    }\n  });\n});\n```\n\n\n### Verify Gates After Every Edit\n\n#### Never trust that an agent edited a file correctly. Add a deterministic verify gate after every agent edit step:\n\n```typescript\n// Agent edits a file\n.step('edit-schema', {\n  agent: 'impl',\n  dependsOn: ['read-schema'],\n  task: `Edit packages/web/lib/db/schema.ts...`,\n  verification: { type: 'exit_code' },\n})\n\n// Deterministic verification — did the edit actually land?\n.step('verify-schema', {\n  type: 'deterministic',\n  dependsOn: ['edit-schema'],\n  command: `if git diff --quiet packages/web/lib/db/schema.ts; then echo \"NOT MODIFIED\"; exit 1; fi\ngrep \"my_new_table\" packages/web/lib/db/schema.ts >/dev/null && echo \"OK\" || (echo \"MISSING\"; exit 1)`,\n  failOnError: false,\n  captureOutput: true,\n})\n.step('fix-schema-verification', {\n  agent: 'impl',\n  dependsOn: ['verify-schema'],\n  task: `Fix the schema edit if verification failed. Output:\\n{{steps.verify-schema.output}}`,\n  verification: { type: 'exit_code' },\n})\n```\n\n#### Edit Gates That Include New Files\n\n```typescript\n.step('edit-gate-capture', {\n  type: 'deterministic',\n  dependsOn: ['implement'],\n  command: `if [ -z \"$(git status --short -- packages/new-adapter tests docs)\" ]; then\n  echo \"NO_CHANGES\"\n  exit 1\nfi\necho \"EDIT_GATE_OK\"`,\n  captureOutput: true,\n  failOnError: false,\n})\n.step('fix-edit-gate', {\n  agent: 'impl',\n  dependsOn: ['edit-gate-capture'],\n  task: `If the edit gate reported NO_CHANGES, inspect the acceptance contract\nand current git status, then add the missing source/test/artifacts.\n\nGate output:\n{{steps.edit-gate-capture.output}}\n\nIf it already passed, do nothing.`,\n  verification: { type: 'exit_code' },\n})\n.step('edit-gate-final', {\n  type: 'deterministic',\n  dependsOn: ['fix-edit-gate'],\n  command: `if [ -z \"$(git status --short -- packages/new-adapter tests docs)\" ]; then\n  echo \"NO_CHANGES\"\n  exit 1\nfi\necho \"EDIT_GATE_FINAL_OK\"`,\n  captureOutput: true,\n  failOnError: true,\n})\n```\n\n\n### Mock Sandbox Pattern\n\n#### When testing code that interacts with Daytona sandboxes, use inline mock objects matching the existing test conventions:\n\n```typescript\nconst daytona = {\n  create: async () => ({\n    id: 'sandbox-id',\n    process: {\n      executeCommand: async (cmd, cwd, env) => ({\n        result: 'output',\n        exitCode: 0,\n      }),\n    },\n    fs: {\n      uploadFile: async () => undefined,\n    },\n    getUserHomeDir: async () => '/home/daytona',\n  }),\n  remove: async () => undefined,\n};\n```\n\n\n### Regression Testing\n\n#### After your new tests pass, always run the **existing test suite** to catch regressions:\n\n```typescript\n.step('run-existing-tests', {\n  type: 'deterministic',\n  dependsOn: ['fix-build'],\n  command: 'npm run orchestrator:test 2>&1 | tail -40',\n  captureOutput: true,\n  failOnError: false,\n})\n\n.step('fix-regressions', {\n  agent: 'impl',\n  dependsOn: ['run-existing-tests'],\n  task: `Check the full test suite for regressions caused by our changes.\n\nTest output:\n{{steps.run-existing-tests.output}}\n\nIf all tests passed, do nothing.\nIf EXISTING tests broke, read the failing test, find what we broke, fix it.\nMost likely cause: constructor signatures changed, new required fields added\nwithout defaults, or import paths shifted.\n\nRun: npm run orchestrator:test\nFix until all tests pass.`,\n  verification: { type: 'exit_code' },\n})\n```\n\n\n### Full Workflow Template\n\n#### Here's the complete pattern for a feature that touches the database:\n\n```typescript\nimport { workflow } from '@relayflows/core';\n\nconst result = await workflow('my-feature')\n  .description('Add feature X with full E2E validation')\n  .pattern('dag')\n  .channel('wf-my-feature')\n  .maxConcurrency(3)\n  .timeout(3_600_000)\n  .repairable()\n\n  .agent('impl', { cli: 'claude', preset: 'worker', retries: 2 })\n  .agent('tester', { cli: 'claude', preset: 'worker', retries: 2 })\n\n  // ── Phase 1: Read ────────────────────────────────────────────────\n  .step('read-target', {\n    type: 'deterministic',\n    command: 'cat path/to/file.ts',\n    captureOutput: true,\n  })\n\n  // ── Phase 2: Implement ───────────────────────────────────────────\n  .step('edit-target', {\n    agent: 'impl',\n    dependsOn: ['read-target'],\n    task: `Edit path/to/file.ts. Current contents:\n{{steps.read-target.output}}\n<specific instructions>\nOnly edit this one file.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('verify-target', {\n    type: 'deterministic',\n    dependsOn: ['edit-target'],\n    command: 'git diff --quiet path/to/file.ts && (echo \"NOT MODIFIED\"; exit 1) || echo \"OK\"',\n    failOnError: false,\n    captureOutput: true,\n  })\n  .step('fix-target-verification', {\n    agent: 'impl',\n    dependsOn: ['verify-target'],\n    task: `Fix the target edit if verification failed. Output:\\n{{steps.verify-target.output}}`,\n    verification: { type: 'exit_code' },\n  })\n\n  // ── Phase 3: Test infrastructure ─────────────────────────────────\n  .step('install-pglite', {\n    type: 'deterministic',\n    command: 'npm install --save-dev @electric-sql/pglite 2>&1 | tail -5',\n    captureOutput: true,\n  })\n  .step('create-test-helpers', {\n    agent: 'tester',\n    dependsOn: ['install-pglite'],\n    task: 'Create tests/helpers/pglite-db.ts with <DDL for your tables>...',\n    verification: { type: 'file_exists', value: 'tests/helpers/pglite-db.ts' },\n  })\n  .step('create-tests', {\n    agent: 'tester',\n    dependsOn: ['create-test-helpers', 'fix-target-verification'],\n    task: 'Create tests/my-feature.test.ts with <test descriptions>...',\n    verification: { type: 'file_exists', value: 'tests/my-feature.test.ts' },\n  })\n\n  // ── Phase 4: Test-fix-rerun loop ─────────────────────────────────\n  .step('run-tests', {\n    type: 'deterministic',\n    dependsOn: ['create-tests'],\n    command: 'npx tsx --test tests/my-feature.test.ts 2>&1 | tail -60',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('fix-tests', {\n    agent: 'tester',\n    dependsOn: ['run-tests'],\n    task: `Fix any test failures. Output:\\n{{steps.run-tests.output}}`,\n    verification: { type: 'exit_code' },\n  })\n  .step('run-tests-final', {\n    type: 'deterministic',\n    dependsOn: ['fix-tests'],\n    command: 'npx tsx --test tests/my-feature.test.ts 2>&1',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('fix-tests-final', {\n    agent: 'tester',\n    dependsOn: ['run-tests-final'],\n    task: `If the final test rerun is red, fix and rerun until green. Output:\\n{{steps.run-tests-final.output}}`,\n    verification: { type: 'exit_code' },\n  })\n\n  // ── Phase 5: Build + regression ──────────────────────────────────\n  .step('build-check', {\n    type: 'deterministic',\n    dependsOn: ['fix-tests-final'],\n    command: 'npx tsc --noEmit 2>&1 | tail -20; echo \"EXIT: $?\"',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('fix-build', {\n    agent: 'impl',\n    dependsOn: ['build-check'],\n    task: `Fix type errors if any. Output:\\n{{steps.build-check.output}}`,\n    verification: { type: 'exit_code' },\n  })\n  .step('run-existing-tests', {\n    type: 'deterministic',\n    dependsOn: ['fix-build'],\n    command: 'npm test 2>&1 | tail -40',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('fix-regressions', {\n    agent: 'impl',\n    dependsOn: ['run-existing-tests'],\n    task: `Fix regressions if any. Output:\\n{{steps.run-existing-tests.output}}`,\n    verification: { type: 'exit_code' },\n  })\n\n  // ── Phase 6: Commit ──────────────────────────────────────────────\n  .step('commit', {\n    type: 'deterministic',\n    dependsOn: ['fix-regressions'],\n    command: [\n      'npx tsx --test tests/my-feature.test.ts',\n      'npm test',\n      'git add <files>',\n      'git commit -m \"feat: ...\"',\n    ].join(' && '),\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('repair-commit', {\n    agent: 'impl',\n    dependsOn: ['commit'],\n    task: `If commit failed, fix the blocker, rerun the feature and regression tests, and create the commit.\nIf commit passed, confirm the commit subject.\nOutput:\n{{steps.commit.output}}`,\n    verification: { type: 'exit_code' },\n  })\n  .step('verify-commit-created', {\n    type: 'deterministic',\n    dependsOn: ['repair-commit'],\n    command: 'git log -1 --pretty=%s | grep -q \"^feat: \" && echo \"COMMIT_OK\" || (echo \"COMMIT_MISSING\"; exit 1)',\n    captureOutput: true,\n    failOnError: true,\n  })\n\n  .onError('retry', { maxRetries: 2, retryDelayMs: 10_000 })\n  .run({ cwd: process.cwd() });\n```\n\n\n### Checklist: Is Your Workflow 80-to-100?\n\n| Check | How |\n|-------|-----|\n| Tests exist | `file_exists` verification on test file |\n| Tests actually run | Deterministic step executes them |\n| Test failures get fixed | Agent step reads output, fixes, re-runs |\n| Final test run is repairable | Deterministic rerun captures output, then a repair owner gets one more pass |\n| Build passes | `npx tsc --noEmit` deterministic step |\n| No regressions | Existing test suite runs after changes |\n| Every edit is verified and repairable | `git diff --quiet` + grep for tracked-only edits; `git status --short -- <paths>` when new files/packages may appear; then a fix step |\n| Commit only happens after green evidence | Final commit step reruns acceptance checks and commits only on zero exit codes |\n\n### Common Anti-Patterns\n\n| Anti-pattern | Why it fails | Fix |\n|-------------|-------------|-----|\n| Tests written but never executed | Agent claims they pass, they don't | Add deterministic `run-tests` step |\n| Single `failOnError: true` test run | First failure kills workflow, no chance to fix | Use repairable run-fix-rerun-final-fix loops |\n| No regression test | New feature works, old features break | Run `npm test` after build check |\n| Agent asked to \"write and run tests\" in one step | Agent writes tests, runs them, they fail, it edits, output is garbled | Separate write/run/fix into distinct steps |\n| PGlite DDL doesn't match Drizzle schema | Tests pass on wrong schema | Derive DDL from schema.ts or test with real migration |\n| Final test output not handed to an agent | Broken tests can stop the run or get ignored | Add a final repair owner before commit |\n| Testing only happy path | Edge cases break in prod | Specify edge case tests in the task prompt |\n| No verify gate after agent edits | Agent exits 0 without writing anything | Add `git diff --quiet` check after every edit, then route failures to a repair step |\n| `git diff --quiet` for new package/test directories | Untracked files are invisible, so valid new artifacts can look like \"no changes\" | Use `git status --short -- <paths>` and a repairable capture → fix → final gate pattern |\n| Committing after `failOnError: false` without checking exits | Broken work can be committed because the shell step returned successfully | In `commit-if-green`, record each exit code and skip commit unless all are zero |\n\n\n# review-fix-signoff-loop\nreason=Spec text mentions \"agent\". Spec text mentions \"relay\". Spec text mentions \"must\". Spec text mentions \"loop\". Spec text mentions \"agents\". Spec text mentions \"both\". Spec text mentions \"work\". Spec text mentions \"covers\". Spec text mentions \"dual\".\n---\nname: review-fix-signoff-loop\ndescription: Use when writing Agent Relay or Ricky workflows that must loop review, fix, and validation with fresh agent context until independent signoff agents, typically Claude and Codex, both agree the work is comprehensively complete. Covers fresh-context iterations, repairable gates, dual reviewer verdict contracts, iteration-count reporting, PR signoff comments, and blocked-state handling.\n---\n\n### Purpose\n\nUse this pattern for high-stakes implementation workflows where a normal \"implement, test, review once\" flow is not enough. The workflow must keep repairing and re-reviewing until independent signoff agents agree the spec is fully wired end to end.\n\nPair this with `writing-agent-relay-workflows` for SDK syntax and `relay-80-100-workflow` for deterministic validation gates.\n\n### Required Shape\n\n- Run deterministic preflight before agents start.\n- Confirm repository root, required specs, declared write scope, credentials needed for PR comments, and whether commit/push/PR creation is in scope.\n- For cross-repo or package-release work, write a scope matrix before implementation: repositories, branches, PRs, packages, providers/features touched, published versions, consuming package manifests, lockfiles, and expected downstream bumps.\n- Probe the CLIs used by later agent steps. For Codex, `codex login status` is not enough; run a tiny `codex exec --ephemeral --json --sandbox read-only -m <supported-model>` prompt and fail early with a clear re-login instruction if it cannot return the expected token.\n- Write preflight evidence to `.workflow-artifacts/<workflow>/iteration-N/preflight.md`.\n- Implement with scoped owners.\n- Use Codex workers for code changes unless the codebase has a reason to prefer another CLI.\n- Split backend, frontend, desktop, tests, docs, or infrastructure into explicit non-overlapping ownership areas.\n- Each worker writes a durable summary artifact with changed files and commands run.\n- Reconcile before validation.\n- Add a deterministic `implementation-reconcile` gate that checks required files, expected API/UI/runtime surfaces, migrations, generated artifacts, and untracked files with `git status --short -- <paths>`.\n- For multi-provider changes, reconcile against the scope matrix: every touched provider/package must be classified as `implemented`, `dependency-only`, `intentionally-deferred`, or `not-applicable`, with proof. Do not let \"we only bumped the package I remembered\" pass this gate.\n- For package-release flows, reconcile producer and consumer state: `npm view <pkg> version`, package manifests, lockfile resolved tarballs/integrities, and `npm ls <pkg>` from every consuming workspace.\n- For CI failures, map each failing job to its exact local command or documented non-local equivalent. Distinguish similarly named gates (for example handler coverage vs acceptance route coverage) and replay the one that actually failed.\n- Use `failOnError: false`, then route the captured output to a repair owner.\n- Run repairable validation.\n- Use capture -> fix -> rerun for typecheck, targeted tests, integration or E2E tests, and regression checks.\n- Include exact failing CI commands when available before broader \"nearby\" checks. A nearby green gate is supporting evidence, not proof that the reported CI failure is fixed.\n- Red validation output is input for a repair agent, not an immediate workflow failure.\n- Write `BLOCKED_NO_COMMIT.md` only for true external blockers.\n- Run fresh-context signoff reviews.\n- Start a new workflow run, new agent names, or otherwise new agent contexts for each loop iteration.\n- Run Claude and Codex signoff reviews independently over the same post-validation repo state.\n- Reviewers must read specs, diff, validation logs, artifacts, and actual files.\n- Break only on dual signoff.\n- The loop may exit only when both reviewers write the exact satisfied verdict and final deterministic acceptance is green.\n- If either reviewer finds issues or is blocked, run a Codex fix pass and start a new fresh-context review iteration.\n- Make the Codex fix pass a non-interactive one-shot worker (`preset: 'worker'`) with a `file_exists` verification for its durable report. Do not rely on interactive PTY idle detection or `/exit` for loop progress.\n- Report final signoff.\n- Write a final `SIGNOFF.md` that includes iteration count, validation evidence, Claude rationale, Codex rationale, remaining risks, and artifact paths.\n- Include the final scope matrix with every repository/package/provider row signed off, deferred with owner/date, or marked not applicable. For release flows, include published and consumed versions.\n- Post the same report to the PR. Resolve the PR from an explicit env var first, then from `gh pr view`.\n\n### Verdict Contract\n\n#### Use a strict text contract so deterministic gates can parse the result:\n\n```text\nVERDICT: COMPREHENSIVELY_SATISFIED | FINDINGS | BLOCKED\nwhy_passed: required when VERDICT is COMPREHENSIVELY_SATISFIED\nend_to_end_wiring_verified: required when VERDICT is COMPREHENSIVELY_SATISFIED\ndeterministic_evidence: required when VERDICT is COMPREHENSIVELY_SATISFIED\nscope_matrix_verified: required when VERDICT is COMPREHENSIVELY_SATISFIED for cross-repo/provider/package work\nremaining_risks: required when VERDICT is COMPREHENSIVELY_SATISFIED\nfinding_id: stable-id when VERDICT is FINDINGS\nseverity: blocker | high | medium | low\nfile: path\nissue: concrete gap\nfix_required: exact change needed\ntest_required: deterministic proof needed\nevidence: commands, files, or spec clause\n```\n\n\n### Scope Matrix\n\n#### Create a machine-readable and human-readable matrix before the first fix pass for work that spans repositories, packages, providers, or CI gates. Keep it updated every iteration.\n\n```text\nrepo | branch | PR | package/provider/surface | expected change | producer version | consumer version | files expected | gates required | status | evidence | owner\n```\n\n\n### Fresh Context Implementation\n\n#### Prefer an outer loop that starts a new Agent Relay workflow run per iteration:\n\n```typescript\nfor (let iteration = 1; ; iteration += 1) {\n  await runIteration(iteration, runStamp); // new workflow name, channel, and agent names\n  clearStartFromAfterResumedIteration();\n  if (hasDualSignoff(iteration)) {\n    writeAndPostSignoffReport(iteration);\n    break;\n  }\n}\n```\n\n\n### Codex Fixer Reliability\n\n#### For review-fix loop steps, prefer this shape:\n\n```typescript\n.agent(`codex-review-fixer-${suffix}`, {\n  cli: 'codex',\n  model: CodexModels.GPT_5_4,\n  preset: 'worker',\n  role: 'Review-finding fixer. Repairs valid findings and hardens tests/proofs.',\n  retries: 2,\n})\n.step('fix-review-findings', {\n  agent: `codex-review-fixer-${suffix}`,\n  dependsOn: ['dual-signoff-gate'],\n  task: `Read iteration artifacts. Fix every valid finding, rerun relevant checks, and write ${dir}/review-fix-report.md.`,\n  verification: { type: 'file_exists', value: `${ROOT}/${dir}/review-fix-report.md` },\n})\n```\n\n\n### PR Signoff Comment\n\n#### Final signoff should be both a durable artifact and a PR comment.\n\n```bash\ngh pr comment \"$PR_NUMBER\" --body-file .workflow-artifacts/my-workflow/pr-comment.md\n```\n\n\n### Blocked State\n\n#### Do not spin forever when progress is impossible. If agents identify a true external blocker, write:\n\n```text\n.workflow-artifacts/<workflow>/iteration-N/BLOCKED_NO_COMMIT.md\n```\n\n\n### Common Mistakes\n\n- Reusing the same reviewer context every loop. Start a new run or new reviewer agents for each iteration.\n- Letting a reviewer write `NO_ISSUES_FOUND` without pass rationale. Require the full verdict contract.\n- Treating green tests as signoff. Green deterministic gates are required evidence, not a substitute for fresh review.\n- Hard-failing the first red validation gate. Capture it, repair it, then rerun.\n- Posting a PR comment before both signoff agents agree on the same final state.\n- Forgetting to count iterations. The final report must say how many loops it took.\n\n\n# writing-agent-relay-workflows\nreason=Spec text mentions \"relay\". Spec text mentions \"covers\". Spec text mentions \"agents\". Spec text mentions \"test\". Spec text mentions \"error\". Spec text mentions \"event\". Spec text mentions \"step\". Spec text mentions \"workers\". Spec text mentions \"parallel\".\n---\nname: writing-agent-relay-workflows\ndescription: Use when building multi-agent workflows with relay broker-sdk. Covers conversation vs pipeline coordination, WorkflowBuilder/DAG steps, agents, {{steps.plan.output}} chaining, repairable verification gates, evidence-based completion, mandatory Claude-then-Codex fresh-eyes review/fix loops with test hardening, channels, chat-native recipes, error handling, event listeners, step sizing, lead+workers teams, and parallel waves.\n---\n\n### Overview\n\nThe relay broker-sdk workflow system orchestrates multiple AI agents (Claude, Codex, Gemini, Aider, Goose) through typed DAG-based workflows. Workflows can be written in **TypeScript** (preferred), **Python**, or **YAML**.\n\n**Language preference:** TypeScript > Python > YAML. Use TypeScript unless the project is Python-only or a simple config-driven workflow suits YAML.\n\n**Pattern selection:** Do not default to `dag` blindly. If the job needs a different swarm/workflow type, consult the `choosing-swarm-patterns` skill when available and select the pattern that best matches the coordination problem.\n\n### When to Use\n\n- Building multi-agent workflows with step dependencies\n- Orchestrating different AI CLIs (claude, codex, gemini, aider, goose)\n- Creating DAG, pipeline, fan-out, or other swarm patterns\n- Needing verification gates, retries, or step output chaining\n- Designing product-contract workflows where failing checks should route to agents for repair instead of stopping the run\n- Dynamic channel management: agents joining/leaving/muting channels mid-workflow\n\n### Non-Negotiable Workflow Checklist\n\nEvery generated workflow should satisfy this checklist before it is considered complete:\n\n1. Start with a deterministic, resumable preflight for repository state, credentials, and declared write scope.\n2. Pick the coordination shape deliberately: Conversation for non-trivial coordination, Pipeline only for linear one-shot handoffs.\n3. Use repairable validation gates: capture red output with `failOnError: false`, hand it to a repair owner, then rerun the same check.\n4. Run the mandatory fresh-eyes loops in order: Claude review/fix/final review/final fix, then Codex review/fix/final review/final fix.\n5. Require review fixers to add or update appropriate tests, fixtures, assertions, or deterministic proofs for testable findings.\n6. Run final deterministic acceptance after the Codex loop and before commit, PR creation, or handoff.\n7. If a real blocker remains, write `BLOCKED_NO_COMMIT` with exact evidence and skip commit/PR creation instead of crashing the workflow.\n8. If the workflow owns shipping, model branch, commit, push, PR creation, and PR URL verification as explicit deterministic steps.\n\n### Default Principle: Workflows Repair Before They Fail\n\n- Run deterministic checks as evidence-capturing gates with `captureOutput: true`.\n- Prefer `failOnError: false` for intermediate validation gates so the workflow can pass the output to a repair agent.\n- Add a repair step immediately after each red-prone gate. The repair agent reads `{{steps.name.output}}`, fixes source/tests/config, reruns the same command locally, and exits only after the gate is green or the blocker is external.\n- Keep final acceptance deterministic, but still put an agent repair step before commit/PR creation. If the repair budget is exhausted or a true external blocker remains, write a blocked artifact and skip commit/PR creation; do not let the workflow end as `FAILED`.\n- Use `.reliable()` or `.repairable()` on SDK versions that support it, especially for product-contract workflows. As of AgentWorkforce/relay#827, retry-mode workflows with agents are repair-aware by default, repair agents run before retrying malformed/failed agent steps, and the SDK covers DAG, pipeline, fan-out, worktree-backed, deterministic-only, and agent-plus-gate shapes.\n\n### Mandatory Fresh-Eyes Review Loops\n\n#### Every workflow must include two comprehensive fresh-eyes review/fix loops before final acceptance, commit, PR creation, or handoff: first Claude, then Codex. This applies even to small workflows and even when deterministic tests pass. Tests prove commands passed; the fresh-eyes loops make independent agents read the actual resulting files and artifacts as if they did not author them.\n\n```text\nverdict: FINDINGS | NO_ISSUES_FOUND | BLOCKED\nfinding_id: short stable id\nseverity: blocker | high | medium | low\nfile: path/to/file\nissue: what is wrong\nfix_required: concrete change needed\ntest_required: test, fixture, assertion, or proof command needed\nstatus: open | fixed | wontfix | blocked\nevidence: commands run, file paths, or blocker details\n```\n\n\n### Choose Your Coordination Style — Conversation vs Pipeline\n\nBefore writing the workflow, decide *how the agents will coordinate*. The relay primitive supports two very different shapes, and picking the wrong one wastes the most valuable thing the SDK gives you.\n\n| Shape | What it is | Use when |\n|---|---|---|\n| **Conversation** (chat-native) | Interactive agents share a channel; messages, `@-mentions`, and ambient awareness drive coordination. Lead and workers spawn in parallel and self-organize. The relay is the coordination layer, not just transport. | Multi-file work, peer review loops, cross-agent feedback, dynamic re-planning, multi-PR coordination, anything with a human-in-the-loop escape, swarms where workers pick up each other's output. |\n| **Pipeline** (one-shot DAG) | Each step runs as a one-shot subprocess (`claude -p`, `codex exec`); steps hand off via `{{steps.plan.output}}` text injection. No agents are alive at the same time; no chat happens. | Linear, well-specified transformations; deterministic data passing; no live agent-to-agent coordination during implementation. The mandatory final Claude-then-Codex review/fix loops still apply. |\n\n**Default to Conversation for any non-trivial work.** Pipeline DAGs are simpler to reason about but they do not exercise the relay primitive — they are a Unix pipe with extra steps. If you would happily write the same task as a single shell pipeline, pipeline-shape is fine. Otherwise, you almost certainly want a Conversation shape.\n\nThe two shapes can mix within one workflow: pipeline-style deterministic preflight → conversation in the middle → pipeline-style commit-and-PR at the end. See **Quick Reference (Conversation)** below and **[Common Patterns → Interactive Team](#interactive-team-lead--workers-on-shared-channel)** for the canonical recipe.\n\n> **A blunt rule of thumb:** if your workflow only uses `agent` steps with `preset: 'worker'` chained by `{{steps.plan.output}}`, you are not using the relay — you are using `claude -p | codex exec`. That may still be the right answer; just make it a deliberate choice.\n\n### Quick Reference (Pipeline shape)\n\n#### > Use this when steps are linear, well-specified, and need no agent-to-agent feedback. For anything with iteration, review, or coordination, jump to **Quick Reference (Conversation shape)** below.\n\n```typescript\nimport { workflow } from '@relayflows/core';\n\nasync function runWorkflow() {\n  const result = await workflow('my-workflow')\n    .description('What this workflow does')\n    .pattern('dag') // or 'pipeline', 'fan-out', etc.\n    .channel('wf-my-workflow') // dedicated channel (auto-generated if omitted)\n    .maxConcurrency(3)\n    .timeout(3_600_000) // global timeout (ms)\n    .repairable()\n\n    .agent('lead', { cli: 'claude', role: 'Architect', retries: 2 })\n    .agent('worker', { cli: 'codex', role: 'Implementer', retries: 2 })\n    .agent('claude-reviewer', { cli: 'claude', role: 'First-pass fresh-eyes reviewer', retries: 1, preset: 'reviewer' })\n    .agent('claude-fixer', { cli: 'claude', role: 'First-pass review-finding fixer', retries: 2 })\n    .agent('codex-reviewer', { cli: 'codex', role: 'Second-pass fresh-eyes reviewer', retries: 1, preset: 'reviewer' })\n    .agent('codex-fixer', { cli: 'codex', role: 'Review-finding fixer', retries: 2 })\n\n    .step('preflight', {\n      type: 'deterministic',\n      command: 'git rev-parse --show-toplevel >/dev/null && echo PREFLIGHT_OK',\n      captureOutput: true,\n      failOnError: true,\n    })\n    .step('plan', {\n      agent: 'lead',\n      dependsOn: ['preflight'],\n      task: `Analyze the codebase and produce a plan.`,\n      retries: 2,\n      verification: { type: 'output_contains', value: 'PLAN_COMPLETE' },\n    })\n    .step('implement', {\n      agent: 'worker',\n      task: `Implement based on this plan:\\n{{steps.plan.output}}`,\n      dependsOn: ['plan'],\n      verification: { type: 'exit_code' },\n    })\n    .step('claude-review', {\n      agent: 'claude-reviewer',\n      dependsOn: ['implement'],\n      task: `Fresh-eyes review the completed workflow output. Read the actual files, diff, repo rules, and available evidence.\nWrite findings to .workflow-artifacts/my-workflow/claude-review.md.\nIf there are no actionable issues, write NO_ISSUES_FOUND.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('claude-fix', {\n      agent: 'claude-fixer',\n      dependsOn: ['claude-review'],\n      task: `Read .workflow-artifacts/my-workflow/claude-review.md.\nFix every valid issue, add or update appropriate tests/proofs for the fix, rerun relevant checks, and update .workflow-artifacts/my-workflow/claude-fix.md.\nIf the review says NO_ISSUES_FOUND, record that no fix was needed.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('claude-review-final', {\n      agent: 'claude-reviewer',\n      dependsOn: ['claude-fix'],\n      task: `Fresh-eyes review the post-fix state from scratch. Do not rely on the prior review or fix summary.\nWrite .workflow-artifacts/my-workflow/claude-review-final.md with either actionable findings or NO_ISSUES_FOUND.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('claude-fix-final', {\n      agent: 'claude-fixer',\n      dependsOn: ['claude-review-final'],\n      task: `If .workflow-artifacts/my-workflow/claude-review-final.md contains findings, fix them, add or update appropriate tests/proofs, and rerun relevant checks.\nIf no fix is possible, write .workflow-artifacts/my-workflow/BLOCKED_NO_COMMIT.md with exact evidence.\nIf it says NO_ISSUES_FOUND, record Claude review signoff.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('codex-review', {\n      agent: 'codex-reviewer',\n      dependsOn: ['claude-fix-final'],\n      task: `Second-pass fresh-eyes review of the post-Claude-fix state. Read the actual files, diff, repo rules, and available evidence.\nWrite findings to .workflow-artifacts/my-workflow/codex-review.md.\nIf there are no actionable issues, write NO_ISSUES_FOUND.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('codex-fix', {\n      agent: 'codex-fixer',\n      dependsOn: ['codex-review'],\n      task: `Read .workflow-artifacts/my-workflow/codex-review.md.\nFix every valid issue, add or update appropriate tests/proofs for the fix, rerun relevant checks, and update .workflow-artifacts/my-workflow/codex-fix.md.\nIf the review says NO_ISSUES_FOUND, record that no fix was needed.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('codex-review-final', {\n      agent: 'codex-reviewer',\n      dependsOn: ['codex-fix'],\n      task: `Fresh-eyes review the post-Codex-fix state from scratch. Do not rely on the prior review or fix summary.\nWrite .workflow-artifacts/my-workflow/codex-review-final.md with either actionable findings or NO_ISSUES_FOUND.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('codex-fix-final', {\n      agent: 'codex-fixer',\n      dependsOn: ['codex-review-final'],\n      task: `If .workflow-artifacts/my-workflow/codex-review-final.md contains findings, fix them, add or update appropriate tests/proofs, and rerun relevant checks.\nIf no fix is possible, write .workflow-artifacts/my-workflow/BLOCKED_NO_COMMIT.md with exact evidence.\nIf it says NO_ISSUES_FOUND, record final review signoff.`,\n      verification: { type: 'exit_code' },\n    })\n    .step('acceptance-after-review', {\n      type: 'deterministic',\n      dependsOn: ['codex-fix-final'],\n      command: 'test ! -f .workflow-artifacts/my-workflow/BLOCKED_NO_COMMIT.md && echo ACCEPTANCE_OK',\n      captureOutput: true,\n      failOnError: true,\n    })\n\n    .onError('retry', { maxRetries: 2, retryDelayMs: 10_000 })\n    .run({ cwd: process.cwd() });\n\n  console.log('Result:', result.status);\n}\n\nrunWorkflow().catch((error) => {\n  console.error(error);\n  process.exit(1);\n});\n```\n\n\n### Quick Reference (Conversation shape)\n\n#### > Use this for any non-trivial work — peer review, multi-file edits, cross-agent feedback, dynamic re-planning. Lead and workers spawn **in parallel** on a shared channel and self-organize via messages. The relay primitive does the coordinating; verification gates downstream of the lead close the workflow.\n\n```typescript\nimport { workflow } from '@relayflows/core';\nimport { ClaudeModels, CodexModels } from '@agent-relay/config';\n\nasync function runWorkflow() {\n  const result = await workflow('my-workflow')\n  .description('Multi-file change with peer review')\n  .pattern('dag')\n  .channel('wf-my-feature')          // dedicated channel — agents share it\n  .maxConcurrency(4)\n  .timeout(3_600_000)\n  .repairable()\n\n  // Interactive agents — no preset, they live on the channel\n  .agent('lead', {\n    cli: 'claude',\n    model: ClaudeModels.OPUS,\n    role: 'Architect + reviewer. Plans, assigns, reviews, posts feedback.',\n    retries: 1,\n  })\n  .agent('impl-a', {\n    cli: 'codex',\n    model: CodexModels.GPT_5_4,\n    role: 'Implementer. Listens on channel for assignments and feedback.',\n    retries: 2,\n  })\n  .agent('impl-b', {\n    cli: 'codex',\n    model: CodexModels.GPT_5_4,\n    role: 'Implementer. Listens on channel for assignments and feedback.',\n    retries: 2,\n  })\n  .agent('claude-reviewer', {\n    cli: 'claude',\n    model: ClaudeModels.OPUS,\n    preset: 'reviewer',\n    role: 'First-pass fresh-eyes reviewer. Reads the final diff and artifacts from scratch.',\n    retries: 1,\n  })\n  .agent('claude-fixer', {\n    cli: 'claude',\n    model: ClaudeModels.SONNET,\n    role: 'First-pass review-finding fixer. Repairs valid findings, adds tests/proofs, and reruns checks.',\n    retries: 2,\n  })\n  .agent('codex-reviewer', {\n    cli: 'codex',\n    model: CodexModels.GPT_5_4,\n    preset: 'reviewer',\n    role: 'Second-pass fresh-eyes reviewer. Reviews the post-Claude-fix state from scratch.',\n    retries: 1,\n  })\n  .agent('codex-fixer', {\n    cli: 'codex',\n    model: CodexModels.GPT_5_4,\n    role: 'Review-finding fixer. Repairs valid findings, adds tests/proofs, and reruns checks.',\n    retries: 2,\n  })\n\n  // Deterministic context — pre-reads files once, posts to the channel for everyone\n  .step('preflight', {\n    type: 'deterministic',\n    command: 'git rev-parse --show-toplevel >/dev/null && echo PREFLIGHT_OK',\n    captureOutput: true,\n    failOnError: true,\n  })\n  .step('context', {\n    type: 'deterministic',\n    dependsOn: ['preflight'],\n    command: 'git ls-files src/',\n    captureOutput: true,\n  })\n\n  // Lead and workers all depend on `context` — they start CONCURRENTLY.\n  // They coordinate over #wf-my-feature, not via {{steps.plan.output}}.\n  .step('lead-coordinate', {\n    agent: 'lead',\n    dependsOn: ['context'],\n    task: `You are the lead on #wf-my-feature. Workers: impl-a, impl-b.\nPost the plan. Assign files. Review their PRs/diffs. Post feedback in-channel.\nWorkers iterate based on your feedback. Exit when both files pass review.`,\n  })\n  .step('impl-a-work', {\n    agent: 'impl-a',\n    dependsOn: ['context'],   // SAME dep as lead → starts in parallel, no deadlock\n    task: `You are impl-a on #wf-my-feature. Wait for the lead's plan.\nImplement your assigned file. Post a completion message. Address feedback.`,\n  })\n  .step('impl-b-work', {\n    agent: 'impl-b',\n    dependsOn: ['context'],   // SAME dep as lead\n    task: `You are impl-b on #wf-my-feature. Wait for the lead's plan.\nImplement your assigned file. Post a completion message. Address feedback.`,\n  })\n\n  // Downstream gates on the lead — lead exits when satisfied.\n  // Capture failures, then hand them to an agent for repair.\n  .step('verify', {\n    type: 'deterministic',\n    dependsOn: ['lead-coordinate'],\n    command: 'npm run typecheck && npm test 2>&1',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('repair-verify', {\n    agent: 'lead',\n    dependsOn: ['verify'],\n    task: `If verification passed, summarize evidence.\nIf it failed, use this output to assign and fix issues, then rerun the command until green:\n{{steps.verify.output}}`,\n    verification: { type: 'exit_code' },\n  })\n  .step('verify-final', {\n    type: 'deterministic',\n    dependsOn: ['repair-verify'],\n    command: 'npm run typecheck && npm test 2>&1',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('claude-review', {\n    agent: 'claude-reviewer',\n    dependsOn: ['verify-final'],\n    task: `First-pass fresh-eyes review of the post-implementation state.\nRead the actual changed files, git diff, repo instructions, task spec, and verification output:\n{{steps.verify-final.output}}\n\nWrite .workflow-artifacts/my-feature/claude-review.md with:\n- actionable findings, each with file paths and required fix\n- or NO_ISSUES_FOUND if there are no remaining issues`,\n    verification: { type: 'exit_code' },\n  })\n  .step('claude-fix', {\n    agent: 'claude-fixer',\n    dependsOn: ['claude-review'],\n    task: `Read .workflow-artifacts/my-feature/claude-review.md.\nIf there are findings, fix every valid one and add or update appropriate tests/proofs. After each fix, rerun the relevant check and review the changed files again.\nKeep iterating locally until this round has no remaining valid issues.\nWrite .workflow-artifacts/my-feature/claude-fix.md with fixes and commands run.\nIf the review says NO_ISSUES_FOUND, write that no fix was needed.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('claude-review-final', {\n    agent: 'claude-reviewer',\n    dependsOn: ['claude-fix'],\n    task: `Perform a fresh post-fix review from scratch. Do not rely on previous review text or the fixer's summary.\nRead files, diff, repo rules, task spec, and evidence. Write .workflow-artifacts/my-feature/claude-review-final.md.\nUse NO_ISSUES_FOUND only if there are no actionable issues left.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('claude-fix-final', {\n    agent: 'claude-fixer',\n    dependsOn: ['claude-review-final'],\n    task: `If the final Claude review found issues, fix them, add or update appropriate tests/proofs, and rerun the relevant checks until green.\nIf no fix is possible, write .workflow-artifacts/my-feature/BLOCKED_NO_COMMIT.md with exact evidence and do not commit.\nIf the final review says NO_ISSUES_FOUND, record signoff in .workflow-artifacts/my-feature/claude-signoff.md.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('verify-after-claude-review', {\n    type: 'deterministic',\n    dependsOn: ['claude-fix-final'],\n    command: 'test ! -f .workflow-artifacts/my-feature/BLOCKED_NO_COMMIT.md && npm run typecheck && npm test 2>&1',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('codex-review', {\n    agent: 'codex-reviewer',\n    dependsOn: ['verify-after-claude-review'],\n    task: `Second-pass fresh-eyes review of the post-Claude-fix state.\nRead the actual changed files, git diff, repo instructions, task spec, and verification output:\n{{steps.verify-after-claude-review.output}}\n\nWrite .workflow-artifacts/my-feature/codex-review.md with:\n- actionable findings, each with file paths and required fix\n- or NO_ISSUES_FOUND if there are no remaining issues`,\n    verification: { type: 'exit_code' },\n  })\n  .step('codex-fix', {\n    agent: 'codex-fixer',\n    dependsOn: ['codex-review'],\n    task: `Read .workflow-artifacts/my-feature/codex-review.md.\nIf there are findings, fix every valid one and add or update appropriate tests/proofs. After each fix, rerun the relevant check and review the changed files again.\nKeep iterating locally until this round has no remaining valid issues.\nWrite .workflow-artifacts/my-feature/codex-fix.md with fixes and commands run.\nIf the review says NO_ISSUES_FOUND, write that no fix was needed.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('codex-review-final', {\n    agent: 'codex-reviewer',\n    dependsOn: ['codex-fix'],\n    task: `Perform a fresh post-Codex-fix review from scratch. Do not rely on previous review text or the fixer's summary.\nRead files, diff, repo rules, task spec, and evidence. Write .workflow-artifacts/my-feature/codex-review-final.md.\nUse NO_ISSUES_FOUND only if there are no actionable issues left.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('codex-fix-final', {\n    agent: 'codex-fixer',\n    dependsOn: ['codex-review-final'],\n    task: `If the final Codex review found issues, fix them, add or update appropriate tests/proofs, and rerun the relevant checks until green.\nIf no fix is possible, write .workflow-artifacts/my-feature/BLOCKED_NO_COMMIT.md with exact evidence and do not commit.\nIf the final review says NO_ISSUES_FOUND, record signoff in .workflow-artifacts/my-feature/codex-signoff.md.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('verify-after-review', {\n    type: 'deterministic',\n    dependsOn: ['codex-fix-final'],\n    command: 'test ! -f .workflow-artifacts/my-feature/BLOCKED_NO_COMMIT.md && npm run typecheck && npm test 2>&1',\n    captureOutput: true,\n    failOnError: true,\n  })\n\n  .onError('retry', { maxRetries: 2, retryDelayMs: 10_000 })\n  .run({ cwd: process.cwd() });\n\n  console.log('Result:', result.status);\n}\n\nrunWorkflow().catch((error) => {\n  console.error(error);\n  process.exit(1);\n});\n```\n\n\n### Default For Serious Implementation: Shadowed Squad Review Loop\n\n- implementer: owns a tight file/subsystem scope and writes the change\n- shadow reviewer: follows the implementer in real time, checks drift against the spec, and leaves feedback early\n- optional validation owner: owns tests, dry-run proof, or fixture coverage when that is a separate deliverable\n- Deterministically read the spec, AGENTS.md / CLAUDE.md, workflow standards, recent local docs, and declared file targets.\n- Lead splits work into bounded squads with non-overlapping ownership.\n- Squads run in parallel. The shadow reads actual files and channel updates, then posts feedback while the implementer is still active.\n- Each implementer writes a self-reflection artifact before external review. It must answer: what changed, what spec items are satisfied, what tests/proofs ran, what risks remain, and how the work follows repo rules.\n- A fresh self-review agent reads the post-implementation files, recent local conventions, AGENTS.md / CLAUDE.md, and related rules. It should not rely on the implementer's summary.\n- The implementer gets that feedback and performs a repair pass.\n- Deterministic gates run with captured output. Red output goes to a repair owner, then the same gate reruns.\n- Run the mandatory fresh-eyes review loops in sequence: Claude reviews the actual final diff and artifacts, a fixer repairs findings and hardens them with appropriate tests/proofs, Claude reviews the post-fix state again, then Codex repeats the same cycle from scratch over the post-Claude-fix state.\n- Optional extra reviewers can be added for high-stakes work, but they do not replace the sequential Claude-then-Codex loops.\n- Final signoff only happens after post-Codex-fix review and final deterministic gates prove the spec is complete, or a blocker artifact explains why it cannot be completed.\n- Critical TypeScript rules:\n- Check the project's `package.json` for `\"type\": \"module\"` — if ESM, use `import`; if CJS, use `require()`. In both cases, wrap execution in an async function instead of raw top-level `await`.\n- `agent-relay run <file.ts>` executes the file as a standalone subprocess — it does NOT inspect exports. The file MUST call `.run()`.\n- Use `.run({ cwd: process.cwd() })` — `createWorkflowRenderer` does not exist\n- Validate with `--dry-run` before running: `agent-relay run --dry-run workflow.ts`\n\n### ⚡ Parallelism — Design for Speed\n\n#### Cross-Workflow Parallelism: Wave Planning\n\n```bash\n# BAD — sequential (14 hours for 27 workflows at ~30 min each)\nagent-relay run workflows/34-sst-wiring.ts\nagent-relay run workflows/35-env-config.ts\nagent-relay run workflows/36-loading-states.ts\n# ... one at a time\n\n# GOOD — parallel waves (3-4 hours for 27 workflows)\n# Wave 1: independent infra (parallel)\nagent-relay run workflows/34-sst-wiring.ts &\nagent-relay run workflows/35-env-config.ts &\nagent-relay run workflows/36-loading-states.ts &\nagent-relay run workflows/37-responsive.ts &\nwait\ngit add -A && git commit -m \"Wave 1\"\n\n# Wave 2: testing (parallel — independent test suites)\nagent-relay run workflows/40-unit-tests.ts &\nagent-relay run workflows/41-integration-tests.ts &\nagent-relay run workflows/42-e2e-tests.ts &\nwait\ngit add -A && git commit -m \"Wave 2\"\n```\n\n#### Declare File Scope for Planning\n\n```typescript\nworkflow('48-comparison-mode')\n  .packages(['web', 'core'])                // monorepo packages touched\n  .isolatedFrom(['49-feedback-system'])      // explicitly safe to parallelize\n  .requiresBefore(['46-admin-dashboard'])    // explicit ordering constraint\n```\n\n#### Within-Workflow Parallelism\n\n```typescript\n// BAD — unnecessary sequential chain\n.step('fix-component-a', { agent: 'worker', dependsOn: ['review'] })\n.step('fix-component-b', { agent: 'worker', dependsOn: ['fix-component-a'] })  // why wait?\n\n// GOOD — parallel fan-out, merge at the end\n.step('fix-component-a', { agent: 'impl-1', dependsOn: ['review'] })\n.step('fix-component-b', { agent: 'impl-2', dependsOn: ['review'] })  // same dep = parallel\n.step('verify-all', { agent: 'reviewer', dependsOn: ['fix-component-a', 'fix-component-b'] })\n```\n\n\n### Failure Prevention\n\n#### 1. Do not use raw top-level `await`\n\n```ts\nasync function runWorkflow() {\n  const result = await workflow('my-workflow')\n    // ...\n    .run({ cwd: process.cwd() });\n\n  console.log('Workflow status:', result.status);\n}\n\nrunWorkflow().catch((error) => {\n  console.error(error);\n  process.exit(1);\n});\n```\n\n#### 2b. Standard preflight template for resumable workflows\n\n```ts\n.step('preflight', {\n  type: 'deterministic',\n  command: [\n    'set -e',\n    'BRANCH=$(git rev-parse --abbrev-ref HEAD)',\n    'echo \"branch: $BRANCH\"',\n    'if [ \"$BRANCH\" != \"fix/your-branch-name\" ]; then echo \"ERROR: wrong branch\"; exit 1; fi',\n    // Files the workflow is allowed to find dirty on entry:\n    //   - package-lock.json: npm install is idempotent and often touches it\n    //   - every file the workflow's edit steps will rewrite: a prior partial\n    //     run may have left them dirty, and the edit step will rewrite\n    //     them cleanly before commit\n    // Everything else is unexpected drift and must fail preflight.\n    'ALLOWED_DIRTY=\"package-lock.json|path/to/file1\\\\\\\\.ts|path/to/file2\\\\\\\\.ts\"',\n    'DIRTY=$({ git diff --name-only; git ls-files --others --exclude-standard; } | sort -u | grep -vE \"^(${ALLOWED_DIRTY})$\" || true)',\n    'if [ -n \"$DIRTY\" ]; then echo \"ERROR: unexpected tracked drift:\"; echo \"$DIRTY\"; exit 1; fi',\n    'if ! git diff --cached --quiet; then echo \"ERROR: staging area is dirty\"; git diff --cached --stat; exit 1; fi',\n    'gh auth status >/dev/null 2>&1 || (echo \"ERROR: gh CLI not authenticated\"; exit 1)',\n    'echo PREFLIGHT_OK',\n  ].join(' && '),\n  captureOutput: true,\n  failOnError: true,\n}),\n```\n\n#### 2c. Picking the right `.join()` for multi-line shell commands\n\n```ts\ncommand: [\n  'set -e',\n  'HITS=$(grep -c diag src/cli/commands/setup.ts || true)',\n  'if [ \"$HITS\" -lt 6 ]; then echo \"FAIL\"; exit 1; fi',\n  'echo OK',\n].join(' && '),\n```\n\n#### 3. Keep final verification boring and deterministic\n\n```bash\ngrep -Eq \"foo|bar|baz\" file.ts\n```\n\n#### 6. Be explicit about shell requirements\n\n```bash\n/opt/homebrew/bin/bash workflows/your-workflow/execute.sh --wave 2\n```\n\n#### 9. Factor repo-specific setup into a shared helper\n\n```ts\n// workflows/lib/cloud-repo-setup.ts\nexport interface CloudRepoSetupOptions {\n  branch: string;\n  committerName?: string;\n  extraSetupCommands?: string[];\n  skipWorkspaceBuild?: boolean;\n}\n\nexport function applyCloudRepoSetup<T>(wf: T, opts: CloudRepoSetupOptions): T {\n  // adds two steps: setup-branch, install-deps\n  // install-deps runs: npm install + workspace prebuilds (build:platform, build:core, etc.)\n  // ...\n}\n```\n\n\n### End-to-End Bug Fix Workflows\n\n- **Capture the original failure**\n- Reproduce the bug first in a deterministic or evidence-capturing step\n- Save exact commands, logs, status codes, or screenshots/artifacts\n- **State the acceptance contract**\n- Define the exact end-to-end success criteria before implementation\n- Include the real entrypoint a user would run\n- **Implement the fix**\n- **Rebuild / reinstall from scratch**\n- Do not trust dirty local state\n- Prefer a clean environment when install/bootstrap behavior is involved\n- **Run targeted regression checks**\n- Unit/integration tests are helpful but not sufficient by themselves\n- **Run a full end-to-end validation**\n- Use the real CLI / API / install path\n- Prefer a clean environment (Docker, sandbox, cloud workspace, Daytona, etc.) for install/runtime issues\n- **Compare before vs after evidence**\n- Show that the original failure no longer occurs\n- **Record residual risks**\n- Call out what was not covered\n- **Ship the result as a PR**\n- Open the pull request from the workflow itself with `createGitHubStep`\n- See [Shipping the Result — Open a PR via `createGitHubStep`](#shipping-the-result--open-a-pr-via-creategithubstep) below\n- A workflow that fixes a bug and stops short of the PR has only done half the loop\n- disposable sandbox / cloud workspace\n- Docker / containerized environment\n- fresh local shell with isolated paths\n- compares candidate validation environments\n- defines the acceptance contract\n- chooses the best swarm pattern\n- then authors the final fix/validation workflow\n\n### Shipping the Result — Open a PR via `createGitHubStep`\n\n#### The minimal \"open a PR\" recipe\n\n```typescript\nimport { workflow } from '@relayflows/core';\nimport { createGitHubStep } from '@agent-relay/sdk';\n\nconst REPO = 'AgentWorkforce/cloud';\nconst BRANCH = `agent-relay/run-${Date.now()}`;\n\nasync function runWorkflow() {\n  await workflow('feature-x')\n    // ... your real implementation, repair, review loops, and final acceptance ...\n    .step('write-marker', {\n      type: 'deterministic',\n      command: `echo DONE`,\n    })\n\n    // Branch off main on the remote.\n    .step('create-branch', createGitHubStep({\n      dependsOn: ['write-marker'],\n      action: 'createBranch',\n      repo: REPO,\n      params: { branch: BRANCH, source: 'main' },\n    }))\n\n    // Commit the change to the branch via Contents API.\n    .step('commit-change', createGitHubStep({\n      dependsOn: ['create-branch'],\n      action: 'createFile',\n      repo: REPO,\n      params: {\n        path: 'CHANGELOG.md',\n        branch: BRANCH,\n        content: '<file body here>',\n        message: 'chore: changelog entry',\n      },\n    }))\n\n    // Open the PR. This is the load-bearing step.\n    .step('open-pr', createGitHubStep({\n      dependsOn: ['commit-change'],\n      action: 'createPR',\n      repo: REPO,\n      params: {\n        title: 'feat: ship feature X',\n        head: BRANCH,\n        base: 'main',\n        body: '## Summary\\n\\n- ...\\n\\n## Test plan\\n\\n- [x] ...',\n        draft: false,\n      },\n      output: { mode: 'data', format: 'json', path: 'html_url' },\n    }))\n\n    .run({ cwd: process.cwd() });\n}\n\nrunWorkflow().catch((error) => {\n  console.error(error);\n  process.exit(1);\n});\n```\n\n\n### Key Concepts\n\n#### Verification Gates\n\n```typescript\nverification: { type: 'exit_code' }                        // preferred for code-editing steps\nverification: { type: 'output_contains', value: 'DONE' }   // optional accelerator\nverification: { type: 'file_exists', value: 'src/out.ts' } // deterministic file check\nverification: { type: 'pr_url', value: 'owner/repo' }      // step must leave behind a PR\n```\n\n#### DAG Dependencies\n\n```typescript\n.step('fix-types',  { agent: 'worker', dependsOn: ['review'], ... })\n.step('fix-tests',  { agent: 'worker', dependsOn: ['review'], ... })\n.step('final',      { agent: 'lead',   dependsOn: ['fix-types', 'fix-tests'], ... })\n```\n\n#### SDK API\n\n```typescript\n// Subscribe an agent to additional channels post-spawn\nrelay.subscribe({ agent: 'security-auditor', channels: ['review-pr-456'] });\n\n// Unsubscribe — agent leaves the channel entirely\nrelay.unsubscribe({ agent: 'security-auditor', channels: ['general'] });\n\n// Mute — agent stays subscribed (history access) but messages are NOT injected into PTY\nrelay.mute({ agent: 'security-auditor', channel: 'review-pr-123' });\n\n// Unmute — resume PTY injection\nrelay.unmute({ agent: 'security-auditor', channel: 'review-pr-123' });\n```\n\n#### Events\n\n```typescript\nrelay.onChannelSubscribed = (agent, channels) => { /* ... */ };\nrelay.onChannelUnsubscribed = (agent, channels) => { /* ... */ };\nrelay.onChannelMuted = (agent, channel) => { /* ... */ };\nrelay.onChannelUnmuted = (agent, channel) => { /* ... */ };\n```\n\n\n### Agent Definition\n\n#### ```typescript\n\n```typescript\n.agent('name', {\n  cli: 'claude' | 'codex' | 'gemini' | 'aider' | 'goose' | 'opencode' | 'droid',\n  role?: string,\n  preset?: 'lead' | 'worker' | 'reviewer' | 'analyst',\n  retries?: number,\n  model?: string,\n  interactive?: boolean, // default: true\n})\n```\n\n#### Model Constants\n\n```typescript\nimport { ClaudeModels, CodexModels, GeminiModels } from '@agent-relay/config';\n\n.agent('planner', { cli: 'claude', model: ClaudeModels.OPUS })    // not 'opus'\n.agent('worker',  { cli: 'claude', model: ClaudeModels.SONNET })  // not 'sonnet'\n.agent('coder',   { cli: 'codex',  model: CodexModels.GPT_5_4 })  // not 'gpt-5.4'\n```\n\n\n### Step Definition\n\n#### Agent Steps\n\n```typescript\n.step('name', {\n  agent: string,\n  task: string,                   // supports {{var}} and {{steps.plan.output}}\n  dependsOn?: string[],\n  verification?: VerificationCheck,\n  retries?: number,\n})\n```\n\n#### Deterministic Steps (Shell Commands)\n\n```typescript\n.step('verify-files', {\n  type: 'deterministic',\n  command: 'test -f src/auth.ts && echo \"FILE_EXISTS\"',\n  dependsOn: ['implement'],\n  captureOutput: true,\n  failOnError: false,\n})\n.step('repair-files', {\n  agent: 'worker',\n  dependsOn: ['verify-files'],\n  task: `If verify-files failed, create or fix the missing file and rerun the check.\nOutput:\n{{steps.verify-files.output}}`,\n  verification: { type: 'exit_code' },\n})\n.step('verify-files-final', {\n  type: 'deterministic',\n  command: 'test -f src/auth.ts && echo \"FILE_EXISTS\"',\n  dependsOn: ['repair-files'],\n  captureOutput: true,\n  failOnError: true,\n})\n```\n\n\n### Common Patterns\n\n#### Mandatory Claude-Then-Codex Review/Fix Loops\n\n```typescript\n.agent('claude-reviewer', {\n  cli: 'claude',\n  preset: 'reviewer',\n  role: 'First-pass fresh-eyes reviewer. Reads actual files, diffs, rules, and evidence from scratch.',\n  retries: 1,\n})\n.agent('claude-fixer', {\n  cli: 'claude',\n  role: 'Fixer for valid Claude review findings. Adds or updates tests/proofs for each fix.',\n  retries: 2,\n})\n.agent('codex-reviewer', {\n  cli: 'codex',\n  preset: 'reviewer',\n  role: 'Second-pass fresh-eyes reviewer. Reviews the post-Claude-fix state from scratch.',\n  retries: 1,\n})\n.agent('codex-fixer', {\n  cli: 'codex',\n  role: 'Fixer for valid Codex review findings. Adds or updates tests/proofs for each fix.',\n  retries: 2,\n})\n\n.step('claude-review', {\n  agent: 'claude-reviewer',\n  dependsOn: ['verify-final'],\n  task: `First-pass fresh-eyes review.\nRead the task spec, AGENTS.md / CLAUDE.md, changed files, final diff, artifacts, and verification evidence:\n{{steps.verify-final.output}}\n\nWrite .workflow-artifacts/<workflow>/claude-review.md.\nUse actionable findings with file paths, severity, and required fixes.\nIf there are no issues, write NO_ISSUES_FOUND.`,\n  verification: { type: 'exit_code' },\n})\n.step('claude-fix', {\n  agent: 'claude-fixer',\n  dependsOn: ['claude-review'],\n  task: `Read .workflow-artifacts/<workflow>/claude-review.md.\nIf it contains findings, fix every valid issue and add or update appropriate tests/proofs. After each fix, rerun targeted checks and review the touched files again.\nKeep iterating locally until this round has no remaining valid issues.\nWrite .workflow-artifacts/<workflow>/claude-fix.md with fixes and commands run.\nIf the review says NO_ISSUES_FOUND, record that no fix was needed.`,\n  verification: { type: 'exit_code' },\n})\n.step('claude-review-final', {\n  agent: 'claude-reviewer',\n  dependsOn: ['claude-fix'],\n  task: `Review the post-Claude-fix state from scratch. Do not rely on prior review text or fixer summaries.\nRead the files, diff, rules, spec, and evidence. Write .workflow-artifacts/<workflow>/claude-review-final.md.\nUse NO_ISSUES_FOUND only if there are no actionable issues left.`,\n  verification: { type: 'exit_code' },\n})\n.step('claude-fix-final', {\n  agent: 'claude-fixer',\n  dependsOn: ['claude-review-final'],\n  task: `If the final Claude review contains findings, fix them, add or update appropriate tests/proofs, rerun relevant checks, and write .workflow-artifacts/<workflow>/claude-fix-final.md.\nIf a finding cannot be fixed, write .workflow-artifacts/<workflow>/BLOCKED_NO_COMMIT.md with exact evidence.\nIf the final review says NO_ISSUES_FOUND, write .workflow-artifacts/<workflow>/claude-signoff.md.`,\n  verification: { type: 'exit_code' },\n})\n.step('verify-after-claude-review', {\n  type: 'deterministic',\n  dependsOn: ['claude-fix-final'],\n  command: 'test ! -f .workflow-artifacts/<workflow>/BLOCKED_NO_COMMIT.md && npm run typecheck && npm test 2>&1',\n  captureOutput: true,\n  failOnError: false,\n})\n.step('codex-review', {\n  agent: 'codex-reviewer',\n  dependsOn: ['verify-after-claude-review'],\n  task: `Second-pass fresh-eyes review of the post-Claude-fix state.\nRead the task spec, AGENTS.md / CLAUDE.md, changed files, final diff, artifacts, and verification evidence:\n{{steps.verify-after-claude-review.output}}\n\nWrite .workflow-artifacts/<workflow>/codex-review.md.\nUse actionable findings with file paths, severity, and required fixes.\nIf there are no issues, write NO_ISSUES_FOUND.`,\n  verification: { type: 'exit_code' },\n})\n.step('codex-fix', {\n  agent: 'codex-fixer',\n  dependsOn: ['codex-review'],\n  task: `Read .workflow-artifacts/<workflow>/codex-review.md.\nIf it contains findings, fix every valid issue and add or update appropriate tests/proofs. After each fix, rerun targeted checks and review the touched files again.\nKeep iterating locally until this round has no remaining valid issues.\nWrite .workflow-artifacts/<workflow>/codex-fix.md with fixes and commands run.\nIf the review says NO_ISSUES_FOUND, record that no fix was needed.`,\n  verification: { type: 'exit_code' },\n})\n.step('codex-review-final', {\n  agent: 'codex-reviewer',\n  dependsOn: ['codex-fix'],\n  task: `Review the post-fix state from scratch. Do not rely on prior review text or fixer summaries.\nRead the files, diff, rules, spec, and evidence. Write .workflow-artifacts/<workflow>/codex-review-final.md.\nUse NO_ISSUES_FOUND only if there are no actionable issues left.`,\n  verification: { type: 'exit_code' },\n})\n.step('codex-fix-final', {\n  agent: 'codex-fixer',\n  dependsOn: ['codex-review-final'],\n  task: `If the final review contains findings, fix them, add or update appropriate tests/proofs, rerun relevant checks, and write .workflow-artifacts/<workflow>/codex-fix-final.md.\nIf a finding cannot be fixed, write .workflow-artifacts/<workflow>/BLOCKED_NO_COMMIT.md with exact evidence.\nIf the final review says NO_ISSUES_FOUND, write .workflow-artifacts/<workflow>/codex-signoff.md.`,\n  verification: { type: 'exit_code' },\n})\n.step('acceptance-after-codex-review', {\n  type: 'deterministic',\n  dependsOn: ['codex-fix-final'],\n  command: 'test ! -f .workflow-artifacts/<workflow>/BLOCKED_NO_COMMIT.md && npm run typecheck && npm test 2>&1',\n  captureOutput: true,\n  failOnError: true,\n})\n```\n\n#### Interactive Team (lead + workers on shared channel)\n\n```typescript\n.agent('lead', {\n  cli: 'claude',\n  model: ClaudeModels.OPUS,\n  role: 'Architect and reviewer — assigns work, reviews, posts feedback',\n  retries: 1,\n  // No preset — interactive by default\n})\n\n.agent('impl-new', {\n  cli: 'codex',\n  model: CodexModels.GPT_5_4,\n  role: 'Creates new files. Listens on channel for assignments and feedback.',\n  retries: 2,\n  // No preset — interactive, receives channel messages\n})\n\n.agent('impl-modify', {\n  cli: 'codex',\n  model: CodexModels.GPT_5_4,\n  role: 'Edits existing files. Listens on channel for assignments and feedback.',\n  retries: 2,\n})\n\n// All three share the same dependsOn — they start concurrently (no deadlock)\n.step('lead-coordinate', {\n  agent: 'lead',\n  dependsOn: ['context'],\n  task: `You are the lead on #channel. Workers: impl-new, impl-modify.\nPost the plan. Assign files. Review their work. Post feedback if needed.\nWorkers iterate based on your feedback. Exit when all files are correct.`,\n})\n.step('impl-new-work', {\n  agent: 'impl-new',\n  dependsOn: ['context'],   // same dep as lead = parallel start\n  task: `You are impl-new on #channel. Wait for the lead's plan.\nCreate files as assigned. Report completion. Fix issues from feedback.`,\n})\n.step('impl-modify-work', {\n  agent: 'impl-modify',\n  dependsOn: ['context'],   // same dep as lead = parallel start\n  task: `You are impl-modify on #channel. Wait for the lead's plan.\nEdit files as assigned. Report completion. Fix issues from feedback.`,\n})\n// Downstream gates on lead (lead exits when satisfied)\n.step('verify', { type: 'deterministic', dependsOn: ['lead-coordinate'], ... })\n```\n\n#### 1. Question / Answer (blocking ask)\n\n```typescript\n.step('integrate', {\n  agent: 'integrator',\n  dependsOn: ['context'],\n  task: `You are the integrator on #wf-feature.\nBefore writing code, post a direct question to @schema-owner asking which\ntable owns the new field. Do NOT proceed until @schema-owner replies in\nchannel. If no reply arrives in 5 minutes, @-mention the lead.`,\n})\n```\n\n#### 2. Broadcast / Ack\n\n```typescript\n.step('lead-coordinate', {\n  agent: 'lead',\n  dependsOn: ['context'],\n  task: `Post the plan to #wf-feature, then @impl-a @impl-b @impl-c.\nWait for each to reply with \"ACK <agent-name>\" before issuing assignments.\nIf any worker hasn't acked in 3 minutes, re-post and ping again.\nOnly after all three have acked, post per-worker assignments.`,\n})\n```\n\n#### 3. Peer Review Handoff\n\n```typescript\n.step('impl-a-work', {\n  agent: 'impl-a',\n  dependsOn: ['context'],\n  task: `Implement src/foo.ts per the lead's assignment.\nWhen done, post to #wf-feature: \"@reviewer ready: src/foo.ts\" — include the\ncommit SHA. Then wait for @reviewer's verdict in channel.\n- If \"APPROVED\", you're done.\n- If \"CHANGES_REQUESTED <notes>\", apply the notes and re-post.\n- If no verdict in 5 min, @-mention the lead.`,\n})\n```\n\n#### 4. Standup / Status Probe\n\n```typescript\n.step('lead-coordinate', {\n  agent: 'lead',\n  task: `... coordinate the team ...\n\nEvery 10 minutes, post a status probe: \"@impl-a @impl-b status?\"\nEach worker should reply with one of:\n  - \"RUNNING <step>\" (still working)\n  - \"BLOCKED <reason>\" (@-mention the lead with the blocker)\n  - \"DONE <artifact>\" (ready for review)\n\nIf a worker is silent for two probes in a row, mark them stalled and\nreassign their work to a peer.`,\n})\n```\n\n#### 5. Hand-Off with Context\n\n```typescript\n.step('impl-a-work', {\n  agent: 'impl-a',\n  task: `... finish your part ...\n\nWhen done, post a handoff to #wf-feature targeting the next worker:\n\"@impl-b HANDOFF: src/foo.ts ready. Touched: <files>. Open question: <if any>.\nTests: <pass/fail summary>. Commit: <sha>.\"`,\n})\n```\n\n#### Pipeline (sequential handoff)\n\n```typescript\n.pattern('pipeline')\n.step('analyze', { agent: 'analyst', task: '...' })\n.step('implement', { agent: 'dev', task: '{{steps.analyze.output}}', dependsOn: ['analyze'] })\n.step('test', { agent: 'tester', task: '{{steps.implement.output}}', dependsOn: ['implement'] })\n```\n\n#### Error Handling\n\n```typescript\n.onError('fail-fast')   // stop on first failure (default)\n.onError('continue')    // skip failed branches, continue others\n.onError('retry', { maxRetries: 3, retryDelayMs: 5000 })\n```\n\n\n### Multi-File Edit Pattern\n\n#### When a workflow needs to modify multiple existing files, **use one agent step per file** with a deterministic verify gate after each. Agents reliably edit 1-2 files per step but fail on 4+.\n\n```yaml\nsteps:\n  - name: read-types\n    type: deterministic\n    command: cat src/types.ts\n    captureOutput: true\n\n  - name: edit-types\n    agent: dev\n    dependsOn: [read-types]\n    task: |\n      Edit src/types.ts. Current contents:\n      {{steps.read-target.output}}\n      Add 'pending' to the Status union type.\n      Only edit this one file.\n    verification:\n      type: exit_code\n\n  - name: verify-types\n    type: deterministic\n    dependsOn: [edit-types]\n    command: 'if git diff --quiet src/types.ts; then echo \"NOT MODIFIED\"; exit 1; fi; echo \"OK\"'\n    captureOutput: true\n    failOnError: false\n\n  - name: fix-types-verification\n    agent: dev\n    dependsOn: [verify-types]\n    task: |\n      If verify-types failed, fix src/types.ts and rerun the verify command.\n      Output:\n      {{steps.verify-files.output}}\n    verification:\n      type: exit_code\n\n  - name: verify-types-final\n    type: deterministic\n    dependsOn: [fix-types-verification]\n    command: 'if git diff --quiet src/types.ts; then echo \"NOT MODIFIED\"; exit 1; fi; echo \"OK\"'\n    captureOutput: true\n    failOnError: true\n\n  - name: read-service\n    type: deterministic\n    dependsOn: [verify-types-final]\n    command: cat src/service.ts\n    captureOutput: true\n\n  - name: edit-service\n    agent: dev\n    dependsOn: [read-service]\n    task: |\n      Edit src/service.ts. Current contents:\n      {{steps.read-target.output}}\n      Add a handlePending() method.\n      Only edit this one file.\n    verification:\n      type: exit_code\n\n  - name: verify-service\n    type: deterministic\n    dependsOn: [edit-service]\n    command: 'if git diff --quiet src/service.ts; then echo \"NOT MODIFIED\"; exit 1; fi; echo \"OK\"'\n    captureOutput: true\n    failOnError: false\n\n  - name: fix-service-verification\n    agent: dev\n    dependsOn: [verify-service]\n    task: |\n      If verify-service failed, fix src/service.ts and rerun the verify command.\n      Output:\n      {{steps.verify-schema.output}}\n    verification:\n      type: exit_code\n\n  - name: verify-service-final\n    type: deterministic\n    dependsOn: [fix-service-verification]\n    command: 'if git diff --quiet src/service.ts; then echo \"NOT MODIFIED\"; exit 1; fi; echo \"OK\"'\n    captureOutput: true\n    failOnError: true\n\n  # Deterministic commit — never rely on agents to commit\n  - name: commit\n    type: deterministic\n    dependsOn: [verify-service-final]\n    command: npm run typecheck && npm test && git add src/types.ts src/service.ts && git commit -m \"feat: add pending status\"\n    captureOutput: true\n    failOnError: false\n\n  - name: repair-commit\n    agent: dev\n    dependsOn: [commit]\n    task: |\n      If commit failed, fix the blocker, rerun npm run typecheck && npm test, and create the commit.\n      If commit passed, confirm the commit subject.\n      Output:\n      {{steps.commit.output}}\n    verification:\n      type: exit_code\n\n  - name: verify-commit-created\n    type: deterministic\n    dependsOn: [repair-commit]\n    command: 'git log -1 --pretty=%s | grep -q \"^feat: add pending status$\" && echo \"COMMIT_OK\" || (echo \"COMMIT_MISSING\"; exit 1)'\n    captureOutput: true\n    failOnError: true\n```\n\n\n### File Materialization: Verify Before Proceeding\n\n#### After any step that creates files, add a deterministic `file_exists` check before proceeding. Non-interactive agents may exit 0 without writing anything (wrong cwd, stdout instead of disk).\n\n```yaml\n- name: verify-files\n  type: deterministic\n  dependsOn: [impl-auth, impl-storage]\n  command: |\n    missing=0\n    for f in src/auth/credentials.ts src/storage/client.ts; do\n      if [ ! -f \"$f\" ]; then echo \"MISSING: $f\"; missing=$((missing+1)); fi\n    done\n    if [ $missing -gt 0 ]; then echo \"$missing files missing\"; exit 1; fi\n    echo \"All files present\"\n  captureOutput: true\n  failOnError: false\n\n- name: fix-missing-files\n  agent: impl-auth\n  dependsOn: [verify-files]\n  task: |\n    If verify-files found missing files, create/fix them and rerun the check.\n    Output:\n    {{steps.verify-files.output}}\n  verification:\n    type: exit_code\n\n- name: verify-files-final\n  type: deterministic\n  dependsOn: [fix-missing-files]\n  command: |\n    missing=0\n    for f in src/auth/credentials.ts src/storage/client.ts; do\n      if [ ! -f \"$f\" ]; then echo \"MISSING: $f\"; missing=$((missing+1)); fi\n    done\n    if [ $missing -gt 0 ]; then echo \"$missing files missing\"; exit 1; fi\n    echo \"All files present\"\n  captureOutput: true\n  failOnError: true\n```\n\n#### Edit Gates Must See Untracked Files\n\n```yaml\n- name: provider-edit-gate-capture\n  type: deterministic\n  dependsOn: [implement-providers]\n  command: |\n    if [ -z \"$(git status --short -- packages/new-provider .workflow-artifacts/my-flow)\" ]; then\n      echo \"NO_PROVIDER_CHANGES\"\n      exit 1\n    fi\n    echo \"PROVIDER_EDIT_GATE_OK\"\n  captureOutput: true\n  failOnError: false\n\n- name: repair-edit-gate\n  agent: provider-worker\n  dependsOn: [provider-edit-gate-capture]\n  task: |\n    If provider-edit-gate-capture reported NO_PROVIDER_CHANGES, inspect git\n    status including untracked files and add the missing provider artifacts.\n    If it already passed, do nothing.\n  verification:\n    type: exit_code\n\n- name: provider-edit-gate-final\n  type: deterministic\n  dependsOn: [repair-edit-gate]\n  command: |\n    if [ -z \"$(git status --short -- packages/new-provider .workflow-artifacts/my-flow)\" ]; then\n      echo \"NO_PROVIDER_CHANGES\"\n      exit 1\n    fi\n    echo \"PROVIDER_EDIT_GATE_FINAL_OK\"\n  captureOutput: true\n  failOnError: false\n\n- name: repair-provider-edit-gate-final\n  agent: provider-worker\n  dependsOn: [provider-edit-gate-final]\n  task: |\n    If provider-edit-gate-final is still red, repair the missing provider\n    artifacts and rerun the check. If repair is impossible, write\n    .workflow-artifacts/my-flow/BLOCKED_NO_COMMIT.md with exact evidence and\n    do not commit.\n    Output:\n    {{steps.edit-gate-final.output}}\n  verification:\n    type: exit_code\n```\n\n\n### Agent Transport Must Not Be The First Hard Gate\n\n#### Interactive lead-and-worker teams are useful, but they are still process\n\n```typescript\n.step('runtime-implementation', {\n  agent: 'impl-runtime',\n  dependsOn: ['context'],\n  task: 'Implement the runtime slice and write .workflow-artifacts/runtime.md',\n})\n.step('adapter-implementation', {\n  agent: 'impl-adapters',\n  dependsOn: ['context'],\n  task: 'Implement adapter wiring and write .workflow-artifacts/adapters.md',\n})\n.step('implementation-reconcile', {\n  type: 'deterministic',\n  dependsOn: ['context'],\n  command: `git status --short -- packages/core packages/*/src/writeback.ts scripts tests .workflow-artifacts\ntest -f scripts/verify-e2e.mjs || echo \"MISSING_E2E\"\ntest -f packages/core/src/runtime/router.ts || echo \"MISSING_ROUTER\"`,\n  captureOutput: true,\n  failOnError: false,\n})\n.step('repair-implementation-reconcile', {\n  agent: 'qa',\n  dependsOn: ['implementation-reconcile'],\n  task: `Finish anything missing before gates run:\\n{{steps.implementation-reconcile.output}}`,\n  verification: { type: 'exit_code' },\n})\n.step('run-e2e', {\n  type: 'deterministic',\n  dependsOn: ['repair-implementation-reconcile'],\n  command: 'npm run verify:e2e',\n  captureOutput: true,\n  failOnError: false,\n})\n```\n\n\n### DAG Deadlock Anti-Pattern\n\n#### ```yaml\n\n```yaml\n# WRONG — deadlock: coordinate depends on context, work-a depends on coordinate\nsteps:\n  - name: coordinate\n    dependsOn: [context]    # lead waits for WORKER_DONE...\n  - name: work-a\n    dependsOn: [coordinate] # ...but work-a can't start until coordinate finishes\n\n# RIGHT — workers and lead start in parallel\nsteps:\n  - name: context\n    type: deterministic\n  - name: work-a\n    dependsOn: [context]    # starts with lead\n  - name: coordinate\n    dependsOn: [context]    # starts with workers\n  - name: merge\n    dependsOn: [work-a, coordinate]\n```\n\n\n### Step Sizing\n\n#### **One agent, one deliverable.** A step's task prompt should be 10-20 lines max.\n\n```yaml\n# Team pattern: lead + workers on a shared channel\nsteps:\n  - name: track-lead-coord\n    agent: track-lead\n    dependsOn: [prior-step]\n    task: |\n      Lead the track on #my-track. Workers: track-worker-1, track-worker-2.\n      Post assignments to the channel. Review worker output.\n\n  - name: track-worker-1-impl\n    agent: track-worker-1\n    dependsOn: [prior-step]  # same dep as lead — starts concurrently\n    task: |\n      Join #my-track. track-lead will post your assignment.\n      Implement the file as directed.\n    verification:\n      type: exit_code\n\n  - name: next-step\n    dependsOn: [track-lead-coord]  # downstream depends on lead, not workers\n```\n\n\n### Supervisor Pattern\n\nWhen you set `.pattern('supervisor')` (or `hub-spoke`, `fan-out`), the runner auto-assigns a supervisor agent as owner for worker steps. The supervisor monitors progress, nudges idle workers, and issues `OWNER_DECISION`.\n\n**Auto-hardening only activates for hub patterns** — not `pipeline` or `dag`.\n\n| Use case | Pattern | Why |\n|----------|---------|-----|\n| Sequential, no monitoring | `pipeline` | Simple, no overhead |\n| Workers need oversight | `supervisor` | Auto-owner monitors |\n| Local/small models | `supervisor` | Supervisor catches stuck workers |\n| All non-interactive | `pipeline` or `dag` | No PTY = no supervision needed |\n\n### Concurrency\n\n**Cap `maxConcurrency` at 4-6.** Spawning 10+ agents simultaneously causes broker timeouts.\n\n| Parallel agents | `maxConcurrency` |\n|-----------------|-------------------|\n| 2-4             | 4 (default safe)  |\n| 5-10            | 5                 |\n| 10+             | 6-8 max           |\n\n### Common Mistakes\n\n| Mistake | Fix |\n|---------|-----|\n| Treating relay as transport, not as a coordination layer (every step is `preset: 'worker'`, every handoff is `{{steps.plan.output}}`) | Default to **Conversation shape** for non-trivial work — interactive agents on a shared channel. Pipeline-shape is only correct when the work could be expressed as a `bash \\| bash \\| bash` pipe. |\n| Interactive agents on a channel whose task strings don't tell them to talk to each other | Pick a [Chat-Native Coordination Recipe](#chat-native-coordination-recipes) (Q/A, Broadcast/Ack, Peer Review, Standup, Hand-Off) and bake it into the task prompt — otherwise you're paying for a chat substrate you're not using |\n| All workflows run sequentially | Group independent workflows into parallel waves (4-7x speedup) |\n| Every step depends on the previous one | Only add `dependsOn` when there's a real data dependency |\n| Self-review step with no timeout | Set `timeout: 300_000` (5 min) — Codex hangs in non-interactive review |\n| One giant workflow per feature | Split into smaller workflows that can run in parallel waves |\n| Adding exit instructions to tasks | Runner handles self-termination automatically |\n| Interactive PTY Codex for one-shot artifact steps | Use `preset: 'worker'` plus `file_exists` or `custom` verification |\n| Setting `timeoutMs` on agents/steps | Use global `.timeout()` only |\n| Using `general` channel | Set `.channel('wf-name')` for isolation |\n| `{{steps.plan.output}}` without `dependsOn: ['X']` | Output won't be available yet |\n| Requiring exact sentinel as only completion gate | Use `exit_code` or `file_exists` verification |\n| Writing 100-line task prompts | Split into lead + workers on a channel |\n| `maxConcurrency: 16` with many parallel steps | Cap at 5-6 |\n| Non-interactive agent reading large files via tools | Pre-read in deterministic step, inject via `{{steps.plan.output}}` |\n| Workers depending on lead step (deadlock) | Both depend on shared context step |\n| Validation gates depending directly on long interactive implementation agents | Add a deterministic implementation-reconcile step and make gates depend on its repair step |\n| `fan-out`/`hub-spoke` for simple parallel workers | Use `dag` instead |\n| `pipeline` but expecting auto-supervisor | Only hub patterns auto-harden. Use `.pattern('supervisor')` |\n| Workers without `preset: 'worker'` in one-shot DAG lead+worker flows | Add preset for clean stdout when chaining `{{steps.plan.output}}` (not needed for interactive team patterns) |\n| Using `_` in YAML numbers (`timeoutMs: 1_200_000`) | YAML doesn't support `_` separators |\n| Workflow timeout under 30 min for complex workflows | Use `3600000` (1 hour) as default |\n| Using `require()` in ESM projects | Check `package.json` for `\"type\": \"module\"` — use `import` if ESM |\n| Raw top-level `await` in workflow files | Executor paths may compile as CJS. Wrap `.run()` in `async function runWorkflow()` for both ESM and CJS files |\n| Using `createWorkflowRenderer` | Does not exist. Use `.run({ cwd: process.cwd() })` |\n| `export default workflow(...)...build()` | No `.build()`. Chain ends with `.run()` — the file must call `.run()`, not just export config |\n| Relative import `'../workflows/builder.js'` | Use `import { workflow } from '@relayflows/core'` |\n| Hardcoded model strings (`model: 'opus'`) | Use constants: `import { ClaudeModels } from '@agent-relay/config'` → `model: ClaudeModels.OPUS` |\n| Thinking `agent-relay run` inspects exports | It executes the file as a subprocess. Only `.run()` invocations trigger steps |\n| `pattern('single')` on cloud runner | Not supported — use `dag` |\n| `pattern('supervisor')` with one agent | Same agent is owner + specialist. Use `dag` |\n| Invalid verification type (`type: 'deterministic'`) | Only `exit_code`, `output_contains`, `file_exists`, `custom` are valid |\n| Chaining `{{steps.plan.output}}` from interactive agents | PTY output is garbled. Use deterministic steps or `preset: 'worker'` |\n| Single step editing 4+ files | Agents modify 1-2 then exit. Split to one file per step with verify gates |\n| Relying on agents to `git commit` | Agents emit markers without running git. Use deterministic commit step |\n| File-writing steps without `file_exists` verification | `exit_code` auto-passes even if no file written |\n| Codex login checked only with `codex login status` | Add a tiny `codex exec --ephemeral --json --sandbox read-only` preflight probe so stale refresh tokens fail before agent steps |\n| Edit gate uses `git diff --quiet` for new files/packages | `git diff` ignores untracked files and can fail a valid implementation with `NO_CHANGES`; use `git status --short -- <paths>` for materialization gates |\n| Hard-stop validation gates in product workflows | A red check stops the agent team at the exact moment it should fix the problem. Capture gate output with `failOnError: false`, add a repair agent step, rerun, and reserve hard failure for exhausted repair budget or external blockers |\n| Final acceptance before repair and dual review | Broken work can stop or commit without giving the team a final chance to fix it. Run repairable gates first, then the Claude-then-Codex review/fix loops, then final deterministic acceptance before commit/PR |\n| Skipping the mandatory dual review loops | Add sequential Claude-then-Codex fresh-eyes review/fix loops after repairable verification and before final acceptance, commit, PR creation, or handoff |\n| Treating optional notification credentials as fatal | Workflow progress gets blocked by a non-core side effect. Prefer primitive/runtime fallbacks such as the Slack primitive's `cloud-relay` or `noop` shape from AgentWorkforce/relay#823 when notification is not the product contract |\n| Manual peer fanout in `handleChannelMessage()` | Use broker-managed channel subscriptions — broker fans out to all subscribers automatically |\n| Client-side `personaNames.has(from)` filtering | Use `relay.subscribe()`/`relay.unsubscribe()` — only subscribed agents receive messages |\n| Agents receiving noisy cross-channel messages during focused work | Use `relay.mute({ agent, channel })` to silence non-primary channels without leaving them |\n| Hardcoding all channels at spawn time | Use `agent.subscribe()` / `agent.unsubscribe()` for dynamic channel membership post-spawn |\n| Using `preset: 'worker'` for Codex in *interactive team* patterns when coordination is needed | Codex interactive mode works fine with PTY channel injection. Drop the preset for interactive team patterns (keep it for one-shot DAG workers where clean stdout matters) |\n| Treating the lead's informal review as final signoff | The lead may review during implementation, but final signoff still requires the mandatory Claude-then-Codex fresh-eyes review/fix loops |\n| Not printing PR URL after `createGitHubStep({ action: 'createPR' })` | Capture `html_url` with `output: { mode: 'data', format: 'json', path: 'html_url' }` and echo or write it in a final deterministic step |\n| Workflow ending without worktree + PR for cross-repo changes | Add `setup-worktree` at start and `push-and-pr` + `cleanup-worktree` at end |\n\n### YAML Alternative\n\n#### ```yaml\n\n```yaml\nversion: '1.0'\nname: my-workflow\nswarm:\n  pattern: dag\n  channel: wf-my-workflow\nagents:\n  - name: lead\n    cli: claude\n    role: Architect\n  - name: worker\n    cli: codex\n    role: Implementer\n  - name: claude-reviewer\n    cli: claude\n    preset: reviewer\n    role: First-pass fresh-eyes reviewer\n  - name: claude-fixer\n    cli: claude\n    role: First-pass review fixer\n  - name: codex-reviewer\n    cli: codex\n    preset: reviewer\n    role: Second-pass fresh-eyes reviewer\n  - name: codex-fixer\n    cli: codex\n    role: Second-pass review fixer\nworkflows:\n  - name: default\n    steps:\n      - name: plan\n        agent: lead\n        task: 'Produce a detailed implementation plan.'\n      - name: implement\n        agent: worker\n        task: 'Implement: {{steps.plan.output}}'\n        dependsOn: [plan]\n        verification:\n          type: exit_code\n      - name: claude-review\n        agent: claude-reviewer\n        dependsOn: [implement]\n        task: 'Review actual files, diff, rules, and evidence. Write .workflow-artifacts/my-workflow/claude-review.md with findings or NO_ISSUES_FOUND.'\n      - name: claude-fix\n        agent: claude-fixer\n        dependsOn: [claude-review]\n        task: 'Fix valid Claude review findings, add or update appropriate tests/proofs, rerun relevant checks, and write .workflow-artifacts/my-workflow/claude-fix.md.'\n      - name: claude-review-final\n        agent: claude-reviewer\n        dependsOn: [claude-fix]\n        task: 'Review the post-Claude-fix state from scratch and write .workflow-artifacts/my-workflow/claude-review-final.md.'\n      - name: claude-fix-final\n        agent: claude-fixer\n        dependsOn: [claude-review-final]\n        task: 'Fix remaining Claude findings, add/update tests or proofs, or write .workflow-artifacts/my-workflow/BLOCKED_NO_COMMIT.md.'\n      - name: codex-review\n        agent: codex-reviewer\n        dependsOn: [claude-fix-final]\n        task: 'Review the post-Claude-fix state from scratch. Write .workflow-artifacts/my-workflow/codex-review.md with findings or NO_ISSUES_FOUND.'\n      - name: codex-fix\n        agent: codex-fixer\n        dependsOn: [codex-review]\n        task: 'Fix valid Codex review findings, add or update appropriate tests/proofs, rerun relevant checks, and write .workflow-artifacts/my-workflow/codex-fix.md.'\n      - name: codex-review-final\n        agent: codex-reviewer\n        dependsOn: [codex-fix]\n        task: 'Review the post-Codex-fix state from scratch and write .workflow-artifacts/my-workflow/codex-review-final.md.'\n      - name: codex-fix-final\n        agent: codex-fixer\n        dependsOn: [codex-review-final]\n        task: 'Fix remaining Codex findings, add/update tests or proofs, or write .workflow-artifacts/my-workflow/BLOCKED_NO_COMMIT.md.'\n      - name: acceptance-after-review\n        type: deterministic\n        dependsOn: [codex-fix-final]\n        command: 'test ! -f .workflow-artifacts/my-workflow/BLOCKED_NO_COMMIT.md && echo ACCEPTANCE_OK'\n        captureOutput: true\n        failOnError: true\n```\n\n\n### Available Swarm Patterns\n\n`dag` (default), `fan-out`, `pipeline`, `hub-spoke`, `consensus`, `mesh`, `handoff`, `cascade`, `debate`, `hierarchical`, `map-reduce`, `scatter-gather`, `supervisor`, `reflection`, `red-team`, `verifier`, `auction`, `escalation`, `saga`, `circuit-breaker`, `blackboard`, `swarm`\n\nSee skill `choosing-swarm-patterns` for pattern selection guidance.\n" }
  ], { value: "run:time", outputPath: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt" });
  const result = await workflow("ricky-vfs-watch-dispatcher-status-ready-for-implementa")
    .description("# VFS-Watch Dispatcher\n\nStatus: ready for implementation\nOwner: proactive-runtime\nTracks: agent-gateway deploy (#827) · integration dispatcher (#855 follow-up)\n\n## Why this spec exists\n\nThe proactive runtime currently has **two conceptual dispatch paths**, and only one of them is shipping:\n\n1. **Synchronous inline dispatch** (live, since #855). Every provider's `handle<Provider>Forward` in `packages/web/lib/integrations/nango-webhook-router.ts` calls `dispatchIntegrationWatchEventBestEffort(...)` immediately after triggering the Nango sync. The dispatcher resolves matching agents by `watch_globs ∩ paths` and invokes `deliverDeploymentTrigger` to spawn a Daytona sandbox.\n2. **VFS-watch dispatch** (designed, not deployed). `cloud/services/agent-gateway/src/watch-subscriber.ts` (`WorkspaceWatchSubscriber` + `matchesAnyGlob`) was built to subscribe to Relayfile filesystem watch events, match each write against `agents.watch_globs`, and dispatch via the Durable Object gateway. The worker has no SST infra file and isn't deployed in the production CF account.\n\nPath 1 is the minimum that gets event-driven personas firing today. Path 2 is the durable, general-purpose substrate the runtime was originally specced against. This document defines what shipping path 2 looks like, why it matters beyond what path 1 already covers, and how the two coexist during the migration.\n\n## What path 1 doesn't cover\n\nThe inline dispatch only fires for events that pass through `nango-webhook-router.ts`. Concretely, that excludes:\n\n- **Non-webhook VFS writes.** Bulk sync results, replay/backfill jobs, `npx workforce admin` operations, and any future direct-write surface (e.g. Relayfile SDK write from a sibling worker) never invoke the inline handler. Agents watching `/<provider>/...` paths are not invoked.\n- **Cross-provider watch globs.** An agent declaring `watch_globs: [\"/**/issues/opened/**\"]` (provider-agnostic; meant to watch issues from any wired integration) gets dispatched only by the specific provider handler that knows about issues. Adding a new provider means touching every cross-cutting persona's wiring assumption.\n- **Failure isolation.** If `handleGitHubForward` throws after the Nango sync but before `dispatchIntegrationWatchEventBestEffort` (or the dispatcher itself throws), the agent never fires *and* there is no durable record of \"event happened, dispatch owed.\" VFS-watch consumes from a stream that survives handler crashes.\n- **Replay.** Operators cannot replay a window of historical events (post-incident, after a persona is added that should have caught past events, etc.). VFS rewind + re-emit is the natural way to do it.\n- **Multi-consumer fan-out.** Today, only the inline dispatcher consumes the event. Future consumers (metrics, audit, secondary projections, sage) each need their own hook in `handleForward`. A VFS-watch stream lets any number of subscribers attach without modifying the producer.\n\nPath 1 is \"good enough\" for the demo loop. Path 2 is the substrate the runtime architecture documents (`docs/proactive-runtime/spec.md`, `docs/proactive-runtime/event-shape.md`) assume.\n\n## Goals\n\n- **G1**: A persona with `watch_globs: [\"/github/issues/opened/**\"]` fires when *any* writer (Nango sync, admin SDK, replay tool, manual write) creates a matching file in Relayfile VFS — not only when a webhook flows through the provider's `handleForward`.\n- **G2**: Inline dispatch (path 1) does not regress during the migration; existing personas keep firing through the same `deliverDeploymentTrigger` they use today.\n- **G3**: Exactly-once delivery for the agent perspective. If both inline and VFS-watch dispatch resolve the same `(workspace, agentId, eventId)`, only one Daytona spawn happens.\n- **G4**: Operator-visible. A given write should be traceable: did it fan out? to which agents? did each delivery succeed? was it deduped? Surfaced in CF Observability and Workers Logs, not buried in a Durable Object.\n\n## Non-goals\n\n- Replacing Relayfile VFS itself. The dispatcher consumes the existing watch stream; the spec assumes Relayfile is already the source of truth.\n- Replacing `deliverDeploymentTrigger`. That function stays the single agent invocation primitive — the dispatcher only changes *who calls it and on what trigger*.\n- Solving the agent-gateway `WorkspaceGatewayDO` cron-tick path. That hop is unblocked by the daytona-auth fix; this spec is scoped to the VFS-watch path.\n\n## Design\n\n### Components\n\n```\n┌──────────────────┐\n│ Relayfile (VFS)  │   --- write event ---->  ┌─────────────────────────┐\n│                  │                          │ agent-gateway worker    │\n└──────────────────┘                          │  (Cloudflare Worker)    │\n       ▲                                      │                         │\n       │ writes from any source:              │  WorkspaceWatchSubscriber\n       │   - nango syncs                      │    - dedupe (workspace, │\n       │   - admin SDK                        │      path, version)     │\n       │   - replay tool                      │    - matchesAnyGlob     │\n       │   - sage / sibling workers           │    - fan out to agents  │\n       │                                      │                         │\n       │                                      │  ↓ for each matched     │\n       │                                      │                         │\n       │                                      │  deliverDeploymentTrigger\n       │                                      │    (shared with inline +│\n       │                                      │     cron path)          │\n       │                                      │       ↓                 │\n       │                                      │  Daytona sandbox runs   │\n       │                                      │  persona.onEvent        │\n       └──────────────────────────────────────┴─────────────────────────┘\n```\n\n### Trigger surface\n\nThe agent-gateway worker exposes a single durable-object-backed entrypoint that consumes Relayfile watch events via **push delivery**: Relayfile POSTs `{ workspace, path, writeId, occurredAt, provider?, eventType?, payload? }` to an authenticated endpoint on agent-gateway whenever a write completes. The endpoint is HMAC-authenticated using `RELAYFILE_INTERNAL_HMAC_SECRET` (the same secret pattern used by other internal worker-to-worker calls).\n\nPush was chosen over pull/WebSocket for three reasons: (1) lowest latency — no per-workspace cursor round-trip; (2) operability — every event is a discrete HTTP invocation visible in CF Observability + Workers Logs without DO introspection; (3) Relayfile already has outbound webhook capability, so this reuses an existing path instead of standing up a new stream protocol. The cost is that Relayfile owns retry on push failure — which it already does for its other outbound webhooks.\n\n### Dispatcher contract\n\n```ts\ntype VfsWatchEvent = {\n  workspaceId: string;\n  path: string;             // e.g. \"/github/issues/opened/123-foo.json\"\n  writeId: string;          // monotonic, per-workspace, dedupe key\n  provider?: string;        // hint when the writer knows it (Nango sync sets this)\n  eventType?: string;       // hint when the writer knows it (\"issues.opened\")\n  payload?: unknown;        // optional, for writers that already have it in hand\n  occurredAt: string;\n};\n\nasync function onVfsWatchEvent(event: VfsWatchEvent): Promise<VfsDispatchResult> {\n  // 1. Dedupe by (workspaceId, writeId). DO storage keys: `dedupe:${ws}:${writeId}`.\n  //    TTL = 24h.\n  if (await isAlreadyDelivered(event)) return { skipped: \"dedupe\" };\n\n  // 2. Match candidate agents in workspaceId where status='active'\n  //    AND array_length(watch_globs,1) > 0\n  //    AND any glob intersects event.path (uses cloud `pathCouldIntersect`).\n  const candidates = await readCandidateAgents(event.workspaceId);\n  const matched = candidates.filter(row => agentMatchesEvent(row, event));\n\n  if (matched.length === 0) return { matched: 0, delivered: 0, failed: 0 };\n\n  // 3. Build payload using the same envelope shape inline dispatch builds.\n  const payload = buildPayload(event);\n\n  // 4. Deliver. Same primitive cron + inline use today. Parallelize across\n  //    matched agents (same semantics as the existing inline dispatcher's\n  //    Promise.allSettled fan-out); per-agent serialization is not enforced\n  //    at the dispatcher layer — Daytona's own sandbox-per-deployment\n  //    isolation is the right place for any single-agent ordering.\n  const outcomes = await Promise.allSettled(\n    matched.map(row => deliverDeploymentTrigger({ workspaceId, agentId: row.id, payload }))\n  );\n\n  // 5. Record dedupe key only on success outcomes; transient failures retry on next event.\n  await recordDedupe(event);\n\n  return { matched: matched.length, ... };\n}\n```\n\nThe matching logic must reuse the **exact** module that inline dispatch uses (`packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent`) — no parallel reimplementation. The agent-gateway worker imports it directly, or it is moved to `packages/core/proactive-runtime/match.ts` so both workers depend on the same library.\n\n### Idempotency / dedupe\n\nTwo writers can produce the same effective event:\n\n- A Nango forward arrives at `cloud-web-worker`, `handleGitHubForward` triggers a sync and calls `dispatchIntegrationWatchEventBestEffort` inline. The sync also writes to Relayfile VFS, which fires a watch event consumed by agent-gateway.\n- An admin replay re-writes a VFS file. No inline dispatch fires (no webhook), only the VFS-watch dispatch.\n\nTo deliver an agent exactly once, both dispatchers share a **dedupe key**:\n\n```\ndedupe key = sha256(workspaceId : agentId : writeId | deliveryId)\n```\n\nEither dispatcher records the key before invoking `deliverDeploymentTrigger`. A second dispatch that finds the key present logs `skipped=\"dedupe\"` and short-circuits.\n\nThe store is the agent-gateway's DO (strong consistency per workspace). `cloud-web-worker` calls into the DO via service binding named **`AGENT_GATEWAY_DEDUPE_BROKER`** to record/check.\n\n**Dedupe TTL: 24h.** Records expire from the DO 24 hours after the originating write. The window is set to:\n\n- comfortably cover any same-day replay (operator replays a window post-incident within hours, not days);\n- bound the DO storage cost (no unbounded growth as workspaces accumulate writes);\n- be long enough that a same-event redelivery (Nango retry storms, gateway restart, etc.) within a working day is caught.\n\n### Coexistence with inline dispatch\n\nThe rollout is two phases over one week:\n\n**Phase A — dual-dispatch.** Inline (`dispatchIntegrationWatchEventBestEffort` in `nango-webhook-router.ts`) and VFS-watch both fire; the dedupe key collapses double-delivery. Lowest risk to existing event flow; cleanest rollback (revert agent-gateway deploy, inline keeps working).\n\n**Phase B — VFS-watch only.** After ~one week of healthy dual-dispatch (verified by `vfs-watch-dispatch` logs showing dedupe coverage matches inline-dispatch coverage 1:1 and `failed=0`), downgrade `dispatchIntegrationWatchEventBestEffort` to a metrics-only no-op. Single dispatcher, one place to reason about ordering, lower duplicate-work cost.\n\nPhase A is the default the implementation PR ships. Phase B is a follow-up PR that flips a single function in `nango-webhook-router.ts`.\n\n### Failure modes & observability\n\nEvery code path emits a single line keyed `area: \"vfs-watch-dispatch\"`, with at minimum:\n\n- `workspaceId`, `path`, `writeId`\n- `matched` count\n- `dedupe` outcome (`first` / `skipped`)\n- `delivered` / `failed` counts\n- `error` chain (cause-walked, matches the persona-bundle-deploy pattern at `route.ts:560`)\n\nWorkers Logs persistent (from #863) is required — these go straight into the same dashboard query operators already use for inline dispatch.\n\nFailure cases the spec is opinionated about:\n\n- **Daytona spawn fails**: log `failed`, do *not* record dedupe key, allow next event to retry. (Identical to current cron tick behaviour now that #886 is fixed.)\n- **DB read fails**: return error, do not record dedupe; watch-stream consumer treats as `retry`.\n- **Watch-stream cursor lag**: the consumer surfaces a `vfs-watch-lag-seconds` metric; alert at >60s sustained.\n\n### Boundary cases\n\n- **Persona deployed mid-write**. A persona registered at `t1` with watch globs that intersect a write at `t0 < t1` does **not** fire (no backfill on registration). Backfill semantics are out of scope; the replay tool (step 7 of the migration plan) covers operator-opt-in backfill over an explicit window.\n- **Persona destroyed mid-write**. The agent row's `status` is checked in the candidate query; destroyed agents are filtered. Already correct via the existing `agentMatchesEvent`.\n- **Watch globs include cross-provider patterns** (`/**/issues/opened/**`). `pathCouldIntersect` already handles glob prefix intersection across multiple providers — covered by the existing helper, no change required.\n- **No agents match the event**. The dispatcher still runs the candidate query (`matched=0`) and logs the outcome. The query is a single indexed read against `agents.workspace_id`; the cost is negligible at any anticipated scale and the resulting log line is load-bearing for the \"did anyone watch this?\" diagnostic. No early-out optimization.\n\n## Migration plan\n\n1. **#827 follow-up: deploy agent-gateway via SST.** Currently `cloud/services/agent-gateway/wrangler.toml` exists but no `infra/agent-gateway.ts` declares `new sst.cloudflare.Worker(...)`. Add it, with the DO bindings + service bindings to `cloud-web-worker` (for the dedupe broker), and the `RELAYFILE_*` envs needed by `WorkspaceWatchSubscriber`.\n2. **Move matching helpers to a shared package.** Either:\n   - Export `agentMatchesEvent` from `packages/core/proactive-runtime/match.ts` and import from both `cloud-web-worker` (inline dispatch) and `agent-gateway` (vfs-watch).\n   - Or service-bind `cloud-web-worker` from `agent-gateway` and call the existing module across the boundary. The first option is simpler.\n3. **Wire dedupe service binding.** `cloud-web-worker` already imports the gateway URL; add a service binding so `dispatchIntegrationWatchEventBestEffort` can check/record dedupe before calling `deliverDeploymentTrigger`.\n4. **Implement `onVfsWatchEvent`.** Push endpoint on the gateway DO (HMAC-authed by `RELAYFILE_INTERNAL_HMAC_SECRET`), as defined in §Trigger surface. Relayfile owns retry on push failure.\n5. **Roll out dual-dispatch (Phase A above).** Observe `vfs-watch-dispatch` logs, confirm dedupe coverage matches inline-dispatch coverage 1:1.\n6. **Downgrade inline dispatch (Phase B).** After ~one week of dual-dispatch with healthy dedupe, change `dispatchIntegrationWatchEventBestEffort` to emit a metrics-only record (no `deliverDeploymentTrigger`). Both surfaces still log; only VFS-watch invokes agents.\n7. **Document replay tool**. Spec it separately, but the dispatcher's design (dedupe on `writeId`, agent-side delivery is idempotent because `deliverDeploymentTrigger` uses the deployment row state) enables an operator command of the form `relayfile admin replay --workspace ... --since ... --globs ...` that re-emits a window of writes back through the VFS-watch stream.\n\n## Test plan\n\n- [ ] Unit: `onVfsWatchEvent` dedupe behaviour across `(first | second | third)` deliveries with the same `writeId`\n- [ ] Unit: matched / unmatched candidate filtering (mirror the existing `integration-watch-dispatcher.test.ts`)\n- [ ] Integration: deploy a persona watching `/github/issues/opened/**`, write a file directly to the workspace VFS via the admin SDK (no webhook involved). Expect `vfs-watch-dispatch` to dispatch and the persona to fire.\n- [ ] Integration: same persona, this time produce the event via a real GitHub webhook. Expect exactly one `deliverDeploymentTrigger` call (dedupe collapses inline + VFS-watch).\n- [ ] Observability: CF logs query for `area: \"vfs-watch-dispatch\"` shows `matched`/`delivered`/`failed`/`dedupe` fields and `workspaceId` for each event.\n- [ ] Failure: stub Daytona to throw; confirm the dispatcher records the failure but does *not* mark the writeId as delivered, so the next VFS event with the same id retries.")
    .pattern("dag")
    .channel("wf-ricky-vfs-watch-dispatcher-status-ready-for-implementa")
    .maxConcurrency(4)
    .timeout(43200000)
    .onError('retry', { maxRetries: 2, retryDelayMs: 10000, repairAgent: "validator-claude", repairRetries: 2 })

    .agent("lead-claude", { cli: "claude", interactive: false, role: "Plans task shape, ownership, non-goals, and verification gates.", retries: 1 })
    .agent("impl-primary-codex", { cli: "codex", role: "Primary implementer for independent file slices and code changes.", retries: 2 })
    .agent("impl-tests-codex", { cli: "codex", role: "Adds or updates tests and validation coverage for the changed surface.", retries: 2 })
    .agent("reviewer-claude", { cli: "claude", preset: "reviewer", role: "Reviews product fit, scope control, and workflow evidence quality.", retries: 1 })
    .agent("reviewer-codex", { cli: "codex", preset: "reviewer", role: "Reviews TypeScript correctness, deterministic gates, and test coverage.", retries: 1 })
    .agent("validator-claude", { cli: "claude", preset: "worker", role: "Runs the 80-to-100 fix loop and verifies final readiness.", retries: 2 })
    .agent("validator-codex", { cli: "codex", preset: "worker", role: "Runs the final Codex review-fix loop and verifies final readiness.", retries: 2 })

    .step("prepare-context", {
      type: 'deterministic',
      command: "mkdir -p '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.txt' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/non-goals.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/deliverables.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan-instructions.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/implementation-instructions.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-checklist.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/pattern-decision.txt' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/loaded-skills.txt' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-matches.json' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/tool-selection.json' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-runtime-boundary.txt' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/matched-skills.md' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt' && echo GENERATED_WORKFLOW_CONTEXT_READY",
      captureOutput: true,
      failOnError: true,
    })

    .step("skill-boundary-metadata-gate", {
      type: 'deterministic',
      dependsOn: ["prepare-context"],
      command: "test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-matches.json' && test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/tool-selection.json' && grep -F 'generation_time_only' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"runtimeEmbodiment\":false' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F 'choosing-swarm-patterns' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F 'composio-cli' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F 'relay-80-100-workflow' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F 'review-fix-signoff-loop' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F 'writing-agent-relay-workflows' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"stage\":\"generation_selection\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"stage\":\"generation_loading\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"effect\":\"metadata\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"stage\":\"generation_rendering\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"effect\":\"pattern_selection\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"stage\":\"generation_rendering\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"effect\":\"workflow_contract\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"stage\":\"generation_rendering\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"effect\":\"validation_gates\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"stage\":\"generation_rendering\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json' && grep -F '\"skillName\":\"review-fix-signoff-loop\"' '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json'",
      captureOutput: true,
      failOnError: true,
    })

    .step('lead-plan', {
      agent: "lead-claude",
      dependsOn: ['skill-boundary-metadata-gate'],
      timeoutMs: 600000,
      task: `Plan the workflow execution from the packaged context files.

Read these files in order:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan-instructions.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/non-goals.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/deliverables.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt

Write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md.
Required headings: Non-goals, Routing contract, Implementation contract.
End the file with GENERATION_LEAD_PLAN_READY.`,
      verification: { type: 'output_contains', value: 'GENERATION_LEAD_PLAN_READY' },
    })

    .step("lead-plan-gate", {
      type: 'deterministic',
      dependsOn: ["lead-plan"],
      command: "node <<'NODE'\nconst fs = require('node:fs');\nconst leadPlanPath = \".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md\";\nconst body = fs.readFileSync(leadPlanPath, 'utf8');\nif (!body.includes('GENERATION_LEAD_PLAN_READY')) throw new Error('lead plan missing required marker: GENERATION_LEAD_PLAN_READY');\nif (!/\\b(non-goals?|out[- ]of[- ]scope|not in scope)\\b/i.test(body)) throw new Error('lead plan missing required marker: Non-goals or Out of scope');\nconst hasRoutingContract = /Routing contract/i.test(body) || /Local execution must run through Agent Relay/i.test(body) || /Run local execution through the generated Agent Relay workflow artifact/i.test(body) || /routes local execution through the generated Agent Relay artifact/i.test(body) || /Use the generated Agent Relay workflow artifact/i.test(body);\nif (!hasRoutingContract) throw new Error('lead plan missing required marker: Routing contract');\nconst hasImplementationContract = /Implementation contract/i.test(body) || /This is an implementation spec/i.test(body);\nif (!hasImplementationContract) throw new Error('lead plan missing required marker: Implementation contract');\nconsole.log('LEAD_PLAN_GATE_OK');\nNODE",
      captureOutput: true,
      failOnError: true,
    })

    .step('implement-artifact', {
      agent: "impl-primary-codex",
      dependsOn: ['lead-plan-gate'],

      timeoutMs: 1200000,
      task: `Implement the requested code-writing workflow slice.

Read these packaged context files before editing:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/implementation-instructions.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/deliverables.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/matched-skills.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt

Own only the declared targets from .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json unless review feedback narrows a required fix.
Declared target files are listed in .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/deliverables.md.

Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Keep execution routing explicit for local, cloud, and MCP callers. Materialize outputs to disk, then stop for deterministic gates.`,
    })

    .step("post-implementation-file-gate", {
      type: 'deterministic',
      dependsOn: ["implement-artifact"],
      command: "node <<'NODE'\nconst fs = require('node:fs');\nconst { execFileSync } = require('node:child_process');\nconst evidencePath = \".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/implementation-file-gate.txt\";\nconst declaredTargets = \"[\\n  \\\"packages/web/lib/integrations/nango-webhook-router.ts\\\",\\n  \\\"cloud/services/agent-gateway/src/watch-subscriber.ts\\\",\\n  \\\"/<provider>/...\\\",\\n  \\\"docs/proactive-runtime/spec.md\\\",\\n  \\\"docs/proactive-runtime/event-shape.md\\\",\\n  \\\"packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent\\\",\\n  \\\"packages/core/proactive-runtime/match.ts\\\",\\n  \\\"/**/issues/opened/**\\\",\\n  \\\"cloud/services/agent-gateway/wrangler.toml\\\",\\n  \\\"infra/agent-gateway.ts\\\",\\n  \\\"/github/issues/opened/**\\\"\\n]\";\nconst changed = collectChangedPaths();\nfs.mkdirSync(require('node:path').dirname(evidencePath), { recursive: true });\nfs.writeFileSync(evidencePath, [`Declared targets:`, declaredTargets, `Changed paths:`, ...changed].flat().join('\\n') + '\\n');\nif (changed.length === 0) throw new Error('implementation produced no repository diff outside workflow artifacts');\nconst materialized = changed.filter((path) => fs.existsSync(path) && fs.statSync(path).isFile());\nif (materialized.length === 0) throw new Error('implementation diff has no materialized file paths to review');\nconsole.log('IMPLEMENTATION_FILE_GATE_OK');\n\nfunction collectChangedPaths() {\n  const tracked = execFileSync('git', ['-c', 'core.quotePath=false', 'diff', '--name-only', '--diff-filter=ACMRT'], { encoding: 'utf8' });\n  const untracked = execFileSync('git', ['-c', 'core.quotePath=false', 'ls-files', '--others', '--exclude-standard'], { encoding: 'utf8' });\n  return [...tracked.split(/\\r?\\n/), ...untracked.split(/\\r?\\n/)]\n    .map((line) => line.trim())\n    .filter(Boolean)\n    .filter((path) => !path.startsWith('.workflow-artifacts/'))\n    .sort();\n}\nNODE",
      captureOutput: true,
      failOnError: true,
    })

    .step("initial-soft-validation", {
      type: 'deterministic',
      dependsOn: ["post-implementation-file-gate"],
      command: "npx tsc --noEmit && npm test --workspace='packages/core' && npm test --workspace='packages/web'",
      captureOutput: true,
      failOnError: false,
    })

    .step("review-claude", {
      agent: "reviewer-claude",
      dependsOn: ["initial-soft-validation"],

      timeoutMs: 600000,
      task: `Review the generated work.

Read:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-checklist.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt

Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-claude.md using this structured verdict format:
verdict: FINDINGS | NO_ISSUES_FOUND | BLOCKED
finding_id: short-stable-id
severity: blocker | high | medium | low
file: path/to/file
issue: what is wrong
fix_required: concrete change needed
test_required: test, fixture, assertion, or proof command needed
status: open | fixed | wontfix | blocked
evidence: commands run, file paths, or blocker details

If there are no actionable issues, write verdict: NO_ISSUES_FOUND.
End the file with REVIEW_COMPLETE.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-claude.md" },
    })

    .step("fix-loop", {
      agent: "validator-claude",
      dependsOn: ["review-claude", "initial-soft-validation"],

      timeoutMs: 1200000,
      task: `Run the 80-to-100 review-fix loop.

Inputs:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-claude.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md

Review feedback:
Read .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-claude.md from disk.

Initial validation output:
{{steps.initial-soft-validation.output}}


If the review says verdict: NO_ISSUES_FOUND, record that no fix was needed.
If the review lists findings, fix every valid issue and add or update appropriate tests, fixtures, assertions, or deterministic proofs for testable findings.
If no fix is possible, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/BLOCKED_NO_COMMIT.md with exact evidence.
Preserve the declared target boundary:
- packages/web/lib/integrations/nango-webhook-router.ts
- cloud/services/agent-gateway/src/watch-subscriber.ts
- /<provider>/...
- docs/proactive-runtime/spec.md
- docs/proactive-runtime/event-shape.md
- packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent
- packages/core/proactive-runtime/match.ts
- /**/issues/opened/**
- cloud/services/agent-gateway/wrangler.toml
- infra/agent-gateway.ts
- /github/issues/opened/**

Tool selection: runner=@agent-relay/sdk; concurrency=1; rule=project default runner @agent-relay/sdk.

Before exiting, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/fix-loop-report.md summarizing the exact fixes you applied or explicitly saying that no repo changes were required, then end that file with FIX_LOOP_COMPLETE.
Re-run typecheck and tests before handing off to post-fix validation.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/fix-loop-report.md" },
    })

    .step("fix-loop-report-gate", {
      type: 'deterministic',
      dependsOn: ["fix-loop"],
      command: "test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/fix-loop-report.md' && tail -n 1 '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/fix-loop-report.md' | tr -d '[:space:]' | grep -Eq '^FIX_LOOP_COMPLETE$'",
      captureOutput: true,
      failOnError: true,
    })

    .step("post-fix-verification-gate", {
      type: 'deterministic',
      dependsOn: ["fix-loop-report-gate"],
      command: "node <<'NODE'\nconst fs = require('node:fs');\nconst { execFileSync } = require('node:child_process');\nconst evidencePath = \".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/implementation-file-gate.txt\";\nconst declaredTargets = \"[\\n  \\\"packages/web/lib/integrations/nango-webhook-router.ts\\\",\\n  \\\"cloud/services/agent-gateway/src/watch-subscriber.ts\\\",\\n  \\\"/<provider>/...\\\",\\n  \\\"docs/proactive-runtime/spec.md\\\",\\n  \\\"docs/proactive-runtime/event-shape.md\\\",\\n  \\\"packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent\\\",\\n  \\\"packages/core/proactive-runtime/match.ts\\\",\\n  \\\"/**/issues/opened/**\\\",\\n  \\\"cloud/services/agent-gateway/wrangler.toml\\\",\\n  \\\"infra/agent-gateway.ts\\\",\\n  \\\"/github/issues/opened/**\\\"\\n]\";\nconst changed = collectChangedPaths();\nfs.mkdirSync(require('node:path').dirname(evidencePath), { recursive: true });\nfs.writeFileSync(evidencePath, [`Declared targets:`, declaredTargets, `Changed paths:`, ...changed].flat().join('\\n') + '\\n');\nif (changed.length === 0) throw new Error('implementation produced no repository diff outside workflow artifacts');\nconst materialized = changed.filter((path) => fs.existsSync(path) && fs.statSync(path).isFile());\nif (materialized.length === 0) throw new Error('implementation diff has no materialized file paths to review');\nconsole.log('IMPLEMENTATION_FILE_GATE_OK');\n\nfunction collectChangedPaths() {\n  const tracked = execFileSync('git', ['-c', 'core.quotePath=false', 'diff', '--name-only', '--diff-filter=ACMRT'], { encoding: 'utf8' });\n  const untracked = execFileSync('git', ['-c', 'core.quotePath=false', 'ls-files', '--others', '--exclude-standard'], { encoding: 'utf8' });\n  return [...tracked.split(/\\r?\\n/), ...untracked.split(/\\r?\\n/)]\n    .map((line) => line.trim())\n    .filter(Boolean)\n    .filter((path) => !path.startsWith('.workflow-artifacts/'))\n    .sort();\n}\nNODE",
      captureOutput: true,
      failOnError: true,
    })

    .step("active-reference-gate", {
      type: 'deterministic',
      dependsOn: ["post-fix-verification-gate"],
      command: "printf '%s\\n' 'No manifest-driven deleted paths to check.' > '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/active-reference-check.txt'",
      captureOutput: true,
      failOnError: true,
    })

    .step("post-fix-validation", {
      type: 'deterministic',
      dependsOn: ["active-reference-gate"],
      command: "npx tsc --noEmit && npm test --workspace='packages/core' && npm test --workspace='packages/web'",
      captureOutput: true,
      failOnError: false,
    })

    .step("final-review-claude", {
      agent: "reviewer-claude",
      dependsOn: ["post-fix-validation"],

      timeoutMs: 600000,
      task: `Re-review the fixed state only.

Read:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-checklist.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt

Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-claude.md using this structured verdict format:
verdict: FINDINGS | NO_ISSUES_FOUND | BLOCKED
finding_id: short-stable-id
severity: blocker | high | medium | low
file: path/to/file
issue: what is wrong
fix_required: concrete change needed
test_required: test, fixture, assertion, or proof command needed
status: open | fixed | wontfix | blocked
evidence: commands run, file paths, or blocker details

If there are no actionable issues, write verdict: NO_ISSUES_FOUND.
End the file with FINAL_REVIEW_CLAUDE_COMPLETE.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-claude.md" },
    })

    .step("final-fix-claude", {
      agent: "validator-claude",
      dependsOn: ["final-review-claude"],

      timeoutMs: 1200000,
      task: `Run the final review-fix pass.

Inputs:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-claude.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md

Review feedback:
Read .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-claude.md from disk.



If the review says verdict: NO_ISSUES_FOUND, record that no fix was needed.
If the review lists findings, fix every valid issue and add or update appropriate tests, fixtures, assertions, or deterministic proofs for testable findings.
If no fix is possible, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/BLOCKED_NO_COMMIT.md with exact evidence.
Preserve the declared target boundary:
- packages/web/lib/integrations/nango-webhook-router.ts
- cloud/services/agent-gateway/src/watch-subscriber.ts
- /<provider>/...
- docs/proactive-runtime/spec.md
- docs/proactive-runtime/event-shape.md
- packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent
- packages/core/proactive-runtime/match.ts
- /**/issues/opened/**
- cloud/services/agent-gateway/wrangler.toml
- infra/agent-gateway.ts
- /github/issues/opened/**

Tool selection: runner=@agent-relay/sdk; concurrency=1; rule=project default runner @agent-relay/sdk.

Before exiting, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/claude-final-fix.md summarizing the exact fixes you applied or explicitly saying that no repo changes were required, then end that file with CLAUDE_FINAL_FIX_COMPLETE.
Re-run typecheck and tests before handing off to post-fix validation.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/claude-final-fix.md" },
    })

    .step("review-codex", {
      agent: "reviewer-codex",
      dependsOn: ["final-fix-claude"],

      timeoutMs: 600000,
      task: `Review the generated work.

Read:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-checklist.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt

Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-codex.md using this structured verdict format:
verdict: FINDINGS | NO_ISSUES_FOUND | BLOCKED
finding_id: short-stable-id
severity: blocker | high | medium | low
file: path/to/file
issue: what is wrong
fix_required: concrete change needed
test_required: test, fixture, assertion, or proof command needed
status: open | fixed | wontfix | blocked
evidence: commands run, file paths, or blocker details

If there are no actionable issues, write verdict: NO_ISSUES_FOUND.
End the file with REVIEW_COMPLETE.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-codex.md" },
    })

    .step("fix-loop-codex", {
      agent: "validator-codex",
      dependsOn: ["review-codex"],

      timeoutMs: 1200000,
      task: `Run the 80-to-100 review-fix loop.

Inputs:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-codex.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md

Review feedback:
Read .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-codex.md from disk.



If the review says verdict: NO_ISSUES_FOUND, record that no fix was needed.
If the review lists findings, fix every valid issue and add or update appropriate tests, fixtures, assertions, or deterministic proofs for testable findings.
If no fix is possible, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/BLOCKED_NO_COMMIT.md with exact evidence.
Preserve the declared target boundary:
- packages/web/lib/integrations/nango-webhook-router.ts
- cloud/services/agent-gateway/src/watch-subscriber.ts
- /<provider>/...
- docs/proactive-runtime/spec.md
- docs/proactive-runtime/event-shape.md
- packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent
- packages/core/proactive-runtime/match.ts
- /**/issues/opened/**
- cloud/services/agent-gateway/wrangler.toml
- infra/agent-gateway.ts
- /github/issues/opened/**

Tool selection: runner=@agent-relay/sdk; concurrency=1; rule=project default runner @agent-relay/sdk.

Before exiting, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/codex-fix-loop-report.md summarizing the exact fixes you applied or explicitly saying that no repo changes were required, then end that file with CODEX_FIX_LOOP_COMPLETE.
Re-run typecheck and tests before handing off to post-fix validation.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/codex-fix-loop-report.md" },
    })

    .step("codex-fix-loop-report-gate", {
      type: 'deterministic',
      dependsOn: ["fix-loop-codex"],
      command: "test -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/codex-fix-loop-report.md' && tail -n 1 '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/codex-fix-loop-report.md' | tr -d '[:space:]' | grep -Eq '^CODEX_FIX_LOOP_COMPLETE$'",
      captureOutput: true,
      failOnError: true,
    })

    .step("post-codex-fix-validation", {
      type: 'deterministic',
      dependsOn: ["codex-fix-loop-report-gate"],
      command: "npx tsc --noEmit && npm test --workspace='packages/core' && npm test --workspace='packages/web'",
      captureOutput: true,
      failOnError: false,
    })

    .step("final-review-codex", {
      agent: "reviewer-codex",
      dependsOn: ["post-codex-fix-validation"],

      timeoutMs: 600000,
      task: `Re-review the fixed state only.

Read:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/review-checklist.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/normalized-spec.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/lead-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/target-context.txt

Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-codex.md using this structured verdict format:
verdict: FINDINGS | NO_ISSUES_FOUND | BLOCKED
finding_id: short-stable-id
severity: blocker | high | medium | low
file: path/to/file
issue: what is wrong
fix_required: concrete change needed
test_required: test, fixture, assertion, or proof command needed
status: open | fixed | wontfix | blocked
evidence: commands run, file paths, or blocker details

If there are no actionable issues, write verdict: NO_ISSUES_FOUND.
End the file with FINAL_REVIEW_CODEX_COMPLETE.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-codex.md" },
    })

    .step("final-fix-codex", {
      agent: "validator-codex",
      dependsOn: ["final-review-codex"],

      timeoutMs: 1200000,
      task: `Run the final review-fix pass.

Inputs:
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-codex.md
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/acceptance-contract.json
- .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/verification-plan.md

Review feedback:
Read .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/final-review-codex.md from disk.



If the review says verdict: NO_ISSUES_FOUND, record that no fix was needed.
If the review lists findings, fix every valid issue and add or update appropriate tests, fixtures, assertions, or deterministic proofs for testable findings.
If no fix is possible, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/BLOCKED_NO_COMMIT.md with exact evidence.
Preserve the declared target boundary:
- packages/web/lib/integrations/nango-webhook-router.ts
- cloud/services/agent-gateway/src/watch-subscriber.ts
- /<provider>/...
- docs/proactive-runtime/spec.md
- docs/proactive-runtime/event-shape.md
- packages/web/lib/proactive-runtime/integration-watch-dispatcher.ts:agentMatchesEvent
- packages/core/proactive-runtime/match.ts
- /**/issues/opened/**
- cloud/services/agent-gateway/wrangler.toml
- infra/agent-gateway.ts
- /github/issues/opened/**

Tool selection: runner=@agent-relay/sdk; concurrency=1; rule=project default runner @agent-relay/sdk.

Before exiting, write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/codex-final-fix.md summarizing the exact fixes you applied or explicitly saying that no repo changes were required, then end that file with CODEX_FINAL_FIX_COMPLETE.
Re-run typecheck and tests before handing off to post-fix validation.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/codex-final-fix.md" },
    })

    .step("final-review-pass-gate", {
      type: 'deterministic',
      dependsOn: ["final-fix-codex"],
      command: "tail -n 1 '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/claude-final-fix.md' | tr -d '[:space:]*' | grep -Eq '^CLAUDE_FINAL_FIX_COMPLETE$' && tail -n 1 '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/codex-final-fix.md' | tr -d '[:space:]*' | grep -Eq '^CODEX_FINAL_FIX_COMPLETE$' && test ! -f '.workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/BLOCKED_NO_COMMIT.md'",
      captureOutput: true,
      failOnError: true,
    })

    .step("final-hard-validation", {
      type: 'deterministic',
      dependsOn: ["final-review-pass-gate"],
      command: "npx tsc --noEmit && npm test --workspace='packages/core' && npm test --workspace='packages/web'",
      captureOutput: true,
      failOnError: true,
    })

    .step("git-diff-gate", {
      type: 'deterministic',
      dependsOn: ["final-hard-validation"],
      command: "node <<'NODE'\nconst fs = require('node:fs');\nconst path = require('node:path');\nconst { execFileSync } = require('node:child_process');\nconst gitDiffPath = \".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/git-diff.txt\";\nconst tracked = execFileSync('git', ['-c', 'core.quotePath=false', 'diff', '--name-status', '--diff-filter=ACMRTD'], { encoding: 'utf8' })\n  .split(/\\r?\\n/)\n  .map((line) => line.trim())\n  .filter(Boolean);\nconst untracked = execFileSync('git', ['-c', 'core.quotePath=false', 'ls-files', '--others', '--exclude-standard'], { encoding: 'utf8' })\n  .split(/\\r?\\n/)\n  .map((line) => line.trim())\n  .filter(Boolean)\n  .map((file) => `A\\t${file}`);\nconst actual = [...tracked, ...untracked]\n  .filter((line) => !line.replace(/^[A-Z]+\\s+/, '').startsWith('.workflow-artifacts/'))\n  .sort();\nfs.mkdirSync(path.dirname(gitDiffPath), { recursive: true });\nfs.writeFileSync(gitDiffPath, `${actual.join('\\n')}\\n`);\nif (actual.length === 0) throw new Error('git diff evidence is empty');\nconsole.log('GIT_DIFF_GATE_OK');\nNODE",
      captureOutput: true,
      failOnError: true,
    })

    .step("regression-gate", {
      type: 'deterministic',
      dependsOn: ["git-diff-gate"],
      command: "npx vitest run",
      captureOutput: true,
      failOnError: true,
    })

    .step('final-signoff', {
      agent: "validator-claude",
      dependsOn: ['regression-gate'],

      timeoutMs: 600000,
      task: `Write .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/signoff.md.

Include:
- files changed
- source changes and implementation diff evidence
- status-prefixed changed-file inventory and command summaries
- dry-run command to execute before runtime launch
- deterministic validation commands
- review verdicts
- PR URL or a clear result location/status when PR creation is intentionally out of scope
- skill application boundary from .workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/skill-application-boundary.json
- remaining risks or environmental blockers
- every current output-manifest path, and no stale cleanup targets unless those targets are in the current manifest

Tool selection: runner=@agent-relay/sdk; concurrency=1; rule=project default runner @agent-relay/sdk.

End with GENERATED_WORKFLOW_READY.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/vfs-watch-dispatcher-status-ready-for-implementa/signoff.md" },
    })

    .run({ cwd: process.cwd() });

  console.log(result.status);
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
