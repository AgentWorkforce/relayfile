import { workflow } from '@relayflows/core';
import * as rickyWorkflowFs from 'node:fs';
import * as rickyWorkflowPath from 'node:path';

// IMPLEMENTATION_WORKFLOW_CONTRACT: implementation specs must produce source changes, tests, non-empty diff evidence, and PR/result reporting.
// RICKY_WORKFLOW_ENV_LOADER: load repo-local env files before spawning workflow agents.

function loadRickyWorkflowEnv(cwd = process.cwd()) {
  for (const file of ['.env.local', '.env']) {
    const path = rickyWorkflowPath.join(cwd, file);
    if (!rickyWorkflowFs.existsSync(path)) continue;
    const body = rickyWorkflowFs.readFileSync(path, 'utf8');
    for (const rawLine of body.split(/\r?\n/)) {
      const line = rawLine.trim();
      if (!line || line.startsWith('#')) continue;
      const match = /^(?:export\s+)?([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.*)$/.exec(line);
      if (!match) continue;
      const [, key, rawValue] = match;
      if (!key || rawValue === undefined || process.env[key] !== undefined) continue;
      process.env[key] = unquoteRickyWorkflowEnvValue(rawValue);
    }
  }
}

function unquoteRickyWorkflowEnvValue(value: string): string {
  const trimmed = value.trim();
  if ((trimmed.startsWith('"') && trimmed.endsWith('"')) || (trimmed.startsWith("'") && trimmed.endsWith("'"))) {
    return trimmed.slice(1, -1);
  }
  return trimmed;
}

function assertRickyWorkflowEnv(names: string[]): void {
  const missing = names.filter((name) => !process.env[name]);
  if (missing.length > 0) {
    throw new Error(`MISSING_ENV_VAR: ${missing.join(', ')}. Add missing values to .env.local or export them before rerunning.`);
  }
}

interface RickyGeneratedContextFile {
  path: string;
  content: string;
}

interface RickyGeneratedTargetContext {
  value: string;
  outputPath: string;
}

function writeRickyGeneratedContextFiles(files: RickyGeneratedContextFile[], targetContext?: RickyGeneratedTargetContext): void {
  for (const file of files) {
    rickyWorkflowFs.mkdirSync(rickyWorkflowPath.dirname(file.path), { recursive: true });
    rickyWorkflowFs.writeFileSync(file.path, ensureTrailingNewline(file.content));
  }

  if (!targetContext) return;

  rickyWorkflowFs.mkdirSync(rickyWorkflowPath.dirname(targetContext.outputPath), { recursive: true });
  const targetContextSourcePath = resolveRickyGeneratedTargetContextPath(targetContext.value);
  if (targetContextSourcePath) {
    rickyWorkflowFs.copyFileSync(targetContextSourcePath, targetContext.outputPath);
    return;
  }

  rickyWorkflowFs.writeFileSync(targetContext.outputPath, ensureTrailingNewline(targetContext.value));
}

function resolveRickyGeneratedTargetContextPath(value: string): string | null {
  if (rickyWorkflowPath.isAbsolute(value)) return null;

  const workspaceRoot = rickyWorkflowFs.realpathSync(process.cwd());
  const candidatePath = rickyWorkflowPath.resolve(workspaceRoot, value);

  try {
    if (!rickyWorkflowFs.existsSync(candidatePath) || !rickyWorkflowFs.statSync(candidatePath).isFile()) {
      return null;
    }

    const realCandidatePath = rickyWorkflowFs.realpathSync(candidatePath);
    if (realCandidatePath === workspaceRoot) return null;
    if (!realCandidatePath.startsWith(`${workspaceRoot}${rickyWorkflowPath.sep}`)) return null;
    return realCandidatePath;
  } catch {
    return null;
  }
}

function ensureTrailingNewline(value: string): string {
  return value.endsWith('\n') ? value : `${value}\n`;
}

async function main() {
  loadRickyWorkflowEnv();
  writeRickyGeneratedContextFiles([
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.txt", content: "# Ricky Slack Nango-Routed Migration Spec\n\n**Status:** draft, depends on PR #480 (`feat/ricky-linear-agent-surface`) merging to main, since it establishes the same Nango-routed pattern for Linear and the shared `webhook-dedup.ts`.\n**Owner:** Cloud + Ricky.\n**Last updated:** 2026-05-07.\n**Primary repos:** `../cloud` (this repo).\n**Reference manifest:** `integrations/slack/ricky-manifest.json` (current direct config) and `integrations/linear/ricky-manifest.json` (post-#480 Nango pattern, the model for this migration).\n\n## 1. Problem Statement\n\nThe Ricky Slack agent v2 surface (PR #412, merged) was built before the rest of the cloud's webhook intake consolidated onto Nango. Today its dedicated Slack app POSTs directly to three Cloud routes:\n\n- `app/api/v1/ricky/slack/events/route.ts` — `app_mention`, `message.im` (async)\n- `app/api/v1/ricky/slack/commands/route.ts` — `/ricky` slash command (synchronous response expected within 3s)\n- `app/api/v1/ricky/slack/interactivity/route.ts` — gate-approval block_actions and view_submission (synchronous response expected within 3s)\n\nEach route calls `readVerifiedSlackBody` from `lib/ricky/slack/signature.ts`, which verifies a Slack HMAC against `Resource.RickySlackSigningSecret.value` / `Resource.SlackSigningSecret.value` (with env-var fallbacks `RICKY_SLACK_SIGNING_SECRET`, `SLACK_SIGNING_SECRET`).\n\nSage's Slack integration uses the opposite pattern: Slack → Nango → `/api/v1/webhooks/nango` → `routeNangoWebhook` → `routeForwardEvent` → `forwardToSage`. Nango's signature is verified once at the Cloud entry; no Slack-app-specific secret in the cloud repo. PR #480 just established the same pattern for Linear AgentSession events (`handleLinearForward` detects `AgentSessionEvent` payloads and dispatches into `dispatchLinearSessionEvent`).\n\nThis spec migrates the Ricky Slack surface to that pattern so the Slack signing secret, `lib/ricky/slack/signature.ts`, and the dedicated routes can all be removed.\n\n## 2. Goals\n\n- Point the Ricky Slack app's `events`, `commands`, and `interactivity` request URLs at Nango using the same URL pattern Sage already uses (`integrations/slack/manifest.json`): `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack`. Operators substitute the integration id at manifest-upload time.\n- Add a Slack-Ricky branch in `packages/web/lib/integrations/nango-webhook-router.ts` that detects events vs slash command vs interactivity payloads and dispatches into existing handlers in `packages/web/lib/ricky/slack/`.\n- Refactor command + interactivity dispatch to use Slack's deferred response pattern (`response_url` for slash commands and most interactivity; `chat.update` for gate-approval message edits) since Nango forwards webhooks fire-and-forget — Slack only sees Nango's 200, not Cloud's body.\n- Delete `packages/web/app/api/v1/ricky/slack/events/route.ts`, `commands/route.ts`, `interactivity/route.ts`, and `lib/ricky/slack/signature.ts`.\n- Update `integrations/slack/ricky-manifest.json` to reflect Nango-routed delivery (mirror the post-#480 `integrations/linear/ricky-manifest.json` shape; use the Nango webhook URL pattern in goal 1 above).\n- Preserve the `oauth/route.ts` flow for OAuth installation — that's not a webhook and stays as-is.\n\n## 3. Non-Goals\n\n- Sage's Slack integration is unchanged. This spec only touches the Ricky Slack app.\n- The Ricky Slack and Sage Slack apps remain separate. Consolidation into a single shared app is explicitly out of scope.\n- The slash command surface (`/ricky run ...`) keeps the same UX and supported subcommands. Only the transport changes.\n- The gate-approval interactivity behavior keeps the same UX. Only the transport changes.\n- View submissions in the current surface only do `response_action: \"clear\"` (modal close); this spec accepts that the Nango-routed flow can't return that synchronously and the modal will close on Slack's 3s client-side timeout. If a future surface needs `response_action: \"errors\"` (field-level validation), that interaction must remain direct (out of scope).\n- URL verification: Nango is assumed to auto-respond to Slack's `url_verification` challenge for forwarded events URLs (same path Sage's Slack manifest uses today via `integrations/slack/manifest.json`). Cutover is a single Slack-app dashboard URL swap. If a future Nango configuration regression breaks this, treat it as an ops issue, not a spec change.\n- No changes to Slack OAuth install flow (`app/api/v1/ricky/slack/oauth/route.ts`).\n- No changes to the underlying `lib/ricky/slack/ingress.ts`, `egress.ts`, `auth.ts`, `store.ts`, `parser.ts`, `proactive.ts` business logic — only the entry/exit shape changes (input becomes a Nango envelope; output becomes deferred `response_url` POSTs / `chat.update` calls instead of synchronous `Response`).\n\n## 4. Constraints From Slack's Webhook Semantics\n\n| Endpoint | Slack expectation | Today | Post-migration |\n|---|---|---|---|\n| `events` | 200 within 3s; body ignored except `url_verification` challenge response | `app/api/v1/ricky/slack/events/route.ts` returns `{ok:true}` synchronously after kicking off `handleRickySlackCommand` | Nango router branch dispatches `handleRickySlackCommand` async; events have no `response_url` so user-facing replies use `chat.postMessage` via egress (already supported). |\n| `commands` | 200 within 3s; body becomes the slash command's response | Returns `Response` synchronously from `handleRickySlackCommand` | Nango router branch dispatches the command async; first `response_url` POST is \"Got it, working on it\" within ~1s; subsequent updates either via additional `response_url` POSTs (within 30 min, up to 5x) or via `chat.postMessage`. |\n| `interactivity` (block_actions on gate buttons) | 200 within 3s; body optional; `chat.update` on the original message is the standard way to reflect state | Returns sync; uses `updateGateSlackMessage` and resumes the run | Nango router branch dispatches async; `chat.update` already happens via `updateGateSlackMessage`. The earlier sync response only confirmed receipt; that's now implicit in Nango's 200. |\n| `interactivity` (view_submission with `response_action: \"clear\"`) | Sync response within 3s; no response = client timeout | Returns `{response_action: \"clear\"}` sync | Modal closes on client timeout (~3s). Acceptable per non-goals; document as known UX trade-off. |\n\nURL verification: Slack POSTs `{type: \"url_verification\", challenge: \"...\"}` once when the events URL changes. With Nango as the receiver, Nango must echo the challenge. If Nango doesn't, manual one-shot verification is required (point URL at Cloud direct, take the challenge, point back to Nango).\n\n## 5. File Changes\n\n### 5.1 New / changed\n\n- `packages/web/lib/integrations/nango-webhook-router.ts`:\n  - Import `handleRickySlackCommand`, `dispatchRickySlackCommand`, and the new interactivity dispatcher (see 5.4) from `lib/ricky/slack/ingress.ts`.\n  - In `routeForwardEvent`, the Slack branch (line 787) currently sends *every* slack-prefixed provider to Sage. Add disambiguation: if `provider === \"slack-ricky\"` (or matched via the alias map; see 5.5), dispatch to `handleRickySlackForward(envelope)`. Otherwise keep current behavior (Sage).\n  - New `handleRickySlackForward(envelope)`: detect payload shape (event vs command vs interactivity), claim dedup via `claimWebhookDelivery({ surface: \"slack\", deliveryId })`, route to the correct ingress handler.\n\n- `packages/web/lib/ricky/slack/ingress.ts`:\n  - Refactor `handleRickySlackCommand` from \"returns `Response`\" to \"POSTs replies to `responseUrl` (if present) or via `chat.postEphemeral`/`chat.postMessage` (if not)\" — return type becomes `Promise<void>` (or `Promise<{ status }>`).\n  - Add `dispatchRickySlackInteractivity(payload)` that mirrors what `interactivity/route.ts` does today but uses `chat.update` + `response_url` POSTs instead of returning `NextResponse.json(...)`.\n  - All existing helpers (`canResolveRickySlackGate`, `updateGateSlackMessage`, etc.) stay; only the wrappers that previously returned `Response` change.\n\n- `packages/web/lib/ricky/slack/egress.ts`:\n  - Add `postSlackResponseUrl({ responseUrl, body })` helper. Verified 2026-05-07 not present in `egress.ts`. Implementation: `await fetch(responseUrl, { method: \"POST\", headers: { \"content-type\": \"application/json\" }, body: JSON.stringify(body) })`. Throw on non-2xx so callers can log + dedup-retry.\n\n- `integrations/slack/ricky-manifest.json`:\n  - Replace `request_url` for `event_subscriptions`, `slash_commands`, and `interactivity` with `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack` (same template Sage's `integrations/slack/manifest.json` uses for its events URL).\n  - Drop `signing_secret_env`-style assumptions in any documentation. Mirror the structure of `integrations/linear/ricky-manifest.json` (`webhook.delivery: \"via Nango\"`, `nango.provider_config_key: \"slack-ricky\"`, `webhook_router` pointer).\n\n### 5.2 Deletions\n\n- `packages/web/app/api/v1/ricky/slack/events/route.ts`\n- `packages/web/app/api/v1/ricky/slack/commands/route.ts`\n- `packages/web/app/api/v1/ricky/slack/interactivity/route.ts`\n- `packages/web/lib/ricky/slack/signature.ts`\n- Any `signature.test.ts` / `events.test.ts` / `commands.test.ts` / `interactivity.test.ts` that exercise the deleted modules. Replace with router-level dispatch tests (see 5.6).\n\n### 5.3 Keep\n\n- `packages/web/app/api/v1/ricky/slack/oauth/route.ts` — OAuth installation, not a webhook.\n- `packages/web/lib/ricky/slack/dedup.ts` — already a thin wrapper around `webhook-dedup.ts` after #480.\n- `packages/web/lib/ricky/slack/{auth,blocks,egress,ingress,parser,proactive,store}.ts` — business logic, only ingress entry shape changes (5.1).\n\n### 5.4 Interactivity dispatcher contract\n\nThe current interactivity route handles two payload shapes:\n\n1. `block_actions` on a gate-approval message (the bulk of the file). Migration: same logic, but instead of `return NextResponse.json({ ok: true })`, do nothing (Nango already returned 200 to Slack). The `chat.update` of the original message via `updateGateSlackMessage` already happens; that's the user-visible feedback.\n2. `view_submission` returning `{response_action: \"clear\"}`. Migration: do nothing (Slack closes the modal client-side after timeout). Document the trade-off.\n\nFor error paths that today return non-2xx (`forbidden`, `not_found`, `gate_not_open`, etc.), surface the error to the user via `chat.postEphemeral` to the original channel (use payload.channel.id) instead of a 403 status. Slack already saw 200 from Nango.\n\n### 5.5 Provider config key registry\n\n`packages/web/lib/integrations/nango-webhook-router.ts` has a `PROVIDER_CONFIG_KEY_TO_PROVIDER` alias map (around line 108). Verified 2026-05-07: it currently lists `slack-relay`, `slack-sage`, `slack-my-senior-dev`, `slack-nightcto` — no `slack-ricky`. Add the line `\"slack-ricky\": \"slack-ricky\",` next to the other \"separate products\" entries. `packages/web/lib/integrations/providers.ts` already has a `slack-ricky` entry (defaultConfigKey `slack-ricky`, vfsRoot `/slack-ricky`); no change needed there.\n\n`isSlackProvider` matches `slack-*` providers indiscriminately. The Slack branch needs explicit disambiguation:\n\n```ts\nif (provider === \"slack-ricky\" || envelope.providerConfigKey === \"slack-ricky\") {\n  await handleRickySlackForward(envelope);\n  return;\n}\nif (isSlackProvider(provider) || looksLikeSlackFrom(envelope.from)) {\n  await repairSlackIntegrationFromForwardEvent(envelope, provider);\n  await forwardToSage(envelope);\n  return;\n}\n```\n\n### 5.6 Tests\n\nReplace the deleted route-level tests with router-dispatch tests in `packages/web/lib/integrations/__tests__/` (or similar):\n\n- `slack-ricky-events-dispatch.test.ts` — feed a fake forward envelope shaped like a Slack event, assert `handleRickySlackCommand` is called with the expected fields, assert dedup short-circuits a second call.\n- `slack-ricky-command-dispatch.test.ts` — feed a fake forward envelope for a slash command, assert `dispatchRickySlackCommand` calls `postSlackResponseUrl` with the expected ack, assert idempotency.\n- `slack-ricky-interactivity-dispatch.test.ts` — feed a block_actions envelope, assert `updateGateSlackMessage` is called and no sync return shape exists.\n\nPGlite-backed tests for the dedup shared with Linear already exist in `packages/web/lib/ricky/linear/__tests__/dedup.test.ts` (post-#480) — no need to duplicate.\n\n## 6. SST / Secrets Cleanup\n\nVerified 2026-05-07 via `grep -rn \"RickySlackSigningSecret\\|SlackSigningSecret\\|RICKY_SLACK_SIGNING_SECRET\\|SLACK_SIGNING_SECRET\"` over `infra/`, `scripts/`, `.github/`, and `packages/`: the SST resources `RickySlackSigningSecret` and `SlackSigningSecret` are **not** declared in `infra/secrets.ts`, **not** present in any `link: [...]` array in `infra/web.ts` or other infra files, **not** in `scripts/set-secrets.sh`, **not** in `.github/workflows/_deploy-cloud-stage.yml`, and **not** in `packages/web/lib/boot/resource-check.ts::REQUIRED_RESOURCES`. The only references are the four reads inside `lib/ricky/slack/signature.ts` (`readLinkedSecret(\"RickySlackSigningSecret\")`, `readLinkedSecret(\"SlackSigningSecret\")`, `process.env.RICKY_SLACK_SIGNING_SECRET`, `process.env.SLACK_SIGNING_SECRET`), all guarded by try/catch fallbacks.\n\nTherefore the cleanup is single-place: deleting `lib/ricky/slack/signature.ts` (per §5.2) removes every reference. No SST six-place reverse-walk needed. No CI/workflow secret seeding to remove.\n\nAfter deploy, confirm:\n\n```bash\ngrep -rn \"RickySlackSigningSecret\\|SlackSigningSecret\\|RICKY_SLACK_SIGNING_SECRET\\|SLACK_SIGNING_SECRET\" packages/ infra/ scripts/ .github/\n```\n\nreturns no matches.\n\n(Note: `workflows/cf-runtime/01-runtime-core.ts` contains the literal string `SLACK_SIGNING_SECRET` inside a code-generation template that emits Cloudflare Worker source — that's unrelated to this repo's Slack signing secrets and stays.)\n\n## 7. Rollout Plan\n\nSequence matters because the Slack app's webhook URLs are global (changing them invalidates direct routes immediately).\n\n1. **Land code changes** (this PR). Routes still exist on `main` until merge, so the Slack app keeps using direct delivery during review.\n2. **Pre-merge: register `slack-ricky` Nango integration**. Mirror Sage's `slack-relay` Nango configuration; the Nango integration id then substitutes into the manifest URL (§5.1). The file-sync routes already use this Nango forward pattern for Linear and Slack-Sage, so no Nango feature work is needed.\n3. **Post-merge, pre-Slack-app-update**: deploy the change. The new Nango router branch is live but unused (Slack still POSTs direct to the now-404 routes; this is brief).\n4. **Slack app dashboard switch**: in the Ricky Slack app's developer dashboard, change the events / slash commands / interactivity request URLs to `https://api.nango.dev/webhook/<slack-ricky-integration-id>/slack`. Single-step cutover (Nango auto-responds to Slack's `url_verification` challenge, same as Sage).\n5. **Bake**: smoke-test slash commands, mentions, gate approvals in a sandbox workspace. Confirm the new deferred-response UX is acceptable (slash commands may take ~1s longer to show the first response than before).\n6. **Document**: add a note to the runbook that any future Slack interactivity requiring `response_action: \"errors\"` (field-level modal validation) cannot use the Nango-routed pattern and must be reverted to a direct route with HMAC.\n\n## 8. Risks and Mitigations\n\n| Risk | Mitigation |\n|---|---|\n| Slash command UX regression (slower first response) | Send the deferred ack (\"Got it, working on it\") within ~500ms via `response_url`; existing callers already chain to subsequent updates. |\n| view_submission timeout shows error to user | Current surface only does `response_action: \"clear\"`; documented trade-off. If a surface needs validation errors later, that interactivity stays direct. |\n| `provider === \"slack-ricky\"` dispatch collides with Sage's slack-* matcher | Disambiguate explicitly (§5.5); add a unit test for the router branching. |\n| Concurrent Slack and Linear deliveries collide on `ricky_webhook_dedup` keys | Composite primary key `(surface, delivery_id)` already handles this — covered by `dedup.test.ts`. |\n\n## 9. Acceptance Criteria\n\n- [ ] `app/api/v1/ricky/slack/{events,commands,interactivity}/route.ts` deleted; `oauth/route.ts` retained.\n- [ ] `lib/ricky/slack/signature.ts` deleted.\n- [ ] No source file references `RickySlackSigningSecret`, `SlackSigningSecret`, `RICKY_SLACK_SIGNING_SECRET`, or `SLACK_SIGNING_SECRET` (verify with `grep -rn`).\n- [ ] `nango-webhook-router.ts` routes `slack-ricky` provider envelopes to the new dispatcher; default Slack-* still goes to Sage.\n- [ ] `lib/ricky/slack/ingress.ts::handleRickySlackCommand` returns `Promise<void>` and uses `responseUrl` / `chat.postEphemeral` for replies.\n- [ ] `integrations/slack/ricky-manifest.json` reflects Nango-routed delivery (mirrors `integrations/linear/ricky-manifest.json` shape).\n- [ ] `npx tsc -p packages/web/tsconfig.json --noEmit` clean.\n- [ ] `npx vitest run packages/web/lib/ricky packages/web/lib/integrations` clean (router-dispatch tests pass).\n- [ ] `npm run web:drizzle-journal:test` clean.\n- [ ] Bot reviewers (coderabbit, codex) addressed.\n\n## 10. References\n\n- PR #412 (`feat(ricky): add Slack agent v2 surface and routes`) — original direct-route implementation.\n- PR #480 (`feat(ricky): add Linear agent webhook surface and shared webhook dedup`) — establishes the Nango-routed pattern this spec mirrors.\n- `packages/web/app/api/v1/webhooks/nango/route.ts` — canonical Nango webhook intake.\n- `packages/web/lib/integrations/nango-webhook-router.ts` — `routeNangoWebhook` / `routeForwardEvent` / Linear AgentSession dispatch (post-#480).\n- `integrations/linear/ricky-manifest.json` (post-#480) — manifest shape model.\n- CLAUDE.md \"Adding a new SST resource\" — six-place rule, applied in reverse for removal here." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md", content: "# Normalized Spec\n\n# Ricky Slack Nango-Routed Migration Spec\n\n**Status:** draft, depends on PR #480 (`feat/ricky-linear-agent-surface`) merging to main, since it establishes the same Nango-routed pattern for Linear and the shared `webhook-dedup.ts`.\n**Owner:** Cloud + Ricky.\n**Last updated:** 2026-05-07.\n**Primary repos:** `../cloud` (this repo).\n**Reference manifest:** `integrations/slack/ricky-manifest.json` (current direct config) and `integrations/linear/ricky-manifest.json` (post-#480 Nango pattern, the model for this migration).\n\n## 1. Problem Statement\n\nThe Ricky Slack agent v2 surface (PR #412, merged) was built before the rest of the cloud's webhook intake consolidated onto Nango. Today its dedicated Slack app POSTs directly to three Cloud routes:\n\n- `app/api/v1/ricky/slack/events/route.ts` — `app_mention`, `message.im` (async)\n- `app/api/v1/ricky/slack/commands/route.ts` — `/ricky` slash command (synchronous response expected within 3s)\n- `app/api/v1/ricky/slack/interactivity/route.ts` — gate-approval block_actions and view_submission (synchronous response expected within 3s)\n\nEach route calls `readVerifiedSlackBody` from `lib/ricky/slack/signature.ts`, which verifies a Slack HMAC against `Resource.RickySlackSigningSecret.value` / `Resource.SlackSigningSecret.value` (with env-var fallbacks `RICKY_SLACK_SIGNING_SECRET`, `SLACK_SIGNING_SECRET`).\n\nSage's Slack integration uses the opposite pattern: Slack → Nango → `/api/v1/webhooks/nango` → `routeNangoWebhook` → `routeForwardEvent` → `forwardToSage`. Nango's signature is verified once at the Cloud entry; no Slack-app-specific secret in the cloud repo. PR #480 just established the same pattern for Linear AgentSession events (`handleLinearForward` detects `AgentSessionEvent` payloads and dispatches into `dispatchLinearSessionEvent`).\n\nThis spec migrates the Ricky Slack surface to that pattern so the Slack signing secret, `lib/ricky/slack/signature.ts`, and the dedicated routes can all be removed.\n\n## 2. Goals\n\n- Point the Ricky Slack app's `events`, `commands`, and `interactivity` request URLs at Nango using the same URL pattern Sage already uses (`integrations/slack/manifest.json`): `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack`. Operators substitute the integration id at manifest-upload time.\n- Add a Slack-Ricky branch in `packages/web/lib/integrations/nango-webhook-router.ts` that detects events vs slash command vs interactivity payloads and dispatches into existing handlers in `packages/web/lib/ricky/slack/`.\n- Refactor command + interactivity dispatch to use Slack's deferred response pattern (`response_url` for slash commands and most interactivity; `chat.update` for gate-approval message edits) since Nango forwards webhooks fire-and-forget — Slack only sees Nango's 200, not Cloud's body.\n- Delete `packages/web/app/api/v1/ricky/slack/events/route.ts`, `commands/route.ts`, `interactivity/route.ts`, and `lib/ricky/slack/signature.ts`.\n- Update `integrations/slack/ricky-manifest.json` to reflect Nango-routed delivery (mirror the post-#480 `integrations/linear/ricky-manifest.json` shape; use the Nango webhook URL pattern in goal 1 above).\n- Preserve the `oauth/route.ts` flow for OAuth installation — that's not a webhook and stays as-is.\n\n## 3. Non-Goals\n\n- Sage's Slack integration is unchanged. This spec only touches the Ricky Slack app.\n- The Ricky Slack and Sage Slack apps remain separate. Consolidation into a single shared app is explicitly out of scope.\n- The slash command surface (`/ricky run ...`) keeps the same UX and supported subcommands. Only the transport changes.\n- The gate-approval interactivity behavior keeps the same UX. Only the transport changes.\n- View submissions in the current surface only do `response_action: \"clear\"` (modal close); this spec accepts that the Nango-routed flow can't return that synchronously and the modal will close on Slack's 3s client-side timeout. If a future surface needs `response_action: \"errors\"` (field-level validation), that interaction must remain direct (out of scope).\n- URL verification: Nango is assumed to auto-respond to Slack's `url_verification` challenge for forwarded events URLs (same path Sage's Slack manifest uses today via `integrations/slack/manifest.json`). Cutover is a single Slack-app dashboard URL swap. If a future Nango configuration regression breaks this, treat it as an ops issue, not a spec change.\n- No changes to Slack OAuth install flow (`app/api/v1/ricky/slack/oauth/route.ts`).\n- No changes to the underlying `lib/ricky/slack/ingress.ts`, `egress.ts`, `auth.ts`, `store.ts`, `parser.ts`, `proactive.ts` business logic — only the entry/exit shape changes (input becomes a Nango envelope; output becomes deferred `response_url` POSTs / `chat.update` calls instead of synchronous `Response`).\n\n## 4. Constraints From Slack's Webhook Semantics\n\n| Endpoint | Slack expectation | Today | Post-migration |\n|---|---|---|---|\n| `events` | 200 within 3s; body ignored except `url_verification` challenge response | `app/api/v1/ricky/slack/events/route.ts` returns `{ok:true}` synchronously after kicking off `handleRickySlackCommand` | Nango router branch dispatches `handleRickySlackCommand` async; events have no `response_url` so user-facing replies use `chat.postMessage` via egress (already supported). |\n| `commands` | 200 within 3s; body becomes the slash command's response | Returns `Response` synchronously from `handleRickySlackCommand` | Nango router branch dispatches the command async; first `response_url` POST is \"Got it, working on it\" within ~1s; subsequent updates either via additional `response_url` POSTs (within 30 min, up to 5x) or via `chat.postMessage`. |\n| `interactivity` (block_actions on gate buttons) | 200 within 3s; body optional; `chat.update` on the original message is the standard way to reflect state | Returns sync; uses `updateGateSlackMessage` and resumes the run | Nango router branch dispatches async; `chat.update` already happens via `updateGateSlackMessage`. The earlier sync response only confirmed receipt; that's now implicit in Nango's 200. |\n| `interactivity` (view_submission with `response_action: \"clear\"`) | Sync response within 3s; no response = client timeout | Returns `{response_action: \"clear\"}` sync | Modal closes on client timeout (~3s). Acceptable per non-goals; document as known UX trade-off. |\n\nURL verification: Slack POSTs `{type: \"url_verification\", challenge: \"...\"}` once when the events URL changes. With Nango as the receiver, Nango must echo the challenge. If Nango doesn't, manual one-shot verification is required (point URL at Cloud direct, take the challenge, point back to Nango).\n\n## 5. File Changes\n\n### 5.1 New / changed\n\n- `packages/web/lib/integrations/nango-webhook-router.ts`:\n  - Import `handleRickySlackCommand`, `dispatchRickySlackCommand`, and the new interactivity dispatcher (see 5.4) from `lib/ricky/slack/ingress.ts`.\n  - In `routeForwardEvent`, the Slack branch (line 787) currently sends *every* slack-prefixed provider to Sage. Add disambiguation: if `provider === \"slack-ricky\"` (or matched via the alias map; see 5.5), dispatch to `handleRickySlackForward(envelope)`. Otherwise keep current behavior (Sage).\n  - New `handleRickySlackForward(envelope)`: detect payload shape (event vs command vs interactivity), claim dedup via `claimWebhookDelivery({ surface: \"slack\", deliveryId })`, route to the correct ingress handler.\n\n- `packages/web/lib/ricky/slack/ingress.ts`:\n  - Refactor `handleRickySlackCommand` from \"returns `Response`\" to \"POSTs replies to `responseUrl` (if present) or via `chat.postEphemeral`/`chat.postMessage` (if not)\" — return type becomes `Promise<void>` (or `Promise<{ status }>`).\n  - Add `dispatchRickySlackInteractivity(payload)` that mirrors what `interactivity/route.ts` does today but uses `chat.update` + `response_url` POSTs instead of returning `NextResponse.json(...)`.\n  - All existing helpers (`canResolveRickySlackGate`, `updateGateSlackMessage`, etc.) stay; only the wrappers that previously returned `Response` change.\n\n- `packages/web/lib/ricky/slack/egress.ts`:\n  - Add `postSlackResponseUrl({ responseUrl, body })` helper. Verified 2026-05-07 not present in `egress.ts`. Implementation: `await fetch(responseUrl, { method: \"POST\", headers: { \"content-type\": \"application/json\" }, body: JSON.stringify(body) })`. Throw on non-2xx so callers can log + dedup-retry.\n\n- `integrations/slack/ricky-manifest.json`:\n  - Replace `request_url` for `event_subscriptions`, `slash_commands`, and `interactivity` with `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack` (same template Sage's `integrations/slack/manifest.json` uses for its events URL).\n  - Drop `signing_secret_env`-style assumptions in any documentation. Mirror the structure of `integrations/linear/ricky-manifest.json` (`webhook.delivery: \"via Nango\"`, `nango.provider_config_key: \"slack-ricky\"`, `webhook_router` pointer).\n\n### 5.2 Deletions\n\n- `packages/web/app/api/v1/ricky/slack/events/route.ts`\n- `packages/web/app/api/v1/ricky/slack/commands/route.ts`\n- `packages/web/app/api/v1/ricky/slack/interactivity/route.ts`\n- `packages/web/lib/ricky/slack/signature.ts`\n- Any `signature.test.ts` / `events.test.ts` / `commands.test.ts` / `interactivity.test.ts` that exercise the deleted modules. Replace with router-level dispatch tests (see 5.6).\n\n### 5.3 Keep\n\n- `packages/web/app/api/v1/ricky/slack/oauth/route.ts` — OAuth installation, not a webhook.\n- `packages/web/lib/ricky/slack/dedup.ts` — already a thin wrapper around `webhook-dedup.ts` after #480.\n- `packages/web/lib/ricky/slack/{auth,blocks,egress,ingress,parser,proactive,store}.ts` — business logic, only ingress entry shape changes (5.1).\n\n### 5.4 Interactivity dispatcher contract\n\nThe current interactivity route handles two payload shapes:\n\n1. `block_actions` on a gate-approval message (the bulk of the file). Migration: same logic, but instead of `return NextResponse.json({ ok: true })`, do nothing (Nango already returned 200 to Slack). The `chat.update` of the original message via `updateGateSlackMessage` already happens; that's the user-visible feedback.\n2. `view_submission` returning `{response_action: \"clear\"}`. Migration: do nothing (Slack closes the modal client-side after timeout). Document the trade-off.\n\nFor error paths that today return non-2xx (`forbidden`, `not_found`, `gate_not_open`, etc.), surface the error to the user via `chat.postEphemeral` to the original channel (use payload.channel.id) instead of a 403 status. Slack already saw 200 from Nango.\n\n### 5.5 Provider config key registry\n\n`packages/web/lib/integrations/nango-webhook-router.ts` has a `PROVIDER_CONFIG_KEY_TO_PROVIDER` alias map (around line 108). Verified 2026-05-07: it currently lists `slack-relay`, `slack-sage`, `slack-my-senior-dev`, `slack-nightcto` — no `slack-ricky`. Add the line `\"slack-ricky\": \"slack-ricky\",` next to the other \"separate products\" entries. `packages/web/lib/integrations/providers.ts` already has a `slack-ricky` entry (defaultConfigKey `slack-ricky`, vfsRoot `/slack-ricky`); no change needed there.\n\n`isSlackProvider` matches `slack-*` providers indiscriminately. The Slack branch needs explicit disambiguation:\n\n```ts\nif (provider === \"slack-ricky\" || envelope.providerConfigKey === \"slack-ricky\") {\n  await handleRickySlackForward(envelope);\n  return;\n}\nif (isSlackProvider(provider) || looksLikeSlackFrom(envelope.from)) {\n  await repairSlackIntegrationFromForwardEvent(envelope, provider);\n  await forwardToSage(envelope);\n  return;\n}\n```\n\n### 5.6 Tests\n\nReplace the deleted route-level tests with router-dispatch tests in `packages/web/lib/integrations/__tests__/` (or similar):\n\n- `slack-ricky-events-dispatch.test.ts` — feed a fake forward envelope shaped like a Slack event, assert `handleRickySlackCommand` is called with the expected fields, assert dedup short-circuits a second call.\n- `slack-ricky-command-dispatch.test.ts` — feed a fake forward envelope for a slash command, assert `dispatchRickySlackCommand` calls `postSlackResponseUrl` with the expected ack, assert idempotency.\n- `slack-ricky-interactivity-dispatch.test.ts` — feed a block_actions envelope, assert `updateGateSlackMessage` is called and no sync return shape exists.\n\nPGlite-backed tests for the dedup shared with Linear already exist in `packages/web/lib/ricky/linear/__tests__/dedup.test.ts` (post-#480) — no need to duplicate.\n\n## 6. SST / Secrets Cleanup\n\nVerified 2026-05-07 via `grep -rn \"RickySlackSigningSecret\\|SlackSigningSecret\\|RICKY_SLACK_SIGNING_SECRET\\|SLACK_SIGNING_SECRET\"` over `infra/`, `scripts/`, `.github/`, and `packages/`: the SST resources `RickySlackSigningSecret` and `SlackSigningSecret` are **not** declared in `infra/secrets.ts`, **not** present in any `link: [...]` array in `infra/web.ts` or other infra files, **not** in `scripts/set-secrets.sh`, **not** in `.github/workflows/_deploy-cloud-stage.yml`, and **not** in `packages/web/lib/boot/resource-check.ts::REQUIRED_RESOURCES`. The only references are the four reads inside `lib/ricky/slack/signature.ts` (`readLinkedSecret(\"RickySlackSigningSecret\")`, `readLinkedSecret(\"SlackSigningSecret\")`, `process.env.RICKY_SLACK_SIGNING_SECRET`, `process.env.SLACK_SIGNING_SECRET`), all guarded by try/catch fallbacks.\n\nTherefore the cleanup is single-place: deleting `lib/ricky/slack/signature.ts` (per §5.2) removes every reference. No SST six-place reverse-walk needed. No CI/workflow secret seeding to remove.\n\nAfter deploy, confirm:\n\n```bash\ngrep -rn \"RickySlackSigningSecret\\|SlackSigningSecret\\|RICKY_SLACK_SIGNING_SECRET\\|SLACK_SIGNING_SECRET\" packages/ infra/ scripts/ .github/\n```\n\nreturns no matches.\n\n(Note: `workflows/cf-runtime/01-runtime-core.ts` contains the literal string `SLACK_SIGNING_SECRET` inside a code-generation template that emits Cloudflare Worker source — that's unrelated to this repo's Slack signing secrets and stays.)\n\n## 7. Rollout Plan\n\nSequence matters because the Slack app's webhook URLs are global (changing them invalidates direct routes immediately).\n\n1. **Land code changes** (this PR). Routes still exist on `main` until merge, so the Slack app keeps using direct delivery during review.\n2. **Pre-merge: register `slack-ricky` Nango integration**. Mirror Sage's `slack-relay` Nango configuration; the Nango integration id then substitutes into the manifest URL (§5.1). The file-sync routes already use this Nango forward pattern for Linear and Slack-Sage, so no Nango feature work is needed.\n3. **Post-merge, pre-Slack-app-update**: deploy the change. The new Nango router branch is live but unused (Slack still POSTs direct to the now-404 routes; this is brief).\n4. **Slack app dashboard switch**: in the Ricky Slack app's developer dashboard, change the events / slash commands / interactivity request URLs to `https://api.nango.dev/webhook/<slack-ricky-integration-id>/slack`. Single-step cutover (Nango auto-responds to Slack's `url_verification` challenge, same as Sage).\n5. **Bake**: smoke-test slash commands, mentions, gate approvals in a sandbox workspace. Confirm the new deferred-response UX is acceptable (slash commands may take ~1s longer to show the first response than before).\n6. **Document**: add a note to the runbook that any future Slack interactivity requiring `response_action: \"errors\"` (field-level modal validation) cannot use the Nango-routed pattern and must be reverted to a direct route with HMAC.\n\n## 8. Risks and Mitigations\n\n| Risk | Mitigation |\n|---|---|\n| Slash command UX regression (slower first response) | Send the deferred ack (\"Got it, working on it\") within ~500ms via `response_url`; existing callers already chain to subsequent updates. |\n| view_submission timeout shows error to user | Current surface only does `response_action: \"clear\"`; documented trade-off. If a surface needs validation errors later, that interactivity stays direct. |\n| `provider === \"slack-ricky\"` dispatch collides with Sage's slack-* matcher | Disambiguate explicitly (§5.5); add a unit test for the router branching. |\n| Concurrent Slack and Linear deliveries collide on `ricky_webhook_dedup` keys | Composite primary key `(surface, delivery_id)` already handles this — covered by `dedup.test.ts`. |\n\n## 9. Acceptance Criteria\n\n- [ ] `app/api/v1/ricky/slack/{events,commands,interactivity}/route.ts` deleted; `oauth/route.ts` retained.\n- [ ] `lib/ricky/slack/signature.ts` deleted.\n- [ ] No source file references `RickySlackSigningSecret`, `SlackSigningSecret`, `RICKY_SLACK_SIGNING_SECRET`, or `SLACK_SIGNING_SECRET` (verify with `grep -rn`).\n- [ ] `nango-webhook-router.ts` routes `slack-ricky` provider envelopes to the new dispatcher; default Slack-* still goes to Sage.\n- [ ] `lib/ricky/slack/ingress.ts::handleRickySlackCommand` returns `Promise<void>` and uses `responseUrl` / `chat.postEphemeral` for replies.\n- [ ] `integrations/slack/ricky-manifest.json` reflects Nango-routed delivery (mirrors `integrations/linear/ricky-manifest.json` shape).\n- [ ] `npx tsc -p packages/web/tsconfig.json --noEmit` clean.\n- [ ] `npx vitest run packages/web/lib/ricky packages/web/lib/integrations` clean (router-dispatch tests pass).\n- [ ] `npm run web:drizzle-journal:test` clean.\n- [ ] Bot reviewers (coderabbit, codex) addressed.\n\n## 10. References\n\n- PR #412 (`feat(ricky): add Slack agent v2 surface and routes`) — original direct-route implementation.\n- PR #480 (`feat(ricky): add Linear agent webhook surface and shared webhook dedup`) — establishes the Nango-routed pattern this spec mirrors.\n- `packages/web/app/api/v1/webhooks/nango/route.ts` — canonical Nango webhook intake.\n- `packages/web/lib/integrations/nango-webhook-router.ts` — `routeNangoWebhook` / `routeForwardEvent` / Linear AgentSession dispatch (post-#480).\n- `integrations/linear/ricky-manifest.json` (post-#480) — manifest shape model.\n- CLAUDE.md \"Adding a new SST resource\" — six-place rule, applied in reverse for removal here.\n\n## Target Context\n\nNone declared." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json", content: "{\n  \"description\": \"# Ricky Slack Nango-Routed Migration Spec\\n\\n**Status:** draft, depends on PR #480 (`feat/ricky-linear-agent-surface`) merging to main, since it establishes the same Nango-routed pattern for Linear and the shared `webhook-dedup.ts`.\\n**Owner:** Cloud + Ricky.\\n**Last updated:** 2026-05-07.\\n**Primary repos:** `../cloud` (this repo).\\n**Reference manifest:** `integrations/slack/ricky-manifest.json` (current direct config) and `integrations/linear/ricky-manifest.json` (post-#480 Nango pattern, the model for this migration).\\n\\n## 1. Problem Statement\\n\\nThe Ricky Slack agent v2 surface (PR #412, merged) was built before the rest of the cloud's webhook intake consolidated onto Nango. Today its dedicated Slack app POSTs directly to three Cloud routes:\\n\\n- `app/api/v1/ricky/slack/events/route.ts` — `app_mention`, `message.im` (async)\\n- `app/api/v1/ricky/slack/commands/route.ts` — `/ricky` slash command (synchronous response expected within 3s)\\n- `app/api/v1/ricky/slack/interactivity/route.ts` — gate-approval block_actions and view_submission (synchronous response expected within 3s)\\n\\nEach route calls `readVerifiedSlackBody` from `lib/ricky/slack/signature.ts`, which verifies a Slack HMAC against `Resource.RickySlackSigningSecret.value` / `Resource.SlackSigningSecret.value` (with env-var fallbacks `RICKY_SLACK_SIGNING_SECRET`, `SLACK_SIGNING_SECRET`).\\n\\nSage's Slack integration uses the opposite pattern: Slack → Nango → `/api/v1/webhooks/nango` → `routeNangoWebhook` → `routeForwardEvent` → `forwardToSage`. Nango's signature is verified once at the Cloud entry; no Slack-app-specific secret in the cloud repo. PR #480 just established the same pattern for Linear AgentSession events (`handleLinearForward` detects `AgentSessionEvent` payloads and dispatches into `dispatchLinearSessionEvent`).\\n\\nThis spec migrates the Ricky Slack surface to that pattern so the Slack signing secret, `lib/ricky/slack/signature.ts`, and the dedicated routes can all be removed.\\n\\n## 2. Goals\\n\\n- Point the Ricky Slack app's `events`, `commands`, and `interactivity` request URLs at Nango using the same URL pattern Sage already uses (`integrations/slack/manifest.json`): `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack`. Operators substitute the integration id at manifest-upload time.\\n- Add a Slack-Ricky branch in `packages/web/lib/integrations/nango-webhook-router.ts` that detects events vs slash command vs interactivity payloads and dispatches into existing handlers in `packages/web/lib/ricky/slack/`.\\n- Refactor command + interactivity dispatch to use Slack's deferred response pattern (`response_url` for slash commands and most interactivity; `chat.update` for gate-approval message edits) since Nango forwards webhooks fire-and-forget — Slack only sees Nango's 200, not Cloud's body.\\n- Delete `packages/web/app/api/v1/ricky/slack/events/route.ts`, `commands/route.ts`, `interactivity/route.ts`, and `lib/ricky/slack/signature.ts`.\\n- Update `integrations/slack/ricky-manifest.json` to reflect Nango-routed delivery (mirror the post-#480 `integrations/linear/ricky-manifest.json` shape; use the Nango webhook URL pattern in goal 1 above).\\n- Preserve the `oauth/route.ts` flow for OAuth installation — that's not a webhook and stays as-is.\\n\\n## 3. Non-Goals\\n\\n- Sage's Slack integration is unchanged. This spec only touches the Ricky Slack app.\\n- The Ricky Slack and Sage Slack apps remain separate. Consolidation into a single shared app is explicitly out of scope.\\n- The slash command surface (`/ricky run ...`) keeps the same UX and supported subcommands. Only the transport changes.\\n- The gate-approval interactivity behavior keeps the same UX. Only the transport changes.\\n- View submissions in the current surface only do `response_action: \\\"clear\\\"` (modal close); this spec accepts that the Nango-routed flow can't return that synchronously and the modal will close on Slack's 3s client-side timeout. If a future surface needs `response_action: \\\"errors\\\"` (field-level validation), that interaction must remain direct (out of scope).\\n- URL verification: Nango is assumed to auto-respond to Slack's `url_verification` challenge for forwarded events URLs (same path Sage's Slack manifest uses today via `integrations/slack/manifest.json`). Cutover is a single Slack-app dashboard URL swap. If a future Nango configuration regression breaks this, treat it as an ops issue, not a spec change.\\n- No changes to Slack OAuth install flow (`app/api/v1/ricky/slack/oauth/route.ts`).\\n- No changes to the underlying `lib/ricky/slack/ingress.ts`, `egress.ts`, `auth.ts`, `store.ts`, `parser.ts`, `proactive.ts` business logic — only the entry/exit shape changes (input becomes a Nango envelope; output becomes deferred `response_url` POSTs / `chat.update` calls instead of synchronous `Response`).\\n\\n## 4. Constraints From Slack's Webhook Semantics\\n\\n| Endpoint | Slack expectation | Today | Post-migration |\\n|---|---|---|---|\\n| `events` | 200 within 3s; body ignored except `url_verification` challenge response | `app/api/v1/ricky/slack/events/route.ts` returns `{ok:true}` synchronously after kicking off `handleRickySlackCommand` | Nango router branch dispatches `handleRickySlackCommand` async; events have no `response_url` so user-facing replies use `chat.postMessage` via egress (already supported). |\\n| `commands` | 200 within 3s; body becomes the slash command's response | Returns `Response` synchronously from `handleRickySlackCommand` | Nango router branch dispatches the command async; first `response_url` POST is \\\"Got it, working on it\\\" within ~1s; subsequent updates either via additional `response_url` POSTs (within 30 min, up to 5x) or via `chat.postMessage`. |\\n| `interactivity` (block_actions on gate buttons) | 200 within 3s; body optional; `chat.update` on the original message is the standard way to reflect state | Returns sync; uses `updateGateSlackMessage` and resumes the run | Nango router branch dispatches async; `chat.update` already happens via `updateGateSlackMessage`. The earlier sync response only confirmed receipt; that's now implicit in Nango's 200. |\\n| `interactivity` (view_submission with `response_action: \\\"clear\\\"`) | Sync response within 3s; no response = client timeout | Returns `{response_action: \\\"clear\\\"}` sync | Modal closes on client timeout (~3s). Acceptable per non-goals; document as known UX trade-off. |\\n\\nURL verification: Slack POSTs `{type: \\\"url_verification\\\", challenge: \\\"...\\\"}` once when the events URL changes. With Nango as the receiver, Nango must echo the challenge. If Nango doesn't, manual one-shot verification is required (point URL at Cloud direct, take the challenge, point back to Nango).\\n\\n## 5. File Changes\\n\\n### 5.1 New / changed\\n\\n- `packages/web/lib/integrations/nango-webhook-router.ts`:\\n  - Import `handleRickySlackCommand`, `dispatchRickySlackCommand`, and the new interactivity dispatcher (see 5.4) from `lib/ricky/slack/ingress.ts`.\\n  - In `routeForwardEvent`, the Slack branch (line 787) currently sends *every* slack-prefixed provider to Sage. Add disambiguation: if `provider === \\\"slack-ricky\\\"` (or matched via the alias map; see 5.5), dispatch to `handleRickySlackForward(envelope)`. Otherwise keep current behavior (Sage).\\n  - New `handleRickySlackForward(envelope)`: detect payload shape (event vs command vs interactivity), claim dedup via `claimWebhookDelivery({ surface: \\\"slack\\\", deliveryId })`, route to the correct ingress handler.\\n\\n- `packages/web/lib/ricky/slack/ingress.ts`:\\n  - Refactor `handleRickySlackCommand` from \\\"returns `Response`\\\" to \\\"POSTs replies to `responseUrl` (if present) or via `chat.postEphemeral`/`chat.postMessage` (if not)\\\" — return type becomes `Promise<void>` (or `Promise<{ status }>`).\\n  - Add `dispatchRickySlackInteractivity(payload)` that mirrors what `interactivity/route.ts` does today but uses `chat.update` + `response_url` POSTs instead of returning `NextResponse.json(...)`.\\n  - All existing helpers (`canResolveRickySlackGate`, `updateGateSlackMessage`, etc.) stay; only the wrappers that previously returned `Response` change.\\n\\n- `packages/web/lib/ricky/slack/egress.ts`:\\n  - Add `postSlackResponseUrl({ responseUrl, body })` helper. Verified 2026-05-07 not present in `egress.ts`. Implementation: `await fetch(responseUrl, { method: \\\"POST\\\", headers: { \\\"content-type\\\": \\\"application/json\\\" }, body: JSON.stringify(body) })`. Throw on non-2xx so callers can log + dedup-retry.\\n\\n- `integrations/slack/ricky-manifest.json`:\\n  - Replace `request_url` for `event_subscriptions`, `slash_commands`, and `interactivity` with `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack` (same template Sage's `integrations/slack/manifest.json` uses for its events URL).\\n  - Drop `signing_secret_env`-style assumptions in any documentation. Mirror the structure of `integrations/linear/ricky-manifest.json` (`webhook.delivery: \\\"via Nango\\\"`, `nango.provider_config_key: \\\"slack-ricky\\\"`, `webhook_router` pointer).\\n\\n### 5.2 Deletions\\n\\n- `packages/web/app/api/v1/ricky/slack/events/route.ts`\\n- `packages/web/app/api/v1/ricky/slack/commands/route.ts`\\n- `packages/web/app/api/v1/ricky/slack/interactivity/route.ts`\\n- `packages/web/lib/ricky/slack/signature.ts`\\n- Any `signature.test.ts` / `events.test.ts` / `commands.test.ts` / `interactivity.test.ts` that exercise the deleted modules. Replace with router-level dispatch tests (see 5.6).\\n\\n### 5.3 Keep\\n\\n- `packages/web/app/api/v1/ricky/slack/oauth/route.ts` — OAuth installation, not a webhook.\\n- `packages/web/lib/ricky/slack/dedup.ts` — already a thin wrapper around `webhook-dedup.ts` after #480.\\n- `packages/web/lib/ricky/slack/{auth,blocks,egress,ingress,parser,proactive,store}.ts` — business logic, only ingress entry shape changes (5.1).\\n\\n### 5.4 Interactivity dispatcher contract\\n\\nThe current interactivity route handles two payload shapes:\\n\\n1. `block_actions` on a gate-approval message (the bulk of the file). Migration: same logic, but instead of `return NextResponse.json({ ok: true })`, do nothing (Nango already returned 200 to Slack). The `chat.update` of the original message via `updateGateSlackMessage` already happens; that's the user-visible feedback.\\n2. `view_submission` returning `{response_action: \\\"clear\\\"}`. Migration: do nothing (Slack closes the modal client-side after timeout). Document the trade-off.\\n\\nFor error paths that today return non-2xx (`forbidden`, `not_found`, `gate_not_open`, etc.), surface the error to the user via `chat.postEphemeral` to the original channel (use payload.channel.id) instead of a 403 status. Slack already saw 200 from Nango.\\n\\n### 5.5 Provider config key registry\\n\\n`packages/web/lib/integrations/nango-webhook-router.ts` has a `PROVIDER_CONFIG_KEY_TO_PROVIDER` alias map (around line 108). Verified 2026-05-07: it currently lists `slack-relay`, `slack-sage`, `slack-my-senior-dev`, `slack-nightcto` — no `slack-ricky`. Add the line `\\\"slack-ricky\\\": \\\"slack-ricky\\\",` next to the other \\\"separate products\\\" entries. `packages/web/lib/integrations/providers.ts` already has a `slack-ricky` entry (defaultConfigKey `slack-ricky`, vfsRoot `/slack-ricky`); no change needed there.\\n\\n`isSlackProvider` matches `slack-*` providers indiscriminately. The Slack branch needs explicit disambiguation:\\n\\n```ts\\nif (provider === \\\"slack-ricky\\\" || envelope.providerConfigKey === \\\"slack-ricky\\\") {\\n  await handleRickySlackForward(envelope);\\n  return;\\n}\\nif (isSlackProvider(provider) || looksLikeSlackFrom(envelope.from)) {\\n  await repairSlackIntegrationFromForwardEvent(envelope, provider);\\n  await forwardToSage(envelope);\\n  return;\\n}\\n```\\n\\n### 5.6 Tests\\n\\nReplace the deleted route-level tests with router-dispatch tests in `packages/web/lib/integrations/__tests__/` (or similar):\\n\\n- `slack-ricky-events-dispatch.test.ts` — feed a fake forward envelope shaped like a Slack event, assert `handleRickySlackCommand` is called with the expected fields, assert dedup short-circuits a second call.\\n- `slack-ricky-command-dispatch.test.ts` — feed a fake forward envelope for a slash command, assert `dispatchRickySlackCommand` calls `postSlackResponseUrl` with the expected ack, assert idempotency.\\n- `slack-ricky-interactivity-dispatch.test.ts` — feed a block_actions envelope, assert `updateGateSlackMessage` is called and no sync return shape exists.\\n\\nPGlite-backed tests for the dedup shared with Linear already exist in `packages/web/lib/ricky/linear/__tests__/dedup.test.ts` (post-#480) — no need to duplicate.\\n\\n## 6. SST / Secrets Cleanup\\n\\nVerified 2026-05-07 via `grep -rn \\\"RickySlackSigningSecret\\\\|SlackSigningSecret\\\\|RICKY_SLACK_SIGNING_SECRET\\\\|SLACK_SIGNING_SECRET\\\"` over `infra/`, `scripts/`, `.github/`, and `packages/`: the SST resources `RickySlackSigningSecret` and `SlackSigningSecret` are **not** declared in `infra/secrets.ts`, **not** present in any `link: [...]` array in `infra/web.ts` or other infra files, **not** in `scripts/set-secrets.sh`, **not** in `.github/workflows/_deploy-cloud-stage.yml`, and **not** in `packages/web/lib/boot/resource-check.ts::REQUIRED_RESOURCES`. The only references are the four reads inside `lib/ricky/slack/signature.ts` (`readLinkedSecret(\\\"RickySlackSigningSecret\\\")`, `readLinkedSecret(\\\"SlackSigningSecret\\\")`, `process.env.RICKY_SLACK_SIGNING_SECRET`, `process.env.SLACK_SIGNING_SECRET`), all guarded by try/catch fallbacks.\\n\\nTherefore the cleanup is single-place: deleting `lib/ricky/slack/signature.ts` (per §5.2) removes every reference. No SST six-place reverse-walk needed. No CI/workflow secret seeding to remove.\\n\\nAfter deploy, confirm:\\n\\n```bash\\ngrep -rn \\\"RickySlackSigningSecret\\\\|SlackSigningSecret\\\\|RICKY_SLACK_SIGNING_SECRET\\\\|SLACK_SIGNING_SECRET\\\" packages/ infra/ scripts/ .github/\\n```\\n\\nreturns no matches.\\n\\n(Note: `workflows/cf-runtime/01-runtime-core.ts` contains the literal string `SLACK_SIGNING_SECRET` inside a code-generation template that emits Cloudflare Worker source — that's unrelated to this repo's Slack signing secrets and stays.)\\n\\n## 7. Rollout Plan\\n\\nSequence matters because the Slack app's webhook URLs are global (changing them invalidates direct routes immediately).\\n\\n1. **Land code changes** (this PR). Routes still exist on `main` until merge, so the Slack app keeps using direct delivery during review.\\n2. **Pre-merge: register `slack-ricky` Nango integration**. Mirror Sage's `slack-relay` Nango configuration; the Nango integration id then substitutes into the manifest URL (§5.1). The file-sync routes already use this Nango forward pattern for Linear and Slack-Sage, so no Nango feature work is needed.\\n3. **Post-merge, pre-Slack-app-update**: deploy the change. The new Nango router branch is live but unused (Slack still POSTs direct to the now-404 routes; this is brief).\\n4. **Slack app dashboard switch**: in the Ricky Slack app's developer dashboard, change the events / slash commands / interactivity request URLs to `https://api.nango.dev/webhook/<slack-ricky-integration-id>/slack`. Single-step cutover (Nango auto-responds to Slack's `url_verification` challenge, same as Sage).\\n5. **Bake**: smoke-test slash commands, mentions, gate approvals in a sandbox workspace. Confirm the new deferred-response UX is acceptable (slash commands may take ~1s longer to show the first response than before).\\n6. **Document**: add a note to the runbook that any future Slack interactivity requiring `response_action: \\\"errors\\\"` (field-level modal validation) cannot use the Nango-routed pattern and must be reverted to a direct route with HMAC.\\n\\n## 8. Risks and Mitigations\\n\\n| Risk | Mitigation |\\n|---|---|\\n| Slash command UX regression (slower first response) | Send the deferred ack (\\\"Got it, working on it\\\") within ~500ms via `response_url`; existing callers already chain to subsequent updates. |\\n| view_submission timeout shows error to user | Current surface only does `response_action: \\\"clear\\\"`; documented trade-off. If a surface needs validation errors later, that interactivity stays direct. |\\n| `provider === \\\"slack-ricky\\\"` dispatch collides with Sage's slack-* matcher | Disambiguate explicitly (§5.5); add a unit test for the router branching. |\\n| Concurrent Slack and Linear deliveries collide on `ricky_webhook_dedup` keys | Composite primary key `(surface, delivery_id)` already handles this — covered by `dedup.test.ts`. |\\n\\n## 9. Acceptance Criteria\\n\\n- [ ] `app/api/v1/ricky/slack/{events,commands,interactivity}/route.ts` deleted; `oauth/route.ts` retained.\\n- [ ] `lib/ricky/slack/signature.ts` deleted.\\n- [ ] No source file references `RickySlackSigningSecret`, `SlackSigningSecret`, `RICKY_SLACK_SIGNING_SECRET`, or `SLACK_SIGNING_SECRET` (verify with `grep -rn`).\\n- [ ] `nango-webhook-router.ts` routes `slack-ricky` provider envelopes to the new dispatcher; default Slack-* still goes to Sage.\\n- [ ] `lib/ricky/slack/ingress.ts::handleRickySlackCommand` returns `Promise<void>` and uses `responseUrl` / `chat.postEphemeral` for replies.\\n- [ ] `integrations/slack/ricky-manifest.json` reflects Nango-routed delivery (mirrors `integrations/linear/ricky-manifest.json` shape).\\n- [ ] `npx tsc -p packages/web/tsconfig.json --noEmit` clean.\\n- [ ] `npx vitest run packages/web/lib/ricky packages/web/lib/integrations` clean (router-dispatch tests pass).\\n- [ ] `npm run web:drizzle-journal:test` clean.\\n- [ ] Bot reviewers (coderabbit, codex) addressed.\\n\\n## 10. References\\n\\n- PR #412 (`feat(ricky): add Slack agent v2 surface and routes`) — original direct-route implementation.\\n- PR #480 (`feat(ricky): add Linear agent webhook surface and shared webhook dedup`) — establishes the Nango-routed pattern this spec mirrors.\\n- `packages/web/app/api/v1/webhooks/nango/route.ts` — canonical Nango webhook intake.\\n- `packages/web/lib/integrations/nango-webhook-router.ts` — `routeNangoWebhook` / `routeForwardEvent` / Linear AgentSession dispatch (post-#480).\\n- `integrations/linear/ricky-manifest.json` (post-#480) — manifest shape model.\\n- CLAUDE.md \\\"Adding a new SST resource\\\" — six-place rule, applied in reverse for removal here.\",\n  \"desiredAction\": {\n    \"kind\": \"generate\",\n    \"summary\": \"# Ricky Slack Nango-Routed Migration Spec **Status:** draft, depends on PR #480 (`feat/ricky-linear-agent-surface`) merging to main, since it establishes the same Nango-routed p...\",\n    \"specText\": \"# Ricky Slack Nango-Routed Migration Spec\\n\\n**Status:** draft, depends on PR #480 (`feat/ricky-linear-agent-surface`) merging to main, since it establishes the same Nango-routed pattern for Linear and the shared `webhook-dedup.ts`.\\n**Owner:** Cloud + Ricky.\\n**Last updated:** 2026-05-07.\\n**Primary repos:** `../cloud` (this repo).\\n**Reference manifest:** `integrations/slack/ricky-manifest.json` (current direct config) and `integrations/linear/ricky-manifest.json` (post-#480 Nango pattern, the model for this migration).\\n\\n## 1. Problem Statement\\n\\nThe Ricky Slack agent v2 surface (PR #412, merged) was built before the rest of the cloud's webhook intake consolidated onto Nango. Today its dedicated Slack app POSTs directly to three Cloud routes:\\n\\n- `app/api/v1/ricky/slack/events/route.ts` — `app_mention`, `message.im` (async)\\n- `app/api/v1/ricky/slack/commands/route.ts` — `/ricky` slash command (synchronous response expected within 3s)\\n- `app/api/v1/ricky/slack/interactivity/route.ts` — gate-approval block_actions and view_submission (synchronous response expected within 3s)\\n\\nEach route calls `readVerifiedSlackBody` from `lib/ricky/slack/signature.ts`, which verifies a Slack HMAC against `Resource.RickySlackSigningSecret.value` / `Resource.SlackSigningSecret.value` (with env-var fallbacks `RICKY_SLACK_SIGNING_SECRET`, `SLACK_SIGNING_SECRET`).\\n\\nSage's Slack integration uses the opposite pattern: Slack → Nango → `/api/v1/webhooks/nango` → `routeNangoWebhook` → `routeForwardEvent` → `forwardToSage`. Nango's signature is verified once at the Cloud entry; no Slack-app-specific secret in the cloud repo. PR #480 just established the same pattern for Linear AgentSession events (`handleLinearForward` detects `AgentSessionEvent` payloads and dispatches into `dispatchLinearSessionEvent`).\\n\\nThis spec migrates the Ricky Slack surface to that pattern so the Slack signing secret, `lib/ricky/slack/signature.ts`, and the dedicated routes can all be removed.\\n\\n## 2. Goals\\n\\n- Point the Ricky Slack app's `events`, `commands`, and `interactivity` request URLs at Nango using the same URL pattern Sage already uses (`integrations/slack/manifest.json`): `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack`. Operators substitute the integration id at manifest-upload time.\\n- Add a Slack-Ricky branch in `packages/web/lib/integrations/nango-webhook-router.ts` that detects events vs slash command vs interactivity payloads and dispatches into existing handlers in `packages/web/lib/ricky/slack/`.\\n- Refactor command + interactivity dispatch to use Slack's deferred response pattern (`response_url` for slash commands and most interactivity; `chat.update` for gate-approval message edits) since Nango forwards webhooks fire-and-forget — Slack only sees Nango's 200, not Cloud's body.\\n- Delete `packages/web/app/api/v1/ricky/slack/events/route.ts`, `commands/route.ts`, `interactivity/route.ts`, and `lib/ricky/slack/signature.ts`.\\n- Update `integrations/slack/ricky-manifest.json` to reflect Nango-routed delivery (mirror the post-#480 `integrations/linear/ricky-manifest.json` shape; use the Nango webhook URL pattern in goal 1 above).\\n- Preserve the `oauth/route.ts` flow for OAuth installation — that's not a webhook and stays as-is.\\n\\n## 3. Non-Goals\\n\\n- Sage's Slack integration is unchanged. This spec only touches the Ricky Slack app.\\n- The Ricky Slack and Sage Slack apps remain separate. Consolidation into a single shared app is explicitly out of scope.\\n- The slash command surface (`/ricky run ...`) keeps the same UX and supported subcommands. Only the transport changes.\\n- The gate-approval interactivity behavior keeps the same UX. Only the transport changes.\\n- View submissions in the current surface only do `response_action: \\\"clear\\\"` (modal close); this spec accepts that the Nango-routed flow can't return that synchronously and the modal will close on Slack's 3s client-side timeout. If a future surface needs `response_action: \\\"errors\\\"` (field-level validation), that interaction must remain direct (out of scope).\\n- URL verification: Nango is assumed to auto-respond to Slack's `url_verification` challenge for forwarded events URLs (same path Sage's Slack manifest uses today via `integrations/slack/manifest.json`). Cutover is a single Slack-app dashboard URL swap. If a future Nango configuration regression breaks this, treat it as an ops issue, not a spec change.\\n- No changes to Slack OAuth install flow (`app/api/v1/ricky/slack/oauth/route.ts`).\\n- No changes to the underlying `lib/ricky/slack/ingress.ts`, `egress.ts`, `auth.ts`, `store.ts`, `parser.ts`, `proactive.ts` business logic — only the entry/exit shape changes (input becomes a Nango envelope; output becomes deferred `response_url` POSTs / `chat.update` calls instead of synchronous `Response`).\\n\\n## 4. Constraints From Slack's Webhook Semantics\\n\\n| Endpoint | Slack expectation | Today | Post-migration |\\n|---|---|---|---|\\n| `events` | 200 within 3s; body ignored except `url_verification` challenge response | `app/api/v1/ricky/slack/events/route.ts` returns `{ok:true}` synchronously after kicking off `handleRickySlackCommand` | Nango router branch dispatches `handleRickySlackCommand` async; events have no `response_url` so user-facing replies use `chat.postMessage` via egress (already supported). |\\n| `commands` | 200 within 3s; body becomes the slash command's response | Returns `Response` synchronously from `handleRickySlackCommand` | Nango router branch dispatches the command async; first `response_url` POST is \\\"Got it, working on it\\\" within ~1s; subsequent updates either via additional `response_url` POSTs (within 30 min, up to 5x) or via `chat.postMessage`. |\\n| `interactivity` (block_actions on gate buttons) | 200 within 3s; body optional; `chat.update` on the original message is the standard way to reflect state | Returns sync; uses `updateGateSlackMessage` and resumes the run | Nango router branch dispatches async; `chat.update` already happens via `updateGateSlackMessage`. The earlier sync response only confirmed receipt; that's now implicit in Nango's 200. |\\n| `interactivity` (view_submission with `response_action: \\\"clear\\\"`) | Sync response within 3s; no response = client timeout | Returns `{response_action: \\\"clear\\\"}` sync | Modal closes on client timeout (~3s). Acceptable per non-goals; document as known UX trade-off. |\\n\\nURL verification: Slack POSTs `{type: \\\"url_verification\\\", challenge: \\\"...\\\"}` once when the events URL changes. With Nango as the receiver, Nango must echo the challenge. If Nango doesn't, manual one-shot verification is required (point URL at Cloud direct, take the challenge, point back to Nango).\\n\\n## 5. File Changes\\n\\n### 5.1 New / changed\\n\\n- `packages/web/lib/integrations/nango-webhook-router.ts`:\\n  - Import `handleRickySlackCommand`, `dispatchRickySlackCommand`, and the new interactivity dispatcher (see 5.4) from `lib/ricky/slack/ingress.ts`.\\n  - In `routeForwardEvent`, the Slack branch (line 787) currently sends *every* slack-prefixed provider to Sage. Add disambiguation: if `provider === \\\"slack-ricky\\\"` (or matched via the alias map; see 5.5), dispatch to `handleRickySlackForward(envelope)`. Otherwise keep current behavior (Sage).\\n  - New `handleRickySlackForward(envelope)`: detect payload shape (event vs command vs interactivity), claim dedup via `claimWebhookDelivery({ surface: \\\"slack\\\", deliveryId })`, route to the correct ingress handler.\\n\\n- `packages/web/lib/ricky/slack/ingress.ts`:\\n  - Refactor `handleRickySlackCommand` from \\\"returns `Response`\\\" to \\\"POSTs replies to `responseUrl` (if present) or via `chat.postEphemeral`/`chat.postMessage` (if not)\\\" — return type becomes `Promise<void>` (or `Promise<{ status }>`).\\n  - Add `dispatchRickySlackInteractivity(payload)` that mirrors what `interactivity/route.ts` does today but uses `chat.update` + `response_url` POSTs instead of returning `NextResponse.json(...)`.\\n  - All existing helpers (`canResolveRickySlackGate`, `updateGateSlackMessage`, etc.) stay; only the wrappers that previously returned `Response` change.\\n\\n- `packages/web/lib/ricky/slack/egress.ts`:\\n  - Add `postSlackResponseUrl({ responseUrl, body })` helper. Verified 2026-05-07 not present in `egress.ts`. Implementation: `await fetch(responseUrl, { method: \\\"POST\\\", headers: { \\\"content-type\\\": \\\"application/json\\\" }, body: JSON.stringify(body) })`. Throw on non-2xx so callers can log + dedup-retry.\\n\\n- `integrations/slack/ricky-manifest.json`:\\n  - Replace `request_url` for `event_subscriptions`, `slash_commands`, and `interactivity` with `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack` (same template Sage's `integrations/slack/manifest.json` uses for its events URL).\\n  - Drop `signing_secret_env`-style assumptions in any documentation. Mirror the structure of `integrations/linear/ricky-manifest.json` (`webhook.delivery: \\\"via Nango\\\"`, `nango.provider_config_key: \\\"slack-ricky\\\"`, `webhook_router` pointer).\\n\\n### 5.2 Deletions\\n\\n- `packages/web/app/api/v1/ricky/slack/events/route.ts`\\n- `packages/web/app/api/v1/ricky/slack/commands/route.ts`\\n- `packages/web/app/api/v1/ricky/slack/interactivity/route.ts`\\n- `packages/web/lib/ricky/slack/signature.ts`\\n- Any `signature.test.ts` / `events.test.ts` / `commands.test.ts` / `interactivity.test.ts` that exercise the deleted modules. Replace with router-level dispatch tests (see 5.6).\\n\\n### 5.3 Keep\\n\\n- `packages/web/app/api/v1/ricky/slack/oauth/route.ts` — OAuth installation, not a webhook.\\n- `packages/web/lib/ricky/slack/dedup.ts` — already a thin wrapper around `webhook-dedup.ts` after #480.\\n- `packages/web/lib/ricky/slack/{auth,blocks,egress,ingress,parser,proactive,store}.ts` — business logic, only ingress entry shape changes (5.1).\\n\\n### 5.4 Interactivity dispatcher contract\\n\\nThe current interactivity route handles two payload shapes:\\n\\n1. `block_actions` on a gate-approval message (the bulk of the file). Migration: same logic, but instead of `return NextResponse.json({ ok: true })`, do nothing (Nango already returned 200 to Slack). The `chat.update` of the original message via `updateGateSlackMessage` already happens; that's the user-visible feedback.\\n2. `view_submission` returning `{response_action: \\\"clear\\\"}`. Migration: do nothing (Slack closes the modal client-side after timeout). Document the trade-off.\\n\\nFor error paths that today return non-2xx (`forbidden`, `not_found`, `gate_not_open`, etc.), surface the error to the user via `chat.postEphemeral` to the original channel (use payload.channel.id) instead of a 403 status. Slack already saw 200 from Nango.\\n\\n### 5.5 Provider config key registry\\n\\n`packages/web/lib/integrations/nango-webhook-router.ts` has a `PROVIDER_CONFIG_KEY_TO_PROVIDER` alias map (around line 108). Verified 2026-05-07: it currently lists `slack-relay`, `slack-sage`, `slack-my-senior-dev`, `slack-nightcto` — no `slack-ricky`. Add the line `\\\"slack-ricky\\\": \\\"slack-ricky\\\",` next to the other \\\"separate products\\\" entries. `packages/web/lib/integrations/providers.ts` already has a `slack-ricky` entry (defaultConfigKey `slack-ricky`, vfsRoot `/slack-ricky`); no change needed there.\\n\\n`isSlackProvider` matches `slack-*` providers indiscriminately. The Slack branch needs explicit disambiguation:\\n\\n```ts\\nif (provider === \\\"slack-ricky\\\" || envelope.providerConfigKey === \\\"slack-ricky\\\") {\\n  await handleRickySlackForward(envelope);\\n  return;\\n}\\nif (isSlackProvider(provider) || looksLikeSlackFrom(envelope.from)) {\\n  await repairSlackIntegrationFromForwardEvent(envelope, provider);\\n  await forwardToSage(envelope);\\n  return;\\n}\\n```\\n\\n### 5.6 Tests\\n\\nReplace the deleted route-level tests with router-dispatch tests in `packages/web/lib/integrations/__tests__/` (or similar):\\n\\n- `slack-ricky-events-dispatch.test.ts` — feed a fake forward envelope shaped like a Slack event, assert `handleRickySlackCommand` is called with the expected fields, assert dedup short-circuits a second call.\\n- `slack-ricky-command-dispatch.test.ts` — feed a fake forward envelope for a slash command, assert `dispatchRickySlackCommand` calls `postSlackResponseUrl` with the expected ack, assert idempotency.\\n- `slack-ricky-interactivity-dispatch.test.ts` — feed a block_actions envelope, assert `updateGateSlackMessage` is called and no sync return shape exists.\\n\\nPGlite-backed tests for the dedup shared with Linear already exist in `packages/web/lib/ricky/linear/__tests__/dedup.test.ts` (post-#480) — no need to duplicate.\\n\\n## 6. SST / Secrets Cleanup\\n\\nVerified 2026-05-07 via `grep -rn \\\"RickySlackSigningSecret\\\\|SlackSigningSecret\\\\|RICKY_SLACK_SIGNING_SECRET\\\\|SLACK_SIGNING_SECRET\\\"` over `infra/`, `scripts/`, `.github/`, and `packages/`: the SST resources `RickySlackSigningSecret` and `SlackSigningSecret` are **not** declared in `infra/secrets.ts`, **not** present in any `link: [...]` array in `infra/web.ts` or other infra files, **not** in `scripts/set-secrets.sh`, **not** in `.github/workflows/_deploy-cloud-stage.yml`, and **not** in `packages/web/lib/boot/resource-check.ts::REQUIRED_RESOURCES`. The only references are the four reads inside `lib/ricky/slack/signature.ts` (`readLinkedSecret(\\\"RickySlackSigningSecret\\\")`, `readLinkedSecret(\\\"SlackSigningSecret\\\")`, `process.env.RICKY_SLACK_SIGNING_SECRET`, `process.env.SLACK_SIGNING_SECRET`), all guarded by try/catch fallbacks.\\n\\nTherefore the cleanup is single-place: deleting `lib/ricky/slack/signature.ts` (per §5.2) removes every reference. No SST six-place reverse-walk needed. No CI/workflow secret seeding to remove.\\n\\nAfter deploy, confirm:\\n\\n```bash\\ngrep -rn \\\"RickySlackSigningSecret\\\\|SlackSigningSecret\\\\|RICKY_SLACK_SIGNING_SECRET\\\\|SLACK_SIGNING_SECRET\\\" packages/ infra/ scripts/ .github/\\n```\\n\\nreturns no matches.\\n\\n(Note: `workflows/cf-runtime/01-runtime-core.ts` contains the literal string `SLACK_SIGNING_SECRET` inside a code-generation template that emits Cloudflare Worker source — that's unrelated to this repo's Slack signing secrets and stays.)\\n\\n## 7. Rollout Plan\\n\\nSequence matters because the Slack app's webhook URLs are global (changing them invalidates direct routes immediately).\\n\\n1. **Land code changes** (this PR). Routes still exist on `main` until merge, so the Slack app keeps using direct delivery during review.\\n2. **Pre-merge: register `slack-ricky` Nango integration**. Mirror Sage's `slack-relay` Nango configuration; the Nango integration id then substitutes into the manifest URL (§5.1). The file-sync routes already use this Nango forward pattern for Linear and Slack-Sage, so no Nango feature work is needed.\\n3. **Post-merge, pre-Slack-app-update**: deploy the change. The new Nango router branch is live but unused (Slack still POSTs direct to the now-404 routes; this is brief).\\n4. **Slack app dashboard switch**: in the Ricky Slack app's developer dashboard, change the events / slash commands / interactivity request URLs to `https://api.nango.dev/webhook/<slack-ricky-integration-id>/slack`. Single-step cutover (Nango auto-responds to Slack's `url_verification` challenge, same as Sage).\\n5. **Bake**: smoke-test slash commands, mentions, gate approvals in a sandbox workspace. Confirm the new deferred-response UX is acceptable (slash commands may take ~1s longer to show the first response than before).\\n6. **Document**: add a note to the runbook that any future Slack interactivity requiring `response_action: \\\"errors\\\"` (field-level modal validation) cannot use the Nango-routed pattern and must be reverted to a direct route with HMAC.\\n\\n## 8. Risks and Mitigations\\n\\n| Risk | Mitigation |\\n|---|---|\\n| Slash command UX regression (slower first response) | Send the deferred ack (\\\"Got it, working on it\\\") within ~500ms via `response_url`; existing callers already chain to subsequent updates. |\\n| view_submission timeout shows error to user | Current surface only does `response_action: \\\"clear\\\"`; documented trade-off. If a surface needs validation errors later, that interactivity stays direct. |\\n| `provider === \\\"slack-ricky\\\"` dispatch collides with Sage's slack-* matcher | Disambiguate explicitly (§5.5); add a unit test for the router branching. |\\n| Concurrent Slack and Linear deliveries collide on `ricky_webhook_dedup` keys | Composite primary key `(surface, delivery_id)` already handles this — covered by `dedup.test.ts`. |\\n\\n## 9. Acceptance Criteria\\n\\n- [ ] `app/api/v1/ricky/slack/{events,commands,interactivity}/route.ts` deleted; `oauth/route.ts` retained.\\n- [ ] `lib/ricky/slack/signature.ts` deleted.\\n- [ ] No source file references `RickySlackSigningSecret`, `SlackSigningSecret`, `RICKY_SLACK_SIGNING_SECRET`, or `SLACK_SIGNING_SECRET` (verify with `grep -rn`).\\n- [ ] `nango-webhook-router.ts` routes `slack-ricky` provider envelopes to the new dispatcher; default Slack-* still goes to Sage.\\n- [ ] `lib/ricky/slack/ingress.ts::handleRickySlackCommand` returns `Promise<void>` and uses `responseUrl` / `chat.postEphemeral` for replies.\\n- [ ] `integrations/slack/ricky-manifest.json` reflects Nango-routed delivery (mirrors `integrations/linear/ricky-manifest.json` shape).\\n- [ ] `npx tsc -p packages/web/tsconfig.json --noEmit` clean.\\n- [ ] `npx vitest run packages/web/lib/ricky packages/web/lib/integrations` clean (router-dispatch tests pass).\\n- [ ] `npm run web:drizzle-journal:test` clean.\\n- [ ] Bot reviewers (coderabbit, codex) addressed.\\n\\n## 10. References\\n\\n- PR #412 (`feat(ricky): add Slack agent v2 surface and routes`) — original direct-route implementation.\\n- PR #480 (`feat(ricky): add Linear agent webhook surface and shared webhook dedup`) — establishes the Nango-routed pattern this spec mirrors.\\n- `packages/web/app/api/v1/webhooks/nango/route.ts` — canonical Nango webhook intake.\\n- `packages/web/lib/integrations/nango-webhook-router.ts` — `routeNangoWebhook` / `routeForwardEvent` / Linear AgentSession dispatch (post-#480).\\n- `integrations/linear/ricky-manifest.json` (post-#480) — manifest shape model.\\n- CLAUDE.md \\\"Adding a new SST resource\\\" — six-place rule, applied in reverse for removal here.\",\n    \"targetFiles\": [\n      \"packages/web/tsconfig.json\",\n      \"packages/web/lib/ricky\",\n      \"packages/web/lib/integrations\"\n    ]\n  },\n  \"targetFiles\": [\n    \"packages/web/tsconfig.json\",\n    \"packages/web/lib/ricky\",\n    \"packages/web/lib/integrations\"\n  ],\n  \"constraints\": [\n    {\n      \"constraint\": \"Non-goal: Sage's Slack integration is unchanged. This spec only touches the Ricky Slack app.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: The Ricky Slack and Sage Slack apps remain separate. Consolidation into a single shared app is explicitly out of scope.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: The slash command surface (`/ricky run ...`) keeps the same UX and supported subcommands. Only the transport changes.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: The gate-approval interactivity behavior keeps the same UX. Only the transport changes.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: View submissions in the current surface only do `response_action: \\\"clear\\\"` (modal close); this spec accepts that the Nango-routed flow can't return that synchronously and the modal will close on Slack's 3s client-side timeout. If a future surface needs `response_action: \\\"errors\\\"` (field-level validation), that interaction must remain direct (out of scope).\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: URL verification: Nango is assumed to auto-respond to Slack's `url_verification` challenge for forwarded events URLs (same path Sage's Slack manifest uses today via `integrations/slack/manifest.json`). Cutover is a single Slack-app dashboard URL swap. If a future Nango configuration regression breaks this, treat it as an ops issue, not a spec change.\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: No changes to Slack OAuth install flow (`app/api/v1/ricky/slack/oauth/route.ts`).\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"Non-goal: No changes to the underlying `lib/ricky/slack/ingress.ts`, `egress.ts`, `auth.ts`, `store.ts`, `parser.ts`, `proactive.ts` business logic — only the entry/exit shape changes (input becomes a Nango envelope; output becomes deferred `response_url` POSTs / `chat.update` calls instead of synchronous `Response`).\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"must remain direct (out of scope).\",\n      \"category\": \"scope\"\n    },\n    {\n      \"constraint\": \"must echo the challenge.\",\n      \"category\": \"other\"\n    },\n    {\n      \"constraint\": \"must be reverted to a direct route with HMAC.\",\n      \"category\": \"other\"\n    }\n  ],\n  \"evidenceRequirements\": [\n    {\n      \"requirement\": \"Relevant tests must pass.\",\n      \"verificationType\": \"custom\"\n    }\n  ],\n  \"acceptanceGates\": [],\n  \"executionPreference\": \"local\",\n  \"pattern\": {\n    \"selected\": \"pipeline\",\n    \"reason\": \"Selected pipeline using choosing-swarm-patterns because cleanup deletion has false-positive risk and should proceed through a linear evidence ladder.\",\n    \"riskLevel\": \"high\",\n    \"specSignals\": [\n      \"multiple target files\",\n      \"evidence requirements present\",\n      \"critical or production constraint\",\n      \"review constraint\",\n      \"deletion or cleanup risk\",\n      \"choosing-swarm-patterns skill loaded\"\n    ]\n  },\n  \"generatedArtifactsDir\": \".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d\",\n  \"requiredLeadPlanHeadings\": [\n    \"Non-goals\",\n    \"Routing contract\",\n    \"Implementation contract\"\n  ],\n  \"requiredLeadPlanSentinel\": \"GENERATION_LEAD_PLAN_READY\",\n  \"implementationContract\": {\n    \"sourceChangesRequired\": true,\n    \"requireNonEmptyDiffEvidence\": true,\n    \"requireResultOrPrReporting\": true\n  },\n  \"routingContract\": {\n    \"local\": \"Run through Agent Relay using the generated workflow artifact.\",\n    \"cloud\": \"Cloud callers receive the same generated artifact contract unless the normalized spec explicitly requests a separate cloud path.\",\n    \"mcp\": \"Generated runtime agents must not use Relaycast management or messaging tools.\"\n  }\n}" },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/non-goals.md", content: "# Non-goals\n\n- Non-goal: Sage's Slack integration is unchanged. This spec only touches the Ricky Slack app.\n- Non-goal: The Ricky Slack and Sage Slack apps remain separate. Consolidation into a single shared app is explicitly out of scope.\n- Non-goal: The slash command surface (`/ricky run ...`) keeps the same UX and supported subcommands. Only the transport changes.\n- Non-goal: The gate-approval interactivity behavior keeps the same UX. Only the transport changes.\n- Non-goal: View submissions in the current surface only do `response_action: \"clear\"` (modal close); this spec accepts that the Nango-routed flow can't return that synchronously and the modal will close on Slack's 3s client-side timeout. If a future surface needs `response_action: \"errors\"` (field-level validation), that interaction must remain direct (out of scope).\n- Non-goal: URL verification: Nango is assumed to auto-respond to Slack's `url_verification` challenge for forwarded events URLs (same path Sage's Slack manifest uses today via `integrations/slack/manifest.json`). Cutover is a single Slack-app dashboard URL swap. If a future Nango configuration regression breaks this, treat it as an ops issue, not a spec change.\n- Non-goal: No changes to Slack OAuth install flow (`app/api/v1/ricky/slack/oauth/route.ts`).\n- Non-goal: No changes to the underlying `lib/ricky/slack/ingress.ts`, `egress.ts`, `auth.ts`, `store.ts`, `parser.ts`, `proactive.ts` business logic — only the entry/exit shape changes (input becomes a Nango envelope; output becomes deferred `response_url` POSTs / `chat.update` calls instead of synchronous `Response`).\n- must remain direct (out of scope)." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/deliverables.md", content: "# Deliverables\n\n## Declared File Targets\n\n- packages/web/tsconfig.json\n- packages/web/lib/ricky\n- packages/web/lib/integrations\n\n## Output Manifest\n\nDeclared target files define the expected source-change boundary." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md", content: "# Verification Plan\n\nRun or satisfy these verification requirements before signoff:\n\n- file_exists gate for declared targets\n- deterministic sanity gate using POSIX grep, git grep, or an equivalent assertion\n- active-reference gate for deleted manifest paths\n- npx tsc --noEmit\n- npx vitest run\n- git diff gate comparing git diff --name-status against the declared change inventory and requiring a non-empty diff\n- PR URL or explicit result summary\n\nGenerated workflow quality:\n\n- Include a real deterministic sanity gate over produced files, not just prose saying one exists.\n- Prefer POSIX grep, git grep, or a small inline assertion command that exits non-zero when expected content/state is missing.\n- If using rg, guard it with command -v rg and provide a grep or git grep fallback.\n- For cleanup or deletion work, persist a changed-files inventory with statuses, active-reference evidence for deleted paths, and command summaries for final signoff.\n- For cleanup or deletion work, start from .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/cleanup-candidate-prescan.txt and cite that exact path in .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/cleanup-report.md so the evidence trail names its prescan input.\n- Keep each agent step bounded to one coherent slice. Split broad implementation or test-writing work into sequential/fan-out steps with deterministic gates between them instead of relying on a single long agent timeout." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan-instructions.md", content: "# Lead Plan Instructions\n\nPlan the workflow execution from the packaged context files, not from the short task prompt.\n\nRequired sections:\n\n- Non-goals\n- Routing contract\n- Implementation contract\n- Deliverables\n- Verification gates\n\nUse this exact section heading in the lead plan. Do not rename \"Non-goals\" to \"Out of scope\" or another synonym.\n\nWrite .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md and end it with GENERATION_LEAD_PLAN_READY.\n\nGeneration-time skill boundary:\n\n- Read .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json and treat it as generator metadata only.\n- Skills are applied by Ricky during selection, loading, and template rendering.\n- Do not claim generated agents load, retain, or embody skill files at runtime unless a future runtime test proves that path.\n\nLoaded skills summary:\n\nrelay-80-100-workflow confidence=1 reason=Spec text mentions \"workflows\". Spec text mentions \"must\". Spec text mentions \"validate\". Spec text mentions \"before\". Spec text mentions \"pattern\". Spec text mentions \"code\". Spec text mentions \"feature\". Spec text mentions \"works\". Spec text mentions \"pglite\". Spec text mentions \"sandbox\". Spec text mentions \"verify\". Spec text mentions \"gates\". Spec text mentions \"after\". Spec text mentions \"every\". Spec text mentions \"edit\". Spec text mentions \"implementation\". Spec text mentions \"tests\". evidence=keyword:workflows, keyword:must, keyword:validate, keyword:before, keyword:pattern, keyword:code, keyword:feature, keyword:works, keyword:pglite, keyword:sandbox, keyword:verify, keyword:gates, keyword:after, keyword:every, keyword:edit, keyword:implementation, keyword:tests\nwriting-agent-relay-workflows confidence=1 reason=Spec text mentions \"workflows\". Spec text mentions \"relay\". Spec text mentions \"step\". Spec text mentions \"agent\". Spec text mentions \"output\". Spec text mentions \"verification\". Spec text mentions \"gates\". Spec text mentions \"owner\". Spec text mentions \"dedicated\". Spec text mentions \"channel\". Spec text mentions \"error\". Spec text mentions \"event\". Spec text mentions \"pattern\". evidence=keyword:workflows, keyword:relay, keyword:step, keyword:agent, keyword:output, keyword:verification, keyword:gates, keyword:owner, keyword:dedicated, keyword:channel, keyword:error, keyword:event, keyword:pattern\nshadcn confidence=0.6 reason=Spec text mentions \"adding\". Spec text mentions \"working\". Spec text mentions \"switch\". evidence=keyword:adding, keyword:working, keyword:switch\nchoosing-swarm-patterns confidence=0.4 reason=Spec text mentions \"agents\". Spec text mentions \"pattern\". evidence=keyword:agents, keyword:pattern" },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/implementation-instructions.md", content: "# Implementation Instructions\n\nIMPLEMENTATION_WORKFLOW_CONTRACT:\n\n- For implementation specs, edit source files and produce code changes, not just plan.md, mapping.json, or analysis artifacts.\n- Keep a non-empty implementation diff outside transient artifact directories.\n- Add or update tests that prove the changed behavior.\n- Keep execution routing explicit for local, cloud, and MCP callers.\n- Materialize outputs to disk, then stop for deterministic gates." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-checklist.md", content: "# Review Checklist\n\nAssess:\n\n- Declared file targets and non-goals.\n- Deterministic gates and evidence quality.\n- Review/fix/final-review 80-to-100 loop shape.\n- Local/cloud/MCP routing clarity.\n- Whether source changes, tests, non-empty diff evidence, and PR/result reporting satisfy the implementation contract." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/pattern-decision.txt", content: "pattern=pipeline; reason=Selected pipeline using choosing-swarm-patterns because cleanup deletion has false-positive risk and should proceed through a linear evidence ladder." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/loaded-skills.txt", content: "relay-80-100-workflow confidence=1 reason=Spec text mentions \"workflows\". Spec text mentions \"must\". Spec text mentions \"validate\". Spec text mentions \"before\". Spec text mentions \"pattern\". Spec text mentions \"code\". Spec text mentions \"feature\". Spec text mentions \"works\". Spec text mentions \"pglite\". Spec text mentions \"sandbox\". Spec text mentions \"verify\". Spec text mentions \"gates\". Spec text mentions \"after\". Spec text mentions \"every\". Spec text mentions \"edit\". Spec text mentions \"implementation\". Spec text mentions \"tests\". evidence=keyword:workflows, keyword:must, keyword:validate, keyword:before, keyword:pattern, keyword:code, keyword:feature, keyword:works, keyword:pglite, keyword:sandbox, keyword:verify, keyword:gates, keyword:after, keyword:every, keyword:edit, keyword:implementation, keyword:tests\nwriting-agent-relay-workflows confidence=1 reason=Spec text mentions \"workflows\". Spec text mentions \"relay\". Spec text mentions \"step\". Spec text mentions \"agent\". Spec text mentions \"output\". Spec text mentions \"verification\". Spec text mentions \"gates\". Spec text mentions \"owner\". Spec text mentions \"dedicated\". Spec text mentions \"channel\". Spec text mentions \"error\". Spec text mentions \"event\". Spec text mentions \"pattern\". evidence=keyword:workflows, keyword:relay, keyword:step, keyword:agent, keyword:output, keyword:verification, keyword:gates, keyword:owner, keyword:dedicated, keyword:channel, keyword:error, keyword:event, keyword:pattern\nshadcn confidence=0.6 reason=Spec text mentions \"adding\". Spec text mentions \"working\". Spec text mentions \"switch\". evidence=keyword:adding, keyword:working, keyword:switch\nchoosing-swarm-patterns confidence=0.4 reason=Spec text mentions \"agents\". Spec text mentions \"pattern\". evidence=keyword:agents, keyword:pattern" },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-matches.json", content: "[{\"id\":\"relay-80-100-workflow\",\"name\":\"relay-80-100-workflow\",\"confidence\":1,\"reason\":\"Spec text mentions \\\"workflows\\\". Spec text mentions \\\"must\\\". Spec text mentions \\\"validate\\\". Spec text mentions \\\"before\\\". Spec text mentions \\\"pattern\\\". Spec text mentions \\\"code\\\". Spec text mentions \\\"feature\\\". Spec text mentions \\\"works\\\". Spec text mentions \\\"pglite\\\". Spec text mentions \\\"sandbox\\\". Spec text mentions \\\"verify\\\". Spec text mentions \\\"gates\\\". Spec text mentions \\\"after\\\". Spec text mentions \\\"every\\\". Spec text mentions \\\"edit\\\". Spec text mentions \\\"implementation\\\". Spec text mentions \\\"tests\\\".\",\"evidence\":[{\"trigger\":\"workflows\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"workflows\\\".\"},{\"trigger\":\"must\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"must\\\".\"},{\"trigger\":\"validate\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"validate\\\".\"},{\"trigger\":\"before\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"before\\\".\"},{\"trigger\":\"pattern\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"pattern\\\".\"},{\"trigger\":\"code\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"code\\\".\"},{\"trigger\":\"feature\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"feature\\\".\"},{\"trigger\":\"works\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"works\\\".\"},{\"trigger\":\"pglite\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"pglite\\\".\"},{\"trigger\":\"sandbox\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"sandbox\\\".\"},{\"trigger\":\"verify\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"verify\\\".\"},{\"trigger\":\"gates\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"gates\\\".\"},{\"trigger\":\"after\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"after\\\".\"},{\"trigger\":\"every\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"every\\\".\"},{\"trigger\":\"edit\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"edit\\\".\"},{\"trigger\":\"implementation\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"implementation\\\".\"},{\"trigger\":\"tests\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"tests\\\".\"}]},{\"id\":\"writing-agent-relay-workflows\",\"name\":\"writing-agent-relay-workflows\",\"confidence\":1,\"reason\":\"Spec text mentions \\\"workflows\\\". Spec text mentions \\\"relay\\\". Spec text mentions \\\"step\\\". Spec text mentions \\\"agent\\\". Spec text mentions \\\"output\\\". Spec text mentions \\\"verification\\\". Spec text mentions \\\"gates\\\". Spec text mentions \\\"owner\\\". Spec text mentions \\\"dedicated\\\". Spec text mentions \\\"channel\\\". Spec text mentions \\\"error\\\". Spec text mentions \\\"event\\\". Spec text mentions \\\"pattern\\\".\",\"evidence\":[{\"trigger\":\"workflows\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"workflows\\\".\"},{\"trigger\":\"relay\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"relay\\\".\"},{\"trigger\":\"step\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"step\\\".\"},{\"trigger\":\"agent\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"agent\\\".\"},{\"trigger\":\"output\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"output\\\".\"},{\"trigger\":\"verification\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"verification\\\".\"},{\"trigger\":\"gates\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"gates\\\".\"},{\"trigger\":\"owner\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"owner\\\".\"},{\"trigger\":\"dedicated\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"dedicated\\\".\"},{\"trigger\":\"channel\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"channel\\\".\"},{\"trigger\":\"error\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"error\\\".\"},{\"trigger\":\"event\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"event\\\".\"},{\"trigger\":\"pattern\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"pattern\\\".\"}]},{\"id\":\"shadcn\",\"name\":\"shadcn\",\"confidence\":0.6,\"reason\":\"Spec text mentions \\\"adding\\\". Spec text mentions \\\"working\\\". Spec text mentions \\\"switch\\\".\",\"evidence\":[{\"trigger\":\"adding\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"adding\\\".\"},{\"trigger\":\"working\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"working\\\".\"},{\"trigger\":\"switch\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"switch\\\".\"}]},{\"id\":\"choosing-swarm-patterns\",\"name\":\"choosing-swarm-patterns\",\"confidence\":0.4,\"reason\":\"Spec text mentions \\\"agents\\\". Spec text mentions \\\"pattern\\\".\",\"evidence\":[{\"trigger\":\"agents\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"agents\\\".\"},{\"trigger\":\"pattern\",\"source\":\"keyword\",\"detail\":\"Spec text mentions \\\"pattern\\\".\"}]}]" },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/tool-selection.json", content: "[{\"stepId\":\"lead-plan\",\"agent\":\"lead-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"implement-artifact\",\"agent\":\"impl-primary-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"review-claude\",\"agent\":\"reviewer-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"review-codex\",\"agent\":\"reviewer-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"fix-loop\",\"agent\":\"validator-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-review-claude\",\"agent\":\"reviewer-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-review-codex\",\"agent\":\"reviewer-codex\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":2,\"rule\":\"project default runner @agent-relay/sdk\"},{\"stepId\":\"final-signoff\",\"agent\":\"validator-claude\",\"runner\":\"@agent-relay/sdk\",\"concurrency\":1,\"rule\":\"project default runner @agent-relay/sdk\"}]" },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json", content: "{\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"boundary\":\"Skills influence Ricky generator selection, loading, template rendering, workflow contract, validation gates, and metadata. Generated runtime agents receive only the rendered workflow instructions; they do not load or embody skill files at runtime.\",\"loadedSkills\":[\"relay-80-100-workflow\",\"writing-agent-relay-workflows\",\"shadcn\",\"choosing-swarm-patterns\"],\"applicationEvidence\":[{\"skillName\":\"relay-80-100-workflow\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected relay-80-100-workflow during workflow generation. Spec text mentions \\\"workflows\\\". Spec text mentions \\\"must\\\". Spec text mentions \\\"validate\\\". Spec text mentions \\\"before\\\". Spec text mentions \\\"pattern\\\". Spec text mentions \\\"code\\\". Spec text mentions \\\"feature\\\". Spec text mentions \\\"works\\\". Spec text mentions \\\"pglite\\\". Spec text mentions \\\"sandbox\\\". Spec text mentions \\\"verify\\\". Spec text mentions \\\"gates\\\". Spec text mentions \\\"after\\\". Spec text mentions \\\"every\\\". Spec text mentions \\\"edit\\\". Spec text mentions \\\"implementation\\\". Spec text mentions \\\"tests\\\".\"},{\"skillName\":\"relay-80-100-workflow\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded relay-80-100-workflow descriptor before template rendering.\"},{\"skillName\":\"writing-agent-relay-workflows\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected writing-agent-relay-workflows during workflow generation. Spec text mentions \\\"workflows\\\". Spec text mentions \\\"relay\\\". Spec text mentions \\\"step\\\". Spec text mentions \\\"agent\\\". Spec text mentions \\\"output\\\". Spec text mentions \\\"verification\\\". Spec text mentions \\\"gates\\\". Spec text mentions \\\"owner\\\". Spec text mentions \\\"dedicated\\\". Spec text mentions \\\"channel\\\". Spec text mentions \\\"error\\\". Spec text mentions \\\"event\\\". Spec text mentions \\\"pattern\\\".\"},{\"skillName\":\"writing-agent-relay-workflows\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded writing-agent-relay-workflows descriptor before template rendering.\"},{\"skillName\":\"shadcn\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected shadcn during workflow generation. Spec text mentions \\\"adding\\\". Spec text mentions \\\"working\\\". Spec text mentions \\\"switch\\\".\"},{\"skillName\":\"shadcn\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded shadcn descriptor before template rendering.\"},{\"skillName\":\"choosing-swarm-patterns\",\"stage\":\"generation_selection\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Selected choosing-swarm-patterns during workflow generation. Spec text mentions \\\"agents\\\". Spec text mentions \\\"pattern\\\".\"},{\"skillName\":\"choosing-swarm-patterns\",\"stage\":\"generation_loading\",\"effect\":\"metadata\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Loaded choosing-swarm-patterns descriptor before template rendering.\"},{\"skillName\":\"choosing-swarm-patterns\",\"stage\":\"generation_rendering\",\"effect\":\"pattern_selection\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Rendered the selected swarm pattern into the workflow builder so Ricky chooses the coordination shape before authoring tasks.\"},{\"skillName\":\"writing-agent-relay-workflows\",\"stage\":\"generation_rendering\",\"effect\":\"workflow_contract\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Rendered 10 workflow tasks with dedicated channel setup, explicit agents, step dependencies, review stages, and final signoff.\"},{\"skillName\":\"relay-80-100-workflow\",\"stage\":\"generation_rendering\",\"effect\":\"validation_gates\",\"behavior\":\"generation_time_only\",\"runtimeEmbodiment\":false,\"evidence\":\"Rendered 12 deterministic gates including initial soft validation, fix-loop checks, final hard validation, git diff, and regression gates.\"}]}" },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-runtime-boundary.txt", content: "Skills influence Ricky generator selection, loading, template rendering, workflow contract, validation gates, and metadata. Generated runtime agents receive only the rendered workflow instructions; they do not load or embody skill files at runtime." },
    { path: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/matched-skills.md", content: "\n# relay-80-100-workflow\nreason=Spec text mentions \"workflows\". Spec text mentions \"must\". Spec text mentions \"validate\". Spec text mentions \"before\". Spec text mentions \"pattern\". Spec text mentions \"code\". Spec text mentions \"feature\". Spec text mentions \"works\". Spec text mentions \"pglite\". Spec text mentions \"sandbox\". Spec text mentions \"verify\". Spec text mentions \"gates\". Spec text mentions \"after\". Spec text mentions \"every\". Spec text mentions \"edit\". Spec text mentions \"implementation\". Spec text mentions \"tests\".\n---\nname: relay-80-100-workflow\ndescription: Use when writing agent-relay workflows that must fully validate features end-to-end before merging. Covers the 80-to-100 pattern - going beyond \"code compiles\" to \"feature works, tested E2E locally.\" Includes PGlite for in-memory Postgres testing, mock sandbox patterns, test-fix-rerun loops, verify gates after every edit, and the full lifecycle from implementation through passing tests to commit.\n---\n\n### Overview\n\nMost agent workflows get features to ~80%: code written, types check, maybe a build passes. This skill covers the **80-to-100 gap** — making workflows that fully validate features end-to-end before committing. The goal: every feature merged via these workflows is **tested, verified, and known-working**, not just \"it compiles.\"\n\n### When to Use\n\n- Writing workflows where the deliverable must be **production-ready**, not just code-complete\n- Features that touch databases, APIs, or infrastructure that can be tested locally\n- Any workflow where \"it compiles\" is not sufficient proof of correctness\n- When you want confidence that the commit actually works before deploying\n\n### Core Principle: Test In The Workflow\n\n#### The key insight: **run tests as deterministic steps inside the workflow itself**. Don't just write test files — execute them, verify they pass, fix failures, and re-run. The workflow doesn't commit until tests are green.\n\n```\nimplement → write tests → run tests → fix failures → re-run → build check → regression check → commit\n```\n\n\n### The Test-Fix-Rerun Pattern\n\n#### Every testable feature in a workflow should follow this three-step pattern:\n\n```typescript\n// Step 1: Run tests (allow failure — we expect issues on first run)\n.step('run-tests', {\n  type: 'deterministic',\n  dependsOn: ['create-tests'],\n  command: 'npx tsx --test tests/my-feature.test.ts 2>&1 | tail -60',\n  captureOutput: true,\n  failOnError: false,  // <-- Don't fail the workflow, let the agent fix it\n})\n\n// Step 2: Agent reads output, fixes issues, re-runs until green\n.step('fix-tests', {\n  agent: 'tester',\n  dependsOn: ['run-tests'],\n  task: `Check the test output and fix any failures.\n\nTest output:\n{{steps.run-tests.output}}\n\nIf all tests passed, do nothing.\nIf there are failures:\n1. Read the failing test file and source files\n2. Fix the issues (could be in test or source)\n3. Re-run: npx tsx --test tests/my-feature.test.ts\n4. Keep fixing until ALL tests pass.`,\n  verification: { type: 'exit_code' },\n})\n\n// Step 3: Deterministic final run — this one MUST pass\n.step('run-tests-final', {\n  type: 'deterministic',\n  dependsOn: ['fix-tests'],\n  command: 'npx tsx --test tests/my-feature.test.ts 2>&1',\n  captureOutput: true,\n  failOnError: true,  // <-- Hard fail if tests still broken\n})\n```\n\n\n### PGlite: In-Memory Postgres for Database Testing\n\n#### Setup\n\n```typescript\n.step('install-pglite', {\n  type: 'deterministic',\n  command: 'npm install --save-dev @electric-sql/pglite 2>&1 | tail -5',\n  captureOutput: true,\n})\n```\n\n#### Test Helper Pattern\n\n```typescript\n// tests/helpers/pglite-db.ts\nimport { PGlite } from '@electric-sql/pglite';\nimport { drizzle } from 'drizzle-orm/pglite';\nimport * as schema from '../../packages/web/lib/db/schema.js';\n\n// Raw DDL matching your Drizzle schema — PGlite doesn't run Drizzle migrations\nconst MY_TABLE_DDL = `\nCREATE TABLE IF NOT EXISTS my_table (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  name TEXT NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n`;\n\nexport async function createTestDb() {\n  const pg = new PGlite();\n  await pg.exec(MY_TABLE_DDL);\n  const db = drizzle(pg, { schema });\n  return { db, pg, schema, cleanup: () => pg.close() };\n}\n```\n\n#### Test Structure\n\n```typescript\n// tests/my-feature.test.ts\nimport { describe, it } from 'node:test';\nimport assert from 'node:assert/strict';\nimport { randomUUID } from 'node:crypto';\nimport { createTestDb } from './helpers/pglite-db.js';\n\ndescribe('my feature', () => {\n  it('does the thing correctly', async () => {\n    const { db, schema, cleanup } = await createTestDb();\n    try {\n      // Arrange\n      const testId = randomUUID();\n      // Act — use your module against the real (in-memory) Postgres\n      // Assert\n      assert.equal(result.name, 'expected');\n    } finally {\n      await cleanup();\n    }\n  });\n});\n```\n\n\n### Verify Gates After Every Edit\n\n#### Never trust that an agent edited a file correctly. Add a deterministic verify gate after every agent edit step:\n\n```typescript\n// Agent edits a file\n.step('edit-schema', {\n  agent: 'impl',\n  dependsOn: ['read-schema'],\n  task: `Edit packages/web/lib/db/schema.ts...`,\n  verification: { type: 'exit_code' },\n})\n\n// Deterministic verification — did the edit actually land?\n.step('verify-schema', {\n  type: 'deterministic',\n  dependsOn: ['edit-schema'],\n  command: `if git diff --quiet packages/web/lib/db/schema.ts; then echo \"NOT MODIFIED\"; exit 1; fi\ngrep \"my_new_table\" packages/web/lib/db/schema.ts >/dev/null && echo \"OK\" || (echo \"MISSING\"; exit 1)`,\n  failOnError: true,\n  captureOutput: true,\n})\n```\n\n\n### Mock Sandbox Pattern\n\n#### When testing code that interacts with Daytona sandboxes, use inline mock objects matching the existing test conventions:\n\n```typescript\nconst daytona = {\n  create: async () => ({\n    id: 'sandbox-id',\n    process: {\n      executeCommand: async (cmd, cwd, env) => ({\n        result: 'output',\n        exitCode: 0,\n      }),\n    },\n    fs: {\n      uploadFile: async () => undefined,\n    },\n    getUserHomeDir: async () => '/home/daytona',\n  }),\n  remove: async () => undefined,\n};\n```\n\n\n### Regression Testing\n\n#### After your new tests pass, always run the **existing test suite** to catch regressions:\n\n```typescript\n.step('run-existing-tests', {\n  type: 'deterministic',\n  dependsOn: ['fix-build'],\n  command: 'npm run orchestrator:test 2>&1 | tail -40',\n  captureOutput: true,\n  failOnError: false,\n})\n\n.step('fix-regressions', {\n  agent: 'impl',\n  dependsOn: ['run-existing-tests'],\n  task: `Check the full test suite for regressions caused by our changes.\n\nTest output:\n{{steps.run-existing-tests.output}}\n\nIf all tests passed, do nothing.\nIf EXISTING tests broke, read the failing test, find what we broke, fix it.\nMost likely cause: constructor signatures changed, new required fields added\nwithout defaults, or import paths shifted.\n\nRun: npm run orchestrator:test\nFix until all tests pass.`,\n  verification: { type: 'exit_code' },\n})\n```\n\n\n### Full Workflow Template\n\n#### Here's the complete pattern for a feature that touches the database:\n\n```typescript\nimport { workflow } from '@relayflows/core';\n\nconst result = await workflow('my-feature')\n  .description('Add feature X with full E2E validation')\n  .pattern('dag')\n  .channel('wf-my-feature')\n  .maxConcurrency(3)\n  .timeout(3_600_000)\n\n  .agent('impl', { cli: 'claude', preset: 'worker', retries: 2 })\n  .agent('tester', { cli: 'claude', preset: 'worker', retries: 2 })\n\n  // ── Phase 1: Read ────────────────────────────────────────────────\n  .step('read-target', {\n    type: 'deterministic',\n    command: 'cat path/to/file.ts',\n    captureOutput: true,\n  })\n\n  // ── Phase 2: Implement ───────────────────────────────────────────\n  .step('edit-target', {\n    agent: 'impl',\n    dependsOn: ['read-target'],\n    task: `Edit path/to/file.ts. Current contents:\n{{steps.read-target.output}}\n<specific instructions>\nOnly edit this one file.`,\n    verification: { type: 'exit_code' },\n  })\n  .step('verify-target', {\n    type: 'deterministic',\n    dependsOn: ['edit-target'],\n    command: 'git diff --quiet path/to/file.ts && (echo \"NOT MODIFIED\"; exit 1) || echo \"OK\"',\n    failOnError: true,\n    captureOutput: true,\n  })\n\n  // ── Phase 3: Test infrastructure ─────────────────────────────────\n  .step('install-pglite', {\n    type: 'deterministic',\n    command: 'npm install --save-dev @electric-sql/pglite 2>&1 | tail -5',\n    captureOutput: true,\n  })\n  .step('create-test-helpers', {\n    agent: 'tester',\n    dependsOn: ['install-pglite'],\n    task: 'Create tests/helpers/pglite-db.ts with <DDL for your tables>...',\n    verification: { type: 'file_exists', value: 'tests/helpers/pglite-db.ts' },\n  })\n  .step('create-tests', {\n    agent: 'tester',\n    dependsOn: ['create-test-helpers', 'verify-target'],\n    task: 'Create tests/my-feature.test.ts with <test descriptions>...',\n    verification: { type: 'file_exists', value: 'tests/my-feature.test.ts' },\n  })\n\n  // ── Phase 4: Test-fix-rerun loop ─────────────────────────────────\n  .step('run-tests', {\n    type: 'deterministic',\n    dependsOn: ['create-tests'],\n    command: 'npx tsx --test tests/my-feature.test.ts 2>&1 | tail -60',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('fix-tests', {\n    agent: 'tester',\n    dependsOn: ['run-tests'],\n    task: `Fix any test failures. Output:\\n{{steps.run-tests.output}}`,\n    verification: { type: 'exit_code' },\n  })\n  .step('run-tests-final', {\n    type: 'deterministic',\n    dependsOn: ['fix-tests'],\n    command: 'npx tsx --test tests/my-feature.test.ts 2>&1',\n    captureOutput: true,\n    failOnError: true,\n  })\n\n  // ── Phase 5: Build + regression ──────────────────────────────────\n  .step('build-check', {\n    type: 'deterministic',\n    dependsOn: ['run-tests-final'],\n    command: 'npx tsc --noEmit 2>&1 | tail -20; echo \"EXIT: $?\"',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('fix-build', {\n    agent: 'impl',\n    dependsOn: ['build-check'],\n    task: `Fix type errors if any. Output:\\n{{steps.build-check.output}}`,\n    verification: { type: 'exit_code' },\n  })\n  .step('run-existing-tests', {\n    type: 'deterministic',\n    dependsOn: ['fix-build'],\n    command: 'npm test 2>&1 | tail -40',\n    captureOutput: true,\n    failOnError: false,\n  })\n  .step('fix-regressions', {\n    agent: 'impl',\n    dependsOn: ['run-existing-tests'],\n    task: `Fix regressions if any. Output:\\n{{steps.run-existing-tests.output}}`,\n    verification: { type: 'exit_code' },\n  })\n\n  // ── Phase 6: Commit ──────────────────────────────────────────────\n  .step('commit', {\n    type: 'deterministic',\n    dependsOn: ['fix-regressions'],\n    command: 'git add <files> && git commit -m \"feat: ...\"',\n    captureOutput: true,\n    failOnError: true,\n  })\n\n  .onError('retry', { maxRetries: 2, retryDelayMs: 10_000 })\n  .run({ cwd: process.cwd() });\n```\n\n\n### Checklist: Is Your Workflow 80-to-100?\n\n| Check | How |\n|-------|-----|\n| Tests exist | `file_exists` verification on test file |\n| Tests actually run | Deterministic step executes them |\n| Test failures get fixed | Agent step reads output, fixes, re-runs |\n| Final test run is hard-gated | `failOnError: true` on last test step |\n| Build passes | `npx tsc --noEmit` deterministic step |\n| No regressions | Existing test suite runs after changes |\n| Every edit is verified | `git diff --quiet` + grep after each agent edit |\n| Commit only happens after all gates | `dependsOn` chains to final verification |\n\n### Common Anti-Patterns\n\n| Anti-pattern | Why it fails | Fix |\n|-------------|-------------|-----|\n| Tests written but never executed | Agent claims they pass, they don't | Add deterministic `run-tests` step |\n| Single `failOnError: true` test run | First failure kills workflow, no chance to fix | Use the three-step test-fix-rerun pattern |\n| No regression test | New feature works, old features break | Run `npm test` after build check |\n| Agent asked to \"write and run tests\" in one step | Agent writes tests, runs them, they fail, it edits, output is garbled | Separate write/run/fix into distinct steps |\n| PGlite DDL doesn't match Drizzle schema | Tests pass on wrong schema | Derive DDL from schema.ts or test with real migration |\n| `failOnError: false` on final test run | Broken tests get committed | Always `failOnError: true` on the gate step |\n| Testing only happy path | Edge cases break in prod | Specify edge case tests in the task prompt |\n| No verify gate after agent edits | Agent exits 0 without writing anything | Add `git diff --quiet` check after every edit |\n\n\n# writing-agent-relay-workflows\nreason=Spec text mentions \"workflows\". Spec text mentions \"relay\". Spec text mentions \"step\". Spec text mentions \"agent\". Spec text mentions \"output\". Spec text mentions \"verification\". Spec text mentions \"gates\". Spec text mentions \"owner\". Spec text mentions \"dedicated\". Spec text mentions \"channel\". Spec text mentions \"error\". Spec text mentions \"event\". Spec text mentions \"pattern\".\n---\nname: writing-agent-relay-workflows\ndescription: Use when building multi-agent workflows with the relay broker-sdk - covers the WorkflowBuilder API, DAG step dependencies, agent definitions, step output chaining via {{steps.plan.output}}, verification gates, evidence-based completion, owner decisions, dedicated channels, dynamic channel management (subscribe/unsubscribe/mute/unmute), swarm patterns, error handling, event listeners, step sizing rules, authoring best practices, and the lead+workers team pattern for complex steps\n---\n\n### Overview\n\nThe relay broker-sdk workflow system orchestrates multiple AI agents (Claude, Codex, Gemini, Aider, Goose) through typed DAG-based workflows. Workflows can be written in **TypeScript** (preferred), **Python**, or **YAML**.\n\n**Language preference:** TypeScript > Python > YAML. Use TypeScript unless the project is Python-only or a simple config-driven workflow suits YAML.\n\n**Pattern selection:** Do not default to `dag` blindly. If the job needs a different swarm/workflow type, consult the `choosing-swarm-patterns` skill when available and select the pattern that best matches the coordination problem.\n\n### When to Use\n\n- Building multi-agent workflows with step dependencies\n- Orchestrating different AI CLIs (claude, codex, gemini, aider, goose)\n- Creating DAG, pipeline, fan-out, or other swarm patterns\n- Needing verification gates, retries, or step output chaining\n- Dynamic channel management: agents joining/leaving/muting channels mid-workflow\n\n### Quick Reference\n\n#### > **Note:** this Quick Reference assumes an **ESM** workflow file (the host `package.json` has `\"type\": \"module\"`). For CJS repos, see rule #1 in **Critical TypeScript rules** below — convert `import { workflow } from '@relayflows/core'` to `const { workflow } = require('@relayflows/core')` and wrap the workflow in `async function main() { ... } main().catch(console.error)` since CJS does not support top-level `await`. **Always check `package.json` before copy-pasting the snippet.**\n\n```typescript\nimport { workflow } from '@relayflows/core';\n\nconst result = await workflow('my-workflow')\n  .description('What this workflow does')\n  .pattern('dag') // or 'pipeline', 'fan-out', etc.\n  .channel('wf-my-workflow') // dedicated channel (auto-generated if omitted)\n  .maxConcurrency(3)\n  .timeout(3_600_000) // global timeout (ms)\n\n  .agent('lead', { cli: 'claude', role: 'Architect', retries: 2 })\n  .agent('worker', { cli: 'codex', role: 'Implementer', retries: 2 })\n\n  .step('plan', {\n    agent: 'lead',\n    task: `Analyze the codebase and produce a plan.`,\n    retries: 2,\n    verification: { type: 'output_contains', value: 'PLAN_COMPLETE' },\n  })\n  .step('implement', {\n    agent: 'worker',\n    task: `Implement based on this plan:\\n{{steps.plan.output}}`,\n    dependsOn: ['plan'],\n    verification: { type: 'exit_code' },\n  })\n\n  .onError('retry', { maxRetries: 2, retryDelayMs: 10_000 })\n  .run({ cwd: process.cwd() });\n\n  console.log('Result:', result.status);\n```\n\n\n### ⚡ Parallelism — Design for Speed\n\n#### Cross-Workflow Parallelism: Wave Planning\n\n```bash\n# BAD — sequential (14 hours for 27 workflows at ~30 min each)\nagent-relay run workflows/34-sst-wiring.ts\nagent-relay run workflows/35-env-config.ts\nagent-relay run workflows/36-loading-states.ts\n# ... one at a time\n\n# GOOD — parallel waves (3-4 hours for 27 workflows)\n# Wave 1: independent infra (parallel)\nagent-relay run workflows/34-sst-wiring.ts &\nagent-relay run workflows/35-env-config.ts &\nagent-relay run workflows/36-loading-states.ts &\nagent-relay run workflows/37-responsive.ts &\nwait\ngit add -A && git commit -m \"Wave 1\"\n\n# Wave 2: testing (parallel — independent test suites)\nagent-relay run workflows/40-unit-tests.ts &\nagent-relay run workflows/41-integration-tests.ts &\nagent-relay run workflows/42-e2e-tests.ts &\nwait\ngit add -A && git commit -m \"Wave 2\"\n```\n\n#### Declare File Scope for Planning\n\n```typescript\nworkflow('48-comparison-mode')\n  .packages(['web', 'core'])                // monorepo packages touched\n  .isolatedFrom(['49-feedback-system'])      // explicitly safe to parallelize\n  .requiresBefore(['46-admin-dashboard'])    // explicit ordering constraint\n```\n\n#### Within-Workflow Parallelism\n\n```typescript\n// BAD — unnecessary sequential chain\n.step('fix-component-a', { agent: 'worker', dependsOn: ['review'] })\n.step('fix-component-b', { agent: 'worker', dependsOn: ['fix-component-a'] })  // why wait?\n\n// GOOD — parallel fan-out, merge at the end\n.step('fix-component-a', { agent: 'impl-1', dependsOn: ['review'] })\n.step('fix-component-b', { agent: 'impl-2', dependsOn: ['review'] })  // same dep = parallel\n.step('verify-all', { agent: 'reviewer', dependsOn: ['fix-component-a', 'fix-component-b'] })\n```\n\n\n### Failure Prevention\n\n#### 1. Do not use raw top-level `await`\n\n```ts\nasync function runWorkflow() {\n  const result = await workflow('my-workflow')\n    // ...\n    .run({ cwd: process.cwd() });\n\n  console.log('Workflow status:', result.status);\n}\n\nrunWorkflow().catch((error) => {\n  console.error(error);\n  process.exit(1);\n});\n```\n\n#### 2b. Standard preflight template for resumable workflows\n\n```ts\n.step('preflight', {\n  type: 'deterministic',\n  command: [\n    'set -e',\n    'BRANCH=$(git rev-parse --abbrev-ref HEAD)',\n    'echo \"branch: $BRANCH\"',\n    'if [ \"$BRANCH\" != \"fix/your-branch-name\" ]; then echo \"ERROR: wrong branch\"; exit 1; fi',\n    // Files the workflow is allowed to find dirty on entry:\n    //   - package-lock.json: npm install is idempotent and often touches it\n    //   - every file the workflow's edit steps will rewrite: a prior partial\n    //     run may have left them dirty, and the edit step will rewrite\n    //     them cleanly before commit\n    // Everything else is unexpected drift and must fail preflight.\n    'ALLOWED_DIRTY=\"package-lock.json|path/to/file1\\\\\\\\.ts|path/to/file2\\\\\\\\.ts\"',\n    'DIRTY=$({ git diff --name-only; git ls-files --others --exclude-standard; } | sort -u | grep -vE \"^(${ALLOWED_DIRTY})$\" || true)',\n    'if [ -n \"$DIRTY\" ]; then echo \"ERROR: unexpected tracked drift:\"; echo \"$DIRTY\"; exit 1; fi',\n    'if ! git diff --cached --quiet; then echo \"ERROR: staging area is dirty\"; git diff --cached --stat; exit 1; fi',\n    'gh auth status >/dev/null 2>&1 || (echo \"ERROR: gh CLI not authenticated\"; exit 1)',\n    'echo PREFLIGHT_OK',\n  ].join(' && '),\n  captureOutput: true,\n  failOnError: true,\n}),\n```\n\n#### 2c. Picking the right `.join()` for multi-line shell commands\n\n```ts\ncommand: [\n  'set -e',\n  'HITS=$(grep -c diag src/cli/commands/setup.ts || true)',\n  'if [ \"$HITS\" -lt 6 ]; then echo \"FAIL\"; exit 1; fi',\n  'echo OK',\n].join(' && '),\n```\n\n#### 3. Keep final verification boring and deterministic\n\n```bash\ngrep -Eq \"foo|bar|baz\" file.ts\n```\n\n#### 6. Be explicit about shell requirements\n\n```bash\n/opt/homebrew/bin/bash workflows/your-workflow/execute.sh --wave 2\n```\n\n#### 9. Factor repo-specific setup into a shared helper\n\n```ts\n// workflows/lib/cloud-repo-setup.ts\nexport interface CloudRepoSetupOptions {\n  branch: string;\n  committerName?: string;\n  extraSetupCommands?: string[];\n  skipWorkspaceBuild?: boolean;\n}\n\nexport function applyCloudRepoSetup<T>(wf: T, opts: CloudRepoSetupOptions): T {\n  // adds two steps: setup-branch, install-deps\n  // install-deps runs: npm install + workspace prebuilds (build:platform, build:core, etc.)\n  // ...\n}\n```\n\n\n### End-to-End Bug Fix Workflows\n\n- **Capture the original failure**\n- Reproduce the bug first in a deterministic or evidence-capturing step\n- Save exact commands, logs, status codes, or screenshots/artifacts\n- **State the acceptance contract**\n- Define the exact end-to-end success criteria before implementation\n- Include the real entrypoint a user would run\n- **Implement the fix**\n- **Rebuild / reinstall from scratch**\n- Do not trust dirty local state\n- Prefer a clean environment when install/bootstrap behavior is involved\n- **Run targeted regression checks**\n- Unit/integration tests are helpful but not sufficient by themselves\n- **Run a full end-to-end validation**\n- Use the real CLI / API / install path\n- Prefer a clean environment (Docker, sandbox, cloud workspace, Daytona, etc.) for install/runtime issues\n- **Compare before vs after evidence**\n- Show that the original failure no longer occurs\n- **Record residual risks**\n- Call out what was not covered\n- disposable sandbox / cloud workspace\n- Docker / containerized environment\n- fresh local shell with isolated paths\n- compares candidate validation environments\n- defines the acceptance contract\n- chooses the best swarm pattern\n- then authors the final fix/validation workflow\n\n### Key Concepts\n\n#### Verification Gates\n\n```typescript\nverification: { type: 'exit_code' }                        // preferred for code-editing steps\nverification: { type: 'output_contains', value: 'DONE' }   // optional accelerator\nverification: { type: 'file_exists', value: 'src/out.ts' } // deterministic file check\n```\n\n#### DAG Dependencies\n\n```typescript\n.step('fix-types',  { agent: 'worker', dependsOn: ['review'], ... })\n.step('fix-tests',  { agent: 'worker', dependsOn: ['review'], ... })\n.step('final',      { agent: 'lead',   dependsOn: ['fix-types', 'fix-tests'], ... })\n```\n\n#### SDK API\n\n```typescript\n// Subscribe an agent to additional channels post-spawn\nrelay.subscribe({ agent: 'security-auditor', channels: ['review-pr-456'] });\n\n// Unsubscribe — agent leaves the channel entirely\nrelay.unsubscribe({ agent: 'security-auditor', channels: ['general'] });\n\n// Mute — agent stays subscribed (history access) but messages are NOT injected into PTY\nrelay.mute({ agent: 'security-auditor', channel: 'review-pr-123' });\n\n// Unmute — resume PTY injection\nrelay.unmute({ agent: 'security-auditor', channel: 'review-pr-123' });\n```\n\n#### Events\n\n```typescript\nrelay.onChannelSubscribed = (agent, channels) => { /* ... */ };\nrelay.onChannelUnsubscribed = (agent, channels) => { /* ... */ };\nrelay.onChannelMuted = (agent, channel) => { /* ... */ };\nrelay.onChannelUnmuted = (agent, channel) => { /* ... */ };\n```\n\n\n### Agent Definition\n\n#### ```typescript\n\n```typescript\n.agent('name', {\n  cli: 'claude' | 'codex' | 'gemini' | 'aider' | 'goose' | 'opencode' | 'droid',\n  role?: string,\n  preset?: 'lead' | 'worker' | 'reviewer' | 'analyst',\n  retries?: number,\n  model?: string,\n  interactive?: boolean, // default: true\n})\n```\n\n#### Model Constants\n\n```typescript\nimport { ClaudeModels, CodexModels, GeminiModels } from '@agent-relay/config';\n\n.agent('planner', { cli: 'claude', model: ClaudeModels.OPUS })    // not 'opus'\n.agent('worker',  { cli: 'claude', model: ClaudeModels.SONNET })  // not 'sonnet'\n.agent('coder',   { cli: 'codex',  model: CodexModels.GPT_5_4 })  // not 'gpt-5.4'\n```\n\n\n### Step Definition\n\n#### Agent Steps\n\n```typescript\n.step('name', {\n  agent: string,\n  task: string,                   // supports {{var}} and {{steps.plan.output}}\n  dependsOn?: string[],\n  verification?: VerificationCheck,\n  retries?: number,\n})\n```\n\n#### Deterministic Steps (Shell Commands)\n\n```typescript\n.step('verify-files', {\n  type: 'deterministic',\n  command: 'test -f src/auth.ts && echo \"FILE_EXISTS\"',\n  dependsOn: ['implement'],\n  captureOutput: true,\n  failOnError: true,\n})\n```\n\n\n### Common Patterns\n\n#### Interactive Team (lead + workers on shared channel)\n\n```typescript\n.agent('lead', {\n  cli: 'claude',\n  model: ClaudeModels.OPUS,\n  role: 'Architect and reviewer — assigns work, reviews, posts feedback',\n  retries: 1,\n  // No preset — interactive by default\n})\n\n.agent('impl-new', {\n  cli: 'codex',\n  model: CodexModels.O3,\n  role: 'Creates new files. Listens on channel for assignments and feedback.',\n  retries: 2,\n  // No preset — interactive, receives channel messages\n})\n\n.agent('impl-modify', {\n  cli: 'codex',\n  model: CodexModels.O3,\n  role: 'Edits existing files. Listens on channel for assignments and feedback.',\n  retries: 2,\n})\n\n// All three share the same dependsOn — they start concurrently (no deadlock)\n.step('lead-coordinate', {\n  agent: 'lead',\n  dependsOn: ['context'],\n  task: `You are the lead on #channel. Workers: impl-new, impl-modify.\nPost the plan. Assign files. Review their work. Post feedback if needed.\nWorkers iterate based on your feedback. Exit when all files are correct.`,\n})\n.step('impl-new-work', {\n  agent: 'impl-new',\n  dependsOn: ['context'],   // same dep as lead = parallel start\n  task: `You are impl-new on #channel. Wait for the lead's plan.\nCreate files as assigned. Report completion. Fix issues from feedback.`,\n})\n.step('impl-modify-work', {\n  agent: 'impl-modify',\n  dependsOn: ['context'],   // same dep as lead = parallel start\n  task: `You are impl-modify on #channel. Wait for the lead's plan.\nEdit files as assigned. Report completion. Fix issues from feedback.`,\n})\n// Downstream gates on lead (lead exits when satisfied)\n.step('verify', { type: 'deterministic', dependsOn: ['lead-coordinate'], ... })\n```\n\n#### Pipeline (sequential handoff)\n\n```typescript\n.pattern('pipeline')\n.step('analyze', { agent: 'analyst', task: '...' })\n.step('implement', { agent: 'dev', task: '{{steps.analyze.output}}', dependsOn: ['analyze'] })\n.step('test', { agent: 'tester', task: '{{steps.implement.output}}', dependsOn: ['implement'] })\n```\n\n#### Error Handling\n\n```typescript\n.onError('fail-fast')   // stop on first failure (default)\n.onError('continue')    // skip failed branches, continue others\n.onError('retry', { maxRetries: 3, retryDelayMs: 5000 })\n```\n\n\n### Multi-File Edit Pattern\n\n#### When a workflow needs to modify multiple existing files, **use one agent step per file** with a deterministic verify gate after each. Agents reliably edit 1-2 files per step but fail on 4+.\n\n```yaml\nsteps:\n  - name: read-types\n    type: deterministic\n    command: cat src/types.ts\n    captureOutput: true\n\n  - name: edit-types\n    agent: dev\n    dependsOn: [read-types]\n    task: |\n      Edit src/types.ts. Current contents:\n      {{steps.read-target.output}}\n      Add 'pending' to the Status union type.\n      Only edit this one file.\n    verification:\n      type: exit_code\n\n  - name: verify-types\n    type: deterministic\n    dependsOn: [edit-types]\n    command: 'if git diff --quiet src/types.ts; then echo \"NOT MODIFIED\"; exit 1; fi; echo \"OK\"'\n    failOnError: true\n\n  - name: read-service\n    type: deterministic\n    dependsOn: [verify-types]\n    command: cat src/service.ts\n    captureOutput: true\n\n  - name: edit-service\n    agent: dev\n    dependsOn: [read-service]\n    task: |\n      Edit src/service.ts. Current contents:\n      {{steps.read-target.output}}\n      Add a handlePending() method.\n      Only edit this one file.\n    verification:\n      type: exit_code\n\n  - name: verify-service\n    type: deterministic\n    dependsOn: [edit-service]\n    command: 'if git diff --quiet src/service.ts; then echo \"NOT MODIFIED\"; exit 1; fi; echo \"OK\"'\n    failOnError: true\n\n  # Deterministic commit — never rely on agents to commit\n  - name: commit\n    type: deterministic\n    dependsOn: [verify-service]\n    command: git add src/types.ts src/service.ts && git commit -m \"feat: add pending status\"\n    failOnError: true\n```\n\n\n### File Materialization: Verify Before Proceeding\n\n#### After any step that creates files, add a deterministic `file_exists` check before proceeding. Non-interactive agents may exit 0 without writing anything (wrong cwd, stdout instead of disk).\n\n```yaml\n- name: verify-files\n  type: deterministic\n  dependsOn: [impl-auth, impl-storage]\n  command: |\n    missing=0\n    for f in src/auth/credentials.ts src/storage/client.ts; do\n      if [ ! -f \"$f\" ]; then echo \"MISSING: $f\"; missing=$((missing+1)); fi\n    done\n    if [ $missing -gt 0 ]; then echo \"$missing files missing\"; exit 1; fi\n    echo \"All files present\"\n  failOnError: true\n```\n\n\n### DAG Deadlock Anti-Pattern\n\n#### ```yaml\n\n```yaml\n# WRONG — deadlock: coordinate depends on context, work-a depends on coordinate\nsteps:\n  - name: coordinate\n    dependsOn: [context]    # lead waits for WORKER_DONE...\n  - name: work-a\n    dependsOn: [coordinate] # ...but work-a can't start until coordinate finishes\n\n# RIGHT — workers and lead start in parallel\nsteps:\n  - name: context\n    type: deterministic\n  - name: work-a\n    dependsOn: [context]    # starts with lead\n  - name: coordinate\n    dependsOn: [context]    # starts with workers\n  - name: merge\n    dependsOn: [work-a, coordinate]\n```\n\n\n### Step Sizing\n\n#### **One agent, one deliverable.** A step's task prompt should be 10-20 lines max.\n\n```yaml\n# Team pattern: lead + workers on a shared channel\nsteps:\n  - name: track-lead-coord\n    agent: track-lead\n    dependsOn: [prior-step]\n    task: |\n      Lead the track on #my-track. Workers: track-worker-1, track-worker-2.\n      Post assignments to the channel. Review worker output.\n\n  - name: track-worker-1-impl\n    agent: track-worker-1\n    dependsOn: [prior-step]  # same dep as lead — starts concurrently\n    task: |\n      Join #my-track. track-lead will post your assignment.\n      Implement the file as directed.\n    verification:\n      type: exit_code\n\n  - name: next-step\n    dependsOn: [track-lead-coord]  # downstream depends on lead, not workers\n```\n\n\n### Supervisor Pattern\n\nWhen you set `.pattern('supervisor')` (or `hub-spoke`, `fan-out`), the runner auto-assigns a supervisor agent as owner for worker steps. The supervisor monitors progress, nudges idle workers, and issues `OWNER_DECISION`.\n\n**Auto-hardening only activates for hub patterns** — not `pipeline` or `dag`.\n\n| Use case | Pattern | Why |\n|----------|---------|-----|\n| Sequential, no monitoring | `pipeline` | Simple, no overhead |\n| Workers need oversight | `supervisor` | Auto-owner monitors |\n| Local/small models | `supervisor` | Supervisor catches stuck workers |\n| All non-interactive | `pipeline` or `dag` | No PTY = no supervision needed |\n\n### Concurrency\n\n**Cap `maxConcurrency` at 4-6.** Spawning 10+ agents simultaneously causes broker timeouts.\n\n| Parallel agents | `maxConcurrency` |\n|-----------------|-------------------|\n| 2-4             | 4 (default safe)  |\n| 5-10            | 5                 |\n| 10+             | 6-8 max           |\n\n### Common Mistakes\n\n| Mistake | Fix |\n|---------|-----|\n| All workflows run sequentially | Group independent workflows into parallel waves (4-7x speedup) |\n| Every step depends on the previous one | Only add `dependsOn` when there's a real data dependency |\n| Self-review step with no timeout | Set `timeout: 300_000` (5 min) — Codex hangs in non-interactive review |\n| One giant workflow per feature | Split into smaller workflows that can run in parallel waves |\n| Adding exit instructions to tasks | Runner handles self-termination automatically |\n| Setting `timeoutMs` on agents/steps | Use global `.timeout()` only |\n| Using `general` channel | Set `.channel('wf-name')` for isolation |\n| `{{steps.plan.output}}` without `dependsOn: ['X']` | Output won't be available yet |\n| Requiring exact sentinel as only completion gate | Use `exit_code` or `file_exists` verification |\n| Writing 100-line task prompts | Split into lead + workers on a channel |\n| `maxConcurrency: 16` with many parallel steps | Cap at 5-6 |\n| Non-interactive agent reading large files via tools | Pre-read in deterministic step, inject via `{{steps.plan.output}}` |\n| Workers depending on lead step (deadlock) | Both depend on shared context step |\n| `fan-out`/`hub-spoke` for simple parallel workers | Use `dag` instead |\n| `pipeline` but expecting auto-supervisor | Only hub patterns auto-harden. Use `.pattern('supervisor')` |\n| Workers without `preset: 'worker'` in one-shot DAG lead+worker flows | Add preset for clean stdout when chaining `{{steps.plan.output}}` (not needed for interactive team patterns) |\n| Using `_` in YAML numbers (`timeoutMs: 1_200_000`) | YAML doesn't support `_` separators |\n| Workflow timeout under 30 min for complex workflows | Use `3600000` (1 hour) as default |\n| Using `require()` in ESM projects | Check `package.json` for `\"type\": \"module\"` — use `import` if ESM |\n| Wrapping in `async function main()` in ESM | ESM supports top-level `await` — no wrapper needed |\n| Using `createWorkflowRenderer` | Does not exist. Use `.run({ cwd: process.cwd() })` |\n| `export default workflow(...)...build()` | No `.build()`. Chain ends with `.run()` — the file must call `.run()`, not just export config |\n| Relative import `'../workflows/builder.js'` | Use `import { workflow } from '@relayflows/core'` |\n| Hardcoded model strings (`model: 'opus'`) | Use constants: `import { ClaudeModels } from '@agent-relay/config'` → `model: ClaudeModels.OPUS` |\n| Thinking `agent-relay run` inspects exports | It executes the file as a subprocess. Only `.run()` invocations trigger steps |\n| `pattern('single')` on cloud runner | Not supported — use `dag` |\n| `pattern('supervisor')` with one agent | Same agent is owner + specialist. Use `dag` |\n| Invalid verification type (`type: 'deterministic'`) | Only `exit_code`, `output_contains`, `file_exists`, `custom` are valid |\n| Chaining `{{steps.plan.output}}` from interactive agents | PTY output is garbled. Use deterministic steps or `preset: 'worker'` |\n| Single step editing 4+ files | Agents modify 1-2 then exit. Split to one file per step with verify gates |\n| Relying on agents to `git commit` | Agents emit markers without running git. Use deterministic commit step |\n| File-writing steps without `file_exists` verification | `exit_code` auto-passes even if no file written |\n| Manual peer fanout in `handleChannelMessage()` | Use broker-managed channel subscriptions — broker fans out to all subscribers automatically |\n| Client-side `personaNames.has(from)` filtering | Use `relay.subscribe()`/`relay.unsubscribe()` — only subscribed agents receive messages |\n| Agents receiving noisy cross-channel messages during focused work | Use `relay.mute({ agent, channel })` to silence non-primary channels without leaving them |\n| Hardcoding all channels at spawn time | Use `agent.subscribe()` / `agent.unsubscribe()` for dynamic channel membership post-spawn |\n| Using `preset: 'worker'` for Codex in *interactive team* patterns when coordination is needed | Codex interactive mode works fine with PTY channel injection. Drop the preset for interactive team patterns (keep it for one-shot DAG workers where clean stdout matters) |\n| Separate reviewer agent from lead in interactive team | Merge lead + reviewer into one interactive Claude agent — reviews between rounds, fewer agents |\n| Not printing PR URL after `gh pr create` | Add a final deterministic step: `echo \"PR: $(cat pr-url.txt)\"` or capture in the `gh pr create` command |\n| Workflow ending without worktree + PR for cross-repo changes | Add `setup-worktree` at start and `push-and-pr` + `cleanup-worktree` at end |\n\n### YAML Alternative\n\n#### ```yaml\n\n```yaml\nversion: '1.0'\nname: my-workflow\nswarm:\n  pattern: dag\n  channel: wf-my-workflow\nagents:\n  - name: lead\n    cli: claude\n    role: Architect\n  - name: worker\n    cli: codex\n    role: Implementer\nworkflows:\n  - name: default\n    steps:\n      - name: plan\n        agent: lead\n        task: 'Produce a detailed implementation plan.'\n      - name: implement\n        agent: worker\n        task: 'Implement: {{steps.plan.output}}'\n        dependsOn: [plan]\n        verification:\n          type: exit_code\n```\n\n\n### Available Swarm Patterns\n\n`dag` (default), `fan-out`, `pipeline`, `hub-spoke`, `consensus`, `mesh`, `handoff`, `cascade`, `debate`, `hierarchical`, `map-reduce`, `scatter-gather`, `supervisor`, `reflection`, `red-team`, `verifier`, `auction`, `escalation`, `saga`, `circuit-breaker`, `blackboard`, `swarm`\n\nSee skill `choosing-swarm-patterns` for pattern selection guidance.\n\n\n# shadcn\nreason=Spec text mentions \"adding\". Spec text mentions \"working\". Spec text mentions \"switch\".\n---\nname: shadcn\ndescription: Manages shadcn components and projects — adding, searching, fixing, debugging, styling, and composing UI. Provides project context, component docs, and usage examples. Applies when working with shadcn/ui, component registries, presets, --preset codes, or any project with a components.json file. Also triggers for \"shadcn init\", \"create an app with --preset\", or \"switch to --preset\".\nuser-invocable: false\nallowed-tools: Bash(npx shadcn@latest *), Bash(pnpm dlx shadcn@latest *), Bash(bunx --bun shadcn@latest *)\n---\n\n# shadcn/ui\n\nA framework for building ui, components and design systems. Components are added as source code to the user's project via the CLI.\n\n> **IMPORTANT:** Run all CLI commands using the project's package runner: `npx shadcn@latest`, `pnpm dlx shadcn@latest`, or `bunx --bun shadcn@latest` — based on the project's `packageManager`. Examples below use `npx shadcn@latest` but substitute the correct runner for the project.\n\n## Current Project Context\n\n```json\n!`npx shadcn@latest info --json`\n```\n\nThe JSON above contains the project config and installed components. Use `npx shadcn@latest docs <component>` to get documentation and example URLs for any component.\n\n## Principles\n\n1. **Use existing components first.** Use `npx shadcn@latest search` to check registries before writing custom UI. Check community registries too.\n2. **Compose, don't reinvent.** Settings page = Tabs + Card + form controls. Dashboard = Sidebar + Card + Chart + Table.\n3. **Use built-in variants before custom styles.** `variant=\"outline\"`, `size=\"sm\"`, etc.\n4. **Use semantic colors.** `bg-primary`, `text-muted-foreground` — never raw values like `bg-blue-500`.\n\n## Critical Rules\n\nThese rules are **always enforced**. Each links to a file with Incorrect/Correct code pairs.\n\n### Styling & Tailwind → [styling.md](./rules/styling.md)\n\n- **`className` for layout, not styling.** Never override component colors or typography.\n- **No `space-x-*` or `space-y-*`.** Use `flex` with `gap-*`. For vertical stacks, `flex flex-col gap-*`.\n- **Use `size-*` when width and height are equal.** `size-10` not `w-10 h-10`.\n- **Use `truncate` shorthand.** Not `overflow-hidden text-ellipsis whitespace-nowrap`.\n- **No manual `dark:` color overrides.** Use semantic tokens (`bg-background`, `text-muted-foreground`).\n- **Use `cn()` for conditional classes.** Don't write manual template literal ternaries.\n- **No manual `z-index` on overlay components.** Dialog, Sheet, Popover, etc. handle their own stacking.\n\n### Forms & Inputs → [forms.md](./rules/forms.md)\n\n- **Forms use `FieldGroup` + `Field`.** Never use raw `div` with `space-y-*` or `grid gap-*` for form layout.\n- **`InputGroup` uses `InputGroupInput`/`InputGroupTextarea`.** Never raw `Input`/`Textarea` inside `InputGroup`.\n- **Buttons inside inputs use `InputGroup` + `InputGroupAddon`.**\n- **Option sets (2–7 choices) use `ToggleGroup`.** Don't loop `Button` with manual active state.\n- **`FieldSet` + `FieldLegend` for grouping related checkboxes/radios.** Don't use a `div` with a heading.\n- **Field validation uses `data-invalid` + `aria-invalid`.** `data-invalid` on `Field`, `aria-invalid` on the control. For disabled: `data-disabled` on `Field`, `disabled` on the control.\n\n### Component Structure → [composition.md](./rules/composition.md)\n\n- **Items always inside their Group.** `SelectItem` → `SelectGroup`. `DropdownMenuItem` → `DropdownMenuGroup`. `CommandItem` → `CommandGroup`.\n- **Use `asChild` (radix) or `render` (base) for custom triggers.** Check `base` field from `npx shadcn@latest info`. → [base-vs-radix.md](./rules/base-vs-radix.md)\n- **Dialog, Sheet, and Drawer always need a Title.** `DialogTitle`, `SheetTitle`, `DrawerTitle` required for accessibility. Use `className=\"sr-only\"` if visually hidden.\n- **Use full Card composition.** `CardHeader`/`CardTitle`/`CardDescription`/`CardContent`/`CardFooter`. Don't dump everything in `CardContent`.\n- **Button has no `isPending`/`isLoading`.** Compose with `Spinner` + `data-icon` + `disabled`.\n- **`TabsTrigger` must be inside `TabsList`.** Never render triggers directly in `Tabs`.\n- **`Avatar` always needs `AvatarFallback`.** For when the image fails to load.\n\n### Use Components, Not Custom Markup → [composition.md](./rules/composition.md)\n\n- **Use existing components before custom markup.** Check if a component exists before writing a styled `div`.\n- **Callouts use `Alert`.** Don't build custom styled divs.\n- **Empty states use `Empty`.** Don't build custom empty state markup.\n- **Toast via `sonner`.** Use `toast()` from `sonner`.\n- **Use `Separator`** instead of `<hr>` or `<div className=\"border-t\">`.\n- **Use `Skeleton`** for loading placeholders. No custom `animate-pulse` divs.\n- **Use `Badge`** instead of custom styled spans.\n\n### Icons → [icons.md](./rules/icons.md)\n\n- **Icons in `Button` use `data-icon`.** `data-icon=\"inline-start\"` or `data-icon=\"inline-end\"` on the icon.\n- **No sizing classes on icons inside components.** Components handle icon sizing via CSS. No `size-4` or `w-4 h-4`.\n- **Pass icons as objects, not string keys.** `icon={CheckIcon}`, not a string lookup.\n\n### CLI\n\n- **Never decode or fetch preset codes manually.** Pass them directly to `npx shadcn@latest apply --preset <code>` for existing projects, or `npx shadcn@latest init --preset <code>` when initializing.\n\n## Key Patterns\n\nThese are the most common patterns that differentiate correct shadcn/ui code. For edge cases, see the linked rule files above.\n\n```tsx\n// Form layout: FieldGroup + Field, not div + Label.\n<FieldGroup>\n  <Field>\n    <FieldLabel htmlFor=\"email\">Email</FieldLabel>\n    <Input id=\"email\" />\n  </Field>\n</FieldGroup>\n\n// Validation: data-invalid on Field, aria-invalid on the control.\n<Field data-invalid>\n  <FieldLabel>Email</FieldLabel>\n  <Input aria-invalid />\n  <FieldDescription>Invalid email.</FieldDescription>\n</Field>\n\n// Icons in buttons: data-icon, no sizing classes.\n<Button>\n  <SearchIcon data-icon=\"inline-start\" />\n  Search\n</Button>\n\n// Spacing: gap-*, not space-y-*.\n<div className=\"flex flex-col gap-4\">  // correct\n<div className=\"space-y-4\">           // wrong\n\n// Equal dimensions: size-*, not w-* h-*.\n<Avatar className=\"size-10\">   // correct\n<Avatar className=\"w-10 h-10\"> // wrong\n\n// Status colors: Badge variants or semantic tokens, not raw colors.\n<Badge variant=\"secondary\">+20.1%</Badge>    // correct\n<span className=\"text-emerald-600\">+20.1%</span> // wrong\n```\n\n## Component Selection\n\n| Need                       | Use                                                                                                 |\n| -------------------------- | --------------------------------------------------------------------------------------------------- |\n| Button/action              | `Button` with appropriate variant                                                                   |\n| Form inputs                | `Input`, `Select`, `Combobox`, `Switch`, `Checkbox`, `RadioGroup`, `Textarea`, `InputOTP`, `Slider` |\n| Toggle between 2–5 options | `ToggleGroup` + `ToggleGroupItem`                                                                   |\n| Data display               | `Table`, `Card`, `Badge`, `Avatar`                                                                  |\n| Navigation                 | `Sidebar`, `NavigationMenu`, `Breadcrumb`, `Tabs`, `Pagination`                                     |\n| Overlays                   | `Dialog` (modal), `Sheet` (side panel), `Drawer` (bottom sheet), `AlertDialog` (confirmation)       |\n| Feedback                   | `sonner` (toast), `Alert`, `Progress`, `Skeleton`, `Spinner`                                        |\n| Command palette            | `Command` inside `Dialog`                                                                           |\n| Charts                     | `Chart` (wraps Recharts)                                                                            |\n| Layout                     | `Card`, `Separator`, `Resizable`, `ScrollArea`, `Accordion`, `Collapsible`                          |\n| Empty states               | `Empty`                                                                                             |\n| Menus                      | `DropdownMenu`, `ContextMenu`, `Menubar`                                                            |\n| Tooltips/info              | `Tooltip`, `HoverCard`, `Popover`                                                                   |\n\n## Key Fields\n\nThe injected project context contains these key fields:\n\n- **`aliases`** → use the actual alias prefix for imports (e.g. `@/`, `~/`), never hardcode.\n- **`isRSC`** → when `true`, components using `useState`, `useEffect`, event handlers, or browser APIs need `\"use client\"` at the top of the file. Always reference this field when advising on the directive.\n- **`tailwindVersion`** → `\"v4\"` uses `@theme inline` blocks; `\"v3\"` uses `tailwind.config.js`.\n- **`tailwindCssFile`** → the global CSS file where custom CSS variables are defined. Always edit this file, never create a new one.\n- **`style`** → component visual treatment (e.g. `nova`, `vega`).\n- **`base`** → primitive library (`radix` or `base`). Affects component APIs and available props.\n- **`iconLibrary`** → determines icon imports. Use `lucide-react` for `lucide`, `@tabler/icons-react` for `tabler`, etc. Never assume `lucide-react`.\n- **`resolvedPaths`** → exact file-system destinations for components, utils, hooks, etc.\n- **`framework`** → routing and file conventions (e.g. Next.js App Router vs Vite SPA).\n- **`packageManager`** → use this for any non-shadcn dependency installs (e.g. `pnpm add date-fns` vs `npm install date-fns`).\n\nSee [cli.md — `info` command](./cli.md) for the full field reference.\n\n## Component Docs, Examples, and Usage\n\nRun `npx shadcn@latest docs <component>` to get the URLs for a component's documentation, examples, and API reference. Fetch these URLs to get the actual content.\n\n```bash\nnpx shadcn@latest docs button dialog select\n```\n\n**When creating, fixing, debugging, or using a component, always run `npx shadcn@latest docs` and fetch the URLs first.** This ensures you're working with the correct API and usage patterns rather than guessing.\n\n## Workflow\n\n1. **Get project context** — already injected above. Run `npx shadcn@latest info` again if you need to refresh.\n2. **Check installed components first** — before running `add`, always check the `components` list from project context or list the `resolvedPaths.ui` directory. Don't import components that haven't been added, and don't re-add ones already installed.\n3. **Find components** — `npx shadcn@latest search`.\n4. **Get docs and examples** — run `npx shadcn@latest docs <component>` to get URLs, then fetch them. Use `npx shadcn@latest view` to browse registry items you haven't installed. To preview changes to installed components, use `npx shadcn@latest add --diff`.\n5. **Install or update** — `npx shadcn@latest add`. When updating existing components, use `--dry-run` and `--diff` to preview changes first (see [Updating Components](#updating-components) below).\n6. **Fix imports in third-party components** — After adding components from community registries (e.g. `@bundui`, `@magicui`), check the added non-UI files for hardcoded import paths like `@/components/ui/...`. These won't match the project's actual aliases. Use `npx shadcn@latest info` to get the correct `ui` alias (e.g. `@workspace/ui/components`) and rewrite the imports accordingly. The CLI rewrites imports for its own UI files, but third-party registry components may use default paths that don't match the project.\n7. **Review added components** — After adding a component or block from any registry, **always read the added files and verify they are correct**. Check for missing sub-components (e.g. `SelectItem` without `SelectGroup`), missing imports, incorrect composition, or violations of the [Critical Rules](#critical-rules). Also replace any icon imports with the project's `iconLibrary` from the project context (e.g. if the registry item uses `lucide-react` but the project uses `hugeicons`, swap the imports and icon names accordingly). Fix all issues before moving on.\n8. **Registry must be explicit** — When the user asks to add a block or component, **do not guess the registry**. If no registry is specified (e.g. user says \"add a login block\" without specifying `@shadcn`, `@tailark`, etc.), ask which registry to use. Never default to a registry on behalf of the user.\n9. **Switching presets** — Ask the user first: **overwrite**, **merge**, or **skip**?\n   - **Overwrite**: `npx shadcn@latest apply --preset <code>`. Overwrites detected components, fonts, and CSS variables.\n   - **Merge**: `npx shadcn@latest init --preset <code> --force --no-reinstall`, then run `npx shadcn@latest info` to list installed components, then for each installed component use `--dry-run` and `--diff` to [smart merge](#updating-components) it individually.\n   - **Skip**: `npx shadcn@latest init --preset <code> --force --no-reinstall`. Only updates config and CSS, leaves components as-is.\n   - **Important**: Always run preset commands inside the user's project directory. `apply` only works in an existing project with a `components.json` file. The CLI automatically preserves the current base (`base` vs `radix`) from `components.json`. If you must use a scratch/temp directory (e.g. for `--dry-run` comparisons), pass `--base <current-base>` explicitly — preset codes do not encode the base.\n\n## Updating Components\n\nWhen the user asks to update a component from upstream while keeping their local changes, use `--dry-run` and `--diff` to intelligently merge. **NEVER fetch raw files from GitHub manually — always use the CLI.**\n\n1. Run `npx shadcn@latest add <component> --dry-run` to see all files that would be affected.\n2. For each file, run `npx shadcn@latest add <component> --diff <file>` to see what changed upstream vs local.\n3. Decide per file based on the diff:\n   - No local changes → safe to overwrite.\n   - Has local changes → read the local file, analyze the diff, and apply upstream updates while preserving local modifications.\n   - User says \"just update everything\" → use `--overwrite`, but confirm first.\n4. **Never use `--overwrite` without the user's explicit approval.**\n\n## Quick Reference\n\n```bash\n# Create a new project.\nnpx shadcn@latest init --name my-app --preset base-nova\nnpx shadcn@latest init --name my-app --preset a2r6bw --template vite\n\n# Create a monorepo project.\nnpx shadcn@latest init --name my-app --preset base-nova --monorepo\nnpx shadcn@latest init --name my-app --preset base-nova --template next --monorepo\n\n# Initialize existing project.\nnpx shadcn@latest init --preset base-nova\nnpx shadcn@latest init --defaults  # shortcut: --template=next --preset=nova (base style implied)\n\n# Apply a preset to an existing project.\nnpx shadcn@latest apply --preset a2r6bw\nnpx shadcn@latest apply a2r6bw\n\n# Add components.\nnpx shadcn@latest add button card dialog\nnpx shadcn@latest add @magicui/shimmer-button\nnpx shadcn@latest add --all\n\n# Preview changes before adding/updating.\nnpx shadcn@latest add button --dry-run\nnpx shadcn@latest add button --diff button.tsx\nnpx shadcn@latest add @acme/form --view button.tsx\n\n# Search registries.\nnpx shadcn@latest search @shadcn -q \"sidebar\"\nnpx shadcn@latest search @tailark -q \"stats\"\n\n# Get component docs and example URLs.\nnpx shadcn@latest docs button dialog select\n\n# View registry item details (for items not yet installed).\nnpx shadcn@latest view @shadcn/button\n```\n\n**Named presets:** `nova`, `vega`, `maia`, `lyra`, `mira`, `luma`\n**Templates:** `next`, `vite`, `start`, `react-router`, `astro` (all support `--monorepo`) and `laravel` (not supported for monorepo)\n**Preset codes:** Version-prefixed base62 strings (e.g. `a2r6bw` or `b0`), from [ui.shadcn.com](https://ui.shadcn.com).\n\n## Detailed References\n\n- [rules/forms.md](./rules/forms.md) — FieldGroup, Field, InputGroup, ToggleGroup, FieldSet, validation states\n- [rules/composition.md](./rules/composition.md) — Groups, overlays, Card, Tabs, Avatar, Alert, Empty, Toast, Separator, Skeleton, Badge, Button loading\n- [rules/icons.md](./rules/icons.md) — data-icon, icon sizing, passing icons as objects\n- [rules/styling.md](./rules/styling.md) — Semantic colors, variants, className, spacing, size, truncate, dark mode, cn(), z-index\n- [rules/base-vs-radix.md](./rules/base-vs-radix.md) — asChild vs render, Select, ToggleGroup, Slider, Accordion\n- [cli.md](./cli.md) — Commands, flags, presets, templates\n- [customization.md](./customization.md) — Theming, CSS variables, extending components\n\n\n# choosing-swarm-patterns\nreason=Spec text mentions \"agents\". Spec text mentions \"pattern\".\n---\nname: choosing-swarm-patterns\ndescription: Use when coordinating multiple AI agents and need to pick the right orchestration pattern - covers 10 patterns (fan-out, pipeline, hub-spoke, consensus, mesh, handoff, cascade, dag, debate, hierarchical) with decision framework and reflection protocol\n---\n\n### Overview\n\n10 orchestration patterns for multi-agent workflows. Pick the simplest pattern that solves the problem — add complexity only when the system proves it's insufficient.\n\n### Quick Decision Framework\n\n#### ```\n\n```\nIs the task independent per agent?\n  YES → fan-out (parallel workers)\n\nDoes each step need the previous step's output?\n  YES → Is it strictly linear?\n    YES → pipeline\n    NO  → dag (parallel where possible)\n\nDoes a coordinator need to stay alive and adapt?\n  YES → Is there one level of management?\n    YES → hub-spoke\n    NO  → hierarchical (multi-level)\n\nIs the task about making a decision?\n  YES → Do agents need to argue opposing sides?\n    YES → debate (adversarial)\n    NO  → consensus (cooperative voting)\n\nDoes the right specialist emerge during processing?\n  YES → handoff (dynamic routing)\n\nDo all agents need to freely collaborate?\n  YES → mesh (peer-to-peer)\n\nIs cost the primary concern?\n  YES → cascade (cheap model first, escalate if needed)\n```\n\n\n### Pattern Reference\n\n| # | Pattern | Topology | Agents | Best For |\n|---|---------|----------|--------|----------|\n| 1 | **fan-out** | Star (SDK center) | N parallel | Independent subtasks (reviews, research, tests) |\n| 2 | **pipeline** | Linear chain | Sequential | Ordered stages (design → implement → test) |\n| 3 | **hub-spoke** | Star (live hub) | 1 lead + N workers | Dynamic coordination, lead reviews/adjusts |\n| 4 | **consensus** | Broadcast + vote | N voters | Architecture decisions, approval gates |\n| 5 | **mesh** | Fully connected | N peers | Brainstorming, collaborative debugging |\n| 6 | **handoff** | Routing chain | 1 active at a time | Triage, specialist routing, support flows |\n| 7 | **cascade** | Tiered escalation | Cheapest → most capable | Cost optimization, production workloads |\n| 8 | **dag** | Dependency graph | Parallel + joins | Complex projects with mixed dependencies |\n| 9 | **debate** | Adversarial rounds | 2+ debaters + judge | Rigorous evaluation, architecture trade-offs |\n| 10 | **hierarchical** | Tree (multi-level) | Lead → coordinators → workers | Large teams, domain separation |\n\n### Pattern Details\n\n#### 1. fan-out — Parallel Workers\n\n```ts\nfanOut([\n  { task: \"Review auth.ts\", name: \"AuthReviewer\" },\n  { task: \"Review db.ts\", name: \"DbReviewer\" },\n], { cli: \"claude\" });\n```\n\n#### 2. pipeline — Sequential Stages\n\n```ts\npipeline([\n  { task: \"Design the API schema\", name: \"Designer\" },\n  { task: \"Implement the endpoints\", name: \"Implementer\" },\n  { task: \"Write integration tests\", name: \"Tester\" },\n]);\n```\n\n#### 3. hub-spoke — Persistent Coordinator\n\n```ts\nhubAndSpoke({\n  hub: { task: \"Coordinate building a REST API\", name: \"Lead\" },\n  workers: [\n    { task: \"Build database models\", name: \"DbWorker\" },\n    { task: \"Build route handlers\", name: \"ApiWorker\" },\n  ],\n});\n```\n\n#### 4. consensus — Cooperative Voting\n\n```ts\nconsensus({\n  proposal: \"Should we migrate to Fastify?\",\n  voters: [\n    { task: \"Evaluate performance\", name: \"PerfExpert\" },\n    { task: \"Evaluate DX\", name: \"DxExpert\" },\n  ],\n  consensusType: \"majority\",\n});\n```\n\n#### 5. mesh — Peer Collaboration\n\n```ts\nmesh({\n  goal: \"Debug the auth flow returning 500\",\n  agents: [\n    { task: \"Check server logs\", name: \"LogAnalyst\" },\n    { task: \"Review auth code\", name: \"CodeReviewer\" },\n    { task: \"Write repro test\", name: \"Tester\" },\n  ],\n});\n```\n\n#### 6. handoff — Dynamic Routing\n\n```ts\nhandoff({\n  entryPoint: { task: \"Triage the request\", name: \"Triage\" },\n  routes: [\n    { agent: { task: \"Handle billing\", name: \"Billing\" }, condition: \"billing, payment\" },\n    { agent: { task: \"Handle tech issues\", name: \"TechSupport\" }, condition: \"error, bug\" },\n  ],\n  maxHandoffs: 3,\n});\n```\n\n#### 7. cascade — Cost-Aware Escalation\n\n```ts\ncascade({\n  tiers: [\n    { agent: { task: \"Answer this\", cli: \"claude\" }, confidenceThreshold: 0.7, costWeight: 1 },\n    { agent: { task: \"Answer this\", cli: \"claude\" }, confidenceThreshold: 0.85, costWeight: 5 },\n    { agent: { task: \"Answer this\", cli: \"claude\" }, costWeight: 20 },\n  ],\n});\n```\n\n#### 8. dag — Directed Acyclic Graph\n\n```ts\ndag({\n  nodes: [\n    { id: \"scaffold\", task: \"Create project scaffold\" },\n    { id: \"frontend\", task: \"Build React UI\", dependsOn: [\"scaffold\"] },\n    { id: \"backend\", task: \"Build API\", dependsOn: [\"scaffold\"] },\n    { id: \"integrate\", task: \"Wire together\", dependsOn: [\"frontend\", \"backend\"] },\n  ],\n  maxConcurrency: 3,\n});\n```\n\n#### 9. debate — Adversarial Refinement\n\n```ts\ndebate({\n  topic: \"Monorepo vs polyrepo for the new platform?\",\n  debaters: [\n    { task: \"Argue for monorepo\", position: \"monorepo\" },\n    { task: \"Argue for polyrepo\", position: \"polyrepo\" },\n  ],\n  judge: { task: \"Judge and decide\", name: \"ArchJudge\" },\n  maxRounds: 3,\n});\n```\n\n#### 10. hierarchical — Multi-Level Delegation\n\n```ts\nhierarchical({\n  agents: [\n    { id: \"lead\", task: \"Coordinate full-stack app\", role: \"lead\" },\n    { id: \"fe-coord\", task: \"Manage frontend\", role: \"coordinator\", reportsTo: \"lead\" },\n    { id: \"be-coord\", task: \"Manage backend\", role: \"coordinator\", reportsTo: \"lead\" },\n    { id: \"fe-dev\", task: \"Build components\", role: \"worker\", reportsTo: \"fe-coord\" },\n    { id: \"be-dev\", task: \"Build API\", role: \"worker\", reportsTo: \"be-coord\" },\n  ],\n});\n```\n\n\n### Reflection Protocol\n\n#### All patterns support reflection — periodic synthesis that enables course correction. Enabled via `reflectionThreshold` on WorkflowOptions.\n\n```ts\n{\n  reflectionThreshold: 10, // trigger after 10 agent messages\n  onReflect: async (ctx) => {\n    // Examine ctx.recentMessages, ctx.agentStatuses\n    // Return adjustments or null\n  },\n}\n```\n\n\n### Common Mistakes\n\n| Mistake | Why It Fails | Fix |\n|---------|-------------|-----|\n| Using mesh for everything | O(n^2) communication, debugging nightmare | Use hub-spoke for most tasks |\n| Pipeline for independent work | Sequential bottleneck | Use fan-out or dag |\n| Hub-spoke for simple parallel tasks | Hub is unnecessary overhead | Use fan-out |\n| Consensus for non-decisions | Voting on implementation tasks wastes time | Use hub-spoke, let lead decide |\n| No circuit breaker on handoff | Infinite routing loops | Always set maxHandoffs |\n| Cascade without confidence parsing | Agents don't report confidence | Convention injection handles this |\n| Hierarchical for 3 agents | Management overhead exceeds benefit | Use hub-spoke for small teams |\n\n### DAG Executor — Proven Pattern\n\n#### Agent Completion: Detect → Release → Collect\n\n```\nAgent writes summary file → Orchestrator polls (5s) → Detects new mtime →\n  Reads summary → Calls client.release(agent) → agent_exited fires → Node marked complete\n```\n\n#### State & Resume\n\n```ts\nsaveState(completed, depsOutput, results, startTime);\n// Restart with --resume to skip completed nodes\n```\n\n\n### YAML Workflow Definition\n\n#### Any pattern can be defined in YAML for portability:\n\n```yaml\nversion: \"1.0\"\nname: feature-dev\npattern: hub-spoke\nagents:\n  - id: lead\n    role: lead\n    cli: claude\n  - id: developer\n    role: worker\n    cli: codex\n    reportsTo: lead\nsteps:\n  - id: plan\n    agent: lead\n    prompt: \"Create a development plan for: {{task}}\"\n    expects: \"PLAN_COMPLETE\"\n  - id: implement\n    agent: developer\n    dependsOn: [plan]\n    prompt: \"Implement: {{steps.plan.output}}\"\n    expects: \"DONE\"\nreflection:\n  enabled: true\n  threshold: 10\ntrajectory:\n  enabled: true\n```\n" }
  ]);
  const result = await workflow("ricky-ricky-slack-nango-routed-migration-spec-status-d")
    .description("# Ricky Slack Nango-Routed Migration Spec\n\n**Status:** draft, depends on PR #480 (`feat/ricky-linear-agent-surface`) merging to main, since it establishes the same Nango-routed pattern for Linear and the shared `webhook-dedup.ts`.\n**Owner:** Cloud + Ricky.\n**Last updated:** 2026-05-07.\n**Primary repos:** `../cloud` (this repo).\n**Reference manifest:** `integrations/slack/ricky-manifest.json` (current direct config) and `integrations/linear/ricky-manifest.json` (post-#480 Nango pattern, the model for this migration).\n\n## 1. Problem Statement\n\nThe Ricky Slack agent v2 surface (PR #412, merged) was built before the rest of the cloud's webhook intake consolidated onto Nango. Today its dedicated Slack app POSTs directly to three Cloud routes:\n\n- `app/api/v1/ricky/slack/events/route.ts` — `app_mention`, `message.im` (async)\n- `app/api/v1/ricky/slack/commands/route.ts` — `/ricky` slash command (synchronous response expected within 3s)\n- `app/api/v1/ricky/slack/interactivity/route.ts` — gate-approval block_actions and view_submission (synchronous response expected within 3s)\n\nEach route calls `readVerifiedSlackBody` from `lib/ricky/slack/signature.ts`, which verifies a Slack HMAC against `Resource.RickySlackSigningSecret.value` / `Resource.SlackSigningSecret.value` (with env-var fallbacks `RICKY_SLACK_SIGNING_SECRET`, `SLACK_SIGNING_SECRET`).\n\nSage's Slack integration uses the opposite pattern: Slack → Nango → `/api/v1/webhooks/nango` → `routeNangoWebhook` → `routeForwardEvent` → `forwardToSage`. Nango's signature is verified once at the Cloud entry; no Slack-app-specific secret in the cloud repo. PR #480 just established the same pattern for Linear AgentSession events (`handleLinearForward` detects `AgentSessionEvent` payloads and dispatches into `dispatchLinearSessionEvent`).\n\nThis spec migrates the Ricky Slack surface to that pattern so the Slack signing secret, `lib/ricky/slack/signature.ts`, and the dedicated routes can all be removed.\n\n## 2. Goals\n\n- Point the Ricky Slack app's `events`, `commands`, and `interactivity` request URLs at Nango using the same URL pattern Sage already uses (`integrations/slack/manifest.json`): `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack`. Operators substitute the integration id at manifest-upload time.\n- Add a Slack-Ricky branch in `packages/web/lib/integrations/nango-webhook-router.ts` that detects events vs slash command vs interactivity payloads and dispatches into existing handlers in `packages/web/lib/ricky/slack/`.\n- Refactor command + interactivity dispatch to use Slack's deferred response pattern (`response_url` for slash commands and most interactivity; `chat.update` for gate-approval message edits) since Nango forwards webhooks fire-and-forget — Slack only sees Nango's 200, not Cloud's body.\n- Delete `packages/web/app/api/v1/ricky/slack/events/route.ts`, `commands/route.ts`, `interactivity/route.ts`, and `lib/ricky/slack/signature.ts`.\n- Update `integrations/slack/ricky-manifest.json` to reflect Nango-routed delivery (mirror the post-#480 `integrations/linear/ricky-manifest.json` shape; use the Nango webhook URL pattern in goal 1 above).\n- Preserve the `oauth/route.ts` flow for OAuth installation — that's not a webhook and stays as-is.\n\n## 3. Non-Goals\n\n- Sage's Slack integration is unchanged. This spec only touches the Ricky Slack app.\n- The Ricky Slack and Sage Slack apps remain separate. Consolidation into a single shared app is explicitly out of scope.\n- The slash command surface (`/ricky run ...`) keeps the same UX and supported subcommands. Only the transport changes.\n- The gate-approval interactivity behavior keeps the same UX. Only the transport changes.\n- View submissions in the current surface only do `response_action: \"clear\"` (modal close); this spec accepts that the Nango-routed flow can't return that synchronously and the modal will close on Slack's 3s client-side timeout. If a future surface needs `response_action: \"errors\"` (field-level validation), that interaction must remain direct (out of scope).\n- URL verification: Nango is assumed to auto-respond to Slack's `url_verification` challenge for forwarded events URLs (same path Sage's Slack manifest uses today via `integrations/slack/manifest.json`). Cutover is a single Slack-app dashboard URL swap. If a future Nango configuration regression breaks this, treat it as an ops issue, not a spec change.\n- No changes to Slack OAuth install flow (`app/api/v1/ricky/slack/oauth/route.ts`).\n- No changes to the underlying `lib/ricky/slack/ingress.ts`, `egress.ts`, `auth.ts`, `store.ts`, `parser.ts`, `proactive.ts` business logic — only the entry/exit shape changes (input becomes a Nango envelope; output becomes deferred `response_url` POSTs / `chat.update` calls instead of synchronous `Response`).\n\n## 4. Constraints From Slack's Webhook Semantics\n\n| Endpoint | Slack expectation | Today | Post-migration |\n|---|---|---|---|\n| `events` | 200 within 3s; body ignored except `url_verification` challenge response | `app/api/v1/ricky/slack/events/route.ts` returns `{ok:true}` synchronously after kicking off `handleRickySlackCommand` | Nango router branch dispatches `handleRickySlackCommand` async; events have no `response_url` so user-facing replies use `chat.postMessage` via egress (already supported). |\n| `commands` | 200 within 3s; body becomes the slash command's response | Returns `Response` synchronously from `handleRickySlackCommand` | Nango router branch dispatches the command async; first `response_url` POST is \"Got it, working on it\" within ~1s; subsequent updates either via additional `response_url` POSTs (within 30 min, up to 5x) or via `chat.postMessage`. |\n| `interactivity` (block_actions on gate buttons) | 200 within 3s; body optional; `chat.update` on the original message is the standard way to reflect state | Returns sync; uses `updateGateSlackMessage` and resumes the run | Nango router branch dispatches async; `chat.update` already happens via `updateGateSlackMessage`. The earlier sync response only confirmed receipt; that's now implicit in Nango's 200. |\n| `interactivity` (view_submission with `response_action: \"clear\"`) | Sync response within 3s; no response = client timeout | Returns `{response_action: \"clear\"}` sync | Modal closes on client timeout (~3s). Acceptable per non-goals; document as known UX trade-off. |\n\nURL verification: Slack POSTs `{type: \"url_verification\", challenge: \"...\"}` once when the events URL changes. With Nango as the receiver, Nango must echo the challenge. If Nango doesn't, manual one-shot verification is required (point URL at Cloud direct, take the challenge, point back to Nango).\n\n## 5. File Changes\n\n### 5.1 New / changed\n\n- `packages/web/lib/integrations/nango-webhook-router.ts`:\n  - Import `handleRickySlackCommand`, `dispatchRickySlackCommand`, and the new interactivity dispatcher (see 5.4) from `lib/ricky/slack/ingress.ts`.\n  - In `routeForwardEvent`, the Slack branch (line 787) currently sends *every* slack-prefixed provider to Sage. Add disambiguation: if `provider === \"slack-ricky\"` (or matched via the alias map; see 5.5), dispatch to `handleRickySlackForward(envelope)`. Otherwise keep current behavior (Sage).\n  - New `handleRickySlackForward(envelope)`: detect payload shape (event vs command vs interactivity), claim dedup via `claimWebhookDelivery({ surface: \"slack\", deliveryId })`, route to the correct ingress handler.\n\n- `packages/web/lib/ricky/slack/ingress.ts`:\n  - Refactor `handleRickySlackCommand` from \"returns `Response`\" to \"POSTs replies to `responseUrl` (if present) or via `chat.postEphemeral`/`chat.postMessage` (if not)\" — return type becomes `Promise<void>` (or `Promise<{ status }>`).\n  - Add `dispatchRickySlackInteractivity(payload)` that mirrors what `interactivity/route.ts` does today but uses `chat.update` + `response_url` POSTs instead of returning `NextResponse.json(...)`.\n  - All existing helpers (`canResolveRickySlackGate`, `updateGateSlackMessage`, etc.) stay; only the wrappers that previously returned `Response` change.\n\n- `packages/web/lib/ricky/slack/egress.ts`:\n  - Add `postSlackResponseUrl({ responseUrl, body })` helper. Verified 2026-05-07 not present in `egress.ts`. Implementation: `await fetch(responseUrl, { method: \"POST\", headers: { \"content-type\": \"application/json\" }, body: JSON.stringify(body) })`. Throw on non-2xx so callers can log + dedup-retry.\n\n- `integrations/slack/ricky-manifest.json`:\n  - Replace `request_url` for `event_subscriptions`, `slash_commands`, and `interactivity` with `https://api.nango.dev/webhook/REPLACE_WITH_NANGO_SLACK_RICKY_INTEGRATION_ID/slack` (same template Sage's `integrations/slack/manifest.json` uses for its events URL).\n  - Drop `signing_secret_env`-style assumptions in any documentation. Mirror the structure of `integrations/linear/ricky-manifest.json` (`webhook.delivery: \"via Nango\"`, `nango.provider_config_key: \"slack-ricky\"`, `webhook_router` pointer).\n\n### 5.2 Deletions\n\n- `packages/web/app/api/v1/ricky/slack/events/route.ts`\n- `packages/web/app/api/v1/ricky/slack/commands/route.ts`\n- `packages/web/app/api/v1/ricky/slack/interactivity/route.ts`\n- `packages/web/lib/ricky/slack/signature.ts`\n- Any `signature.test.ts` / `events.test.ts` / `commands.test.ts` / `interactivity.test.ts` that exercise the deleted modules. Replace with router-level dispatch tests (see 5.6).\n\n### 5.3 Keep\n\n- `packages/web/app/api/v1/ricky/slack/oauth/route.ts` — OAuth installation, not a webhook.\n- `packages/web/lib/ricky/slack/dedup.ts` — already a thin wrapper around `webhook-dedup.ts` after #480.\n- `packages/web/lib/ricky/slack/{auth,blocks,egress,ingress,parser,proactive,store}.ts` — business logic, only ingress entry shape changes (5.1).\n\n### 5.4 Interactivity dispatcher contract\n\nThe current interactivity route handles two payload shapes:\n\n1. `block_actions` on a gate-approval message (the bulk of the file). Migration: same logic, but instead of `return NextResponse.json({ ok: true })`, do nothing (Nango already returned 200 to Slack). The `chat.update` of the original message via `updateGateSlackMessage` already happens; that's the user-visible feedback.\n2. `view_submission` returning `{response_action: \"clear\"}`. Migration: do nothing (Slack closes the modal client-side after timeout). Document the trade-off.\n\nFor error paths that today return non-2xx (`forbidden`, `not_found`, `gate_not_open`, etc.), surface the error to the user via `chat.postEphemeral` to the original channel (use payload.channel.id) instead of a 403 status. Slack already saw 200 from Nango.\n\n### 5.5 Provider config key registry\n\n`packages/web/lib/integrations/nango-webhook-router.ts` has a `PROVIDER_CONFIG_KEY_TO_PROVIDER` alias map (around line 108). Verified 2026-05-07: it currently lists `slack-relay`, `slack-sage`, `slack-my-senior-dev`, `slack-nightcto` — no `slack-ricky`. Add the line `\"slack-ricky\": \"slack-ricky\",` next to the other \"separate products\" entries. `packages/web/lib/integrations/providers.ts` already has a `slack-ricky` entry (defaultConfigKey `slack-ricky`, vfsRoot `/slack-ricky`); no change needed there.\n\n`isSlackProvider` matches `slack-*` providers indiscriminately. The Slack branch needs explicit disambiguation:\n\n```ts\nif (provider === \"slack-ricky\" || envelope.providerConfigKey === \"slack-ricky\") {\n  await handleRickySlackForward(envelope);\n  return;\n}\nif (isSlackProvider(provider) || looksLikeSlackFrom(envelope.from)) {\n  await repairSlackIntegrationFromForwardEvent(envelope, provider);\n  await forwardToSage(envelope);\n  return;\n}\n```\n\n### 5.6 Tests\n\nReplace the deleted route-level tests with router-dispatch tests in `packages/web/lib/integrations/__tests__/` (or similar):\n\n- `slack-ricky-events-dispatch.test.ts` — feed a fake forward envelope shaped like a Slack event, assert `handleRickySlackCommand` is called with the expected fields, assert dedup short-circuits a second call.\n- `slack-ricky-command-dispatch.test.ts` — feed a fake forward envelope for a slash command, assert `dispatchRickySlackCommand` calls `postSlackResponseUrl` with the expected ack, assert idempotency.\n- `slack-ricky-interactivity-dispatch.test.ts` — feed a block_actions envelope, assert `updateGateSlackMessage` is called and no sync return shape exists.\n\nPGlite-backed tests for the dedup shared with Linear already exist in `packages/web/lib/ricky/linear/__tests__/dedup.test.ts` (post-#480) — no need to duplicate.\n\n## 6. SST / Secrets Cleanup\n\nVerified 2026-05-07 via `grep -rn \"RickySlackSigningSecret\\|SlackSigningSecret\\|RICKY_SLACK_SIGNING_SECRET\\|SLACK_SIGNING_SECRET\"` over `infra/`, `scripts/`, `.github/`, and `packages/`: the SST resources `RickySlackSigningSecret` and `SlackSigningSecret` are **not** declared in `infra/secrets.ts`, **not** present in any `link: [...]` array in `infra/web.ts` or other infra files, **not** in `scripts/set-secrets.sh`, **not** in `.github/workflows/_deploy-cloud-stage.yml`, and **not** in `packages/web/lib/boot/resource-check.ts::REQUIRED_RESOURCES`. The only references are the four reads inside `lib/ricky/slack/signature.ts` (`readLinkedSecret(\"RickySlackSigningSecret\")`, `readLinkedSecret(\"SlackSigningSecret\")`, `process.env.RICKY_SLACK_SIGNING_SECRET`, `process.env.SLACK_SIGNING_SECRET`), all guarded by try/catch fallbacks.\n\nTherefore the cleanup is single-place: deleting `lib/ricky/slack/signature.ts` (per §5.2) removes every reference. No SST six-place reverse-walk needed. No CI/workflow secret seeding to remove.\n\nAfter deploy, confirm:\n\n```bash\ngrep -rn \"RickySlackSigningSecret\\|SlackSigningSecret\\|RICKY_SLACK_SIGNING_SECRET\\|SLACK_SIGNING_SECRET\" packages/ infra/ scripts/ .github/\n```\n\nreturns no matches.\n\n(Note: `workflows/cf-runtime/01-runtime-core.ts` contains the literal string `SLACK_SIGNING_SECRET` inside a code-generation template that emits Cloudflare Worker source — that's unrelated to this repo's Slack signing secrets and stays.)\n\n## 7. Rollout Plan\n\nSequence matters because the Slack app's webhook URLs are global (changing them invalidates direct routes immediately).\n\n1. **Land code changes** (this PR). Routes still exist on `main` until merge, so the Slack app keeps using direct delivery during review.\n2. **Pre-merge: register `slack-ricky` Nango integration**. Mirror Sage's `slack-relay` Nango configuration; the Nango integration id then substitutes into the manifest URL (§5.1). The file-sync routes already use this Nango forward pattern for Linear and Slack-Sage, so no Nango feature work is needed.\n3. **Post-merge, pre-Slack-app-update**: deploy the change. The new Nango router branch is live but unused (Slack still POSTs direct to the now-404 routes; this is brief).\n4. **Slack app dashboard switch**: in the Ricky Slack app's developer dashboard, change the events / slash commands / interactivity request URLs to `https://api.nango.dev/webhook/<slack-ricky-integration-id>/slack`. Single-step cutover (Nango auto-responds to Slack's `url_verification` challenge, same as Sage).\n5. **Bake**: smoke-test slash commands, mentions, gate approvals in a sandbox workspace. Confirm the new deferred-response UX is acceptable (slash commands may take ~1s longer to show the first response than before).\n6. **Document**: add a note to the runbook that any future Slack interactivity requiring `response_action: \"errors\"` (field-level modal validation) cannot use the Nango-routed pattern and must be reverted to a direct route with HMAC.\n\n## 8. Risks and Mitigations\n\n| Risk | Mitigation |\n|---|---|\n| Slash command UX regression (slower first response) | Send the deferred ack (\"Got it, working on it\") within ~500ms via `response_url`; existing callers already chain to subsequent updates. |\n| view_submission timeout shows error to user | Current surface only does `response_action: \"clear\"`; documented trade-off. If a surface needs validation errors later, that interactivity stays direct. |\n| `provider === \"slack-ricky\"` dispatch collides with Sage's slack-* matcher | Disambiguate explicitly (§5.5); add a unit test for the router branching. |\n| Concurrent Slack and Linear deliveries collide on `ricky_webhook_dedup` keys | Composite primary key `(surface, delivery_id)` already handles this — covered by `dedup.test.ts`. |\n\n## 9. Acceptance Criteria\n\n- [ ] `app/api/v1/ricky/slack/{events,commands,interactivity}/route.ts` deleted; `oauth/route.ts` retained.\n- [ ] `lib/ricky/slack/signature.ts` deleted.\n- [ ] No source file references `RickySlackSigningSecret`, `SlackSigningSecret`, `RICKY_SLACK_SIGNING_SECRET`, or `SLACK_SIGNING_SECRET` (verify with `grep -rn`).\n- [ ] `nango-webhook-router.ts` routes `slack-ricky` provider envelopes to the new dispatcher; default Slack-* still goes to Sage.\n- [ ] `lib/ricky/slack/ingress.ts::handleRickySlackCommand` returns `Promise<void>` and uses `responseUrl` / `chat.postEphemeral` for replies.\n- [ ] `integrations/slack/ricky-manifest.json` reflects Nango-routed delivery (mirrors `integrations/linear/ricky-manifest.json` shape).\n- [ ] `npx tsc -p packages/web/tsconfig.json --noEmit` clean.\n- [ ] `npx vitest run packages/web/lib/ricky packages/web/lib/integrations` clean (router-dispatch tests pass).\n- [ ] `npm run web:drizzle-journal:test` clean.\n- [ ] Bot reviewers (coderabbit, codex) addressed.\n\n## 10. References\n\n- PR #412 (`feat(ricky): add Slack agent v2 surface and routes`) — original direct-route implementation.\n- PR #480 (`feat(ricky): add Linear agent webhook surface and shared webhook dedup`) — establishes the Nango-routed pattern this spec mirrors.\n- `packages/web/app/api/v1/webhooks/nango/route.ts` — canonical Nango webhook intake.\n- `packages/web/lib/integrations/nango-webhook-router.ts` — `routeNangoWebhook` / `routeForwardEvent` / Linear AgentSession dispatch (post-#480).\n- `integrations/linear/ricky-manifest.json` (post-#480) — manifest shape model.\n- CLAUDE.md \"Adding a new SST resource\" — six-place rule, applied in reverse for removal here.")
    .pattern("pipeline")
    .channel("wf-ricky-ricky-slack-nango-routed-migration-spec-status-d")
    .maxConcurrency(1)
    .timeout(14400000)
    .onError('retry', { maxRetries: 2, retryDelayMs: 1000 })

    .agent("lead-claude", { cli: "claude", interactive: false, role: "Plans task shape, ownership, non-goals, and verification gates.", retries: 1 })
    .agent("impl-primary-codex", { cli: "codex", role: "Primary implementer for the generated code-writing workflow.", retries: 2 })
    .agent("impl-tests-codex", { cli: "codex", role: "Adds or updates tests and validation coverage for the changed surface.", retries: 2 })
    .agent("reviewer-claude", { cli: "claude", preset: "reviewer", role: "Reviews product fit, scope control, and workflow evidence quality.", retries: 1 })
    .agent("reviewer-codex", { cli: "codex", preset: "reviewer", role: "Reviews TypeScript correctness, deterministic gates, and test coverage.", retries: 1 })
    .agent("validator-claude", { cli: "claude", preset: "worker", role: "Runs the 80-to-100 fix loop and verifies final readiness.", retries: 2 })

    .step("prepare-context", {
      type: 'deterministic',
      command: "mkdir -p '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.txt' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/non-goals.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/deliverables.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan-instructions.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/implementation-instructions.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-checklist.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/pattern-decision.txt' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/loaded-skills.txt' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-matches.json' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/tool-selection.json' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-runtime-boundary.txt' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/matched-skills.md' && echo GENERATED_WORKFLOW_CONTEXT_READY",
      captureOutput: true,
      failOnError: true,
    })

    .step("skill-boundary-metadata-gate", {
      type: 'deterministic',
      dependsOn: ["prepare-context"],
      command: "test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-matches.json' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/tool-selection.json' && grep -F 'generation_time_only' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"runtimeEmbodiment\":false' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F 'relay-80-100-workflow' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F 'writing-agent-relay-workflows' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F 'shadcn' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F 'choosing-swarm-patterns' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"stage\":\"generation_selection\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"stage\":\"generation_loading\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"effect\":\"metadata\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"stage\":\"generation_rendering\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"effect\":\"pattern_selection\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"stage\":\"generation_rendering\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"effect\":\"workflow_contract\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"stage\":\"generation_rendering\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json' && grep -F '\"effect\":\"validation_gates\"' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json'",
      captureOutput: true,
      failOnError: true,
    })

    .step('lead-plan', {
      agent: "lead-claude",
      dependsOn: ['skill-boundary-metadata-gate'],
      timeoutMs: 600000,
      task: `Plan the workflow execution from the packaged context files.

Read these files in order:
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan-instructions.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/non-goals.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/deliverables.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json


Write .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md.
Required headings: Non-goals, Routing contract, Implementation contract.
End the file with GENERATION_LEAD_PLAN_READY.`,
      verification: { type: 'output_contains', value: 'GENERATION_LEAD_PLAN_READY' },
    })

    .step("lead-plan-gate", {
      type: 'deterministic',
      dependsOn: ["lead-plan"],
      command: "node <<'NODE'\nconst fs = require('node:fs');\nconst leadPlanPath = \".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md\";\nconst body = fs.readFileSync(leadPlanPath, 'utf8');\nif (!body.includes('GENERATION_LEAD_PLAN_READY')) throw new Error('lead plan missing required marker: GENERATION_LEAD_PLAN_READY');\nif (!/\\b(non-goals?|out[- ]of[- ]scope|not in scope)\\b/i.test(body)) throw new Error('lead plan missing required marker: Non-goals or Out of scope');\nconst hasRoutingContract = /Routing contract/i.test(body) || /Local execution must run through Agent Relay/i.test(body) || /Run local execution through the generated Agent Relay workflow artifact/i.test(body) || /routes local execution through the generated Agent Relay artifact/i.test(body) || /Use the generated Agent Relay workflow artifact/i.test(body);\nif (!hasRoutingContract) throw new Error('lead plan missing required marker: Routing contract');\nconst hasImplementationContract = /Implementation contract/i.test(body) || /This is an implementation spec/i.test(body);\nif (!hasImplementationContract) throw new Error('lead plan missing required marker: Implementation contract');\nconsole.log('LEAD_PLAN_GATE_OK');\nNODE",
      captureOutput: true,
      failOnError: true,
    })

    .step('implement-artifact', {
      agent: "impl-primary-codex",
      dependsOn: ['lead-plan-gate'],

      timeoutMs: 1200000,
      task: `Implement the requested code-writing workflow slice.

Read these packaged context files before editing:
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/implementation-instructions.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/deliverables.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/matched-skills.md


Own only the declared targets from .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json unless review feedback narrows a required fix.
Declared target files are listed in .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/deliverables.md.

Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Keep execution routing explicit for local, cloud, and MCP callers. Materialize outputs to disk, then stop for deterministic gates.`,
    })

    .step("post-implementation-file-gate", {
      type: 'deterministic',
      dependsOn: ["implement-artifact"],
      command: "test -f 'packages/web/tsconfig.json' && test -d 'packages/web/lib/ricky' && test -d 'packages/web/lib/integrations' && find 'packages/web/lib/ricky' 'packages/web/lib/integrations' -type f \\( -name '*.ts' -o -name '*.tsx' \\) -print0 | xargs -0 grep -Eq 'export|function|class|workflow\\('",
      captureOutput: true,
      failOnError: true,
    })

    .step("initial-soft-validation", {
      type: 'deterministic',
      dependsOn: ["post-implementation-file-gate"],
      command: "npx tsc --noEmit && npx vitest run",
      captureOutput: true,
      failOnError: false,
    })

    .step("review-claude", {
      agent: "reviewer-claude",
      dependsOn: ["initial-soft-validation"],

      timeoutMs: 600000,
      task: `Review the generated work.

Read:
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-checklist.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md


Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-claude.md ending with REVIEW_COMPLETE.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-claude.md" },
    })

    .step("review-codex", {
      agent: "reviewer-codex",
      dependsOn: ["initial-soft-validation"],

      timeoutMs: 600000,
      task: `Review the generated work.

Read:
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-checklist.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md


Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-codex.md ending with REVIEW_COMPLETE.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-codex.md" },
    })

    .step("read-review-feedback", {
      type: 'deterministic',
      dependsOn: ["review-claude", "review-codex"],
      command: "test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-claude.md' && test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-codex.md' && grep -F 'REVIEW_COMPLETE' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-claude.md' && grep -F 'REVIEW_COMPLETE' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-codex.md' && cat '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-claude.md' '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-codex.md' | tee '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-feedback.md'",
      captureOutput: true,
      failOnError: true,
    })

    .step('fix-loop', {
      agent: "validator-claude",
      dependsOn: ['read-review-feedback', 'initial-soft-validation'],

      timeoutMs: 1200000,
      task: `Run the 80-to-100 fix loop.

Inputs:
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-feedback.md

Review feedback:
{{steps.read-review-feedback.output}}

Initial validation output:
{{steps.initial-soft-validation.output}}

Fix only concrete review or validation findings. Preserve the declared target boundary:
- packages/web/tsconfig.json
- packages/web/lib/ricky
- packages/web/lib/integrations

Tool selection: runner=@agent-relay/sdk; concurrency=1; rule=project default runner @agent-relay/sdk.

Before exiting, write .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/fix-loop-report.md summarizing the exact fixes you applied or explicitly saying that no repo changes were required, then end that file with FIX_LOOP_COMPLETE.
Re-run typecheck and tests before handing off to post-fix validation.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/fix-loop-report.md" },
    })

    .step("fix-loop-report-gate", {
      type: 'deterministic',
      dependsOn: ["fix-loop"],
      command: "test -f '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/fix-loop-report.md' && tail -n 1 '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/fix-loop-report.md' | tr -d '[:space:]' | grep -Eq '^FIX_LOOP_COMPLETE$'",
      captureOutput: true,
      failOnError: true,
    })

    .step("post-fix-verification-gate", {
      type: 'deterministic',
      dependsOn: ["fix-loop-report-gate"],
      command: "test -f 'packages/web/tsconfig.json' && test -d 'packages/web/lib/ricky' && test -d 'packages/web/lib/integrations' && find 'packages/web/lib/ricky' 'packages/web/lib/integrations' -type f \\( -name '*.ts' -o -name '*.tsx' \\) -print0 | xargs -0 grep -Eq 'export|function|class|workflow\\('",
      captureOutput: true,
      failOnError: true,
    })

    .step("active-reference-gate", {
      type: 'deterministic',
      dependsOn: ["post-fix-verification-gate"],
      command: "printf '%s\\n' 'No manifest-driven deleted paths to check.' > '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/active-reference-check.txt'",
      captureOutput: true,
      failOnError: true,
    })

    .step("post-fix-validation", {
      type: 'deterministic',
      dependsOn: ["active-reference-gate"],
      command: "npx tsc --noEmit && npx vitest run",
      captureOutput: true,
      failOnError: false,
    })

    .step("final-review-claude", {
      agent: "reviewer-claude",
      dependsOn: ["post-fix-validation"],

      timeoutMs: 600000,
      task: `Re-review the fixed state only.

Read:
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-checklist.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md


Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/final-review-claude.md ending with FINAL_REVIEW_CLAUDE_PASS.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/final-review-claude.md" },
    })

    .step("final-review-codex", {
      agent: "reviewer-codex",
      dependsOn: ["post-fix-validation"],

      timeoutMs: 600000,
      task: `Re-review the fixed state only.

Read:
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/review-checklist.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/normalized-spec.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/acceptance-contract.json
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/lead-plan.md
- .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/verification-plan.md


Tool selection: runner=@agent-relay/sdk; concurrency=2; rule=project default runner @agent-relay/sdk.

Write .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/final-review-codex.md ending with FINAL_REVIEW_CODEX_PASS.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/final-review-codex.md" },
    })

    .step("final-review-pass-gate", {
      type: 'deterministic',
      dependsOn: ["final-review-claude", "final-review-codex"],
      command: "tail -n 1 '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/final-review-claude.md' | tr -d '[:space:]*' | grep -Eq '^FINAL_REVIEW_CLAUDE_PASS$' && tail -n 1 '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/final-review-codex.md' | tr -d '[:space:]*' | grep -Eq '^FINAL_REVIEW_CODEX_PASS$'",
      captureOutput: true,
      failOnError: true,
    })

    .step("final-hard-validation", {
      type: 'deterministic',
      dependsOn: ["final-review-pass-gate"],
      command: "npx tsc --noEmit && npx vitest run",
      captureOutput: true,
      failOnError: true,
    })

    .step("git-diff-gate", {
      type: 'deterministic',
      dependsOn: ["final-hard-validation"],
      command: "{ git diff --name-only -- 'packages/web/tsconfig.json' 'packages/web/lib/ricky' 'packages/web/lib/integrations'; git ls-files --others --exclude-standard -- 'packages/web/tsconfig.json' 'packages/web/lib/ricky' 'packages/web/lib/integrations'; } > '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/git-diff.txt' && sort -u '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/git-diff.txt' -o '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/git-diff.txt' && test -s '.workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/git-diff.txt'",
      captureOutput: true,
      failOnError: true,
    })

    .step("regression-gate", {
      type: 'deterministic',
      dependsOn: ["git-diff-gate"],
      command: "npx vitest run",
      captureOutput: true,
      failOnError: true,
    })

    .step('final-signoff', {
      agent: "validator-claude",
      dependsOn: ['regression-gate'],

      timeoutMs: 600000,
      task: `Write .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/signoff.md.

Include:
- files changed
- source changes and implementation diff evidence
- status-prefixed changed-file inventory and command summaries
- dry-run command to execute before runtime launch
- deterministic validation commands
- review verdicts
- PR URL or a clear result location/status when PR creation is intentionally out of scope
- skill application boundary from .workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/skill-application-boundary.json
- remaining risks or environmental blockers
- every current output-manifest path, and no stale cleanup targets unless those targets are in the current manifest

Tool selection: runner=@agent-relay/sdk; concurrency=1; rule=project default runner @agent-relay/sdk.

End with GENERATED_WORKFLOW_READY.`,
      verification: { type: 'file_exists', value: ".workflow-artifacts/generated/ricky-slack-nango-routed-migration-spec-status-d/signoff.md" },
    })

    .run({ cwd: process.cwd() });

  console.log(result.status);
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
