{
  "id": "compact_54wg45v19brm",
  "version": 1,
  "type": "compacted",
  "compactedAt": "2026-09-02T01:09:01.227Z",
  "sourceTrajectories": [
    "traj_773f096ymzai"
  ],
  "dateRange": {
    "start": "2026-09-02T00:59:53.622Z",
    "end": "2026-09-02T01:09:00.470Z"
  },
  "summary": {
    "totalDecisions": 1,
    "totalEvents": 2,
    "uniqueAgents": [
      "default"
    ]
  },
  "decisionGroups": [
    {
      "category": "testing",
      "decisions": [
        {
          "question": "Advance only to published, verified launch pins",
          "chosen": "Advance only to published, verified launch pins",
          "reasoning": "npm latest now resolves to agent-relay 11.10.0 and relayfile 0.10.52. The immutable Gist revision ed2a9ae1e96fbe474861c33b95cd7bcae1ab640c hashes to 398214e8c0208b968018f6ddf85f8f1fb8d1582c35e05ba24140cfeaad14d42e and its validate --dry-run succeeds. Update repository pins and public checksum references; do not mutate the Gist.",
          "fromTrajectory": "traj_773f096ymzai"
        }
      ]
    }
  ],
  "keyLearnings": [],
  "keyFindings": [],
  "filesAffected": [
    "docs/guides/live-workspace.md",
    "examples/live-workspace/LAUNCH_PLAYBOOK.md",
    "examples/live-workspace/README.md",
    "examples/live-workspace/package-lock.json",
    "examples/live-workspace/package.json",
    "examples/live-workspace/proof.mjs",
    "examples/live-workspace/proof.test.mjs",
    ".agentworkforce/trajectories/compacted/compact_54wg45v19brm_2026-09-02.json",
    ".agentworkforce/trajectories/compacted/compact_54wg45v19brm_2026-09-02.md"
  ],
  "commits": [],
  "narrative": "This session was a re-audit of Relayfile PR #456 (branch `docs/live-review-launch-0901`), which packages the public launch demo for the live-workspace review flow: one agent reviews live, uncommitted code in a fresh Daytona sandbox, a second agent fixes the same file after a hash-gated handoff, and the laptop independently verifies the final bytes. The branch had already landed five hardening commits (`28b5e98` launch live review before the PR, `57eb457` pin and verify launch script, `5cfd5c3` fail closed before executing launch script, `2c51294` make launch command portable and self-contained, `b470565` match Node floor to pinned launch graph). The remaining review finding was that the public prerequisite list omitted the Claude provider requirement even though the demo cannot run without it. The work closed that finding and audited each independently pinned launch surface against published artifacts. `docs/guides/live-workspace.md` and `examples/live-workspace/README.md` now state the demo requires `a Bash environment with mktemp, Node.js 22.22.0+, npm, curl, and access to a Claude provider connectable during setup` — matching the requirement already present in `examples/live-workspace/LAUNCH_PLAYBOOK.md`. The self-contained launch recipes moved from Gist revision `ce68bc90b3324e4b51c160152cc6aaa02513ae68` / SHA-256 `47be2579…eb873` to the immutable revision `ed2a9ae1e96fbe474861c33b95cd7bcae1ab640c` / SHA-256 `398214e8c0208b968018f6ddf85f8f1fb8d1582c35e05ba24140cfeaad14d42e`, keeping the fail-closed curl → `node -e` SHA-256 check → execute chain intact and the Gist itself unmutated. The immutable Gist revision independently pins agent-relay 11.10.0 with Relayfile/SDK 0.10.51, its validated combination. The separate lower-level example advanced from agent-relay 11.9.0 → 11.10.0 and relayfile 0.10.51 → 0.10.52 across `examples/live-workspace/package.json`, the regenerated `package-lock.json` (including all five `@agent-relay/broker-*` platform packages and a transitive `typebox` 1.3.24 → 1.3.25 bump), the `MINIMUM_AGENT_RELAY_VERSION`/`MINIMUM_RELAYFILE_VERSION` constants in `proof.mjs`, the `parseCliVersion`/`compareVersions` fixtures and the `portableAgentRelayCommand` expectation in `proof.test.mjs`, and the `npx agent-relay@11.10.0 …` attach/release commands quoted in the README. Verification was gate-by-gate rather than by inspection: 10 `node --test proof.test.mjs` cases, the `validate --dry-run` run of the new Gist revision, exact installed CLI versions checked against npm `latest`, recomputed SHA-256 of the downloaded script, markdown structure, shell syntax, and a diff-hygiene pass. The re-audit produced nine coordinated documentation/example/trajectory updates ready for one reviewed branch commit.",
  "decisions": [
    {
      "question": "Should the launch docs pin whatever versions are convenient, or only published, independently verified artifacts?",
      "chosen": "Advance the checked-in lower-level proof to published agent-relay 11.10.0 and relayfile 0.10.52; select the verified immutable Gist revision ed2a9ae1e96fbe474861c33b95cd7bcae1ab640c, which independently pins agent-relay 11.10.0 with Relayfile/SDK 0.10.51.",
      "reasoning": "npm `latest` was confirmed as agent-relay 11.10.0 / relayfile 0.10.52 for the checked-in proof. The separate immutable Gist revision's own 11.10.0/0.10.51 combination was recomputed as 398214e8…4d42e and its `validate --dry-run` succeeded. The two runnable surfaces are pinned and validated independently.",
      "impact": "The docs state each surface's exact version scope: all self-contained recipes use one immutable checksum-verified Gist revision, while the checked-in lower-level proof uses the latest published Relayfile package."
    },
    {
      "question": "Update the Gist in place, or point at a new immutable revision?",
      "chosen": "Do not mutate the Gist; select the already-published latest immutable revision and update the repository's `LIVE_REVIEW_REV` + `LIVE_REVIEW_SHA256` pair.",
      "reasoning": "The docs pin a revision SHA and verify a content hash. The selected revision already existed and was independently fetched, hashed, and dry-run before the repository references changed.",
      "impact": "The old pin ce68bc90… stays valid for anyone who copied it; the checksum gate remains a real integrity check rather than a formality."
    },
    {
      "question": "Is the Claude provider a setup detail or a stated prerequisite of the public demo?",
      "chosen": "Promote it to an explicit prerequisite in the top-level guide and README, matching the language already in LAUNCH_PLAYBOOK.md.",
      "reasoning": "The demo cannot complete without a connected Claude provider, so omitting it turns a known precondition into a mid-demo failure in front of a launch audience.",
      "impact": "Resolved the last outstanding PR #456 review finding; all three public entry points now list the same prerequisite set."
    },
    {
      "question": "How far should a version bump propagate?",
      "chosen": "Bump the whole graph together — package.json, package-lock.json, proof.mjs minimum-version constants, proof.test.mjs fixtures, and every `npx agent-relay@<v>` command quoted in prose.",
      "reasoning": "The example enforces its own floor at runtime via MINIMUM_AGENT_RELAY_VERSION / MINIMUM_RELAYFILE_VERSION; leaving any of manifest, lockfile, constants, or docs behind produces a preflight that passes while the docs lie (or vice versa).",
      "impact": "`npm ci` and `npm run preflight` agree with the printed attach/release commands; no split-brain between what is installed and what is documented."
    }
  ],
  "conventions": [
    {
      "pattern": "Every public one-liner follows fetch → verify → execute, chained with `&&`: `mktemp` into `${TMPDIR:-/tmp}`, `curl -fsSL .../raw/$LIVE_REVIEW_REV/live-review.sh`, then an inline `node -e` that recomputes SHA-256 and throws `SHA-256 mismatch` before the script is ever run.",
      "rationale": "Fails closed. A tampered or truncated download never reaches execution, and the check needs no dependency beyond Node, which is already a stated prerequisite.",
      "scope": "Any curl-to-shell command published in docs/guides/ or examples/live-workspace/."
    },
    {
      "pattern": "Pin remote scripts by immutable revision SHA plus a separately declared content hash (`LIVE_REVIEW_REV` + `LIVE_REVIEW_SHA256`), never by a mutable `raw/` or branch URL.",
      "rationale": "The revision makes the fetch reproducible; the hash makes it verifiable even if the host is compromised. Rotating a pin means selecting a new immutable revision, never a mutable HEAD URL.",
      "scope": "All externally hosted launch/demo scripts."
    },
    {
      "pattern": "Keep the three public entry points — docs/guides/live-workspace.md, examples/live-workspace/README.md, examples/live-workspace/LAUNCH_PLAYBOOK.md — semantically aligned on their command block and prerequisites.",
      "rationale": "Readers arrive at different doors; a divergence between them is how the Claude-provider prerequisite went missing in the first place.",
      "scope": "The live-workspace launch documentation set."
    },
    {
      "pattern": "Version floors are asserted in code and tested: `MINIMUM_AGENT_RELAY_VERSION`/`MINIMUM_RELAYFILE_VERSION` in proof.mjs, with dependency-free `parseCliVersion` and `compareVersions` covered by fixtures in proof.test.mjs (including a strictly-below case such as `compareVersions(\"0.10.51\", \"0.10.52\") < 0`).",
      "rationale": "A read-only `npm run preflight` catches a stale CLI before any Daytona sandbox is provisioned, which is the expensive and demo-visible step.",
      "scope": "examples/live-workspace and any example that shells out to pinned CLIs."
    },
    {
      "pattern": "Ship the verification evidence with the claim: tests, `validate --dry-run`, installed-CLI-version check, recomputed checksum, markdown structure, shell syntax, and diff hygiene were each run as named gates.",
      "rationale": "Launch docs are executed verbatim by strangers; \"looks right\" is not a gate.",
      "scope": "Launch-readiness reviews on this repo."
    }
  ],
  "lessons": [
    {
      "lesson": "Prerequisite lists drift out of sync across duplicated docs faster than the commands themselves do.",
      "context": "The Claude-provider requirement was already documented in LAUNCH_PLAYBOOK.md but absent from live-workspace.md and README.md — the command blocks had been kept in sync across all three, the prose had not.",
      "recommendation": "When touching a shared launch command, diff the surrounding prose across all copies too, not just the fenced block. Consider grepping for the prerequisite sentence as a check."
    },
    {
      "lesson": "Bumping a pinned CLI version is a five-file edit, not a manifest edit.",
      "context": "11.9.0 → 11.10.0 touched package.json, package-lock.json (agent-relay plus every `@agent-relay/broker-*` optional platform package and transitive typebox), the constants in proof.mjs, the fixtures and `portableAgentRelayCommand` expectation in proof.test.mjs, and README prose commands.",
      "recommendation": "Use the existing tests as the tripwire — proof.test.mjs asserts the exact `npx agent-relay@<version>` string, so a partial bump fails the suite rather than shipping. Keep that assertion pinned to a literal version for exactly this reason. (Consistent with the repo's recurring lockfile-drift problem.)"
    },
    {
      "lesson": "Verify a pin against the registry and by execution before writing it into a doc, not after.",
      "context": "npm `latest` resolution, the SHA-256 recomputation, and the `validate --dry-run` of Gist revision ed2a9ae all ran ahead of the doc edits.",
      "recommendation": "Order the work as resolve → verify → execute → document. A pin that has never been run is a claim, not a pin."
    },
    {
      "lesson": "Immutability of a referenced artifact is a property to protect deliberately, not assume.",
      "context": "A mutable Gist HEAD can change, but the launch recipes use a full immutable revision SHA and independently verify the fetched bytes.",
      "recommendation": "Always publish the full revision segment and content hash together; never replace either with a mutable raw URL."
    }
  ],
  "openQuestions": [
    "The refreshed Gist pin (agent-relay 11.10.0, Relayfile/SDK 0.10.51) passed its immutable checksum and `validate --dry-run`; if a full two-agent non-dry-run has not already been captured, that remains the final operator rehearsal before launch."
  ]
}
