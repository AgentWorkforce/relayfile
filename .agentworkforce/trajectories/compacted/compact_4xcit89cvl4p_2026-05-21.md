# Trajectory Compaction: 2026-05-14 - 2026-05-21

## Summary
This work session cluster spans a week of relayfile runtime hardening and workspace-primitive contract closure. The most impactful chain was issues #180→#182→#183: issue #180 hardened the mount daemon lifecycle with process-scan fallback discovery for orphan daemons (stale pid files, reparented child binaries); issue #182 stacked on top to add writeback guards requiring explicit Dirty/deletePending local mutation state before retrying writes (preventing hash-drift from remote hydration from triggering false writebacks); issue #183 then optimized bootstrap performance by adding decoded-byte contentHash to tree/events endpoints, skipping ReadFile calls when local bytes match, and fetching remaining files through a bounded concurrent worker pool. A separate fix addressed cloud refresh-token rotation races with a one-shot disk-credential reload/retry before entering degraded re-login state. In parallel, a large multi-agent workflow (ricky-workspace-primitive-skills-pr-39) drove contract closure across four repos (relayfile, cloud, relayfile-adapters, skills). Key canonical decisions were locked: `<provider>/LAYOUT.md` over `.layout.md`, existing YAML frontmatter (`date`, `generated_at`, `covers`) over the skill's blockquote header, sibling `.schema.json` as the writeback discovery surface, and `writeback retry` as the supported replay path. The workflow patched missing skill manifest files, fixed Python SDK and mounted skill text still referencing the retired `.layout.md` path, added hosted by-edited regression coverage in cloud, and updated adapter fallback contract docs. A Codex validator agent independently discovered that `http.Client.Timeout` enforces a whole-request cap over body reads independent of context cancellation, leading to its removal in favor of granular transport timeouts. Two diagnostic sessions investigated confusing `status=lagging` with `lagSeconds=0` for providers lacking cursor/watermark progress. The root cause was absent ingress state rather than timed backlog. A PR (#153) added CLI diagnostics querying sync ingress to render the actual reason.

## Key Decisions (7)
| Question | Decision | Impact |
|----------|----------|--------|
| How should writeback retries be gated to prevent false writebacks from hash drift? | Require explicit Dirty state (set by watcher/local-create) for write retries and a separate deletePending flag for delete retries | internal/mountsync/syncer.go now gates writeback paths on explicit local mutation flags, preventing remote-only changes from being echoed back as writes |
| How to handle stale pid files and orphan mount daemons? | Process-scan fallback: scan live relayfile mount commands to discover orphan daemons even when .relay/mount.pid is wrong | Stop/status/start commands now work correctly even with corrupted pid state; PR feedback further required live process corroboration for legacy bare pid files and path-token boundary matching |
| How to optimize bootstrap performance for large workspaces? | Server-side contentHash on tree/events, client skip-on-hash, and bounded parallel bootstrap reads | Bootstrap skips files whose local bytes already match the tree hash; remaining files are fetched through a bounded concurrent worker pool instead of sequentially |
| How to handle concurrent CLI processes rotating cloud refresh tokens? | One-shot reload of disk credentials and retry after refresh-token auth rejection, before entering degraded re-login state | internal/relayfile/store.go absorbs the rotation race without forcing user re-authentication |
| What is the canonical provider layout path? | `<provider>/LAYOUT.md` — update skill contracts to match runtime, do not migrate runtime to `.layout.md` | Python SDK and mounted skill text referencing `.layout.md` were patched across repos |
| Should http.Client.Timeout be used for large-workspace mount convergence? | Remove whole-request http.Client.Timeout in favor of granular transport timeouts plus context cancellation | Large workspace bootstrap no longer times out mid-stream during slow body reads |
| What writeback states should be exposed to agents? | Expose only `pending` and `dead` via `writeback list`; remove `succeeded`/`failed` from the contract | Simplifies agent-facing writeback monitoring contract and aligns CLI with actual backend capabilities |

## Conventions Established
- **Stack dependent issue branches: #182 built on #180's `codex/issue-180-mount-daemon-lifecycle` branch**: Issues triggered by the same root cause (double-daemon state race) share implementation context; stacking preserves dependency while keeping unrelated backup files untouched (scope: Branch management for causally related issues)
- **Run `scripts/check-contract-surface.sh` after any handler or OpenAPI changes**: Ensures `openapi/relayfile-v1.openapi.yaml` stays in sync with `internal/httpapi/server.go` (scope: All relayfile HTTP API changes)
- **Use repo-specific worktrees for cross-repo contract changes; land adapters first, then cloud, then relayfile**: Each repo has its own tests, release cadence, and local dirty state; dependency order prevents consuming contracts before they exist (scope: Multi-repo workspace-primitive skill gap work)
- **Digest file updates must emit normal file events but `/digests/*` writes must not trigger digest regeneration**: Mounted workspaces need to receive changed digest files, but recursion would cause infinite regeneration loops (scope: Any code path that writes to /digests/)
- **Preserve terminal provider state (closed, merged, archived) as data, not file deletes**: Upstream object not being deleted means the record should remain readable; agents need historical state (scope: All ingest/sync/provider mutation paths)

## Lessons Learned
- Hash drift alone is not a reliable signal for local mutation — remote hydration and corrupted state can cause identical symptoms (Issue #182 writeback guard: the syncer was retrying writes based solely on hash mismatch, which could be triggered by remote-side changes) - Always gate writeback retries on explicit local mutation state (Dirty flag from watcher, deletePending from local delete handler) rather than inferring mutation from content differences
- net/http Client.Timeout enforces a hard deadline over the entire request including body read, independent of context cancellation (Codex validator agent discovered this as a 6th defect during live testing of large-workspace mount convergence; bootstrap streams were being killed mid-read) - For streaming or long-running HTTP body reads, use granular transport-level timeouts (TLSHandshakeTimeout, ResponseHeaderTimeout) plus per-operation context deadlines instead of Client.Timeout
- Provider status=lagging with lagSeconds=0 means absent cursor/watermark, not zero backlog (Diagnostic investigation found 6 providers reported as lagging with no measured lag, no errors, no dead letters — only Linear had ingress/cursor progress) - When displaying ambiguous server state, query underlying signals (ingress events, cursor presence) and render the actual reason rather than passing through the bare status code
- Multi-agent review workflows surface real defects but produce high coordination overhead (The ricky-child-update-last-week-workflow took 71 hours wall-clock with 7 agents cycling through plan→implement→review→fix→re-review→codex-review→codex-fix; the initial review verdict was BLOCKED) - For bounded implementation slices, a single review+fix pass is usually sufficient; reserve multi-pass review cycles for security-critical or cross-repo contract changes
- Fetching remote refs non-destructively is safer than pulling when repos have local conflicts or dirty files (relayfile had unresolved merge conflicts and cloud had dirty files that git pull would overwrite; the workflow used fetch + inspection instead) - When coordinating across repos with potentially dirty state, use `git fetch` and inspect `origin/main` rather than pulling, to preserve in-progress agent or user work

## Open Questions
- Issues #180/#182/#183 have uncommitted working-tree changes (syncer.go, store.go, syncer_test.go, etc.) on codex/issue-182-writeback-guard — these need to be committed and PR'd or the work will be lost
- The 500-event digest cap in cloud has not been resolved — needs either removal, pagination/chunking, or explicit machine-readable warning semantics
- by-edited/<date>/ fallback indexes are still unimplemented across all three repos despite being required by the activity-summary skill contract
- Workspace timezone support for digest window boundaries remains UTC-only; no wall-clock scheduler exists for daily/weekly closing windows
- The websocket.go change in the working tree (contentHash forwarding for issue #183) needs review to ensure it doesn't break existing event consumers
- Notion writeback discovery/layout mismatch found during PR39 review — layout advertises pages/comments/databases writeback but discovery only covers database page create

## Stats
- Sessions: 8, Agents: default, orchestrator, lead-claude, impl-codex, reviewer-claude, validator-claude, reviewer-codex, validator-codex, master-lead, Files: 13, Commits: 3
- Date range: 2026-05-14T09:38:05.074Z - 2026-05-21T12:08:51.996Z