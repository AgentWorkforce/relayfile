{
  "private": true,
  "version": "0.1.1",
  "description": "relayfile — real-time filesystem for humans and agents",
  "workspaces": [
    "packages/core",
    "packages/sdk",
    "packages/cli"
  ],
  "scripts": {
    "workflow": "agent-relay run",
    "build": "npm run build --workspace=packages/sdk",
    "typecheck": "npm run typecheck --workspace=packages/sdk"
  },
  "dependencies": {
    "@agent-relay/sdk": "latest"
  },
  "devDependencies": {
    "agent-trajectories": "^0.5.2",
    "tsx": "^4.21.0"
  }
}
