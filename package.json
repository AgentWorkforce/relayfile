{
  "private": true,
  "description": "relayfile — real-time filesystem for humans and agents",
  "workspaces": [
    "packages/relayfile-sdk",
    "packages/relayfile"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "workflow": "agent-relay run",
    "build": "npm run build --workspace=packages/relayfile-sdk",
    "typecheck": "npm run typecheck --workspace=packages/relayfile-sdk"
  },
  "dependencies": {
    "@agent-relay/sdk": "latest"
  },
  "devDependencies": {
    "patch-package": "^8.0.1",
    "tsx": "^4.21.0"
  }
}
