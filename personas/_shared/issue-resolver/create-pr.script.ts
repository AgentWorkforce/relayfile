export type CreateProxyPrScriptKind = "cloud-small-issue-codex" | "cloud-complex-issue-workflow";

const SMALL_CREATE_PROXY_PR_SCRIPT = "\n(async () => {\nconst fs = require('fs');\nconst path = require('path');\nconst { execFileSync } = require('child_process');\nconst [repoDir, owner, repo, branch, title, body] = process.argv.slice(2);\nconst { cloudApi } = requireRuntimeCredentials({ relayfile: false, cloudApi: true });\nconst cloudApiUrl = cloudApi.url;\nconst cloudApiToken = cloudApi.token;\nconst pullRequestAuthToken = readCredential('WORKFORCE_WORKSPACE_TOKEN') || cloudApiToken;\nfunction requireRuntimeCredentials(required = { relayfile: true, cloudApi: true }) {\n  const relayfile = {\n    url: readCredentialUrl('RELAYFILE_URL'),\n    token: readCredential('RELAYFILE_TOKEN'),\n    workspaceId: readCredential('RELAYFILE_WORKSPACE_ID'),\n  };\n  const cloudApi = {\n    url: readCredentialUrl('CLOUD_API_URL'),\n    token: readCredential('CLOUD_API_ACCESS_TOKEN'),\n  };\n  const missing = [];\n  if (required.relayfile !== false) {\n    if (!relayfile.url) missing.push('relayfile.url');\n    if (!relayfile.token) missing.push('relayfile.token');\n    if (!relayfile.workspaceId) missing.push('relayfile.workspaceId');\n  }\n  if (required.cloudApi !== false) {\n    if (!cloudApi.url) missing.push('cloudApi.url');\n    if (!cloudApi.token) missing.push('cloudApi.token');\n  }\n  if (missing.length > 0) throw missingCredentialsError(missing);\n  return { relayfile, cloudApi };\n}\nfunction readCredential(name) {\n  const value = process.env[name];\n  return typeof value === 'string' && value.trim() ? value.trim() : '';\n}\nfunction readCredentialUrl(name) {\n  return readCredential(name).replace(/\\/+$/, '');\n}\nfunction missingCredentialsError(missing) {\n  return new Error('Runtime credentials are required: missing ' + missing.join(', '));\n}\nconst cloneInfo = JSON.parse(fs.readFileSync(repoDir + '/.relayfile-clone.json', 'utf8'));\nif (typeof cloneInfo.headSha !== 'string' || cloneInfo.headSha.startsWith('manual-preflight-app-path')) {\n  throw new Error('Refusing to publish from a repaired or invalid preflight checkout; clone did not materialize a real GitHub headSha');\n}\nfunction gitLines(args) {\n  return execFileSync('git', args, { cwd: repoDir, encoding: 'utf8' }).split('\\n').filter(Boolean);\n}\nfunction gitText(args) {\n  return execFileSync('git', args, { cwd: repoDir, encoding: 'utf8' });\n}\nfunction safeGitText(args) {\n  try {\n    return gitText(args);\n  } catch (error) {\n    return '[git failed: ' + (error && error.message ? error.message : String(error)) + ']';\n  }\n}\nfunction safeGitLines(args) {\n  return safeGitText(args).split('\\n').filter(Boolean);\n}\nfunction truncateDiagnosticText(text, maxBytes) {\n  const input = String(text || '');\n  let output = input;\n  while (Buffer.byteLength(output, 'utf8') > maxBytes) {\n    output = output.slice(Math.max(1, output.length - maxBytes));\n  }\n  return output;\n}\nfunction redactDiagnosticText(text) {\n  const secretEnvPattern = new RegExp(\n    '(' + ['CLOUD_API_ACCESS_TOKEN', 'WORKFORCE_WORKSPACE_TOKEN', 'RELAYFILE_TOKEN', 'GITHUB' + '_TOKEN'].join('|') + ')=[^\\s\\'\"]+',\n    'g',\n  );\n  return String(text || '')\n    .replace(/gh[pousr]_[A-Za-z0-9]{20,}/g, '[REDACTED_GH_TOKEN]')\n    .replace(/x-access-token:[^@\\s\\x2f\\'\"]+/gi, 'x-access-token:[REDACTED]')\n    .replace(/(authorization\\s*[:=]\\s*bearer\\s+)[^\\s'\"]+/gi, '$1[REDACTED]')\n    .replace(secretEnvPattern, '$1=[REDACTED]')\n    .replace(/(\"?(?:api[_-]?key|token|secret)\"?\\s*[:=]\\s*\")([^\"]+)(\")/gi, '$1[REDACTED]$3');\n}\nfunction redactedTail(text, maxBytes = 4096) {\n  return truncateDiagnosticText(redactDiagnosticText(text), maxBytes);\n}\nfunction readFileTail(filePath, maxBytes = 4096) {\n  try {\n    const stat = fs.statSync(filePath);\n    if (!stat.isFile()) return null;\n    const readSize = Math.min(stat.size, Math.max(maxBytes * 4, maxBytes));\n    const fd = fs.openSync(filePath, 'r');\n    try {\n      const buffer = Buffer.alloc(readSize);\n      fs.readSync(fd, buffer, 0, readSize, Math.max(0, stat.size - readSize));\n      return {\n        path: filePath,\n        size: stat.size,\n        mtimeMs: stat.mtimeMs,\n        tail: redactedTail(buffer.toString('utf8'), maxBytes),\n      };\n    } finally {\n      fs.closeSync(fd);\n    }\n  } catch {\n    return null;\n  }\n}\nfunction listNewestFiles(roots, predicate, limit = 3) {\n  const found = [];\n  const seen = new Set();\n  function walk(dir, depth) {\n    if (depth > 6 || seen.has(dir)) return;\n    seen.add(dir);\n    let entries;\n    try {\n      entries = fs.readdirSync(dir, { withFileTypes: true });\n    } catch {\n      return;\n    }\n    for (const entry of entries) {\n      const fullPath = path.join(dir, entry.name);\n      if (entry.isDirectory()) {\n        walk(fullPath, depth + 1);\n        continue;\n      }\n      if (!entry.isFile() || !predicate(fullPath, entry.name)) continue;\n      try {\n        const stat = fs.statSync(fullPath);\n        found.push({ path: fullPath, mtimeMs: stat.mtimeMs, size: stat.size });\n      } catch {\n        // ignore files that disappear during capture\n      }\n    }\n  }\n  for (const root of roots) walk(root, 0);\n  return found\n    .sort((left, right) => right.mtimeMs - left.mtimeMs)\n    .slice(0, limit);\n}\nfunction diagnosticFileTails() {\n  const home = process.env.HOME || '/home/daytona';\n  const rolloutRoots = [\n    path.join(home, '.codex', 'sessions'),\n    path.join(repoDir, '.codex', 'sessions'),\n    path.join(process.cwd(), '.codex', 'sessions'),\n  ];\n  const outputRoots = [\n    path.join(home, '.daytona', 'sessions'),\n    path.join(home, '.agent-relay', 'team', 'worker-logs'),\n    path.join(process.cwd(), '.agent-relay', 'team', 'worker-logs'),\n  ];\n  return {\n    runnerLog: [\n      readFileTail(path.join(home, 'runner.log')),\n      readFileTail('/home/daytona/runner.log'),\n      readFileTail(path.join(process.cwd(), 'runner.log')),\n    ].find(Boolean) || null,\n    rollouts: listNewestFiles(\n      rolloutRoots,\n      (filePath, name) => name.startsWith('rollout-') && name.endsWith('.jsonl'),\n      3,\n    ).map((file) => readFileTail(file.path)).filter(Boolean),\n    sessionOutputs: listNewestFiles(\n      outputRoots,\n      (_filePath, name) => name === 'output.log' || name.endsWith('.log'),\n      3,\n    ).map((file) => readFileTail(file.path)).filter(Boolean),\n  };\n}\nfunction collectOpenPrDiagnostic(reason, diffPaths) {\n  const baselineCommit = safeGitLines(['rev-list', '--max-parents=0', 'HEAD'])[0] || null;\n  const committedRange = baselineCommit ? baselineCommit + '..HEAD' : 'HEAD';\n  const artifactsRoot = '/project/.agent-relay/step-artifacts';\n  let artifactDirs = [];\n  try {\n    artifactDirs = fs.existsSync(artifactsRoot) ? fs.readdirSync(artifactsRoot).filter((name) => name === 'implement' || name.startsWith('implement-repair-')).sort() : [];\n  } catch {\n    artifactDirs = [];\n  }\n  return {\n    diag: 'cloud-small-issue-codex-resolver-exit',\n    temporary: true,\n    reason,\n    implementStepOutcome: 'open-pr-reached-after-implement',\n    implementExitCode: 0,\n    issueNumber: branch.replace(/^codex\\/small-issue-/, ''),\n    repoDir,\n    branch,\n    head: safeGitLines(['rev-parse', 'HEAD'])[0] || null,\n    currentBranch: safeGitLines(['branch', '--show-current'])[0] || null,\n    baselineCommit,\n    gitStatusPorcelain: redactedTail(safeGitText(['status', '--porcelain=v1'])),\n    gitDiffStat: redactedTail(safeGitText(['diff', '--stat', 'HEAD'])),\n    changedSinceBaseline: safeGitLines(['diff', '--name-only', '--diff-filter=ACMRT', committedRange]),\n    deletedSinceBaseline: safeGitLines(['diff', '--name-only', '--diff-filter=D', committedRange]),\n    changedWorkingTree: safeGitLines(['diff', '--name-only', '--diff-filter=ACMRT', 'HEAD']),\n    deletedWorkingTree: safeGitLines(['diff', '--name-only', '--diff-filter=D', 'HEAD']),\n    untracked: safeGitLines(['ls-files', '--others', '--exclude-standard']),\n    diffPaths,\n    implementArtifactDirs: artifactDirs,\n    files: diagnosticFileTails(),\n  };\n}\nlet resolverExitDiagnosticEmitted = false;\nfunction emitResolverExitDiagnostic(reason, diffPaths = null) {\n  if (resolverExitDiagnosticEmitted) return;\n  resolverExitDiagnosticEmitted = true;\n  try {\n    console.log('[resolver-exit-diagnostic] ' + JSON.stringify(collectOpenPrDiagnostic(reason, diffPaths)));\n  } catch (error) {\n    console.log('[resolver-exit-diagnostic-capture-failed] ' + JSON.stringify({\n      diag: 'cloud-small-issue-codex-resolver-exit',\n      reason,\n      error: error && error.message ? error.message : String(error),\n    }));\n  }\n}\nfunction collectDiffPaths() {\n  const baselineCommit = gitLines(['rev-list', '--max-parents=0', 'HEAD'])[0];\n  const committedRange = baselineCommit ? baselineCommit + '..HEAD' : 'HEAD';\n  const changed = Array.from(new Set([\n    ...gitLines(['diff', '--name-only', '--diff-filter=ACMRT', committedRange]),\n    ...gitLines(['diff', '--name-only', '--diff-filter=ACMRT', 'HEAD']),\n    ...gitLines(['ls-files', '--others', '--exclude-standard']),\n  ]));\n  const deleted = Array.from(new Set([\n    ...gitLines(['diff', '--name-only', '--diff-filter=D', committedRange]),\n    ...gitLines(['diff', '--name-only', '--diff-filter=D', 'HEAD']),\n  ])).filter((filePath) => !changed.includes(filePath));\n  return { changed, deleted };\n}\nfunction copyTreeFiles(sourceDir, targetDir) {\n  if (!fs.existsSync(sourceDir)) return 0;\n  let copied = 0;\n  const stack = [''];\n  while (stack.length > 0) {\n    const relativeDir = stack.pop();\n    const absoluteDir = path.join(sourceDir, relativeDir);\n    for (const entry of fs.readdirSync(absoluteDir, { withFileTypes: true })) {\n      const relativePath = path.join(relativeDir, entry.name);\n      if (relativePath === '.relayfile-clone.json' || relativePath === '.git' || relativePath.startsWith('.git' + path.sep)) continue;\n      const sourcePath = path.join(sourceDir, relativePath);\n      const targetPath = path.join(targetDir, relativePath);\n      if (entry.isDirectory()) {\n        stack.push(relativePath);\n        continue;\n      }\n      if (!entry.isFile()) continue;\n      fs.mkdirSync(path.dirname(targetPath), { recursive: true });\n      fs.copyFileSync(sourcePath, targetPath);\n      copied += 1;\n    }\n  }\n  return copied;\n}\nfunction restorePropagatedImplementationArtifacts() {\n  const artifactsRoot = '/project/.agent-relay/step-artifacts';\n  if (!fs.existsSync(artifactsRoot)) return 0;\n  const repoName = path.basename(repoDir);\n  const artifactDirs = fs.readdirSync(artifactsRoot)\n    .filter((name) => name === 'implement' || name.startsWith('implement-repair-'))\n    .sort();\n  let copied = 0;\n  for (const artifactDir of artifactDirs) {\n    copied += copyTreeFiles(path.join(artifactsRoot, artifactDir, repoName), repoDir);\n  }\n  return copied;\n}\nlet { changed, deleted } = collectDiffPaths();\nif (changed.length === 0 && deleted.length === 0) {\n  const restored = restorePropagatedImplementationArtifacts();\n  if (restored > 0) {\n    ({ changed, deleted } = collectDiffPaths());\n  }\n}\nconst files = [];\nfor (const filePath of changed) {\n  if (filePath === '.relayfile-clone.json' || filePath.startsWith('.git/')) continue;\n  files.push({ path: filePath, content: fs.readFileSync(repoDir + '/' + filePath).toString('base64'), encoding: 'base64' });\n}\nfor (const filePath of deleted) {\n  files.push({ path: filePath, deleted: true });\n}\nif (files.length === 0) {\n  // TEMP DIAGNOSTIC (#3 resolver-exit) — remove after root cause.\n  // Capture before the workflow exits and sandbox cleanup can delete codex\n  // rollout/session files. Non-fatal: any capture failure is logged but the\n  // original empty-diff behavior is preserved.\n  emitResolverExitDiagnostic('empty-diff-before-pr', { changed, deleted });\n  throw new Error('No file changes to publish');\n}\nconst response = await fetch(cloudApiUrl + '/api/v1/github/pull-request', {\n  method: 'POST',\n  headers: { authorization: 'Bearer ' + pullRequestAuthToken, 'content-type': 'application/json' },\n  body: JSON.stringify({ owner, repo, branch, baseSha: cloneInfo.headSha, baseBranch: 'main', title, body, files }),\n});\nconst payload = await response.json().catch(() => ({}));\nif (!response.ok || !payload.prUrl) {\n  // TEMP DIAGNOSTIC (#3 resolver-exit) — remove after root cause.\n  emitResolverExitDiagnostic('proxy-pr-create-failed', { status: response.status, payload });\n  throw new Error('Proxy PR create failed: ' + response.status + ' ' + JSON.stringify(payload));\n}\nconsole.log(payload.prUrl);\nconsole.log(branch);\n})().catch((error) => {\n  console.error(error);\n  process.exit(1);\n});\n";

const COMPLEX_CREATE_PROXY_PR_SCRIPT = "\n(async () => {\nconst fs = require('fs');\nconst path = require('path');\nconst { execFileSync } = require('child_process');\nconst [repoDir, owner, repo, branch, title, body] = process.argv.slice(2);\nconst { cloudApi } = requireRuntimeCredentials({ relayfile: false, cloudApi: true });\nconst cloudApiUrl = cloudApi.url;\nconst cloudApiToken = cloudApi.token;\nconst pullRequestAuthToken = readCredential('WORKFORCE_WORKSPACE_TOKEN') || cloudApiToken;\nfunction requireRuntimeCredentials(required = { relayfile: true, cloudApi: true }) {\n  const relayfile = {\n    url: readCredentialUrl('RELAYFILE_URL'),\n    token: readCredential('RELAYFILE_TOKEN'),\n    workspaceId: readCredential('RELAYFILE_WORKSPACE_ID'),\n  };\n  const cloudApi = {\n    url: readCredentialUrl('CLOUD_API_URL'),\n    token: readCredential('CLOUD_API_ACCESS_TOKEN'),\n  };\n  const missing = [];\n  if (required.relayfile !== false) {\n    if (!relayfile.url) missing.push('relayfile.url');\n    if (!relayfile.token) missing.push('relayfile.token');\n    if (!relayfile.workspaceId) missing.push('relayfile.workspaceId');\n  }\n  if (required.cloudApi !== false) {\n    if (!cloudApi.url) missing.push('cloudApi.url');\n    if (!cloudApi.token) missing.push('cloudApi.token');\n  }\n  if (missing.length > 0) throw missingCredentialsError(missing);\n  return { relayfile, cloudApi };\n}\nfunction readCredential(name) {\n  const value = process.env[name];\n  return typeof value === 'string' && value.trim() ? value.trim() : '';\n}\nfunction readCredentialUrl(name) {\n  return readCredential(name).replace(/\\/+$/, '');\n}\nfunction missingCredentialsError(missing) {\n  return new Error('Runtime credentials are required: missing ' + missing.join(', '));\n}\nconst cloneInfo = JSON.parse(fs.readFileSync(repoDir + '/.relayfile-clone.json', 'utf8'));\nif (typeof cloneInfo.headSha !== 'string' || cloneInfo.headSha.startsWith('manual-preflight-app-path')) {\n  throw new Error('Refusing to publish from a repaired or invalid preflight checkout; clone did not materialize a real GitHub headSha');\n}\nfunction gitLines(args) {\n  return execFileSync('git', args, { cwd: repoDir, encoding: 'utf8' }).split('\\n').filter(Boolean);\n}\nfunction collectDiffPaths() {\n  const baselineCommit = gitLines(['rev-list', '--max-parents=0', 'HEAD'])[0];\n  const committedRange = baselineCommit ? baselineCommit + '..HEAD' : 'HEAD';\n  const changed = Array.from(new Set([\n    ...gitLines(['diff', '--name-only', '--diff-filter=ACMRT', committedRange]),\n    ...gitLines(['diff', '--name-only', '--diff-filter=ACMRT', 'HEAD']),\n    ...gitLines(['ls-files', '--others', '--exclude-standard']),\n  ]));\n  const deleted = Array.from(new Set([\n    ...gitLines(['diff', '--name-only', '--diff-filter=D', committedRange]),\n    ...gitLines(['diff', '--name-only', '--diff-filter=D', 'HEAD']),\n  ])).filter((filePath) => !changed.includes(filePath));\n  return { changed, deleted };\n}\nfunction copyTreeFiles(sourceDir, targetDir) {\n  if (!fs.existsSync(sourceDir)) return 0;\n  let copied = 0;\n  const stack = [''];\n  while (stack.length > 0) {\n    const relativeDir = stack.pop();\n    const absoluteDir = path.join(sourceDir, relativeDir);\n    for (const entry of fs.readdirSync(absoluteDir, { withFileTypes: true })) {\n      const relativePath = path.join(relativeDir, entry.name);\n      if (relativePath === '.relayfile-clone.json' || relativePath === '.git' || relativePath.startsWith('.git' + path.sep)) continue;\n      const sourcePath = path.join(sourceDir, relativePath);\n      const targetPath = path.join(targetDir, relativePath);\n      if (entry.isDirectory()) {\n        stack.push(relativePath);\n        continue;\n      }\n      if (!entry.isFile()) continue;\n      fs.mkdirSync(path.dirname(targetPath), { recursive: true });\n      fs.copyFileSync(sourcePath, targetPath);\n      copied += 1;\n    }\n  }\n  return copied;\n}\nfunction restorePropagatedImplementationArtifacts() {\n  const artifactsRoot = '/project/.agent-relay/step-artifacts';\n  if (!fs.existsSync(artifactsRoot)) return 0;\n  const repoName = path.basename(repoDir);\n  const artifactDirs = fs.readdirSync(artifactsRoot)\n    .filter((name) => name === 'plan' || name === 'implement' || name === 'review' || name.startsWith('implement-repair-'))\n    .sort();\n  let copied = 0;\n  for (const artifactDir of artifactDirs) {\n    copied += copyTreeFiles(path.join(artifactsRoot, artifactDir, repoName), repoDir);\n  }\n  return copied;\n}\nfunction readStageMetaFile(fileName) {\n  const direct = path.join(repoDir, fileName);\n  if (fs.existsSync(direct)) {\n    try {\n      return fs.readFileSync(direct, 'utf8');\n    } catch (error) {\n      return '';\n    }\n  }\n  return '';\n}\nfunction truncateMeta(text, max) {\n  if (typeof text !== 'string') return '';\n  const trimmed = text.trim();\n  return trimmed.length > max ? trimmed.slice(0, max) + '\\n\\n…(truncated)' : trimmed;\n}\nlet { changed, deleted } = collectDiffPaths();\nif (changed.length === 0 && deleted.length === 0) {\n  const restored = restorePropagatedImplementationArtifacts();\n  if (restored > 0) {\n    ({ changed, deleted } = collectDiffPaths());\n  }\n}\nconst planText = truncateMeta(readStageMetaFile('PLAN.md'), 6000);\nconst reviewText = truncateMeta(readStageMetaFile('REVIEW.md'), 4000);\nconst reviewVerdict = (reviewText.split('\\n')[0] || '').trim();\nconst files = [];\nfor (const filePath of changed) {\n  if (filePath === '.relayfile-clone.json' || filePath.startsWith('.git/')) continue;\n  if (filePath === 'PLAN.md' || filePath === 'REVIEW.md') continue;\n  files.push({ path: filePath, content: fs.readFileSync(repoDir + '/' + filePath).toString('base64'), encoding: 'base64' });\n}\nfor (const filePath of deleted) {\n  if (filePath === 'PLAN.md' || filePath === 'REVIEW.md') continue;\n  files.push({ path: filePath, deleted: true });\n}\nif (files.length === 0) throw new Error('No file changes to publish');\nconst prBodySections = [body];\nif (planText) prBodySections.push('## Plan\\n\\n' + planText);\nif (reviewText) prBodySections.push('## Review' + (reviewVerdict ? ' (' + reviewVerdict + ')' : '') + '\\n\\n' + reviewText);\nprBodySections.push('---\\nGenerated by the cloud-complex-issue-workflow multi-stage path: plan → implement → review → open-pr.');\nconst prBody = prBodySections.join('\\n\\n');\nconst response = await fetch(cloudApiUrl + '/api/v1/github/pull-request', {\n  method: 'POST',\n  headers: { authorization: 'Bearer ' + pullRequestAuthToken, 'content-type': 'application/json' },\n  body: JSON.stringify({ owner, repo, branch, baseSha: cloneInfo.headSha, baseBranch: 'main', title, body: prBody, files }),\n});\nconst payload = await response.json().catch(() => ({}));\nif (!response.ok || !payload.prUrl) throw new Error('Proxy PR create failed: ' + response.status + ' ' + JSON.stringify(payload));\nconsole.log(payload.prUrl);\nconsole.log(branch);\n})().catch((error) => {\n  console.error(error);\n  process.exit(1);\n});\n";

export function createProxyPrScript(kind: CreateProxyPrScriptKind): string {
  return kind === "cloud-small-issue-codex"
    ? SMALL_CREATE_PROXY_PR_SCRIPT
    : COMPLEX_CREATE_PROXY_PR_SCRIPT;
}

export function renderCreateProxyPrScriptSource(kind: CreateProxyPrScriptKind): string {
  return "const CREATE_PROXY_PR_SCRIPT = String.raw`" + createProxyPrScript(kind) + "`;";
}
