# Permissions Rubric

Permissions cases are deterministic ACL boundary checks. Passing cases should
show allowed reads, denied writes or reads, no unintended file mutation, and no
restricted subtree leakage in directory listings.
